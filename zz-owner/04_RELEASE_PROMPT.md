# 04 - One release prompt

Replace the release family with one lean `release_prompt.md` that spans the
owner's upload and pulls procedures from `docs/agent/support/`. Retire the old
prompt-shaped support material, rescue only the ruled live-site read route, and
trim the outbox. Execute after leg 03 lands. Produce the commits below; execute
no later leg and do not perform a release.

Authored against `9ae0d50`. The owner overrides obstructing placement rules for
these overhaul briefs in `C:\Dev\SMR-BugFixPack\zz-owner`; the firing freeze and
all substantive owner rulings remain in force.

## Live progress

- [COMPLETE - `3424bbc`] Retire `SITE_AUDIT.md`, preserve its narrow live-site
  read route in support, repoint the two live consumers and verify the cut.
- [COMPLETE - this commit] Trim the resolved v9/v10 `Held` block from
  `RELEASE_OUTBOX.md`; its live ledger rules and released history remain intact.
- [IN PROGRESS - sources read; inventory pending] Replace `RELEASE.md`,
  `PUBLIC_SURFACE_SWEEP.md` and `POST_UPLOAD_CLOSE.md` atomically with one lean
  prompt and the support it calls; dissolve reporter section 4, verify and
  commit, consuming this brief.

Update the list at every commit. Split a unit immediately if evidence creates a
new commit boundary; exactly one unfinished unit is in progress and its text
states useful current state.

## Read path and fixed authority

Start with `git log --oneline -6`, `git pull`, `git status --short`. Compare the
tree with authoring baseline `9ae0d50`; read the leg 02/03 diffs before trusting
old paths. Read `.claude/promptsrot/00_CHAIN_SPEC.md` completely and the answered
2026-09-15 rulings at the top of `.claude/DECISIONS.md`. Apply `doc-editing` and
`prompt-authoring`; apply `smr-bug-library` before changing its source. Read all
target Must_Read_Headers.

Read `docs/README.md`, `docs/agent/prompts/README.md`,
`docs/agent/support/README.md`, `docs/UPLOAD_WORKFLOW.md`, and these source files
as they exist after leg 03:

- `docs/agent/prompts/perma/RELEASE.md`
- `docs/agent/prompts/perma/PUBLIC_SURFACE_SWEEP.md`
- `docs/agent/prompts/perma/POST_UPLOAD_CLOSE.md`
- `docs/agent/prompts/perma/SITE_AUDIT.md`
- `docs/agent/prompts/perma/RELEASE_OUTBOX.md`
- `docs/FIELD_REPORT_REPLIES.md`

Read `docs/agent/STATE.md` because it has known live pointers to both the release
family and `SITE_AUDIT`. For defect identity, grep `docs/agent/bugs/INDEX.md` and
then open only the field-route/frozen-row passages in `F104.md` and `F105.md`.
Read the editable `smr-bug-library` source that leg 03 used, not merely its
generated mirror. Search for additional records through the bug/fact INDEXes;
do not read either whole folder.

Fixed rulings: prompts holds prompts; its map stays; `RELEASE_OUTBOX.md` stays in
`prompts/` as the one ledger exception; there is one lean `release_prompt.md`;
supporting procedures live in `docs/agent/support/` and the prompt points to
them. Replies have only `docs/FIELD_REPORT_REPLIES.md`, are pull-only, use the
owner as transport, and gain no new rule. CUT is the default; retention needs a
written reason using the placement question. Never edit `LINUX_DISPATCH.md` or
`HANDOFF_ORCHESTRATOR.md`, even to repair citations. No prompt fires until the
owner lifts the freeze in words.

## Commit 1 - Cut `SITE_AUDIT`, rescue the read route

Delete `SITE_AUDIT.md` as a prompt and remove its prompt-map row. Preserve only
the approximately 2.9 KB route the owner identified: the two things an agent may
never do, section 1's control for identifying the newest successful live Pages
deployment, and section 6's reading traps. Put the compact, model-neutral
reference in `docs/agent/support/` and add an accurate support-map row. It is a
read procedure, not a prompt: no orientation, todo ceremony, filing/report
template, broad five-page audit or speculative health sweep survives merely
because it existed.

Repoint `STATE.md`'s live-deployment read route to the support document in the
same commit. Sweep live consumers for the old full and relative paths and repair
only genuine live dependencies. Historical reports, bug/fact entries, archive
material and dated records retain their old citations. Do not edit the exempt
handoff prompt.

Compare the saved passages beside the source deletion. The support reference
must still establish that committed and deployed are different, derive the live
sha from a successful deployment, distrust stale recorded shas/counts, use an
independent control rather than repeating a bad reader, and never publish or
edit store strings as a substitute for a live-page correction. Do not keep the
rest as history or a retirement note in house docs.

## Commit 2 - Trim `RELEASE_OUTBOX`

Keep the Must_Read_Header rules, Pending lifecycle, current release ledger and
released history needed to prevent a change shipping twice. Cut the spent v9/v10
staging block under `Held` and other instructions whose trigger has already
resolved. Remove the citation to nonexistent
`prompts/SURFACE_AUDIT_FABLE.md`; do not replace it with a fictional successor.
Review every owner ruling and live obligation before trimming: wordiness and
spent staging are removable, a still-live ledger entry or ruling is not.

Update lifecycle pointers from `RELEASE.md` to `release_prompt.md` only if the
new prompt lands in this commit; otherwise defer those pointer edits to commit 3
so no intermediate commit points at a missing file. Do not turn released history
into a progress narrative about the overhaul.

## Commit 3 - Consolidate the release family atomically

Create `docs/agent/prompts/perma/release_prompt.md` and delete `RELEASE.md`,
`PUBLIC_SURFACE_SWEEP.md` and `POST_UPLOAD_CLOSE.md` in the same commit. Replace
their map rows with one description that matches the resulting prompt. The
prompt is the short orchestrator; operational detail becomes mapped support.
A useful split is a surface-update reference and a post-upload-close reference,
but names and boundaries follow the actual retained obligations rather than
these suggestions. Do not create a second fireable front door in support.

The prompt must retain this lifecycle without dilution:

1. derive the release batch from `RELEASE_OUTBOX.md`, emit counts, update the
   required player surfaces and pass the preflight gates;
2. hand off to the owner and HOLD at the upload boundary;
3. tell the owner exactly how to resume and leave the one-line resumable marker;
4. often in a fresh session, verify the upload from the tree/receipts rather
   than a handoff claim, then run the post-upload writeback and comment restore;
5. clear the outbox only after the owner confirms the upload, and finish the
   records/counts.

The 2026-09-13 ruling is load-bearing: the prompt spans the owner's upload,
pauses at old section 2 and resumes at old section 4, often in another session.
Making it lean does not turn close-out into a separate errand. Preserve the
agent-never-uploads rail, editor/version rail, zero-hit-is-failure controls,
post-upload comment-restoration hazard, store-body/site ordering rulings and
only the current owner receipts. Cut dated update narration and duplicated
checklists when the retained support procedure carries the actual obligation.

Remove `RELEASE.md:65`'s standing reply-update step. Dissolve
`PUBLIC_SURFACE_SWEEP.md` section 4 exactly as follows:

- its third bullet's owner ruling about naming the other mod survives verbatim
  at its existing home in `docs/FIELD_REPORT_REPLIES.md`; verify the quote and
  condition rather than copying another version;
- its fourth bullet's issue identities belong in the F104/F105 entries; verify
  those destination passages and add only information genuinely missing;
- its fifth bullet contains the durable tooling fact: `split_bugs.render_entry`
  emits only `FRONT_FIELDS` and silently drops an invented front-matter key.
  Put one concise authoring warning in the editable `smr-bug-library` source,
  without reviving `row_status` as a general-purpose field, then regenerate its
  mirror in the same commit;
- cut the remaining reporter procedure. Do not move tracker reading, reply
  chasing or posted-versus-draft ceremony into the release prompt or support.

Repair live citations to the three removed prompts, including outbox rules,
STATE pointers, map descriptions and `FIELD_REPORT_REPLIES.md` lines that claim
section 4 remains the doctrine. Leave historical citations alone. Record the
known dead citations in `HANDOFF_ORCHESTRATOR.md` for that prompt's next handoff
session, but do not edit the file: this is the spec's deliberate LOUD exception.

## Evidence and acceptance

Before each unit, inventory exact source/destination files, byte lengths and
SHA-256 hashes; every reported total carries its command, filter, HEAD and
reconciled members. Use `git diff --stat 9ae0d50..HEAD -- <paths>` to decide which
inherited passages moved; re-read changed groups only. Before reporting a corpus
or map count, emit it with `python tools/doccheck.py --emit-counts` or the focused
gate and enumerate its members.

For every old/new path use `git grep -n -F -- <path>` plus explicit
`rg -n -F -- <path> .claude/ zz-owner/`. Search relative forms separately.
Classify live, historical and exempt hits; count the presence side and prove the
remaining live unresolved set empty. A word hit is not a path citation. Decode
compressed inputs before an absence claim.

For every cut, make a disposition table: source passage, keep/cut decision,
reason, destination if any, and the destination passage actually inspected.
For every retained support passage, show the prompt pointer that consumes it.
Read the final prompt as a future release session and trace both the pre-upload
and fresh-session post-upload route. This proves topology and retained meaning,
not that a release was run.

Run `python tools/doccheck.py` before every commit. Regenerate only when needed;
inspect peer bug/fact dirt first and review generated diffs. Use
`git commit -F <msgfile> -- <exact paths>`, never bypass hooks and never change
branches in the main checkout.

Derived facts, measured at `9ae0d50`:

| fact | measurement | falsifier |
|---|---|---|
| the five release inputs exist and the prompt map declares their current roles | direct file/map read at `9ae0d50` | `git diff --stat 9ae0d50..HEAD -- docs/agent/prompts/` |
| `STATE.md` points to `SITE_AUDIT.md` and the old release family | `rg -n 'SITE_AUDIT|RELEASE.md|PUBLIC_SURFACE_SWEEP' docs/agent/STATE.md` at `9ae0d50` | rerun that command after prior legs |
| F104/F105 already carry issue #1/#2 identity while the unknown-key behavior is authoring-tool behavior | focused entry and `split_bugs`/skill reads at `9ae0d50` | `rg -n 'issue #1|issue #2|FRONT_FIELDS|row_status' docs/agent/bugs/F104.md docs/agent/bugs/F105.md tools .claude/skills .agents/skills` |

## Scope, stops and close

Out of scope: the leg 05 perma cleanup, root/live-chain review, game code,
playtests, an actual release, store/site publication and reply work. Route an
in-scope discovery to its commit; append anything owned by a later leg to that
brief and always append audit evidence to `zz-owner/07_TERMINAL_QA.md` Inbox.
Finding a problem does not authorize unrelated repair.

Stop and report on dirty/peer-owned targets in this leg, contradictory authority,
a missing owner decision, RED outside the lane, a trim that changes meaning or
loses a ruling, an unopened destination passage, or a cut that cannot be proved.
Recheck status immediately before every write and attribute shared work by
commit/diff.

In the final result commit, append the outbox to leg 07 and `git rm` this brief.
Report commits, exact cuts/moves, preserved obligations, evidence, DEPARTURES,
SUGGESTIONS and the executed model from the transcript. Do not claim the chain
is ready, the release workflow has run, GREEN proves meaning, or that nothing
cited a file based on default `rg`. Say only that leg 04 landed and name what
remains.
