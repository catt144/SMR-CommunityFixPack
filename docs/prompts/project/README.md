# Project prompt chain — run in filename order, delete as you go

**Created 2026-08-01 (owner direction). This folder is a self-consuming queue:
when it is empty except this README and the final QA prompt, the project work
between the bug-list audit and the F86 adjudication is DONE and the owner is
free to playtest.** The final QA prompt deletes this README and itself last.

Design rule (owner, 2026-08-01): **many focused prompts over few big ones** —
each prompt is sized to finish comfortably in one session's context, and
nothing is left hanging between sessions except through the written inbox
mechanism below.

## The chain

| # | file | model | owner needed? | what it drains |
|---|------|-------|---------------|----------------|
| 0 | ~~`0_harness_gate_and_probe_teardown_opus.md`~~ **✅ DONE 2026-08-01, file consumed** | Opus | — | owed F49-strip A/B code-gate leg (RAN CLEAR: `68/74` → `63/0/15/0`) · stale probe `99` teardown (deleted; sweep now returns one declared hit, `97`) |
| 1 | ~~`1_playtest_reorg_and_policy_sweep_opus.md`~~ **✅ DONE 2026-08-01, file consumed** | Opus | — (the §4 adoption needed no ask — blanket pre-clearance) | PT-54 retired unrun (C/D/E → Tier-1 legs; **A/B not absorbed → routed to prompt 3**) · four new checklist riders (F35, C32, F80, F82) · F74 rider merged into F53(a)'s · **FIX_POLICY §4 amendment APPLIED** (activates one owner decision on F29/F57(a) → **routed to prompt 7**) · MOD_DESCRIPTION community-standing facts (one conditional → **prompt 4**) · BUGS/STATUS consistency sweep (100 index rows agree with their heading tags; counts re-derived) |
| 2 | ~~`2_f86_phase0_measurements_opus.md`~~ **✅ DONE 2026-08-01, file consumed** | Opus | — (one sitting, spent) | both F86 engine measurements, log `Mars.exe-20260801-14.59.57`: **GT creation DEFERS** (measured twice, incl. GT-creates-GT with a live `WaitMsg` receipt → **rains takes the wrapper shape; F02's falsy guard is not load-bearing, kept as defence in depth**) · **autosave hook FIRES** (`autosave=true err=false`, console-forced via the engine's own `Autosave`) · probe `97` torn down — **the stale-probe sweep now returns ZERO hits in both repos** · TestKit `99_FixtureCarry` repair committed |
| 3 | ~~`3_f86_phase1_tier1_spec_fable.md`~~ **✅ DONE 2026-08-01, file consumed** | Fable | — | final Tier-1 spec = `SAVE_SAFETY_REDESIGN.md` **§6.2a** (C34 rider rides the rains pass; **mid-session F81a reconcile TAKEN**; StormWedgeHeal reorder specced) · enumeration re-derived: **exactly 13** (+1 inert route-(c) site — no non-compliant 14th) · **adjudication §4.4 CLOSED** (closure enters saves via `likes_data`→GameVar, inert) · `F86_TIER1_BUILD_PROMPT.md` written (5 legs incl. PT-54 A/B reshaped) |
| 4 | ~~`4_f86_phase2_tier1_build_fable.md`~~ **SPLIT 2026-08-01 (rule 3), file consumed** | Fable | — (legs 1-4 RUN with owner) | Tier-1 BUILT (4 units + probe realign, `d899a54..a9d68e3`; TestKit thru `49b40da`) · **legs 1-4 PASSED on log evidence** (ledger in 4b) · F89 filed mid-leg · leg-3 id-less clause amended (owner-cleared) |
| 4b | ~~`4b_f86_phase2_tier1_close_opus.md`~~ **✅ DONE 2026-08-01, file consumed** | Opus | — (leg 5 run with owner) | **TIER 1 VERIFIED — all five legs run** (`c6180ad`; logs `Mars.exe-20260801-16.42.31`/`-17.11.08`/`-19.14.11`, test-2 lineage) · **F02/F78/F81/F88 → `tested`** (row + tag both) · C34 rider verified · PT-54 absorption table marked RUN (C/D/E + the carried A/B as leg 4) · STATUS F86 rewritten to post-Tier-1 truth · **leg 5 read Tier-1 CLEAN** (`Meteors`/`MeteorStorm` alive on vanilla bodies, zero Tier-1-named lines, residue below the allowed list) **but surfaced F86 Site 2 still leaking — 80 `Opt_DroneOverhaul` orphan errors, owner-ruled out of scope → routed to prompt 5** · **MOD_DESCRIPTION no-precedent sentence NOT published** (unsoftened; its gate corrected from Tier 1 to Tier 2) · consumed `F86_TIER1_BUILD_PROMPT.md` + itself |
| 5 | ~~`5_f86_phase3_tier2_build_opus.md`~~ **SPLIT 2026-08-01 (rule 3), file consumed** | Opus | — (builds needed none) | **TIER 2 BUILT** (`88f3154`, `44e6af2`, `6f0cb95`, `ef7d49c`, `e197190`; TestKit `6eb3c0b`, `7bfa274`) — all four modules moved off their blocking bodies onto verified-synchronous seams, **F86 Site 2 repaired**, **the `ArrivalDeaths`(a) design pass RUN and it found a route** (so it was built, not deferred; nothing routed to prompt 12) · per-site dispositions recorded for all five sites · F21 **downgraded `tested`→`fixed`** (PT-43 measured a retired body) · three probes realigned off replaced behaviour, sweep clean · **PT-58 written with predictions P1-P7 BEFORE any run** · ⚠️ **the leg is UNRUN, so the D10/D12 unhold is NOT recorded** → 5b |
| 5b | ~~`5b_f86_phase3_tier2_leg_opus.md`~~ **✅ DONE 2026-08-01, file consumed** | Opus | — (PT-58 run with owner) | **✅ TIER 2 VERIFIED — PT-58 PASSED ALL SEVEN PREDICTIONS AND F86 SITE 2 IS CLOSED** (`77683c8`; logs `Mars.exe-20260801-21.27.58` pack-ON / `-21.54.16` pack-REMOVED, test-2 lineage, article `T2-UNINSTALL`): **zero `Opt_DroneOverhaul` orphan errors against leg 5's 80**, over 73 idle drones; zero `[LUA ERROR]` of any kind; zero mentions of any Tier-2 module. **Both proven leak sites repaired and verified** · ⭐ **D10/D12 UNHELD** (9/10 runnable) · **no status flip earned** — F53/F55/F21 stay `fixed`, the leg verified save safety not function · **MOD_DESCRIPTION note UPDATED, NOT deleted** — Reading A's precondition is met but the owner deferred the wording call to launch, and 5b's delete instruction was wrong · 3 limits recorded (moonlight gate untested, no Tier-3 clearance, P2's second clause unmeasurable in retail) · one item routed to prompt 12 (asserts-build introspection sitting, clears 8 `[install]` SKIPs) |
| 6 | ~~`6_audit_candidate_sweeps_opus.md`~~ **✅ DONE 2026-08-01, file consumed** | Opus | — | all four sweeps run, **two overturn a recorded audit verdict** (`BUG_LIST_AUDIT.md` **§10**, new): **C04 CONFIRMED → filed `F90`** (surface dust storms break underground cables/pipes — full chain read, cross-map `MultiMapSupplyGrid` fragment + map-blind `table.rand`; sibling tell: the production pass 16 lines above guards the shared-fragment case and the break pass does not; **nothing built** — §3a shape question → prompt 7) · **C32 DOWNGRADED** (no route in current Src; GromGor's predicate fires on destroyed-but-unrebuilt buildings; owner's 1.0.7 challenge answered **"trigger yes, mechanism no"** — asteroids never expire on 1.0.7) → **F04's §9 demotion lost its reasoning; the tier decision is prompt 7's** · **F35 scope re-confirmed from source** (the broken migration fixup is still broken verbatim; nothing else covers `WindTurbine_Large`; a second unrecorded reason the pass must hook `PostLoadGame`) · **fredware #11 = a real GAP, not an overlap → `C35`**, deliberately NOT promoted (harm unproven) · C32's live rider **rewritten, not deleted** (old trigger no longer occurs; old pass/fail rule would have false-positived on any meteor strike) · owner web-checks (a)+(b) still open → **routed to prompt 12** · STATUS counts re-derived (**102 rows = 90 F + 12 D**, 35 C) |
| 6b | ~~`6b_residual_candidate_sweeps_opus.md`~~ **SPLIT 2026-08-02 (rule 3) after 5 of 8 sweeps, file consumed** | Opus | no | **C18 CLOSED `wontfix — intent`** (labels are exact-string with no inheritance, `Building.lua:370-425,:427-444`; the tech names its four buildings and pays those four) — **and the hunt for its intent statement found a real defect next door: `C38` filed, VERIFIED, → prompt 7** (Astrogeologist promises an unqualified "Extractor production +10%" and pays 10 of the 12 buildable extractors) · **C19 CLOSED — declined**: Relaunched put the distance term at the *consumer* (`Dome.lua:256-259`, `ColonistMinDistToIgnorePassage` 1200m, design comment on the line) plus an 8-dome hop cap; the residue is F52's deliberately-open half, and ⚠️ **taking ChoGGi's OG shape would have degraded F53's reachability test** · **C20 mechanism LOCATED** (the only `Msg("SectorScanned")` emitter is a game-time thread opening with `Sleep(10)`; probe scans have no pause gate) but **deferred-vs-lost is CANNOT DETERMINE** → checklist rider written using the HUD toast as an exact free proxy · **C21 destruction route VERIFIED → prompt 7** (Sinkhole is the only mystery set-piece in the game without `indestructible`; full meteor→`DoneObject` chain read) with the soft-lock **located not proven**, and the obvious guess — meteors killing the anomaly — **checked and ruled out** · **C25 patch question answered from source**: 1.0.6 replaced the entire Jumbo Cave scenario (`Anomaly.lua:26-33`) and left this wedge byte-identical, ⚠️ and that rework is **save-vintage gated, not build gated** (`UndergroundDome.lua:16-19`) · two checklist riders added (C20, C25) |
| 6c | ~~`6c_residual_candidate_sweeps_part2_opus.md`~~ **✅ DONE 2026-08-02, file consumed** | Opus | no | all three jobs run; **nothing filed, counts unchanged and re-derived** (102 = 90 F + 12 D; 38 C) · **the five SkiRich OG candidates went 4-0 against their own reading**: **C27, C28, C29, C30 all CLOSED — no defect in Relaunched**, and ⭐ **three of them looked like "the missing code" only because the code is written somewhere other than where the symptom points** (Signal Boosters commits the extender's radius through the *hub's* `Effect_Code`, whose forced reconnect recurses into `linked_extenders`; supply-pod pins survive a generic path that really would strand them — `PinnableObject:Done` unpins **without force** while `RocketBase:CanBeUnpinned()` is unconditionally false — only because every rocket class force-unpins in its **own** `Done`) · **C26 stays `cand`, CANNOT DETERMINE** — the engine ships two savegame heals named for the exact symptom (`RequiresMaintenance.lua:531-574`) but ⚠️ `AppliedSavegameFixups` is pre-seeded at new-game, so they never run on a save started on our build (C25's vintage gate again); no producer in Src, both obvious guesses ruled out in writing, next step is a console dump → playtest, not chain · ⚠️ **6b's label rule CORRECTED in three places** — `AddToCityLabels` is a **combined method**, so parents DO contribute labels; **C18's verdict is unaffected** (parents contribute *role* names, never *building-type* names) and the corrected rule + the colony-vs-city forwarding fact are now in `ENGINE_FACTS.md`, as is `Init`/`Done` combining with **opposite order** · **F82 TRACE FINISHED — mechanism found, and it is neither option the entry weighed**: no removal path exists at all (the notification registers a **position**, not the grid), it clears only on the preset's `Expiration = 120000` with `GameTime = false`, which runs on a **real-time** thread → **2 real minutes regardless of game speed**; sibling tell is `PowerLeak`/`LifeSupportLeak` carrying no `Expiration`; ⚠️ symmetric half not in the original report — an **unrepaired** split also stops being reported — **routed to prompt 7** (the removal *key* is the design question) · **F80 SOURCE AUDIT RUN — theory survives with an exact predicate** (`traverse_dir` never normalised to ±1, `TrainTransport.lua:374`; a stride ≠ ±1 hits a missing `link_edge` and **hard-returns** `:417`, dropping the tail silently) and it explains **both** public symptoms from one function (waits *and* walks); ⛔ **trigger NOT proven — stays `investigating`**, and the old "canonical orientation" suspicion is **corrected** (it is the magnitude, not the sign) · **both checklist riders rewritten**: F82 to a falsifiable ≈120 REAL seconds with a game-speed discriminator, F80 to waits-vs-walks plus a one-way-hole test that can falsify the theory outright |
| 7 | ~~`7_audit_candidate_decisions_opus.md`~~ **✅ DONE 2026-08-02, file consumed** | Opus | **review — 2 open decisions** | all ten packages run, **each sub-item its own §4 subject**. **SIX F-ROWS FILED, APPROVED AND SPECCED, NONE BUILT** (→ prompt 8): **F91** track-shell leak (C33 — and our own F44 path reproduces it; fix is an amendment to `Fix_TrackSalvageWipe`, no new module) · **F92** Saint blessing label mismatch (C22 — **§1.1 preset patch**, and Saint is R1, not the "breakthrough trait" the brief guessed) · **F93** dust-devil descriptor read from the **camera's** map (C23 item 2) · **F94** asteroid-visit precedence (C24 — **R1 = any parked supply rocket**; ⚠️ it must own the predicate, so F72's advertised chaining is lost) · **F95** Astrogeologist's short extractor list (C38 — settled by the unused shared `Extractors` label: the curation rule is "every buildable extractor", and 12/10 closes exactly) · **F96** the destructible St. Elmo sinkhole (C21 — one boolean, side effects enumerated). **F90 APPROVED with its shape ANSWERED: §3a LAYER 3, no body copy** — and ⛔ the "cheaper-looking" `Break` interception is **unsound** (8 call sites incl. `CaveInRubble`, so it would eat legitimate cave-in damage) · **F82 CLOSED `wontfix — intent`** (a timed *event announcement*, not a state warning — three controls; its P3-vs-P2 question is void) · **C23 item 3 declined on shape** (all four routes over-reach; F89's disposition) · **F04 tier DECIDED — option (ii)**: the Steam thread is **unassigned**, not C32's; GOLD not restored because that would be the same 1.0.6-era claim with the sign reversed · counts re-derived by counting: **108 rows = 96 F + 12 D**, 38 C. ✅ **PACKAGE 0 DECIDED BY THE OWNER, SAME DAY: CONVERT** — F29 items 1+3 and F57(a) become §1.4 wrappers in prompt 8's batch, after which the pack holds **zero R3 §1.5 replacements** and one more persisted mod field leaves the save. ⚠️ **C23 ITEM 1 ALSO DECIDED, SAME DAY — BUILD IT, PROVISIONALLY** (owner: *"not locked. I want the QA run to personally review it"*). The intent case became data-driven — **`DustDevils_VeryHigh_3` is authored `6..8 @ 50%` and the multiply makes that range unreachable**, plus three controls for gate-then-count and OG presets identical across all eight. ⛔ **But the cost rose in the same session**: the line is inside the `DustDevils` thread body, so the fix **owns a sleeping game-time thread = a 14th §3a site**, carrying the F88 timer-re-roll trap and the `Fix_MeteorFrequency` uninstall trap. Routed to prompt 8 with the spec and a self-split warning; **prompt 12 gains JOB 8 — personally review the decision, reversal is a legitimate outcome** |
| 8 | ~~`8_f86_phase4_conversion_batch_opus.md`~~ **SPLIT 2026-08-02 (rule 3) after the whole conversion batch, file consumed** | Opus | — (conversions needed none) | **EVERY CONVERSION IN THE BATCH IS DONE, one commit each** — §5.4-A: `Fix_SmallLandscapeSites` (`69c02b9`, **zero copied logic** — the shipped function already took the bound as a parameter, so §5.4's "no wrapper can reach the loop" objection was correctable, not merely superseded), `Fix_NightShiftWork` (`26f0b57`, shift 1/2 now identical **by construction** instead of by a hand-verified byte claim), `Fix_GeneForging` (`ab7d432`, additive, now via `SetGlobal`'s §1.4b read-back), `Fix_ShuttleHubOffAvailable` (`388c72a`), `Fix_UpgradeModifierLeak` (`21990fb`, `orig` proven a no-op from `Building:Init`+`ApplyUpgrade`) · **PACKAGE 0 BUILT** — F29 items 1+3 (`1471533`; item 1's post-wrapper **retires an unrecorded risk** — the copy truncated `GetObjectsByLabel`'s return assuming it was not the live label, true but never written down, now verified; item 3's pre-wrapper reorders before `AddToLabel`/`SetCommand`, checked safe against the **measured deferred-GT-creation** fact) and F57(a) (`8f58f30`) → ⭐ **the pack now holds ZERO R3 §1.5 replacements**, and `SMRFixPack_rocket_fuel_key` is gone **from existing saves too** (the spec said the wrapper needs no memory, which does not by itself remove the field a save already carries) · ⛔ **`Fix_TrainCargoDumping` NOT CONVERTED — stop condition 1** (`10cd2b4`): §5.4's "verified feasible" route **does not exist** — `GetTargetAmount` is a **native** `TaskRequest` metatable method published as a savegame **permanent** through the mod-blacklisted `PersistGatherPermanents`, 148 call sites, no key; the one route that would work mutates a **persisted property** on a live object, worse on §3a grounds than the copy — **moved to group C, §5.4 counts corrected to 5/4/10/3**, routed to prompt 12 for a second opinion · **nothing filed or closed, counts unchanged and re-derived** (108 = 96 F + 12 D; 38 C) · ⚠️ **the conversions are UNRUN** — equivalence is argued, not observed; the leg is 8b's · two job-7 drift instances appended (a **table column that asserts its own verification** over a list, laundering every row to the best one; and a module summary that repeated a reason its own body had withdrawn eight days earlier) |
| 8b | ~~`8b_f86_phase4_approved_fixes_opus.md`~~ **DONE 2026-08-02, file consumed** | Opus | — (PT-60 run with owner) | the seven prompt-7-approved fixes (F90-F96) + their probes + **ONE leg covering them AND prompt 8's eight unrun conversions** |
| 8b2 | ~~`8b2_pt60_leg_completion_opus.md`~~ **✅ DONE 2026-08-02 (`b23e22e`), file consumed** — *(row was left un-struck until chain prompt 9 caught it 2026-08-02; logged as a job-7 instance)* | Opus | launch leg | **SPLIT from 8b 2026-08-02 (rule 3) with PT-60 PARTLY RUN.** 8b built all seven fixes, wrote seven probes, and ran most of the leg with the owner: ⭐ **F95's defect AND fix observed** (`1 1 0 0` → `1 1 1 1` on one colony) and ⭐ **F90's defect OBSERVED IN PLAY for the first time** (constructed fixture, `underground 0 → 15` during a surface-only storm, plateauing when it ended, cave-ins swept out); **all 15 changes install `active` (79/79) with zero `[LUA ERROR]` on the ENABLE PATH**. ⛔ **Carries one UNVERIFIED FIX**: `Fix_AstrogeologistExtractors`' heal was **not idempotent** (identity-keyed presence test cannot match across a save boundary → +10% per load, unbounded); fixed, fixture `f95 healed` waiting. Remaining: F96's designed-but-unrun live test, F91, F90's pack-ON leg, and the main `TEST 2H` run |
| 8c | ~~`8c_c23_dust_devil_count_opus.md`~~ **DONE 2026-08-02, file consumed** | Opus | launch leg (**PT-61 owed**) | **C23 item 1 BUILT as `F97`** (`Fix_DustDevilSpawnGate`, `b43f1d9`; probe + `DustDevils` logger, TestKit `7733f79`) ⭐⭐ **AND THE §3a COST ITS APPROVAL ACCEPTED DID NOT MATERIALISE — the entry's route claim was FALSIFIED on re-verification.** The spec's *"the only precise route is owning the scheduler body"* is wrong: `GetDustDevilsDescr` has **three callers, all inside that one thread**, and the descriptor read is **1:1 with the count draw**, so §3a **layer 3** reaches the defect by *substituting* the descriptor rather than mutating it — the possibility the spec's "shared preset table" reasoning skipped, and the route the binding 3→2→1 ordering therefore requires. **Owner confirmed the route change before any code was written.** Built as a §1.4b post-wrapper on `OverrideDisasterDescriptor` (4 callers, one per disaster, keyed on `original.class`), deliberately one level below `GetDustDevilsDescr` so it never contends with F93, whose `DataPatch` runner re-installs its own body whenever that global holds anything else. **Result: no 14th §3a site, no sleeping mod thread, no §1.5 reconstruction, no version latch, no restart and therefore no F88 timer re-roll**; existing saves are reached with no load-time action, uninstall self-heals within one wave, and the only residue is an inert plain-data descriptor copy marked `SMRFixPack_spawn_gate` that carries no function values by construction (§3a per-site disposition: `SAVE_SAFETY_REDESIGN.md` **§8**, recorded as **no new site**) · ⚠️ **UNRUN — the leg is `PLAYTEST_CHECKLIST.md` PT-61**, attended, **predictions P1-P10 written before any run**, with a within-session A/B (`SMRFixPack_Disabled.DustDevilSpawnGate`, honoured per call) and **two setup traps recorded that would each cost a sitting** (a terraformed colony produces zero dust devils for unrelated reasons; setting `DustStormsDisabled` parks the whole scheduler) · ⚠️ **the RATE question is NOT closed** and the approval stays provisional · **prompt 12's job 8 CORRECTED IN PLACE** — half of it was dissolved by the build, and reversal now costs almost nothing · **three job-7 drift instances routed**, one of them a **new axis**: a *route* claim that was wrong while every citation under it was right, and wrong in the **opposite direction** to prompt 8's phantom route the same week · counts re-derived: **109 rows = 97 F + 12 D; 38 C; 80 modules / 74 default-active; 86 probes** |
| 9 | ~~`9_d10_workshops_build_opus.md`~~ **✅ DONE 2026-08-02, file consumed — ⏸️ NOTHING BUILT, BY OWNER DECISION** | Opus | decision TAKEN | **D10 is PARKED / on hold — low priority, post-release** (owner, 2026-08-02, mid-prompt): the module ships opt-in as specced, but its text goes through **our own `ModItemLocTable`** rather than English-only strings, which is what moves it behind release. Nothing technical blocked it (F86 hold discharged, PT-56 passed) — it was re-prioritised · **no code, no probe, no PT-57 row, no leg** · ⛔ **the approved spec was RE-DERIVED FROM Src FIRST AND THREE CLAIMS FAILED**: there are **four** workshops not three (`TVStudioWorkshopCCP1` — DLC in the OG game, base content in Relaunched, so every player can build it), the `upgrade1` citation is **false** (no `upgrade1_id`, so that modifier never applies — ⭐ a false citation under a *true* conclusion, which survives on `Workplace:OnModifiableValueChanged`), and unemployment clauses are on **7 of 29** faction defs not every one; plus two facts the spec never had — `max_workers` is **hard-clamped at 20** while `consumption_amount` is not (VR lands *exactly* on 20 at +100%, so a paired dial must clamp its own percent per template), and raising capacity without staffing it **lowers** the Comfort payout · ⭐ **the routed [S36] "devs fixed unemployment in 1.0" claim CHECKED AND FALSE** — `StatusEffect_Unemployed` is still icon-only, so D10's premise held · ⭐⭐ **TWO DEFECTS FILED EN ROUTE, NEITHER FINDABLE FROM THE D10 PLAN**: **F98** — re-checking the precedent *F84's own entry had asked to be re-checked* showed **our shipped `Fix_TechDescriptionBuilding` (F25) is a NO-OP in retail** (re-used translation ids are discarded at `T()` construction), so **F25 is demoted in both places and is no longer localisation precedent**; and **C39** (`Policy_Automation_ServiceAutomation` cuts `max_workers` by *label* while its performance compensation keys on *class*, and the four Workshops are the only label members outside it — sign NOT determined, needs eyes) · ⛔ **F84 UN-BUNDLED from D10** (T1 is all appends and costs nothing in any language; F84(a) is a deletion — never the same question) and **its user decision is now TAKEN**: route 3, our own loc tables · counts re-derived by counting: **110 rows = 98 F + 12 D; 39 C**, modules/probes unchanged at **80/74** and **86** · three things routed to prompt 10, four job-7 instances to prompt 12 |
| 10 | ~~`10_d12_no_homeless_build_opus.md`~~ **✅ DONE 2026-08-02, file consumed — ⚠️ BUILT BUT UNRUN** | Opus | launch leg (**PT-62 owed**) | **D12 BUILT as `Opt_NoHomeless`** (`14e708f`; probe + TestKit `a979270`) — opt-in/off by default, own module, own flag, own gate, `Opt_ResidencyControl` as donor PATTERN only; the wrapper acts **only when the composed answer below it is empty**, which makes it order-independent against D07 on the same seam and is exactly the tie case, and it can only ever return a Community from the city label so **no colonist can be put outside by construction** · ⛔ **THE PREMISE WAS RE-DERIVED FIRST AND IT HELD BYTE-VERBATIM** (`Colonist.lua:2675`/`:2676`/`:2680-2681` in the pinned build), so **the routed [S36] "1.0 squashed homelessness" claim is FALSE for this half** — ⚠️ but **one citation in the approved spec was off by a hop**: `FindEmigrationDome`'s only caller is `Colonist.lua:1603` inside `TryToEmigrate`, not the heavy-update block the entry named (substance survives, corrected in place, routed to prompt 12's job-7 list as the **mirror image** of prompt 9's false-citation-under-a-true-conclusion) · ⭐⭐ **THE ROUTED PLAYER HYPOTHESIS WAS RIGHT, AND CHECKING IT BEFORE BUILDING IS WHAT CHOSE THE DESIGN** — filed as **`C40`**: `Crowded Living` grants **+3 `Residence.capacity` gated on the Ministry of Culture's LIVE `working` flag** and every withdrawal **EVICTS** the tail residents colony-wide (five-link route verified vs Src; ⛔ the gating is **intended** and the ministry advertises it, so the open part is the **law's own description**, which interpolates the static parameter only — harm unproven, frequency unmeasured, nothing built) · ⭐ **the entry's OPEN QUESTION IS DECIDED — the NARROW reading** (push only a colonist for whom no residence in their dome is `IsSuitable`), on three reasons, and the third is C40: under the **broad** reading a colonist evicted by a transient ministry outage would have been shipped out of their dome **permanently**. **The trap was invisible from the D12 plan** · one deliberate departure from the spec recorded on the entry (the destination must have *suitable* housing — a **suitability** test, not a free-space test, because free slots colony-wide were **zero** in the origin scenario and a free-space filter would have made the module inert in its own case) · **PT-62 written with predictions P1-P13 BEFORE any run**, incl. a **narrow-reading control**, a **no-destination control**, and a Mod-Manager-disable uninstall half; ⚠️ **the leg needs a PROVISIONED FIXTURE** (a specialist dome holding colonists it cannot house, in a colony with no spare beds) and four setup traps are recorded, the first of which reads exactly like the fix doing nothing · counts re-derived by counting: **82 `Code/` files; 81 modules / 74 default-active** (opt-in adds none); **87 probes**; **110 rows = 98 F + 12 D; 40 C** · **PROBE SWEEP: clean** · three things routed to prompt 11 — ⚠️ **two of them CLOSED later the same evening**: `DustDevilSpawnGate` **PASSES** and the **F98 loc control RAN and returned `userdata`** (log `Mars.exe-20260802-20.43.08`; the one FAIL before it was a *probe* defect, `forbidden` used as scaffolding colliding with 8c's real guard). Only **C40** stays routed · ⭐ **the D12 RULE WAS THEN REWRITTEN by owner review** — subject test is vanilla's `need_work` (move the unemployed; never the employed, never anyone the game calls outside the workforce, so a homeless Senior stays as the build-more-retirement-homes signal), plus a **dome precondition** (a Nursery or Retirement Home must be built, tourist housing excluded) and a rebuilt infopanel row that states its own consequence. ⚠️ **PT-62 PARTLY RUN 2026-08-02 (attended) — NOT PASSED, and the results are written up at the top of the checklist item.** ⭐ **The core seam result IS established**: same colonist, same moment, `vanilla says false nil` → `with D12 DomeBasic shuttle` — the tie is broken, and the destination reads `free 0`, which is the suitability-not-free-space design working. P5 (nobody outside except to board transit), P8b, P11 (zero `[LUA ERROR]` under load) and the suite all passed, and the exemption was **observed live and unprompted** (17 homeless → 13 movable, holding back Seniors, the employed and the transiently unable) · ⛔ **P4/P6 NOT ESTABLISHED** — the drain fought an inflow (20 → 43 homeless while flagged) and those numbers are recorded as meaning nothing · ⭐⭐ **AND THE LEG FOUND A REAL DEFECT THE READING COULD NOT**: the policy pushed but never DECLINED, so vanilla re-offered the same dome to the people it had just moved out — **measured at 6 colonists en route INTO flagged domes**. Three changes built in response, **all UNRUN**: the symmetric veto, a `ChooseDome` filter for the second entry path (arrivals, which the emigration veto never saw), and a repair to the A/B lever `SMRFixPack_Disabled.NoHomeless`, which was **silently dead** — the PT-61 trap again, *a failure that looks like an answer* · ⚠️ **the D12 RULE CHANGED FIVE TIMES under live owner review in one sitting, so the adjudication was ROUTED rather than self-approved — prompt 12 gains JOB 9** (owner: *"let the Fable QA session look at our findings before we make any hard decisions. That is the purpose of the task chain"*), with the two hardest calls flagged for independent checking: the veto **overrides a positive shipped answer** for the first time, and the `ChooseDome` filter is trait-based and cannot see employment · one pack-wide gap found and deliberately NOT widened into: `SMRFixPack_Disabled` is dead for **D03 and D07** too |
| 11 | ~~`11_f76_depot_picker_repair_opus.md`~~ **✅ DONE 2026-08-02, file consumed — ⛔ NOTHING BUILT, AND THE ENTRY WAS REFUTED RATHER THAN REPAIRED** | Opus | attended sitting RUN | **F76's recorded mechanism is FALSIFIED and both its claims are UNREPRODUCED; disposition ROUTED to prompt 12 JOB 10 by owner decision** (not self-closed — this session would have been closing a P1 by re-reading the report of the person who is also the adjudicator, on partly-negative evidence) · ⛔ **JOB 1 (game-free design) REFUTED THE ENTRY FROM SOURCE BEFORE ANY LIVE WORK**: `XWindow.box` and `terminal.GetMousePos()` are **one coordinate space** (`scale` multiplies sizes, never coordinates; the decisive control is vanilla's own `XDialog.lua:139`, which feeds the mouse position straight into `GetMouseTarget`, which hit-tests `.box`), so the entry's "coordinate-space mismatch" cannot happen and **its "Fix sketch" would have INTRODUCED the displacement it meant to remove** · the click-through needed no second defect (`ItemMenuBase:OnMouseButtonDown` hands L-clicks to the world, `ItemsMenu.lua:510-518` → `SelectionChange` → the picker closes itself) · ⚠️ **the entry's one numeric forensic evidenced nothing** — `box=(886,13)-(1054,442)` solves backwards to a single anchor ≈(969,442) with **no clamp firing**, and the separately-recorded `(1731,665)` anchor implies scale 1.786 in x / 1.504 in y, so the two readings are **different clicks and may not be paired** · **ATTENDED SITTING RUN**: **M1** `UIL.GetSafeArea()` → `0 0 3840 2160` (whole screen — the clamp reading is dead); **M2** `anchor (2051,887)` vs live `mouse (2058,885)`, `box (1967,458)-(2135,887)` centred on anchor x with its **bottom edge AT anchor y**, container overhang `90 × 1.425 = 128`/side, ⭐ **`mw 168 / mh 429` byte-for-byte the 2026-07-27 forensic** — same scale, so **the original box was correct placement too**; **click test DID NOT REPRODUCE** (owner clicked the hex, it loaded — recorded as did-not-reproduce, not as fixed, and the `F76MISS` hook was not installed so its silence proves nothing) · ⭐⭐ **THE REPORTER'S PREMISE IS CORRECTED** (owner, unprompted: *"I was under the impression that the resource picker was a rendering we was doing as a bug fix, not vanilla"*) — it is vanilla, **verified by grepping all 82 `Code/` files**, F74's only hook being a refuse-only guard that pass-throughs on a depot; the load-bearing *"this giant machine parts logo isn't normal"* was therefore an **interpretation, and it is the one the entry is named for** · ⛔ **`MOD_DESCRIPTION`'s F76 `[DRAFT NOTE]` BLOCKED IN PLACE** (it promises players the exact claim measurement falsified) · ⚠️ **the OG witness is a DIFFERENT SYMPTOM** (`BUG_LIST_AUDIT.md` §2.2, *"the icon … does not appear"*), never reproduced, **deliberately NOT filed as a new row** — filing it would have changed the counts on a session whose finding is that this entry over-claimed → job 10 · **M5 left open for OTHER setups**: one pass logged `mouse (6148,2350)` outside `desktop.box`, which would slam the dialog into a corner — but the setup is unchanged since 2026-07-27 and passes #4-#7 read `anchor ≈ mouse`, so there is **no persistent offset on this machine** · ⭐ **three ENGINE_FACTS banked regardless of F76**: box/mouse are one space; `UIL.GetSafeArea()` returns four **absolute** numbers **and the global `GetSafeArea()` documented at `LuaExports.lua:488` DOES NOT EXIST** (stale exported doc); `terminal.desktop.scale` as a read-back control for the user's UI Scale option · item 1 (`DustDevilSpawnGate`) needed no action — prompt 10's own correction discharged it · counts re-derived by counting and unchanged: **82 `Code/` files; 110 rows = 98 F + 12 D; 40 C; 87 probes** · **PROBE SWEEP: clean** · **three job-7 seed lines**, one a NEW axis: **a hedge does not survive being quoted** |
| 12 | `12_final_qa_backward_check_fable.md` | **Fable** | review | ⭐⭐ **JOB 0 FIRST (owner, 2026-08-01, "we cannot be wrong about this"): re-verify the "OFF is three different things" doctrine against PRIMARY evidence, then sweep ALL documentation including playtest RESULTS on the answer** — it re-sizes D13 (if it holds, the cleaner's population is every player who ever installed the pack, whatever their toggles) and voids any disposition that leaned on a module being opt-in/off-by-default · then the backward QA over everything: notes consumed, docs consistent, nothing owed/missed/wrong; writes the QA report; **then (job 7, owner-requested 2026-08-01) the documentation structure review — assess every doc the agents read/maintain and recommend how to stop the recurring drift; `DOC_STRUCTURE_REVIEW.md`, recommendations only**; empties this folder |

Model routing lives in the FILENAME only — prompt bodies are model-neutral
(project rule). ~~Order is strict through 8c~~ **— 8c is DONE, so the strictly-ordered
stretch of the chain is finished; what it leaves owed is a LEG (PT-61), not a
prompt.** (The rule was: **8b before 8c**, because F93 and C23 item 1
patch the same dust-devil subsystem and building both against one unrun leg
makes the result unattributable. It held — 8b's PT-60 ran first, and F97 was then
deliberately placed one call below F93's seam so the two cannot interfere.)
Prompts 9-11 are independent of
each other and may run in any order after their gates open (9/10 after **5b**
verifies — prompt 5 built the repairs but did not run their leg; 11 anytime
after 1). 12 runs last, when the folder holds only it
and this README.

## ⭐ Owner blanket pre-clearance (2026-08-01, recorded verbatim in intent)

The owner cleared, in advance and "regardless of flags", **all work items
derived from the 2026-08-01 audit-and-adjudication conversation** that the
chain's author judged appropriate. Scope and limits, stated precisely:

- **What it clears:** the approval STEP on items already evidenced in that
  conversation — the §4 amendment adoption (prompt 1), the
  `Opt_DroneOverhaul` carve-out (prompt 5), building the audit's verified
  candidates when their own §4 package passes (prompts 6b/7/8), and the
  residual-candidate sweeps (prompt 6b).
- **What it does NOT clear:** the EVIDENCE bars — a package that fails its
  own intent/reachability/§4a test still does not ship (the clearance
  removes the ask, not the bar); anything the owner previously REJECTED
  outright (e.g. the C1 wording fallback) stays rejected; NEW scope that
  did not come from that conversation still stops and asks (rule 2 below).

## Chain mechanics (binding for every prompt here)

1. **Inbox/outbox.** Each prompt ends with a `## Notes from upstream` section.
   On completion, a session APPENDS its handoff notes to the NEXT prompt's
   `## Notes from upstream` (or to whichever later prompt actually owns a
   discovered item), commits, then **deletes its own file in the same commit**.
2. **Discovered work is routed, not dropped.** New owner-work found mid-prompt:
   decide WHICH later prompt owns it and WHEN it must happen, append it there
   with one line of reasoning. **Unsure which prompt owns it → STOP AND ASK
   the owner** — never guess a route, never silently expand your own scope.
3. **Self-split on context pressure (owner rule).** If a session judges the
   remaining work might not fit its context, it STOPS at a clean commit
   boundary, writes a continuation prompt (same number + letter suffix, e.g.
   `5b_…_opus.md`) carrying precise state + remaining units, commits, and
   ends. A continuation prompt is a normal chain member. Never push a job to
   the edge of a context window.
4. **New defects:** file in `BUGS.md` (never here, never FUTURE_IDEAS), then
   route any follow-up per rule 2.
4b. **Documentation drift is EVIDENCE — capture it even when you fix it**
   (owner, 2026-08-02). Prompt 12's job 7 diagnoses the project's doc
   practices from a corpus of observed drift (stale tags, diverged
   duplicates, dangling pointers, expired gates, counts gone stale). Any
   session that catches an instance appends one line to prompt 12's job-7
   seed list — what drifted, where, how caught — **even if the fix took ten
   seconds**. A silently-corrected instance is destroyed evidence; the owner
   is deliberately accumulating this corpus through prompts 7-11 before the
   review runs.
5. **Every prompt obeys `WORKFLOW.md` "Authoring a prompt" elements 1-7**:
   live todo list (one item per commit-and-verify unit), `git log`+`git pull`
   staleness check first, scope fence, stop conditions, what-may-not-be-
   claimed, self-deletion, and the stale-probe gate before ANY test.
6. Commit convention:
   `git -c user.name="SMR-BugFixPack" -c user.email="154917955+catt144@users.noreply.github.com"`,
   messages via `git commit -F <file>` (no embedded double quotes), push the
   fix pack (TestKit stays local-only).
7. **STATUS.md is updated by every session that changes counts or state** —
   counts live there and nowhere else.
8. ⛔ **SEALED DOCUMENT — `docs/reports/BLIND_AUDIT.md` (owner rule,
   2026-08-02).** **Every prompt in this chain except 12 is FORBIDDEN from
   reading, grepping, summarising or acting on that file or its contents.**
   It is a **blind control**: a fresh session produced it having deliberately
   read no project docs, and its whole evidential value is that it was written
   without the project's own conclusions in view. **Prompt 12 job 6b** examines
   its merit against the full record it was forbidden to see — doing its own
   pass FIRST, then opening the sealed key — precisely so that neither reading
   anchors the other. **A prompt that reads it early destroys that
   independence, and the contamination cannot be detected afterwards.**
   If a broad search surfaces its contents by accident: **stop, discard, do not
   use it, and record that it happened in your handoff notes.** The blanket
   pre-clearance does NOT cover opening it; only prompt 12, at job 6b, may.

## Deliberately NOT in this chain (recorded so QA doesn't flag them)

- **The drone track** (D06 rebuild, D08, drone playtests, cleanup mod):
  owned by `docs/prompts/DRONE_PROJECT_PROMPT.md`; its design decision is a
  standing owner decision. PT-52 A/B/B2 stay frozen.
- **D13 (uninstall procedure + standalone cleaner)**: its own spec gate
  forbids speccing before Tier 1/2 land AND verify, and it is release-phase
  work — it comes AFTER the playtest campaign, before release. Prompt 12
  carries it forward as the standing next item.
  ⭐ **Clarified 2026-08-01 (owner): D13 is a HARD LAUNCH DEPENDENCY — "we
  won't launch till it does."** Its absence from this chain is a *sequencing*
  fact, not a deprioritisation: the cleaner's target list is the OUTPUT of the
  builds, so it cannot be designed until they are done. **The corollary binds
  every prompt here: build every reachable repair NOW, and never defer a site
  to the cleaner as a way to descope** — a cleaner hand-off is a valid
  disposition only *after* the in-pack attempt has been made and the route
  proven absent (FIX_POLICY §3a).
- **The playtest campaign itself** (PT-53, needs-eyes riders, the PT backlog):
  that is what the owner is freed FOR; prompt 1 leaves the checklist current.
- **Pack split / D11 / C1 wording fallback / FUTURE_IDEAS**: undecided or
  parked by explicit owner rule — not work.
- ⏸️ **D10 (workshops) — PARKED 2026-08-02 (owner), post-release, low priority.**
  Its chain prompt (9) ran and consumed itself, but **built nothing**: the owner
  re-scoped the item mid-prompt to ship its text through **our own
  `ModItemLocTable`**, which the pack has no pipeline for yet. Like D13 this is a
  *sequencing* fact — nothing technical blocks it — but unlike D13 it is **not a
  launch dependency**. **It is not owed and must not be reported as outstanding.**
  What the prompt left behind is worth more than the build would have been: a full
  route re-derivation on the D10 entry (three approved-spec claims falsified),
  plus **F98** and **C39** filed en route. **F84 travelled with it** — its user
  decision is taken (our own translations) and it is now blocked on that same
  capability, so it too lands after release.
