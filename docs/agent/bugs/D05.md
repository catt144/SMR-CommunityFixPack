---
id: "D05"
seq: 68
row: 68
title: "Opt-in modules had no player-usable enable surface"
status: "tested"
status_source: "tag"
priority: "dsgn"
evidence: "high"
row_status: "tested — PT-51 PASS 2026-07-27 (entry)"
updated: "2026-08-12"
copies: []
moved_to: "C:\\Dev\\SMR-OptInPack\\docs\\agent\\bugs\\D05.md"
---
### D05 — Optional modules had no player-usable enable surface  `[tested 2026-07-27 late (PT-51 PASS in full, archive): items.lua Mod Options toggles + metadata default_options + 00_Core bridge; probe OptionsMenu PASS in all armed legs; live both ways, restart-persistent, log clean — ⛔ MOVED 2026-08-12 → `SMR-OptInPack/docs/agent/bugs/D05.md`, which is now the record]`

⛔ **MOVED 2026-08-12 — this entry's record now lives in the Community Opt-In Pack.**

`C:\Dev\SMR-OptInPack\docs\agent\bugs\D05.md` — moved whole and byte-preserved by
the `split-optins` chain when `(the Mod Options enable surface itself)` left this pack (commit `0efb87e`).

The file stays here as a signpost: `INDEX.md` is GENERATED and `seq` must run 1..N
contiguous, so a deletion would renumber a hundred unrelated entries, and hundreds of
historical references to **D05** would resolve to nothing instead of one hop away.
The front matter above is the row as it stood at the move; the status word is kept
because the record did not change — it relocated.

