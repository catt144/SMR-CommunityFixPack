---
id: "C92"
seq: 188
row: 223
title: "Boundaries of Knowledge counts the hidden, unreachable UndergroundExploitation tech; repeatables retain completion and are not the blocker"
status: "cand"
status_source: "tag"
priority: "P2"
evidence: "owner verified the clean current-patch original and all non-repeatable tech complete; read-only retail census of a copy on 1.1.0.403908 finds UndergroundExploitation the sole failed counted requirement, hidden with no normal reveal/research route; both repeatables completed once; desk controls execute shipped state and achievement bodies; no live achievement award attempted"
row_status: "cand 2026-09-13 - filed from a Steam bug report; original repeatable cause withdrawn after a live census identified the hidden UndergroundExploitation requirement"
updated: "2026-09-13"
copies: []
---
### C92 - Boundaries of Knowledge counts a hidden unreachable technology (P2) `[cand 2026-09-13: live predicate barrier + source control; no fix built]`

**MEASURED:** `UndergroundExploitation` is the sole failed requirement in the
reporter's supplied Sol 490 save. It is hidden, unresearched, non-repeatable and
counted by the vanilla achievement filter. Both repeatables are already
`"researched"` with one completion each. The original repeatable-state cause and
fix sketch are **WITHDRAWN**; this preserves the player's report and corrects
the mechanism. Investigation: [C92_INVESTIGATION.md](../reports/C92_INVESTIGATION.md).

#### Field report and owner verification

**INHERITED report:** Steam bug-report channel, read 2026-09-13, title
"Achievement Bugged - The borders of knowledge": the player researched all
available sciences in the new tree and did not receive the achievement. Reported
platform Steam, game `1.1.0.403908`. Attachment: tech-tree overview; subsequently
the original `Autosave Sol 490.savegame.sav`.

**Owner authority (2026-09-13):** The owner personally loaded that original and
verified all non-repeatable technologies complete, a clean original with no mods,
and the current patch. Accepted as authority. A hidden requirement is outside the
visible completed tree and reconciles the owner's check with the failing predicate.

**MEASURED file identity:**
`C:\Users\stkot\Downloads\Autosave Sol 490.savegame.sav`, SHA256
`68d340ad0f1a2b13047034e21a7fc84199c66f069858f190be1e9f38d0ff9bb7`.
Only a byte copy `C92READ.savegame.sav` was used by this investigation.

#### Source route - all citations below are game 1.1.0.403908

**SOURCE achievement:** `ResearchedAllTechs`, display name "The Boundaries of
Knowledge", description "Researched all non-Breakthrough Technologies."
(`Data/Achievement.lua:364-369`). `Lua/Achievements.lua:1-19` tracks Habitation,
Hi-Tech, Industry, Logistics, MegaProjects, Society, Space, Sustainability and
Underground. Its local filter is:

```lua
return (tech.save_in or "") == "" and not IsInitiative(tech.id)
```

**SOURCE counted population:** `Research:IsTechGroupResearched` walks each
cluster, and `IsTechClusterResearched` requires every filtered tech to satisfy
`IsTechResearched` (`Lua/Research.lua:180-202`). The real preset iterator skips
obsolete techs but does not skip hidden techs (`CommonLua/Preset.lua:1802-1815`).

**SOURCE blocker:** `Data/Tech.lua:10635-10660` defines
`UndergroundExploitation`, group `Underground_1`, with `LockState="hidden"` and
`Unknown=true`. It has no `RequireTech`, no `save_in`, no `Obsolete=true`, and
inherits `CanBeResearched=true`, `Repeatable=false` (`Lua/TechTree.lua:255-267`).
Its authored effect modifies underground water production by 20%.

**SOURCE reveal/research route:** `Tech:IsVisibleOnMap` refuses hidden techs
(`TechTree.lua:434-436`). `Tech:CheckUnlockPrerequisites` refuses an unresearched
tech with no connections while it is not already unlocked (`:443-459`);
`UnhideUnlockedTechs` uses that check (`:182-192`). `Player:CanResearch` permits
only enabled techs or already-researched repeatables (`:868-873`).
`GetRandomResearchableTech` explicitly excludes presets whose default LockState
is hidden (`Lua/MarsStoryBits.lua:237-249`); normal anomaly unlocks use that route
(`Lua/Buildings/Anomaly.lua:379-381`).

**SOURCE named-route search:** Searching the installed Src tree for
`UndergroundExploitation` finds its preset, the old law/policy definitions,
the obsolete-law removal and the production consumer, but no named grant,
unlock, or incoming tech connection. This is a named-route absence, not a claim
that a console, cheat or another mod cannot forcibly unlock it. On 1.0.7.396349
it was a law/policy rather than a Tech; do not install this repair there by label.

**SOURCE + MEASURED conclusion:** The achievement demands a hidden ordinary
tech which the normal tree and random discovery routes do not expose. The
reporter's completed visible tree can therefore still fail Underground's check.

#### Live control

**MEASURED unattended**, read-only on the supplied copy, retail `1.1.0.403908`,
Japan, day 490, DLCs norman + thomas, 3 tech points, no game rules. Normal rig
mods loaded: Test Kit, Fix Pack, Opt-In Pack. The original clean-save verification
is the owner's authority; this diagnostic leg itself was not unmodded.
Retained evidence: [r2 census](../../archive/c92_read_r2_Mars.exe-20260913-03.50.53.log)
and [r3 control](../../archive/c92_read_r3_Mars.exe-20260913-03.53.17.log).

| observation | verdict |
|---|---|
| MEASURED Habitation, Hi-Tech, Industry, Logistics, MegaProjects, Society, Space, Sustainability | vanilla group check true |
| MEASURED Underground | vanilla group check false; sole counted unresearched tech `UndergroundExploitation` |
| MEASURED `UndergroundExploitation` | state hidden, raw flag nil, repeatable false, connections 0, normal research false, normal unhide check false |
| MEASURED `MartianCopyrithgts` | cluster Pride / Society, state researched, raw flag true, times 1, repeatable true |
| MEASURED `MartianPatents` | cluster Society_1 / Society, state researched, raw flag true, times 1, repeatable true |
| MEASURED vanilla filter plus exclusion of repeatables | all-group predicate remains false |
| MEASURED vanilla filter plus exclusion only of `UndergroundExploitation` | all-group predicate true; read-only comparison, no unlock call |

**MEASURED limitation:** The owner's account already reports this achievement
unlocked. That is account state, not a property of the reporter's save or evidence
the reporter received it. No actual award was attempted or observed. C92 remains
`cand`; the live evidence proves the predicate barrier, not a shipped repair.

#### Why the original cause was wrong

**SOURCE:** Completion is retained in `UIPlayer.tech_researched`
(`CommonLua/Libs/Research/Research.lua:147-164`). `GetTechState` gives researched
flags priority over lock state (`:46-54`). `Player:UIResearch` records completion
before emitting `TechResearched` (`Lua/TechTree.lua:1200-1221`), and permits
subsequent repeat research without clearing the flag. The UI explicitly displays
the repeat icon in its **researched** branch instead of a checkmark
(`Lua/XDef/XTechNode.generated.lua:176-185`). It is not evidence of non-completion.

**SOURCE + desk control:** [desk_c92_achievement.py](../../../tools/desk_c92_achievement.py)
executes those shipped bodies and the achievement listener with a recording-only
unlock sink. It confirms vanilla can request the achievement with completed
repeatables; an unresearched repeatable legitimately blocks it; exempting only
the orphan preserves that first-completion requirement and ordinary incomplete
techs still block. The original filing is retained in git `1b7d695`.

**SOURCE:** `IsTechRepeatable` delegates to `Research:IsTechRepeatable`, which
honors `g_ForceTechRepeatable` overrides (`Research.lua:150-159`). It can disagree
with `tech.Repeatable`; neither requires a C92 repeatable exemption.

#### Solution options - investigation handoff, no module built

**INFERRED recommendation:** Exempt only the verified orphan from this
achievement's counted population, preserving other tech requirements and the
vanilla tracked-group scope. The live comparison establishes the proposed
predicate's effect. The local vanilla filter is not directly assignable by a mod;
a focused additional achievement listener is a possible runtime seam. Do not
change shared `IsTechResearched` semantics or silently mark the tech researched.

**SOURCE recovery requirement:** The vanilla listener returns immediately on
repeat completions (`Achievements.lua:21-23`). This completed save has no remaining
ordinary first completion to trigger a corrected listener. The fix agent must
include an eligible repeat-event recheck and/or an ordered post-load recheck,
using normal engine achievement restrictions. Simply changing the predicate
while retaining the first-time-only trigger does not recover this reporter.

**INFERRED alternative:** Expose the tech through a normal tree connection.
That adds its authored production benefit and requires a gameplay placement
decision; it is broader than achievement bookkeeping. Exempting every hidden
tech is also broader and can waive valid future requirements. Options and
acceptance demands are in [the investigation](../reports/C92_INVESTIGATION.md#solution-options-for-the-fix-agent).

**SOURCE separate scope:** Terraforming is absent from the vanilla tracked list.
Adding it makes the requirement stricter and does not repair this hidden-tech
barrier. Keep that change separate. No player reply or owner scope call is
requested by this investigation.

#### 2026-09-13 placement investigation - corrections and recommendation

The follow-up [C92_PLACEMENT.md](../reports/C92_PLACEMENT.md) supersedes the
first addendum's placement, asset-origin and residue conclusions. **Status stays
`cand`; no fix or live award was attempted.** Owner scope is checklist **171**.

**MEASURED packed truth:** `pack_truth.py` in the report's evidence directory
at HEAD `ccd4ff5`, Steam build `24995074`, decodes the Data/Lua packs and both
installed DLCs with presence controls. `Data/Tech.lua`, `Data/TechGroup.lua`,
the named law/production consumers and the production-control dependencies
are byte-identical to ModTools source. No alternative shipped position or
additional named DLC reference was found. This is a named-reference search;
it does not independently exclude arbitrary dynamically constructed IDs.

**SOURCE positive family evidence:** the target explicitly belongs to
`Underground_1`. The prior five-member conversion census was incomplete:
`Policy_UndergroundMiningPermits` became `UndergroundDeepMining`, and
`Policy_UndergroundWaterPermits` became `UndergroundWaterExtraction`.
`SavegameFixups.TransformLawsToTechs_v2` (`1.1.0 Lua/Factions/Laws.lua:1136`)
explicitly includes those old laws in its conversion/refund list. Both
successors occupy Underground I's satellite row and retain the old
`NoUndergroundAndAsteroids` restriction; the target lacks that Condition.
These are direct sibling controls, not proof of an exact intended hex.
Additional renamed conversions further refute the old claim that ID and loc-ID
matching bounded the entire cohort. Conversely, `MartianDiet` already existed
as a 1.0.7 breakthrough with the same research icon; its matching ID did not
establish a new law-to-tech conversion.

**INFERRED intent:** an unfinished conversion remains the strongest reading of
its live consumer, authored effect/text, incompatible hidden state and completed
siblings. It is not a SOURCE fact about a designer's intent; a deliberate hold
cannot be excluded just because an Obsolete flag is absent.

**MEASURED correction - no 44% water bonus:** offline shipped-body controls
produce water 5000 -> 6000 and stockpiled extractor output 5000 -> 6000 through
separate paths. `WaterExtractorBase:ProduceSupply` and its grid modifier callback
(`1.1.0 Lua/Buildings/WaterExtractor.lua:73,125`) do not call
`SingleResourceProducer:CalcProductionAmount` (`BuildingComponents.lua:1356`).
The declarative water effect complements the hardcoded stockpile consumer.
Unlocking this tech does not justify changing either production body to cure
the previously alleged double application. These are desk measurements with
explicit fixtures, not a live production reading.

**MEASURED asset bounds:** all installed FLPK directories and all research DDS
were surveyed; base-tech icon presence controls pass. No dedicated icon was
identified. The law variants are the same cave/magnifier glyph colored for
UI states. The earlier claim that a bespoke icon has never been drawn is
**withdrawn**: an installed-pack search cannot establish that. Two purported
bespoke orphan icons are pixel-identical to existing funding art; two further
orphans resolve to old technologies under different IDs. The owner's additional
Industry/Hi-Tech pass found retired `closed_loop_extraction.png` as a plausible
substitute, already owned by an obsolete maintenance-removal technology; no
positive C92 icon assignment was recovered. Detailed inventory,
visual limits and a local contact sheet are linked from the report.

**SOURCE + INFERRED placement:** `Underground_1` is supported by authored data
and renamed siblings. Exact coordinates and prerequisites remain unproven.
The owner's Hi-Tech I and Industry V holes have no recovered link to this tech.
Their supplied board screenshot confirms the single Underground ring is
visibly complete; no added satellite is compelled by the authored group.
The published designer diary explicitly describes room for future expansion;
an empty slot or orphan asset alone is insufficient to file another defect.
The best finish-route hypothesis is an Underground I satellite connected via
`UndergroundDeepMining` and/or `UndergroundWaterExtraction`, with the exact
edge and slot retained as owner design choices rather than recovered intent.

**SOURCE + MEASURED residue correction:** unlocking/researching persists
vanilla lock/research state and the water label modifier. Uninstalling does
not automatically remove them, but the earlier "unremovable" claim is false:
clearing the research flag and removing the original keyed label modifier stops
both bonuses in desk controls. That is not a verified save migration or refund
plan. Obsolete-only retirement retains the preset and bonus; actual deletion
with a retained researched flag makes the unchanged stockpile consumer
nil-index. Future vendor changes need separate controls for those two cases.

**INFERRED recommendation:** keep the narrow achievement exemption as the
proposed mod-side repair; defer restoration/placement pending a deliberate
scope choice. Its motivation is the proven achievement barrier and absence of
research/save edits, not the withdrawn 44% claim. The report specifies guarded
runtime evaluation, decline on repaired/retired/absent or newly reachable tech,
normal achievement restrictions, and recovery for the already-completed save.
Completing the tech is feasible; it requires a placement and save-residue
contract that this investigation has not play-tested.
