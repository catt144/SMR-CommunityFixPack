---
id: "F46"
seq: 46
row: 46
title: "Trains dump cargo at stations with resource disabled"
status: "tested"
status_source: "tag"
priority: "P2"
evidence: "high"
row_status: "tested — PT-23 PASS 2026-07-28 (entry)"
updated: "2026-09-12"
copies: []
---
### F46 — Trains dump cargo at stations with the resource disabled (P2, high)  `[tested: Code/Fix_TrainCargoDumping.lua — PT-23 PASS 2026-07-28 both halves on the live 5-station network; archived]`

#### 2026-09-12 - native suspended-demand premise MEASURED

The 2026-09-09 unknown C-side premise below is now settled on a real Station
request in retail **1.1.0.403908**. On a paused copy of manual USA Sol47,
Concrete demand was native userdata, enabled=true, flags=1548, actual=2500,
target=2500. Adding only rfSuspended (65536) changed flags to 67084 and
enabled=false, but **target stayed 2500**. Clearing/restoring returned flags,
enabled state and target exactly. Thus suspension does not itself make the
vanilla UnloadAll cap zero on this sampled request; the enabled-resource guard
still has a real positive cap to reject. No stub or native metatable patch.

[Complete control log](../../archive/logs/stillneeded_f46_Mars.exe-20260912-01.00.26-6a91a190.log):
:223 baseline, :224 suspended, :225 cleared, :226 restored, :227 control/restore
both true. SHA256 `a24941a12a7ae1269a62a65587c6fd829405f5be983dc6b69635b3feac4260db`.
Source consumer remains `Lua/Units/Train.lua:794-796`; disabled demand flag path
is `Lua/Buildings/MultiResourceDepot.lua:264-273`.

**This is a native premise measurement, not an unloading or cure witness.**
No train ran, no route exception executed, no game-speed change or save write.
The original current-route harm and actual repaired unloading remain unobserved.
All 121 original saves were externally backed up and hash-verified unchanged;
zero additional autosaves. Copy/probe removed and TestKit metadata restored
byte-exact. [One-module review](../reports/still-needed/Fix_TrainCargoDumping.md).
Historical status and dated records below remain unchanged; owner checklist 156.

`Train:UnloadAll` (`Train.lua:783-803`) unloads everything with room, no
`station:IsResourceEnabled(res)` check (disable only removes the demand from
task_requests, `StorageDepot.lua:583-587,641-668`). Cargo planner then treats it as
"forbidden" stock and dispatches trains to haul it back out (`Train.lua:868,905-939`) —
resource ping-pong. **Fix:** override `UnloadAll` with enabled-check (allow dump if no
station on route accepts, to avoid stranding).
*Implemented as sketched:* full replacement of `Train:UnloadAll` with the enabled check;
the dump is still allowed when no other station on `city.train_track_routes[train.track]`
accepts the resource, and always when `is_stopping` (a refabbed train destroys its cargo,
`Train.lua:85-86,457-458`). Loading was already correct (both paths check
`dest:IsResourceEnabled`, `Train.lua:905-912,930-939`), so undeliverable cargo only arises
when something changes mid-trip.
*PT-23 observation (2026-07-28):* with the resource forbidden at EVERY station and no
drone coverage, an isolated station's forbidden stock stays put indefinitely — loading
only targets accepting destinations, so the stock has no train exit; drones are the only
mover. Expected statics, vanilla-consistent, not the fix; trains verifiably dump their
carried load (the designed branch) instead of stranding.

⛔ **CONVERSION ATTEMPTED AND DECLINED 2026-08-02 — the module STAYS a §1.5 full
replacement** (chain prompt 8, stop condition 1: *"a conversion is NOT
byte-equivalent when you diff carefully → it is a B-class in disguise; skip it,
record why, route to prompt 12"*). `SAVE_SAFETY_REDESIGN.md` §5.4 listed this in
**group A** — "verified feasible", layer 3, *"the disabled-resource cap comes
from the demand request's `GetTargetAmount`… return 0 for a disabled resource and
vanilla's `Min(carried, station_cap)` yields 0 by itself"*. ⚠️ **That route does
not exist, and the §5.4 row was written without checking where the method is
declared.** Three findings, each independently disqualifying:
1. **`GetTargetAmount` is not Lua.** The only Lua declaration of that name in all
   of Src is `ResourcePile:GetTargetAmount` (`Lua\ResourcePile.lua:79`), a
   different class. `station.demand[res]` is a **TaskRequest**:
   `AddDemandRequest` → `AddRequest` → `CreateRequest` → `Request_New`
   (`CommonLua\TaskRequest.lua:109-137`), a native constructor. Its methods live
   on the native metatable `Request_GetMeta()` returns. There is no class table
   to chain.
2. **Patching that metatable would desynchronise the savegame permanents.**
   `OnMsg.PersistGatherPermanents` publishes `permanents["TaskRequest.
   GetTargetAmount"] = meta.GetTargetAmount` (`CommonLua\TaskRequest.lua:34-45`),
   and `PersistGatherPermanents` is one of the three hooks **blacklisted for
   mods** (FIX_POLICY §3a), so we could not re-register ours. Replacing a
   function the permanents table indexes by name is a save-integrity risk, which
   is the exact class of harm §3a exists to prevent.
3. **There is no key, and 148 call sites.** `GetTargetAmount` is read across the
   whole request economy; from `(request)` alone a wrapper cannot tell it is
   inside `Train:UnloadAll`, and this fix's two deliberate escape hatches
   (`route_accepts_elsewhere`, `is_stopping`) need the **train**, which the
   request does not carry. §3a requires the wrapper be scoped by the narrowest
   thing that actually separates the call sites; nothing here does.

**A second route was looked for and also rejected, recorded so prompt 12 need not
re-derive it.** The decision *is* computable up front — `carried`,
`station_cap`, `is_stopping` and `route_accepts_elsewhere` are all pure reads —
so a pre-wrapper could swap `station.storable_resources` for a filtered copy,
`return orig(self)`, and restore under `pcall` (F90's shape). It is
behaviourally exact, but it trades a body copy for a **temporary mutation of a
persisted property** (`storable_resources` is a `prop_table` property,
`SharedStorageBaseVisualOnly.lua:6`) on a live station inside a command path —
strictly WORSE on §3a grounds than the copy it would replace, on a module that
§5.4 itself certifies is *already save-safe*. Not built.

**Disposition: `Fix_TrainCargoDumping` moves from §5.4 group A to group C ("no
route — the body copy is the right technique"), making the group counts 5 / 4 /
10 / 3.** The module is unchanged; only the sweep's classification was wrong.
Routed to chain prompt 12 for review, and to its job-7 doc-drift seed list (a
recorded "verified feasible" that had not been verified).

⚖️ **OWNER DECISION 2026-08-10: re-classified group C → group B** ("real
route, but needs a design pass"), making the counts **5 / 5 / 9 / 3**. Grounds:
the storable_resources pre-wrapper above IS a behaviourally-exact route — it
was rejected on §3a cost-benefit (temporary mutation of a persisted property
on a live station), not because no route exists, and "group C = no route" was
therefore the wrong cell. **Nothing is scheduled**: the module keeps its body
copy, the §3a rejection stands as this group's "open question" row, and the
move commits nobody to the conversion — it re-grounds the skip honestly.


## ⭐ 2026-09-09 — game 1.1.0: the fix is RE-ARMED on the 1.1.0 body (hotfix 2, ck123) — premise UNESTABLISHED

⛔ **Nothing above this line is edited.** The 1.0.7 record stands as written. This section
adds only what game **1.1.0.403908** changed, read against BOTH shipped bodies (the live
1.1.0 tree and the archived 1.0.7 tree, `C:\Dev\SMR-SrcArchive\1.0.7.396349\Src`).

**Between 2026-09-08 and this edit the fix was ABSENT on 1.1.0.** [[F114]]: our 1.0.7 copy
of `Train:UnloadAll` indexed `station.demand[res]` unguarded, 1.1.0 stations list
lock-hidden resources (BlackCube, Seeds) as storable with no demand request, and the train
never left its platform (confirmed live, 157 throws). The owner's gate (ck106) switched the
module off on 1.1.0. Owner ruling **ck123 (2026-09-08): repair, on top of the gate.**

**What 1.1.0 changed in the body — three things, all carried.** A three-way diff (1.0.7 vs
1.1.0 vs our old copy; our copy's non-FIX lines matched 1.0.7 byte for byte): (1) the
inbound-cancel loop nil-guards `dest.demand[res]` (`Train.lua:785-787`); (2) the cap read
is hoisted and nil-guarded — `local demand = station.demand and station.demand[res]` /
`demand and demand:GetTargetAmount() or 0` (`:794-795`, the F114 line); (3) a BlackCube hook
`BlackCubeMystery_AdjustStored(station.city, unload_amnt)` after the unload (`:800-802`).
No enabled-resource check was added: `Min(carried, station_cap)` still decides the unload.

**What hotfix 2 shipped** (`Code/Fix_TrainCargoDumping.lua`): the 1.1.0 body
(`:779-805`) with the F46 guard re-applied after the cap read, both escape hatches kept
(nowhere-else-accepts, `is_stopping`). The helper `route_accepts_elsewhere` no longer
reads `st.task_requests` — its stated reason was 1.0.7's `UniversalStorageDepotBase`
implementation; 1.1.0's `IsResourceEnabled = IsStoring` reads `self.demand[res]` and
`rfSuspended` (`MultiResourceDepot.lua:242-247`), which is what the helper now tests. The
F114 gate is KEPT as the branch guard with its sense inverted (apply where
`MultiResourceDepotBase` exists and declares `IsResourceEnabled`, decline on 1.0.7), plus a
behaviour probe that runs the SHIPPED `UnloadAll` on a stub train at a station listing a
resource with no demand request — the F114 input — and applies only if the shipped body
survives it. Three-valued: 1.0.7 (no Multi base, `UniversalStorageDepotBase` present) ⇒
decline, not suspect; anything else ⇒ decline AND `update_suspect`. Manifest pinned (`SRC:`
`:779-805`; `DEFECT:` `Min\(carried,\s*station_cap\)`, FIX_POLICY §2b's own refactor-trap
example; the absence limit is stated in the file). `bodycheck.py` OK, seen RED both ways.

⛔ **THE PREMISE IS NOT ESTABLISHED, and no patch note may say "fixed".** On 1.1.0
`SetAcceptResource` SUSPENDS the demand request (`rfSuspended`, `:251-290`) instead of
removing it, and the request stays in `task_requests`. Whether a suspended request still
reports a POSITIVE `GetTargetAmount()` — the thing that makes the shipped body dump — is
C-side (`GetTargetAmount` has no Lua definition outside `ResourcePile.lua:99`) and nobody
has opened it; the re-verification says "plausibly persists". Circumstantial only: 1.1.0's
own load side treats stock at a disabled station as forbidden excess to haul out
(`:873`, `:897`, `:912`), so vanilla expects stock to land there. **If the target reads 0 the
guard is inert and harmless** (the cap is already 0) and 1.1.0 fixed F46 itself. The
10-second console control and the in-play control are on the checklist (LINK 04b block)
and join the post-99 sitting.

**Desk control, not a test** (lupa 5.4, the shipped bodies and ours through the pack's
real `Require`): on the 1.1.0 shape the module applies and the installed body dumps 0 at a
station with the resource off while another route station accepts it, 5000 when the
station accepts, 5000 when nowhere accepts, 5000 when `is_stopping`, adjusts the BlackCube
count by the amount unloaded, and survives the F114 input; the shipped 1.1.0 body dumps
5000 on the same off-station stub — GIVEN the stub's premise that a suspended request
reports a target. On the 1.0.7 shape it declines with the branch reason and no suspect
mark; on a 1.1.0 depot base over the 1.0.7 body it declines with the mark.

⛔ **NOT tested.** Nothing has run in a game; no train has unloaded on 1.1.0 with this body,
and no train has been seen leaving its platform with the pack on since F114. Status word
unchanged (`tested` is the 1.0.7 record).
