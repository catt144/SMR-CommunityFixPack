---
id: "C74"
seq: 169
row: 204
title: "Rare Metals Extractor hammer and MOXIE pump play no strike FX: the moment tracker looks presets up by animation INDEX while they are keyed by NAME, and the hammer/pump have no AnimMetadata presets at all — fix PROVEN LIVE (attended)"
status: "filed"
status_source: "tag"
priority: "P3"
evidence: "MEASURED (owner console, attended) + LIVE FIX PROVEN attended + source-read"
row_status: "filed 2026-09-10 — two vanilla defects, cause MEASURED, console fix heard in sync; build is owner decision 139"
updated: "2026-09-10"
copies: []
---
### C74 — hit-moment FX never fire on the Rare Metals hammer and the MOXIE pump (P3, cosmetic, vanilla, both trees) `[filed 2026-09-10: MEASURED + live fix proven; build = checklist 139]`

**Field report (Steam, relayed by the owner 2026-09-10):** *"The Rare Metal
Extractor's hammer doesn't make a sound, even though there was one in the
original."* (the original = Surviving Mars, 2018).

#### The two defects (both Lua/data, identical in 1.0.7.396349 and 1.1.0.403908 — PASSING)

1. **Index vs name.** `BaseBuilding:TrackMultipleHitMoments` takes `local anim =
   obj:GetAnim(1)` and asks `obj:GetAnimMomentsCount(anim, "Hit")` (1.1.0
   `Lua/Buildings/BaseBuilding.lua:1045-1046`, again `:1053`, `:1065`; 1.0.7
   `:731-732`). `GetAnim(1)` returns the state **INDEX** — MEASURED: `anim 20
   number` (log `Mars.exe-20260910-11.47.24-6a91a190.log:255`, `:257`). The lookup
   is keyed by **NAME**: `preset_group[anim]` (`CommonLua/Classes/AnimMoment.lua:8`),
   so `preset_group[20]` is nil and the count is ALWAYS 0 — even with a correct
   preset present (the same log lines: preset registered, count 0). **§4 hard tell,
   sibling contradiction:** every other caller converts first —
   `GetChannelData` (`AnimMoment.lua:107`, `GetStateName(self:GetAnim(channel))`,
   the path `TimeToMoment` and the Stirling generator use),
   `GetAllAnimMoments` (`Building.lua:3347`), `ElectricityStorage.lua:187`,
   `RocketBase.lua:583`. `Metatron.lua:52-57` repeats the unconverted shape.
2. **No presets.** Moments are Lua data (`EF-086`): only 5 entity groups carry
   `AnimMetadata` presets in the base game + DLC (`AdvancedStirlingGenerator`,
   `BakeryHands`, three cave-ins); `UniversalExtractorHammer`, `MoxiePump` and
   `Metatron` have none. Our tools found no `Hit` in any `.hgacl` because moments
   are not stored there on this engine.

Either defect alone silences the FX; the fix needs both halves.

#### Two skins — only the classic ones are defective

`PreciousMetalsExtractor` has `entity = "UniversalExtractor"` (hammer, build-menu
icon) and `entity2 = "RareMetalExtractorCP3"` (spinning drill), the drill being
*"the default during placement"* for BlueSun, Brazil, NASA, Roscosmos, SpaceY,
Japan, ISRO (`Data/BuildingTemplate/PreciousMetalsExtractor.lua:4-12`, `:34-35`;
`Building.lua:333`; `Construction.lua:1004-1012`); both are offered and a finished
building shows **Change Skin** (`Infopanel.generated.lua:489-513`) — route walked.
The CP3 skins are FX-less **by design**: the drill has its own continuous
`UniversalExtractor_Steam_02` (`ActionFXParticles.lua:~1910-1926`), and the MOXIE's
`MoxieCP3Pump` has only an `ActionFXRemove` of the smoke
(`ActionFXRemove.lua:3-8`). Screenshots (owner, dev capture, fine for public use):
`agent/reports/c74_skins/` (full-res outside git, `C:\Dev\SMR-ScreenCaptures\c74_skins\`).

#### MEASURED, attended (owner at the keyboard, 1.1.0.403908, NASA colony, 2026-09-10)

| object | readout (`class state HitCount`) | hand-fired `PlayFX("Working","hit-moment1",bld,part)` |
|---|---|---|
| Rare Metals, hammer skin | `UniversalExtractorHammer working 0`, tracker dead | strike sound **and** steam puff |
| Rare Metals, drill skin | `RareMetalExtractorCP3Drill working 0` | nothing (no hammer target — control) |
| MOXIE, classic | `MoxiePump working 0`, tracker dead | pump sound |
| MOXIE, CP3 | `MoxieCP3Pump working 0` | nothing; `FX Particles MOXIE_Steam … has invalid source: Target` is CAUSED BY OUR console line passing no target, not vanilla |

Authored FX that are dead: Rare Metals 11 (8 `ActionFXSound`, 3
`ActionFXParticles`, `Target = "UniversalExtractorHammer"`, 1.1.0
`ActionFXSound.lua:17156-17248`, `ActionFXParticles.lua:860-932`); MOXIE 5
(`LoopPeaks`/`LoopSteam` on `MoxiePump`, `MOXIE_Steam` at `Steam1`/`Steam2`,
`ActionFXSound.lua:17136-17186`, `ActionFXParticles.lua:844-895`); Metatron 7
`MetatronRotation` `End1..7` particles — **predicted**, not measured (rare mystery
unit). ⚠️ **Corrected 2026-09-10 (sweep, `reports/C74_SOUND_SWEEP.md`):** Metatron's
7 rotate SOUNDS are keyed `hit-moment1..7`, but its thread only emits `Start<i>`/`End<i>`
(`Metatron.lua:78`, `:86`), so those sounds are unreachable even with a preset and a
conversion; only the particles are C74's. Likewise the hammer's `hit-moment4` (2
sounds) can never fire: the class list stops at 3 (`MetalsExtractor.lua:25`) and is
cycled over the preset's hit count, so with 2 hits only `hit-moment1`/`2` play.

**Reach (sweep 2026-09-10):** five classes turn this tracker on; only the hammer and
the MOXIE have FX keyed to it (Electrolyzer, MicroGExtractor: none;
PreciousMineralsExtractorBase: no template). The same no-preset silence hits a SECOND,
correctly name-keyed tracker (`TrackAllMoments`) on five more units — filed as `C77`.
Samples ship (`Sounds.fpk`: `extractorUniversal_workpeak5.opus` 23,042 B …).

#### LIVE FIX PROVEN, attended (same sitting)

Console only, gone at quit: (a) `PlaceObj('AnimMetadata', {group =
"UniversalExtractorHammer", id = "working", Moments = { {Type = "Hit", Time =
3083}, {Type = "Hit", Time = 9250} }})` (¼ and ¾ of the measured 12,333 ms loop);
(b) the hammer's class `GetAnimMoments` wrapped to convert a numeric anim via
`GetStateName` (class-level, not `CObject` — a runtime base-class patch misses
flattened subclasses, `EF-058`); power off/on. Owner: **thunks and steam puffs,
"perfectly in sync"** with the visible strikes. ⇒ the game's OWN tracker fires
the authored FX once both halves exist — rewire, not drive.

#### OWED live test — MOXIE times + the C77 readout (restated from the 09-10 handoff §2a)

Owner at a **classic** (not white/CP3) working MOXIE, not saving meanwhile. Each
line parse-checked with `lupa` (a must-fail control failed as expected) 2026-09-10;
after each, ask for a log flush and grep the newest `Mars.exe-*.log` for `C74 `.
1. Preset at ¼ and ¾ (placeholders), with a `GetAnimEntity` discriminator so a 0
   later separates a wrong group key from wrong times:
   `local p = SelectedObj:GetAttach("MoxiePump") local d = p:GetAnimDuration() PlaceObj('AnimMetadata', {group = "MoxiePump", id = "working", Moments = { {Type = "Hit", Time = MulDivRound(d, 1, 4)}, {Type = "Hit", Time = MulDivRound(d, 3, 4)} }}) print("C74 moxie: dur", d, "animent", GetAnimEntity(p:GetEntity(), GetStateName(p:GetAnim(1))), "state", GetStateName(p:GetAnim(1)))`
   ⛔ If `animent` is not `MoxiePump`, re-key the group to it before going on.
2. Class-level conversion on the pump's own class (a `CObject` patch misses flattened
   subclasses, `EF-058`); expect `preset 2 count 2`:
   `local p = SelectedObj:GetAttach("MoxiePump") local cls = g_Classes[p.class] local orig = cls.GetAnimMoments cls.GetAnimMoments = function(self, anim, t) if type(anim) == "number" then anim = GetStateName(anim) end return orig(self, anim, t) end local g = Presets.AnimMetadata.MoxiePump print("C74 moxie: preset", g and g.working and #(g.working.Moments or {}) or "none", "count", p:GetAnimMomentsCount(p:GetAnim(1), "Hit"))`
3. Power off/on; the owner listens (`hit-moment1` = `LoopPeaks` + `LoopSteam` + steam at
   `Steam2`; `hit-moment2` = `LoopPeaks` + steam at `Steam1`). Retime until in sync (a
   second `PlaceObj` replaces the entry), then power-cycle:
   `local p = SelectedObj:GetAttach("MoxiePump") local d = p:GetAnimDuration() local a, b = MulDivRound(d, 1, 4), MulDivRound(d, 3, 4) PlaceObj('AnimMetadata', {group = "MoxiePump", id = "working", Moments = { {Type = "Hit", Time = a}, {Type = "Hit", Time = b} }}) print("C74 moxie: retimed", a, b, "of", d, "count", p:GetAnimMomentsCount(p:GetAnim(1), "Hit"))`
   (edit the two `MulDivRound(d, n, m)`). Record the final times here.
4. **C77 readout, same sitting, nothing selected** (desk-run against stubs: absent /
   no-attach / found all print right — the first draft's `pick and pick(o) or o` read the
   building itself when an attach was missing, caught by that desk run):
   `local out = {} local function probe(cls, pick) local list = MainMap:MapGet(true, cls) local o = list and list[1] if not o then out[#out+1] = cls .. "=absent" return end local t = o if pick then t = pick(o) end if not IsValid(t) then out[#out+1] = cls .. "=noattach" return end out[#out+1] = string.format("%s[%d]:%s/%s/%d", cls, #list, t.class, GetStateName(t:GetAnim(1)), #GetAllAnimMoments(t)) end probe("Shuttle") probe("RCDriller") probe("RCTerraformer") probe("WaterExtractorBase", function(o) return o:GetAttach("WaterExtractorPump") end) probe("TheExcavatorBase", function(o) return o.arm end) probe("MOXIEBase", function(o) return o:GetAttach("MoxiePump") end) probe("PreciousMetalsExtractor", function(o) return o:GetAttach("UniversalExtractorHammer") end) print("C74 sweep: " .. table.concat(out, " "))`
   Run it BEFORE step 2 on a fresh load, or the MOXIE entry reads the patched class.
   A `/0` counts only for a unit that is working/animating at that moment (Shuttle is
   usually idle in its hub — `absent`/idle is vacuous, not a pass). Optional wiring
   proof per unit: hand-fire one FX, e.g. `PlayFX("working", "Hit", <extractor>)`.
Metatron: same method as steps 1–3, only if the owner wants it (rare).

#### Instrument record

The first strike-timing probe FAILED as an instrument: all 3 hammer spots are
root-bound (`travel 0` over 1,645 samples), and its desk control had assumed spots
follow the animation — it proved the arithmetic, not that premise. No bone-position
API is exported. The native `GetStateMoments` is overridden by
`AnimMoment.lua:334`. Pack and TestKit touch none of this path.

#### Fix shape (NOT built — owner decision 139; design goes through FIX_POLICY)

- **Conversion:** a classdef-time wrap (safe per `EF-058` amended) so a numeric
  anim is converted with `GetStateName` before the name-keyed lookup. Behaviour
  change only where every current answer is an empty table. §2a guard = behaviour
  probe: skip when an index already resolves a known preset. ⛔ Enumerate every
  `GetAnimMoments`/`GetAnimMomentsCount`/`GetAnimMoment` caller passing an index
  before choosing the wrap site (narrowest that covers the tracker + Metatron).
- **Presets:** `UniversalExtractorHammer`/`working` Hit at 3083 and 9250 (heard in
  sync); `MoxiePump`/`working` and `Metatron` need their times (placeholder test,
  then the owner's ear). Guard: skip any group/id that already exists.
- **Save-safety:** presets and class methods never enter a save; the tracker thread
  is vanilla's own (`track_multiple_hit_thread`) — uninstall-clean by construction,
  to be confirmed by the §3a checks at build time.
- **Patch note must explain the two skins** (drill/CP3 stays silent by design).

**Falsifier:** a later build where `GetAnimMomentsCount(obj:GetAnim(1), "Hit")`
returns non-zero on the hammer with no mod loaded — vanilla fixed both halves.
