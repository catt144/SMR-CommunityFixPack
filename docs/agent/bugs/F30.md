---
id: "F30"
seq: 30
row: 30
title: "Lake placement entombs RC builder + drones"
status: "fixed"
status_source: "tag"
priority: "P1"
evidence: "high"
row_status: "fixed"
updated: "2026-09-12"
copies: []
---
### F30 — Lake placement entombs RC builder + drones (P1, high)  `[fixed: Code/Fix_LakeEntombment.lua]`

#### 2026-09-12 - still-needed sweep: Current implementation and exemption

On **1.1.0.403908**, the explicit ScatterUnits exemption is an RCConstructorBase obstruction-clearing command whose construction_clearing field is this site (`Lua/Buildings/ConstructionSite.lua:1918`). RCConstructorBase clears that field before ordinary RoverWork (:233). The installed wrapper performs a map-wide eligible Unit scan after PlacePrefab and assigns ExitImpassable; it is command-only, not the older bounding-box/teleport sketch. Drone movement battery gating uses EmergencyPower/NoBattery, not the Freeze cold command. Escape requires a suitable destination; the immediate cure and map-wide effects were not newly witnessed.

Current review: [one-module evidence](../reports/still-needed/Fix_LakeEntombment.md). Historical dates and status below remain unchanged. Owner decisions: checklist 156.

Two-part defect. (a) `ConstructionSite:ScatterUnitsUnderneath` (`ConstructionSite.lua:1722-1737`)
exempts the RC Constructor building the site. (b) Scatter runs at `Complete` (:1574-1580)
BEFORE `LandscapeLake:GameInit` digs the basin (`LandscapeLake.lua:32-35,215-292`);
`Unit:ExitImpassable` (`Units\Unit.lua:642-649`) no-ops while ground is still passable, so
even scattered drones stay; terrain then drops + `RebuildPassability` seals them. Drones
drain battery → Freeze (`Drone.lua:1478-1524`), read as dead. Devs ship a partial rescue
fixup for rovers only (`BaseRover.lua:736-745`). **Fix:** wrap `LandscapeLake:PlacePrefab` —
after its `RebuildPassability`, sweep units in bbox with `not map:IsPassable(unit)` →
`SetCommand("ExitImpassable")` / teleport to `GetPassablePointNearby`.

