---
id: "F45"
seq: 45
row: 45
title: "Damaged tracks can't be salvaged at all (sort crash)"
status: "tested"
status_source: "tag"
priority: "P1"
evidence: "high"
row_status: "tested"
updated: "2026-09-23"
copies: []
---
### F45 — Damaged tracks can't be salvaged at all (P1, high)  `[tested: Code/Fix_BrokenTrackSalvage.lua — wrapper + LoadGame sweep; PT-03 PASS 2026-07-26 (ReportBrokenTrack 7 sites / 0 bad; broken element salvaged cleanly)]`

#### 2026-09-23 build response

**Retired from the 1.1.1 pack:** `Fix_BrokenTrackSalvage` and its load/editor registrations and TestKit probe requirements are removed. The status above records the historical fix, not a newly attended 1.1.1 result.

**SOURCE, archived 1.1.1.405907:** TrackElement.lua:467-637 excludes repair sites from the physical sort and rehomes them; Track.lua:466-474 excludes them from processing. The F124 rebase now preserves these native bodies. Old stamped node_idx values are inert vanilla fields.

No new save cleanup is added. Ordinary state already repaired stays repaired; no new executable residue is introduced by deletion. Current controls are in `tools/desk_gamepatch_retirements.py` (F45 uses `tools/desk_f124_track.py`); historical tests below retain their original builds and limits.

See [build report](../reports/GAMEPATCH_1.1.1_BUILD_2026-09-23.md) for commands, controls, claim limits and owner play.

#### Historical filing and evidence

`TrackBase:BreakTrackElement` (`Track.lua:618-659`) copies element params to the repair
site but NOT `node_idx` → stays `false` (`TrackElement.lua:164`). Every salvage path then
hits `table.sort(all_elements, function(a,b) return a.node_idx < b.node_idx end)`
(`TrackElement.lua:458-464`) → boolean<number comparison error BEFORE any deletion; click
silently does nothing. Affects salvage click, Ctrl+click, infopanel Salvage button, and
clicking the repair site. Meteors routinely break tracks → matches "can't salvage /
undeletable track" reports (incl. after station destruction). **Fix:** wrap
`BreakTrackElement` to stamp `element.broken.node_idx = element.node_idx`; belt-and-braces
tolerant sort + LoadGame sweep stamping existing repair sites.


---
✅ **2026-09-09 — KEEP CONFIRMED ON 1.1.0, and a vanilla residue filed as C55.**
1.1.0's new pre-sort `ProcessAllElements()` (`TrackElement.lua:473-476`) cannot
walk a track holding a repair site (two objects on one hex, `Track.lua:641-644`),
so it logs the `Tracks.lua:616` assert and returns without stamping `node_idx`
— our stamp is still what makes the salvage sort possible. Source-read by the
hotfix-2 terminal audit; not measured; no status moved.
