---
id: "F34"
seq: 34
row: 34
title: "Landscape nil-guard bundle (latent crash paths)"
status: "fixed*"
status_source: "tag"
priority: "P3"
evidence: "med"
row_status: "fixed* — ⭐⭐ 2026-08-12 (`corun-pt60` sitting, owner-driven): item (d)'s route documentation was WRONG IN THREE PLACES and is now re-derived from Src; the guarded state is **OBSERVED REACHABLE IN PLAY** for the first time (owner staged a drone embark into an RC Commander and dropped a flatten over it mid-board: **20/20 drones aboard, no `ExitImpassable`, no error line**), pairing with the probe's controlled PASS (`boarding unit skipped, ordinary unit reported exactly once`). ⛔ The ONE live route is a drone embarking an **RC Commander** — the rocket route is UNREACHABLE (a rocket is a `Building` and obstructs the mark), RC Transport sets `Embark` nowhere, and no train or shuttle sets it at all. Stays `fixed*`, NOT re-graded: no A/B is possible (pack-off needs a full restart, D13). Owner's ~2 s embark freeze recorded and attributed, not proven (entry)"
updated: "2026-08-12"
copies: []
---
### F34 — Landscape nil-guard bundle (P3, med/latent)  `[fixed*: Code/Fix_LandscapeUnitFilter.lua — item (d) only; (a)(b)(c) verified NOT actionable, see below]`

**Audit 2026-07-30 (reachability): item (d) R2, one wording correction.**
Shipped Lua sets the "Embark" command only on DRONES (rocket/rover/transporter
boarding) — the fix header's "boarding colonists" overstates; colonists board
via other commands and were never protected by this filter in the vanilla
sibling either. The player-visible save is boarding drones plus the lost
dedup. Full block in REACHABILITY_AUDIT.md.
(a) `ClearWasteRockConstructionSite:GameInit` (`:60-63`) unguarded `Landscapes[self.mark]`;
(b) `LandscapeMarkEnd` (`Landscaping.lua:200-206`) unguarded nil mark;
(c) lake `landscape_grid` overlap only assert-guarded (`LandscapeConstructionController.lua:502-507`,
release asserts stripped → silent corruption);
(d) `LandscapeForEachUnit` dead embark filter (`Landscaping.lua:455-469`).
*Re-read 2026-07-25 (wave 3). Only (d) is a defect that reaches the player, and it is not
merely a nil guard:*
* **(d) — FIXED.** The local `filter_embark` is built and then `callback` is handed to
  `Landscape_ForEachObject` instead. That it is a copy-paste slip and not a decision is
  settled twice over: `LandscapeForEachStockpile` immediately above (`:436-451`) is written
  identically and DOES pass its `filter_parent`; and the ordinary construction site applies
  exactly the same Embark rule to exactly the same question —
  `ConstructionSite:GetUnitsUnderneath` passes `exit_impassable_filter` = `obj.command ~=
  "Embark"` (`ConstructionSite.lua:1713-1720`). Two things were lost: the Embark exclusion
  and the `passed` de-duplication every sibling keeps. Live consumer:
  `LandscapeConstructionSite:GetUnitsUnderneath` (`LandscapeConstructionSite.lua:21-27`) →
  `ScatterUnitsUnderneath` (`ConstructionSite.lua:1722-1740`) →
  `SetCommand("ExitImpassable")`, so landscaping over a boarding point drags colonists out
  of the vehicle they are entering, repeatedly for any duplicate. Full replacement of the
  global, reproducing the file-local `foreach_params_unit`.
  Probe: `LandscapeUnitFilter` in `30_Probes_Wave3.lua`.
* **(a) — not actionable.** Unreachable as written: `GameInit` is a combined method
  (`DefineCombinedMethod("GameInit", "procall", "Object")`), so even if `Landscapes[self.mark]`
  were nil the error is swallowed by `procall`; and no shipped path creates the site without
  its mark. Adding a guard means replacing a method for no player-visible effect.
* **(b) — not actionable.** Both shipped callers are already guarded:
  `LandscapeConstructionController:Deactivate` returns early on `not landscape`
  (`:242-246`) before reaching `LandscapeMarkEnd` (`:251`), and `:515` runs only after
  `LandscapeMarkStart` returned a landscape (`:493-496`). Latent / mod-facing only.
* **(c) — not a guard, a redesign.** `assert(landscape_grid:get(sx, sy) == 0)` followed by
  an unconditional `set` means an overlapping mark is silently overwritten. Making it safe
  requires deciding what to do with the contested hexes (skip them, and the site has a
  hole; refuse the placement, and the player loses an action that works today). That is a
  behavior change, not a nil guard (FIX_POLICY §4). Left recorded.

---

## ⭐⭐ 2026-08-12 — (d)'s route was wrong three ways, and the owner's challenge found it

**How this happened:** the `corun-pt60` sitting offered (d) as an opportunistic rider. The
owner challenged the premise twice with screenshots before agreeing to stage anything, and
both challenges were correct enough to force a full re-derivation from
`ModTools\Src` (game 1.0.7.396349, read 2026-08-12). **The fix is unchanged and still
correct. Its paperwork was not.**

### The three wrong claims, all now corrected

| where | what it said | what Src says |
|---|---|---|
| registered title (user-facing, and `MOD_DESCRIPTION` is rebuilt from these at launch prep) | *"no longer drags boarding **colonists** out"* | **drones only.** `Embark` is never set on a colonist. The 2026-07-30 audit caught this and the title was never changed — ⛔ **fix the title at launch prep** |
| module header, `Fix_LandscapeUnitFilter.lua` | *"placing a landscaping job over a rocket, **train or shuttle** boarding point"* | **no train or shuttle sets `Embark` anywhere in the shipped tree.** Both named classes are fictional here |
| this entry, 2026-07-30 | *"(rocket/rover/**transporter** boarding)"* | **RC Transport sets `Embark` nowhere** (`Lua/Units/RCTransport.lua`, zero hits) |

### Every `Embark` site in the shipped tree, and which are reachable

`RocketBase.lua:1926` · `LanderRocket.lua:1042` · `RocketExpedition.lua:941` ·
`RCRover.lua:275` (inside `RCRover:DroneEnter`) · `CargoTransporterNew.lua:835` ·
`Drone.lua:567`.

* ⭐ **THE ONE LIVE ROUTE — a drone embarking an RC Commander.** `RCRover` *is* the
  RC Commander (`display_name = T(7678, "RC Commander")`, `:28`). It is a `Unit`
  (`RCRover` → `BaseRover` → `DroneBase` → `Vehicle` (`Unit.lua:978`) → `Unit`), and
  **`Unit.__parents` carries `DoesNotObstructConstruction`** (`Unit.lua:3`) — the class whose
  own comment reads *"marks ObjectGrid GridObjects that shouldn't block construction"*
  (`Construction.lua:1454-1455`). `GetObstructors` passes that class as `non_obstructors`
  (`:1514-1515` → consumed `:1502`; the audit corrected the sitting's `:1516`/`:1503` cites,
  one line off each) and `ObstructorsQuery` accepts `classes = {"Building"}` only (`:1448`),
  so a landscape mark places straight over a parked commander.
* ⛔ **THE ROCKET ROUTE IS UNREACHABLE — the owner's finding.** A landed rocket is a
  `Building`, so it *is* an obstructor and the tool refuses the mark
  (`ConstructionStatus.BlockingObjects`, *"Objects underneath are blocking construction"*,
  `Construction.lua:64`). Screenshot-witnessed by the owner: the reserved pad zone holds
  until the rocket has fully lifted off. ⇒ the route this entry and the module header both
  named is in the same class as items (a)/(b)/(c): real in code, unreachable in play.
* ⛔ RC Transport: no `Embark` site. ⛔ Train/shuttle: no `Embark` site.
* ✅ **The three sites the list carried unruled are RULED by the audit (2026-08-12), and
  the ONE-live-route claim survives all three.** `Drone.lua:567` (`Drone:Idle` with a
  parent) is the *inside-the-RC-Commander* re-entry state — the same route, not a new one
  (`Drone.lua:2847` names the command "Inside RC Commander"). `RocketExpedition.lua:941`
  and `CargoTransporterNew.lua:835` both flag freshly-spawned drones `Embark` transiently
  while unloading them at the parent's own spawn spot (`drone:SetPos(spawn_pos)`, a spot on
  the parent model) — and `CargoTransporterNew`'s only inheritor is `UniversalRocket`
  (`UniversalRocket.lua:37`), so in both flows the drone sits inside a landed rocket's
  footprint, where the mark is refused by the **same** `Building` obstruction that kills
  the rocket route. No second live route exists.

### When the sweep actually runs (this is what made the first staging attempt miss)

`ScatterUnitsUnderneath` is called at **site creation** (`ConstructionSite:Initialize`,
`:1043`) and again at completion (`:1574`, *"repeat the check to make sure any units who were
commanded before placement but arrived after it know to get away"*). ⚠️ **A mark that already
exists sweeps nothing** — the owner's first attempt parked a commander inside an existing
zone and unpacked, and *nothing could have fired*: unpacking is `ExitRover`
(`Drone.lua:1757`), not `Embark`, and no site was created. The window is: drones mid-embark
**while a new mark is placed over them**.

### ⭐ The state, observed reachable in play — 2026-08-12, owner's hands and eyes

Owner verbatim, relayed live through `CP60.Note`
(`docs/archive/cp60sitting_Mars.exe-20260812-13.38.15.log:1029`):

> *"I parked an rc commander, ordered them to return to the commander and board while
> dropping a flatten land. There was a very brief moment when i dropped the landscape they
> all stopped and frozen and then they recovered and boarded (maybe a 2 second pause in the
> embark process)"*

**Result: 20/20 drones aboard** (`Drones 20/20 — Drones have been recalled in the RC
Commander`), **zero `ExitImpassable`, zero error lines in a 1,082-line log.** Owner
disclosure: cheats are enabled on the rig and **none were used on the RC Commander during
the test**. Pairs with the controlled half from the same session's suite —
`PASS LandscapeUnitFilter [behavior] boarding unit skipped, ordinary unit reported exactly
once` (log `:727`). ⇒ **the "SOURCE-ONLY, unobserved in play" gap opened 2026-07-30 is
CLOSED.**

⚠️ **The ~2 s freeze is ATTRIBUTED, NOT PROVEN.** It is not the defect's shape — the defect
issues `SetCommand("ExitImpassable")` (`:1738`), which would **abort** the boarding, not
pause and resume it. It fits `map:RebuildPassability(ls.pass_bbox)` in
`ClearWasteRockConstructionSite:InitBlockPass` (`:69-76`): the mark makes those hexes
impassable and every unit in the area re-paths at once, vanilla behaviour with or without
this fix. ⛔ **No A/B was available** (a pack disable needs a full restart, D13), so this is
an attribution. A future leg wanting proof must run the same stage with the pack off.

**Status: stays `fixed*`.** The asterisk was always about (a)(b)(c) being unpatched. No
re-grade to `tested` — precedent is F21, which stayed `fixed` after a witnessed organic
restamp, because witnessing the protected outcome is not the same as witnessing the
unprotected one.

