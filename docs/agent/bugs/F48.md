---
id: "F48"
seq: 48
row: 48
title: "Station-connector savegame fixup no-op (paren misplaced)"
status: "blocked"
status_source: "tag"
priority: "P3"
evidence: "high"
row_status: "blocked"
updated: "2026-08-03"
copies: []
---
### F48 — Station-connector savegame fixup no-op (P3, high defect / low impact)  `[blocked — the corrected pass is too invasive to ship untested; see below]`
`Station.lua:1346`: `ProcessTrackElements(ResolveMap(track, track.elements))` — paren
misplaced, should be `ProcessTrackElements(ResolveMap(track), track.elements)`; migration
no-ops (may contribute to "tracks won't connect" on old saves). **Fix:** re-run corrected
pass in one-shot LoadGame sweep.
*Blocked 2026-07-25 (wave 3), during the sanitizer build.* The defect is confirmed exactly
as written — `ResolveMap(track, track.elements)` returns one value, `elements` arrives nil,
`#elements == 0` is true for nil in this engine (`Tracks.lua:808`) and the function returns
immediately, so `SavegameFixups.A_StationConnectorElements3` (`Station.lua:1341-1354`)
re-orders nothing. What blocks the repair is what the corrected call actually does:
`ProcessTrackElements` → `OrderTrackElements` (`Tracks.lua:520-624`) **clears and rebuilds
`el.connections` and rewrites `node_idx` on every element**, then the caller repositions
every element in Z and recomputes pillars and sections (`:840-900`). Its only failure
handling is `assert(false, "unable to find the expected number of track elements")`, and
assert does not unwind in this engine — so a track it cannot walk (a broken element, a
repair site — i.e. exactly F45's situation) has its connections half-rewritten and
execution continues. Running that over every track of every save on load, with no way to
test it in-game from this seat, risks more than the P3 it repairs.
**To unblock:** an in-game test on a save with (a) a healthy multi-station network and
(b) a meteor-damaged track, comparing `track.start_el`/`end_el` and route formation before
and after. If it holds up, the pass belongs in `90_SaveSanitizer.lua` behind a one-shot
`SMRFixPack_*` flag on `UIColony` so it cannot re-run every load.
*2026-07-26: that test is now written up as **PT-37** in `docs/PLAYTEST_CHECKLIST.md`
(exact console commands for both cases) and sits on the user's in-person list. PASS on
both cases → implement in the sanitizer, skipping tracks that carry repair sites; a dirty
FAIL on the damaged-track case → close `wontfix — repair riskier than the defect`.*

**2026-08-03 — PT-37's exact console procedure moved HERE from the checklist.**
The redesign reduced the checklist's human surface to Bug/Setup/Requires; the
agent hands these in the sitting, one command per line. Fixture: ≥2 stations
connected by track, a running train, one meteor-damaged track (SAVE-A extended
works; `CheatTriggerMarsquake()` near a track can produce the damage).

- **Case A (healthy track):** pick an intact track and note its endpoints —
  `qa_t = MainCity.labels.TrackBase[1]` ·
  `print(qa_t.start_el, qa_t.end_el, #qa_t.elements)` — then run the CORRECTED
  call the repair would ship:
  `ProcessTrackElements(ResolveMap(qa_t), qa_t.elements)` ·
  `qa_t.start_el = qa_t.elements[1]  qa_t.end_el = qa_t.elements[#qa_t.elements]`.
  Re-print the endpoints; the route must still form, the train still run,
  nothing visual change. **Save, reload, check again.**
- **Case B (the meteor-damaged track — the risky one):** same steps with
  `qa_t` set to the damaged track (pick the right index from
  `MainCity.labels.TrackBase`). Expect the console to print the "unable to
  find the expected number of track elements" assert — that is fine *iff
  nothing corrupts*: after it, the repair site must still be salvageable
  (F45), the rest of the network must still route, and a save + reload must
  come back clean.
- **Readings.** **UNBLOCKS F48** = case A a stable no-op-or-better AND case B
  failing *cleanly* (assert printed, network intact after reload) → the repair
  ships in `90_SaveSanitizer.lua` behind a one-shot flag, skipping tracks that
  carry repair sites. **CONFIRMS THE BLOCK** = case B leaving a track that
  will not route, a stuck train, or a save that reloads broken → close
  `wontfix — repair riskier than the defect`, recording exactly what broke.
  [NEVER RUN] — all of the above; that is what PT-37 exists to change.

⭐ **2026-08-04 — the instrument was Src-verified before the sitting leans on it**
(`corun-batch-1` prompt 1). Three corrections, all visible per chain rule 5; the
procedure above is left standing so the changes are legible.

**Reachability: fine.** `ProcessTrackElements(map, elements, start_element,
adjust_iter)` (`Tracks.lua:807`) is a plain file-scope global and is not in
`ModEnvBlacklist` — callable from the console and the mod env. The corrected
call's first argument is a **map**, which is what `ResolveMap(qa_t)` returns;
`ResolveMap` is a C global taking **one** argument
(`CommonLua/LuaExportedDocs/Game/realm.lua:92`), which is exactly why the shipped
`ResolveMap(track, track.elements)` silently drops `elements`.

**(a) The failure path is NARROWER than this entry says.** `OrderTrackElements`'
failure branch (`Tracks.lua:614-620`) is `assert(false, "unable to find the
expected number of track elements")` → (EF-008, it does not unwind) → **it
restores the original element order** from the `table.copy` taken at `:569` →
`return` nil. `ProcessTrackElements` then hits `if not OrderTrackElements(...)
then return end` (`:818`) and returns **before any Z-repositioning, section
building or pillar work**. The blocking sentence above — *"the caller
repositions every element in Z and recomputes pillars and sections"* — describes
the SUCCESS path only, and overstates the failure case.

**(b) But the residue is real, and this entry UNDER-states it.** The restore
fixes the array order and nothing else. Left behind permanently, and saved:
`start_el.connections = {}` plus a rewritten `connections` on every element the
walk reached (`:578-580`, `:606-617`); a forced `start_el.node_idx = 1` (`:579`)
while the success-path renumbering (`:632-635`) never runs — **a duplicate
`node_idx` is the signature to look for**; and leftover hash keys
`elements[<el>] = true` that are never cleared.

**(c) ⛔ A meteor-damaged track may not sample the failure path at all, and the
source cannot settle it.** `BreakTrackElement` (`Track.lua:618-659`) does **not**
remove the element from `track.elements` — it sets `element.broken = <site>`,
hides it, and places a `TrackConstructionSite`, whose `__parents` are
`{ "ConstructionSite", "TrackGridElement" }` (`TrackElement.lua:580-583`). Two
`TrackGridElement`s therefore occupy the broken hex, while the walk's
`HexGetTrackGridElement` is `hex_grid:GetObject(q, r, "TrackGridElement")`
(`TrackElement.lua:1-3`) and returns exactly one of them. If it hands back the
hidden original the walk **succeeds** and case B is case A again — **the decider
is UNSAMPLED, and a clean result there is not a pass.** ⇒ **Case B must be gated
on a pre-mutation read** that asks the live `object_hex_grid` for each element's
own hex and compares identity; run the corrected call only when that read
predicts a failing walk.

**(d) Do NOT hand-assign `start_el`/`end_el`.** `ProcessTrackElements` sets both
itself on the success path (`:820-822`), so the procedure's assignment line is
redundant there — and on the FAILURE path it writes endpoints the shipped repair
would never write, making case B look more destructive than the repair is.

---

## ⭐⭐ 2026-08-05 — PT-37 RUN (corun-batch-1 sitting, owner attended)

Log: `docs/archive/cb1sitting_Mars.exe-20260805-14.28.49.log`. Save
`CB1STAGE.savegame.sav`, a `Copy-Item` of `TEST2H TRAIN`
(MD5 `103B320A1434513BC8773553096A8958`), build 1.0.7.396349 retail, pack
**81/81 active AS READ**, map `BlankBigCanyonCMix_09`, speed read back 0→3.
FORCED: the fixup call, and case B's meteor. ORGANIC: everything the network did
in response.

### CASE A — PASS, and it is a repair, not merely a no-op

Track `TrackBase#12454`, **280 elements** (⚠️ prep's brief called the longest
track 43 elements — an artifact of `ReadTrackFixture` printing only the first
three; the picker takes the true longest).

`TRY ProcessTrackElements(ResolveMap(t), t.elements) [FIRST EXECUTION] ok=true result=nil`

| | before | after call | after save+reload |
|---|---|---|---|
| `start_el` / `end_el` | `#12432` / `#12476` | unchanged | unchanged |
| `n` | 280 | 280 | 280 |
| `node_idx` | 1…280 contiguous | identical | identical |
| duplicate `node_idx` | none | none | none |
| `connections_total` | **559** | **558** | **558** |

**Predictions 1 and 2 HOLD** — their named falsifiers were a change to
`start_el`/`end_el` or a duplicate `node_idx`; neither occurred.

⭐ **The one changed number is the interesting one.** A linear chain of 280
elements has 279 links, so a symmetric connection list holds exactly
`2 × 279 = 558` — which is what the corrected call produced; the *before* value
held one entry too many. **Consistent with the pass removing one stale or
asymmetric connection**, i.e. doing the repair F48 exists to do rather than
merely not breaking anything. ⚠️ Inference, not proof: the count alone does not
identify which connection changed, and no per-connection dump was taken.
**The 558 survived save+reload** — whatever it fixed persisted through
serialization, which is the property that actually matters for a save sanitizer.

**Owner verdict, Tier A, verbatim:** *"I seen a train pass through every station
atleast once"* — end-to-end route formation across the network, not merely "a
train is moving". Network unchanged at 17 tracks / 926 elements / 0 broken.

### CASE B — ⛔ THE DECIDER IS UNSAMPLED, and prediction 3 is FALSIFIED

Meteor forced onto mid-element `#12275` → **8 broken elements, 8 repair sites,
1 `repair_cgs`**. The pre-mutation gate then read:

`WALK track=#12454 elements=280 checked=280 shadowed=0 missing=0 | PREDICTED OrderTrackElements walk: SUCCEEDS`

**`CB1.Leg1CaseB()` REFUSED TO RUN.** That refusal is the most valuable thing the
harness did all night: without it the sitting would have produced a "clean pass"
on a track that was never unwalkable, and F48 would have been unblocked on
evidence that proved nothing.

⭐⭐ **This SETTLES the C-side question (c) says the source cannot settle.**
`HexGetTrackGridElement` = `hex_grid:GetObject(q, r, "TrackGridElement")` returns
the **hidden original element**, NOT the `TrackConstructionSite` — measured with 8
broken elements present and the walk still finding all 280.

⇒ **`OrderTrackElements` SUCCEEDS on a meteor-damaged track; the assert path is
not reachable that way at all.**

⇒ **The premise this entry is BLOCKED on is contradicted for the cited
scenario.** The blocking sentence reads *"a track it cannot walk (a broken
element, a repair site — i.e. exactly F45's situation)"*. F45's situation does
**not** produce an unwalkable track.

⛔ **What this does NOT establish:** that the repair is safe in general. It
refutes the cited route to the assert, not every route. A path that removes an
element from the hex grid while leaving it in `track.elements` could still reach
it; none was found, and none was searched for hard.

### ⚖️ OWNER DECISION OWED — routed to `PLAYTEST_CHECKLIST.md`

The unblock criterion was written as *"case A a stable no-op-or-better AND case B
failing cleanly"*. The run produced **case A better-than-no-op** and **case B
does not fail at all** — a different answer than the criterion anticipated, and
arguably a stronger one. Whether that unblocks the sanitizer repair is the
owner's call. **Status left `blocked` pending it.**

