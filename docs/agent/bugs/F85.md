---
id: "F85"
seq: 90
row: 97
title: "Breakthrough choice popups + Assembly \"Colony Values\" choice ride real-time waiters — a save in their open window voids the choice"
status: "filed"
status_source: "tag"
priority: "P3"
evidence: "latent"
row_status: "filed 2026-07-30 by the popup audit — tier **U**. ⭐⭐ **SETTLING OBSERVATION RAN 2026-08-10 (`corun-batch-2` sitting) and the entry's own two-way fork CANNOT EXPRESS THE RESULT: the halves split.** ⛔ The REBIND ROUTE IS REFUTED — the retail key-bindings screen exists and carries NO save action at all (owner read it twice); `ActionBindable` is real in source and was never confirmed to surface in the UI. ✅ The SAVE IS NOT REFUSED — a timer-driven save LANDED 39 s inside the open `ShowBreakthroughChoicePopup` modal (popup `Lua 1:10:59`, save `Lua 1:11:38`), and reloading it VOIDED the choice: popup gone, available techs still 50. So the defect is REAL on its own site and the one player route the entry named does not exist — a THIRD CELL. Severity/tier/build = OWNER DECISION (checklist 2026-08-10). ⚠️ §3.6 (the `dont_pause` distress popup) is now the interesting half and was NOT run. NO fix until the owner rules. ⭐⭐ **THE Ctrl-F9 CHECK RAN 2026-08-11 (`corun-pt15` attended sitting) AND CLOSES THE OPEN OBSERVATION — Ctrl-F9 IS NOT A ROUTE.** All three witnesses negative (57 `.sav` unchanged and no file matching `Quick`; no `Game saved:`/`Save failed:` in the marker window; owner saw nothing) — and then a STRUCTURAL control rather than three absences: `Platform.cheats=nil` (corrected from this entry's recorded `false`), `idQuickSave=nil` and `idQuickLoad=nil` against **437 actions / 433 with ids and `poscontrol=true`**, so the lookup works and the action genuinely is not built (`GameCheatShortcuts.generated.lua:17-18` gates it on `Platform.cheats`). **No save-the-game action exists at all** — the six `save`-matching ids are editor utilities. ⚠️ The save was NOT refused: `CanSaveGame()` returned `raw=persist type=string truthy=true` (EF-048). Disposition still OWNER's. ⚠️ Harness finding: console `print()` does NOT reach the log — only `say`/ModLog does (entry)"
updated: "2026-08-11"
copies: []
---
### F85 — Breakthrough choice popups and the Assembly "Colony Values" choice ride real-time waiters; any save landing in their open window silently voids the choice (P3, LATENT — tier U)  `[filed 2026-07-30 by the popup audit — NOTHING BUILT, settling observation queued; no fix until U resolves]`

The F83 family's latent members — found by the popup/deferred-consequence audit
(`docs/reports/POPUP_CONSEQUENCE_AUDIT.md` §3.3, which is the full evidence; this entry
is the record of claim). Four popups carry heavyweight consequences in code
that runs **after** a `WaitPopupNotification` inside a **real-time** thread —
the thread class that does not survive a save/load:

- **`ShowBreakthroughChoicePopup`** (`Anomaly.lua:696-714`,
  `CreateRealTimeThread` :703): after the wait,
  `UIColony:SetTechDiscovered(techs[res].id)` (:708) + the caller's callback —
  **the breakthrough discovery itself**. Three callers/presets: subsurface
  anomaly (`Anomaly.lua:393`), planetary anomaly (`PlanetaryAnomaly.lua:268` —
  the anomaly is already consumed, `DoneObject` :351), and the Breakthrough
  law (`LawDef-Research.lua:78`).
- **`AssemblyChoicePopup`** (`Factions.lua:1191-1236`, spawned
  `CurrentMap:CreateRealTimeThread` from `AssemblyBase:GameInit`,
  `MartianAssembly.lua:8`): after the wait, `ApplyAssemblyChoice` (:1214) —
  faction weights, initial laws, colonist standings, `ElectMembers()`. The
  **entire politics initialization**; `GameInit` never re-runs, so a lost
  choice would leave the Assembly built and the politics system dead.

**Why this is latent rather than live — the modal shield.** All four presets
open immediately (`start_minimized = false`), modal + input-suppressed +
game-pausing (`PopupNotification:Init`). No autosave can fire (game time is
paused) and the quicksave shortcut (Ctrl-F9) is eaten by the popup's
`OnShortcut`. So in ordinary play **no save can exist inside the window**, and
loading any older save replays the trigger. The shield is UI reachability, NOT
the save system: `CanSaveGame` has no popup clause, and Quick Save is
`ActionBindable` while `PopupPropagateShortcuts`
(`MarsMessageQuestionBox.lua:1-9`) lets **F9 and F11 through the modal layer**
— a player who rebinds Quick Save onto one of those keys can produce the
poisoned save. **Settling observation (needs-eyes item 3 in the audit):**
rebind Quick Save to F9, open any choice popup, press it — if a save lands,
this is R2-by-rebind and worth an owner decision; if the binding or save is
refused, this drops to I/R4 and stays documentation.

**Intent — unintended (sibling contradiction):** every subsystem that carries
real consequences through a popup wait (challenges, storybits, sequences,
status-effect popups) waits in a **game-time** thread, which the engine
persists by default (ENGINE_FACTS); these sites chose the one thread class the
persist machinery cannot save. The modal shield makes the choice harmless
today, which is why this files at P3/U rather than P2.

**Also folded in here (audit §3.6, R3-edge, no fix proposed):** the game's one
`dont_pause` popup — the distress-call confirmation
(`RivalColonies.lua:535-555`) — is the only popup window where the game runs,
so a sol-change autosave can land under it. That save loses only the popup
itself (nothing is committed before its wait — self-healing, harm ≈ 0), but any
async popup **queued behind it** at that moment (including a storybit's, which
has no corner notification to resurrect it) is dropped from `g_PopupQueue`
with its game-time waiter left blocked forever — event lost, `g_StoryBitActive`
ghost, a non-OneTime storybit dead for that colony. Requires the player
mid-distress-flow at the exact autosave tick with a second popup queued —
recorded for completeness.

**No fix ships on this entry until the observation runs** (revised-§4
discipline: a U tier authorizes an observation, not a build). If it proves
real, the audit's §7.3 names the shape: move each consequence into a game-time
thread and let the real-time side only present UI — per site, no shared-
machinery surgery.

## ⛔⛔ 2026-08-10 — THE SETTLING OBSERVATION RAN. THE DEFECT IS CONFIRMED AND THE ROUTE IS REFUTED — the entry's own two-way fork cannot express the result (`corun-batch-2` sitting)

Retail `Mars.exe` **1.0.7.396349**, staged copy `CB2SIT.savegame.sav` (`Copy-Item` of `TEST2H TRAIN`, MD5 `103B320A1434513BC8773553096A8958`), map `BlankBigCanyonCMix_09`, sol 333, pack **81/81 active AS READ**, **0 `[LUA ERROR]` lines in the whole 2,881-line log**. Log `docs/archive/cb2sitting_Mars.exe-20260810-15.30.16.log`. **This entry's settling observation reads:** *"rebind Quick Save to
F9, open any choice popup, press it — if a save lands, this is R2-by-rebind and
worth an owner decision; if the binding or save is refused, this drops to I/R4
and stays documentation."* **Both branches are wrong, because the two halves
split.**

### Half 1 — THE BINDING IS REFUSED. There is no Quick Save action to rebind.

The retail key-bindings screen **exists** (owner screenshot: Report Bug,
Politics, Pan Up/Down/Left/Right, Rotate Camera ×4, Zoom In/Out, Orbit Camera,
Pause Game, Speed Up …) and carries **no save-related action at all**. The
owner read it twice and challenged the instruction, which is the only reason
this was checked rather than assumed.
**Condition sampled:** the owner's own read of the retail options bindings list.
⚠️ **NOT** a source enumeration — `ModTools\Src` is not installed on the test
machine, so this is an eyes-negative with its condition stated, not a proof.
⇒ *"a player who rebinds Quick Save onto F9"* is **not a route on this build**.
The `ActionBindable` property this entry cites is real in source and was never
confirmed to surface in the retail UI.

⭐⭐ **SOURCE READ 2026-08-10 (terminal-audit follow-up), superseding the caveat
above — `ModTools\Src` WAS on the machine after all** (the Relaunched Steam
installdir is literally `Project Spark`; ledger S12 corrected). The game's
ONLY Quick Save action is `idQuickSave` (`Data\XDef\GameCheatShortcuts.lua:
1990-2003`, `ActionShortcut "Ctrl-F9"` — note: **Ctrl-F9, not the F9 this
entry's old advice assumed**, `ActionBindable true`), a child of the `Cheats`
node whose `__condition` is `Platform.cheats` (`:11-17`). The GENERATED
executable (`Lua\XDef\GameCheatShortcuts.generated.lua:18-19`) compiles that
condition as `local cond = Platform.cheats / if cond then … end` wrapping the
whole block, whose last two members are `idQuickSave`/`idQuickLoad`
(`:2207-2231`) — and `Platform.cheats = false` on retail is already a
MEASURED fact (it is why F101's `TestMeteor` is missing). No other definition
of the action exists in the tree.

⚖️ **OWNER CHALLENGE 2026-08-11, and it binds — the claim above is an
INFERENCE CHAIN, not an observation:** *"We have no real evidence that quick
saves don't exist; the agent always thought they lived on F9. The only
evidence we have is that quick saves are not a keybind change option."*
Correct on both counts: the SAMPLED fact is only that the bindings screen
carries no save row; "pressing Ctrl-F9 on retail does nothing" is predicted
by the generated code and has never been tried — and a source-derived UI
claim briefed without eyes is EXACTLY the failure that produced this entry's
dead F9-rebind advice. ⇒ **Settling observation queued (rides the PT-20 redo
sitting, ~10 s):** in a colony on retail, press **Ctrl-F9** — a quicksave
loading screen / `QuickSave.savegame.sav` on disk FALSIFIES the inference and
makes the default binding a live player route into this defect; nothing
happening confirms it. **Disposition stays OPEN until that check runs.**
(Sibling note either way: `idQuickSave`'s own `ActionState` consults
`CanSaveGame()`, which this leg measured permitting a save inside the modal.)

### Half 2 — THE SAVE IS NOT REFUSED. A save landed 39 seconds inside the modal.

With no rebind available the sitting used a timer instead: a real-time thread
sleeping 45 s, then the proven `SaveGame` primitive, started immediately before
`ShowBreakthroughChoicePopup` — F85's **own site**, not a stand-in. A real-time
thread was required precisely because choice popups pause the game.

| | reading, verbatim |
|---|---|
| baseline | `PPOPUP available breakthrough techs = 50 (read ok=true)` |
| popup opens | `TRY ShowBreakthroughChoicePopup [FIRST EXECUTION] ok=true result=nil` — `Lua 1:10:59` |
| the save | `SAVE SaveGame returned err=false name=CB2F85.savegame.sav` — `Lua 1:11:38-39` |
| on disk | `CB2F85.savegame.sav`, 54,395,258 bytes, 16:41:56 |
| the reload | `LOAD OK #5 file=CB2F85.savegame.sav — LoadGame returned no error` |
| after | `POPUPSUBJ breakthrough choice popup: available techs = 50 (read ok=true)` — and **no popup on screen** (owner) |

**⇒ The pending choice is VOIDED.** The popup is gone, the breakthrough was
never discovered, and the choice cannot be answered any more. That is exactly
what this entry predicts: the real-time waiter (`Anomaly.lua:703`) does not
persist.

**Witness — the log's own line ORDER, and the count as a second check.** A save
taken before the popup opened would sit *above* the `PPOPUP` block; it sits
below it. And had the owner answered before the timer fired,
`SetTechDiscovered` would have dropped the count to 49 — it read **50** both
before the popup and after the reload.

### ⚖️ The third cell — ROUTED TO THE OWNER, not decided here

`CanSaveGame` has no popup clause: **measured, correct.** The rebind route:
**dead.** So the defect is real and the one player route this entry named does
not exist. Severity, tier, and whether anything is built are the owner's call —
`PLAYTEST_CHECKLIST.md` "Decisions waiting on you", 2026-08-10.
⚠️ **The interesting half is now §3.6**, the distress-call popup: the game's one
`dont_pause` popup, i.e. the only place a vanilla autosave could reach a save
under a popup with no rebind involved. That rider was NOT run this sitting.

⛔ **Not claimed:** NOT reachability by an ordinary player. NOT `tested`. The
popup was FORCED, so nothing here says a real subsurface-anomaly or
Breakthrough-law popup behaves identically, though it is the same function. The
`AssemblyChoicePopup` half of this entry was not touched at all.

## ⭐⭐ 2026-08-11 — THE Ctrl-F9 CHECK RAN. The inference is CONFIRMED, and it is no longer an inference (`corun-pt15` prompt 2, attended sitting)

The owner's 08-11 challenge above demanded this, and it was right to: *"pressing
Ctrl-F9 on retail does nothing"* had never been tried. It has now. Retail
`Mars.exe` **1.0.7.396349**, staged copy `CP15STAGE.savegame.sav`, map
`BlankBigTerraceCMix_10`, pack **81/81 active AS READ**, colony day 17.
Log `docs/archive/cp15sitting_Mars.exe-20260811-15.09.30.log`.

### All three witnesses came back negative

| witness | reading |
|---|---|
| ⭐ **on disk** | save dir held **57 `.sav` before and after** the press; **no file matching `Quick`** anywhere in the directory; nothing written since 11:13 except `account.dat` and the launch `.vdf` |
| **the log** | between the `F85 BEFORE` and `F85 AFTER` markers: **no `Game saved:`, no `Save failed:`** — nothing at all |
| **the owner's eyes** | *"I didn't get any save notification, we are still not sure quick saves actually exist"* (log **L412**, verbatim) |

**⛔ Three absences are not a mechanism, so a structural control was taken** —
because an absence cannot distinguish *"the action does not exist"* from *"the
keypress never reached the game"* (the console eats keystrokes), and this entry
has already been burned once by a UI claim briefed without eyes.

### The control, and what it establishes (log **L408**)

```
Platform.cheats=nil type=nil | idQuickSave=nil | idQuickLoad=nil
actions=437 with_id=433 poscontrol=true
save_like=DE_SaveDefaultMap,DE_SaveDefaultMapEntityList,E_CameraSave,
          OpenSavegameLocation,ResaveAllGameMaps,ResaveAllPrefabs
```

* **`Platform.cheats` is falsy — and reads `nil`, not `false`.** This entry's
  08-10 section records it as "`Platform.cheats = false` … a MEASURED fact".
  Both are falsy so the conclusion is unaffected, but the **recorded value is
  corrected to `nil` as read**.
* **`idQuickSave` does not exist as an action.** `XAction:new{ActionId =
  "idQuickSave"}` is constructed inside `if cond then` with `cond =
  Platform.cheats` (`Lua\XDef\GameCheatShortcuts.generated.lua:17-18`, the
  action at `:2207-2220`). On retail that block never runs. ⇒ **there is no
  action to fire, bind, or press** — a strictly stronger claim than "the key did
  nothing", and it makes the console-focus confound irrelevant rather than
  arguing it away.
* **`poscontrol=true` is why the absence counts.** `XShortcutsTarget` exists and
  carries **437 actions, 433 with ids**, and `ActionById` resolves successfully
  against a known-present id. So `idQuickSave=nil` is a real absence, not a
  broken lookup.
* **No save-the-game action exists at all.** The only `save`-matching ids are
  map/prefab/camera **editor** utilities plus a folder-opener. That
  independently corroborates the half the owner questioned: the bindings screen
  carries no save row because the action set contains no save action to list.

**⚠️ And the save was NOT refused.** `CanSaveGame()` returned **`raw=persist
type=string truthy=true`** — a *string*, and truthy. `idQuickSave`'s own
`ActionState` is `return not CanSaveGame() and "disabled"`, so a truthy return
means the action would not have been disabled. Nothing about the save system
blocked this; the action simply is not there. (EF-048 earns its keep again —
`== true` would have read that as false.)

⇒ **Ctrl-F9 is not a live player route into this defect.** The settling
observation queued on 08-11 is CLOSED, confirming the source prediction — by
observation plus a structural read, not by absence alone. **Disposition
(severity, tier, whether anything is built) remains the owner's open decision**;
this leg routed evidence only and decided nothing.

### ⚠️ Harness finding worth keeping — console `print()` does not reach the log

The control was first run as a bare console `print()` and **produced no line in
the log file** (403 lines before and after, with `FlushLogFile()` already
called). Only the harness's `say`/`ModLog` path lands. It had to be re-run
wrapped in `CP15.Note(...)` to become citable evidence. ⇒ **any future attended
sitting that gathers evidence via bare `print()` will produce screenshots and no
archived record.** EF-047 family (what the archived log does and does not
capture); routed as an engine-fact candidate at close-out.
