---
id: "F35"
seq: 35
row: 35
title: "Large Wind Turbine buff lost in old saves (fixup bug)"
status: "fixed"
status_source: "tag"
priority: "P2"
evidence: "high"
row_status: "fixed"
updated: "2026-08-11"
copies: []
---
### F35 — Large Wind Turbine buff lost in old saves (P2, high)  `[fixed: Code/90_SaveSanitizer.lua]`
Current `FrictionlessComposites` data is CORRECT (`Data\TechPreset.lua:796-821` targets
WindTurbine, WindTurbine_Large, WindTurbine_Diffuser). But the migration fixup
`SavegameFixups.WindTurbine_Large_ReapplyModifiers` (`Buildings\WindTurbine.lua:78-88`)
only reapplies the `WindTurbine_Diffuser` label — never `WindTurbine_Large`. Saves that
researched the tech pre-hotfix keep unbuffed Large Turbines forever. Matches review
report ("polymer upgrade works now, frictionless doesn't"). Rotation lock is by design
(`can_rotate_during_placement = false`). **Fix:** one-shot LoadGame sweep: if tech
researched and no colony label-modifier for `WindTurbine_Large.electricity_production`,
add it (mirror the fixup, corrected).
*Implemented as sketched*, in the consolidated sanitizer, and generalised one step: the
pass is driven by the tech preset's OWN `Effect_ModifyLabel` entries rather than a
hard-coded label list, so it restores whatever the tech says it grants (today: all three of
WindTurbine / WindTurbine_Large / WindTurbine_Diffuser at +100% `electricity_production`)
and does nothing if a game update changes them. Confirmed the three labels are disjoint —
a building is added to a label named after its own class (`Building:AddToCityLabels`,
`Building.lua:427-443`), and the three turbine templates are separate classes — so nothing
else was covering Large turbines. Conservative: any existing percent modifier for that
property on that label counts as "already buffed" and the label is skipped, so the pass
cannot double-buff, and it is idempotent across loads.
*QA audit 2026-07-25 — one HIGH defect found and repaired:* the pass originally hooked
`OnMsg.LoadGame`, but `UnpersistGame` fires `Msg("LoadGame")` BEFORE `FixupSavegame`
(`CommonLua\Savegame.lua:810-813`). On the first load of a save the shipped
`WindTurbine_Large_ReapplyModifiers` fixup had not yet been applied to, the pass ran ahead
of it, saw the Diffuser label bare, buffed it — and then the fixup unconditionally added
its own +100% (`WindTurbine.lua:80-87` has no already-buffed check): +200% baked into the
save permanently. Repair: the handler now hooks `OnMsg.PostLoadGame`, which fires after
fixups (`Savegame.lua:813`); the "any percent modifier → skip" guard then holds in every
ordering. Also hardened while in there (latent, dormant today): `amount` is now scaled via
`GetModifiablePropScale(prop)` the way the live tech apply scales it (`Tech.lua:298-301`) —
all three shipped effects have Amount 0, but the pass is preset-driven by design. The
LoadGame-vs-fixup ordering cannot be discriminated by the probe (it drives the pass
directly); PT-35 case C remains the only true fixture.
Probe: `SaveSanitizerTurbineBuff` in `30_Probes_Wave3.lua`.
**✅ SCOPE QUESTION CLOSED 2026-08-01 — MEASURED, and the answer is that our
scope is RIGHT: the live label path works, for all three labels including
`WindTurbine_Large`.** Owner at the keyboard, log
`Mars.exe-20260801-14.59.57-6a22b86d.log`, a **pre-research save loaded fresh**
so our own pass could not have run first (it early-returns at
`90_SaveSanitizer.lua:58` when the tech is not researched — verified in the same
sitting) and **no reload anywhere in the measurement window**:
* **Before** (tech not researched): all three labels
  `NO MODIFIERS` — a clean baseline, nothing pre-supplied by anyone.
* **After** the tech landed: every one of `WindTurbine`, `WindTurbine_Large`,
  `WindTurbine_Diffuser` carries `prop=electricity_production percent=100`,
  **keyed by the vanilla `Effect_ModifyLabel` objects with `id=GameEffect` — NOT
  `SMRFixPack_F35_*`**, so it is the tech's own apply and demonstrably not our
  sanitizer's work.
* **And it reaches the buildings, not just the colony table** — Power doubled on
  all three, exactly: Wind Turbine 9.3 → 18.6, Large 18.6 → 37.2, Shrouded
  (Diffuser) 29.8 → 59.5. So label *membership* is intact for these classes too,
  and the +100% compounds with the 86% elevation boost multiplicatively
  (`5 × 1.86 × 2 = 18.6`) rather than adding.
* **Trigger stated exactly:** the tech was granted with
  `UIColony:SetTechResearched("FrictionlessComposites")`, not researched
  naturally — it is a **Breakthrough** (`TechPreset.lua:799`, `group =
  "Breakthroughs"`) and was not obtainable in that colony. That is the same
  funnel natural completion uses: `Research:SetTechResearched` →
  `tech:EffectsApply(UIColony)` (`Research.lua:313`) → `Effect_ModifyLabel:OnApplyEffect`
  (`MarsGameEffects.lua:161`) → `SetLabelModifier`; the natural paths
  (`Research.lua:803`, `:841`) reach line 313 through that same function and
  differ only in the `notify` flag, which only adds the popup.
* ⚠️ **A trap this sitting walked into, recorded so nobody repeats it:** the
  first "after" reading was taken while **Low-G Turbines** was completing, not
  Frictionless Composites. `LowGTurbines` (`TechPreset.lua:2830-2844`, group
  Physics) grants two `Effect_UnlockUpgrade` entries (Polymer Blades) and
  **no label modifier at all** — so its three `NO MODIFIERS` readings were
  correct and would have been mis-filed as a P1 defect. Low-G Turbines is the
  "polymer upgrade works" half of the witness's own sentence. Always confirm
  with `IsTechResearched("FrictionlessComposites")` before reading.
**Consequence: the fix is NOT aimed one layer too shallow.** F35 is the old-save
migration failure it was filed as, the pass is not quietly repairing a live
defect on every load, and "witnessed and fixed" is now claimable. What this does
NOT establish: that the Nov 2025 witness was mistaken about *their* game — this
measures build 1.0.7.396349's live path, not theirs.

## ⛔ 2026-08-10 — the PT-35 turbine FIXTURE's grant instrument was wrong as parked; corrected before it ran

`corun-batch-1`'s parked leg-5 source (`git show
530df63:docs/agent/prompts/corun-batch-1/99_CB1Legs.lua.txt`) proposed
`CheatResearchAll()` to grant Frictionless Composites, on the stated reasoning
that *"it skips UNDISCOVERED breakthroughs, which FrictionlessComposites is
not."* **That reasoning is unverified and the premise is false on this build.**
Leg 5 never ran in that sitting, so the error was never executed; found by
`corun-batch-2` prep Src-verifying the instrument before the sitting leans on
it (rule 10, first-execution discipline).

- `CheatResearchAll` researches a tech only when
  `discoverable or UIColony:IsTechDiscovered(tech_id)` (`Lua\Cheats.lua:84`).
- `TechFields["Breakthroughs"].discoverable = **false**`
  (`Data\TechFieldPreset.lua:279-283`).
- `FrictionlessComposites` is `group = "Breakthroughs"`
  (`Data\TechPreset.lua:796-802`).

⇒ On a save where the breakthrough has not been **discovered**,
`CheatResearchAll()` grants it **nothing** — and the fixture read afterwards
would show `researched=false` with no explanation, or worse, the leg would
research the entire rest of the tree as a side effect and call the fixture
built. Discovery status was never read on `TEST2H TRAIN`; only
`researched=false` was (`docs/archive/cb1confirm_Mars.exe-20260804-20.30.21.log:379`).

✅ **Use the grant this entry's own 2026-08-01 measurement used**:
`UIColony:SetTechResearched("FrictionlessComposites")` — `Research.lua:276`,
which calls `SetTechDiscovered` itself at `:285`, so discovery is not a
precondition. It is the same funnel natural completion uses (the walkthrough is
above), it is one targeted call instead of a colony-wide research sweep, and it
is **already proven by execution** in the 2026-08-01 reading. Read
`IsTechDiscovered` alongside `IsTechResearched` either way, so the fixture
record states which it started from.

**~~⚠ OPEN SCOPE QUESTION~~ — added 2026-08-01 by the bug-list audit (§2.2, row
F35, ⚠), closed the same day by the reading above.** The witness thread this entry cites ("polymer upgrade works now,
frictionless doesn't"; TheNightglow Nov 2025 [S27], Frictionless Composites
named with a player-derived formula) **may be describing a LIVE label miss in a
current game, not the old-save migration failure this fix repairs.** If so, our
scope is one layer too shallow: the pass would be quietly repairing on every
load something that should never have broken, and the player-visible defect
would persist for a whole session before the next load. **The rider that
decides it is in `PLAYTEST_CHECKLIST.md` §6** (bug-list-audit table) — research
the tech in a live session and read `UIColony.label_modifiers` **without
reloading**, because our own auto-running pass masks the answer the moment a
save is loaded. Until that reading exists, "witnessed and fixed" is not
claimable for F35.

**✅ SOURCE CROSS-CHECK RUN 2026-08-01 (chain prompt 6, job 3) — scope
CONFIRMED from the other side; nothing filed.** Prompt 2 settled the live half
by measurement, so this was the residual source pass. Three results, all read
in 1.0.7.396349 Src this session:
1. **The defect this entry claims is still present, verbatim.**
   `SavegameFixups.WindTurbine_Large_ReapplyModifiers`
   (`Lua\Buildings\WindTurbine.lua:78-88`) still reapplies **only**
   `WindTurbine_Diffuser` — one `SetLabelModifier` call, no `WindTurbine`, no
   `WindTurbine_Large`. The entry's cited lines are re-verified, not inherited.
2. **Nothing else in the game covers it.** `WindTurbine_Large` appears nowhere
   in any Lua `SetLabelModifier` or savegame fixup in the whole Src tree — a
   grep returns only its `BuildingTemplate` and FX presets. So the old-save
   miss has no vanilla repair on our pinned build and our pass is not
   redundant.
3. **The one 1.0.7 fixup that touches this machinery cannot cover F35 — but it
   does independently justify the PostLoadGame hook.**
   `SavegameFixups.Move_Effect_ModifyLabel_FromCitiesBackToColony`
   (`Lua\MarsGameEffects.lua:180+`) *relocates* existing `Effect_ModifyLabel`
   modifiers out of per-city containers and onto the colony; it never
   re-derives a missing one from a tech preset, so it is no substitute. What
   it does mean is that an old save can carry the turbine modifier on a
   **city** while `UIColony.label_modifiers` — the table our pass reads
   (`90_SaveSanitizer.lua:64`) — is still bare. Had the pass stayed on
   `OnMsg.LoadGame`, it would have run before that move (`Msg("LoadGame")`
   :810 → `FixupSavegame` :811 → `Msg("PostLoadGame")` :813,
   `CommonLua\Savegame.lua`), seen nothing, and double-applied. **That is the
   2026-07-25 QA repair's failure mode reached by a second, previously
   unrecorded route** — the repair holds against both; recorded so nobody
   "simplifies" the hook back.

## ✅ 2026-08-10 — THE PT-35 TURBINE FIXTURE EXISTS, and the corrected grant is now proven BY EXECUTION (`corun-batch-2` sitting, leg S)

Retail `Mars.exe` **1.0.7.396349**, staged copy `CB2SIT.savegame.sav` (`Copy-Item` of `TEST2H TRAIN`, MD5 `103B320A1434513BC8773553096A8958`), map `BlankBigCanyonCMix_09`, sol 333, pack **81/81 active AS READ**, **0 `[LUA ERROR]` lines in the whole 2,881-line log**. Log `docs/archive/cb2sitting_Mars.exe-20260810-15.30.16.log`. Fresh reload of the staged copy first, so the fixture carries no
bombardment state.

| | reading, verbatim |
|---|---|
| before | `PT35 FrictionlessComposites researched=false (ok=true) discovered=nil (ok=true) \| 0 Large, 6 plain, 12 Diffuser turbine(s)` |
| the grant | `TRY SetTechResearched(FrictionlessComposites) ok=true result=true` |
| after | `PT35 FrictionlessComposites researched=1 (ok=true) discovered=163 (ok=true)` |
| owner places | `PT35 … \| 1 Large, 6 plain, 12 Diffuser \| when=leg S, before the fixture save` |
| fixture gate | `READ PT35-turbine-half-fixture(a Large Wind Turbine exists) = 1 \| population=1 \| APPLICABLE=true` |
| saved | `SAVE SaveGame returned err=false name=PT35FIXTURE.savegame.sav` — on disk 54,424,001 bytes, 17:14 |

⭐ **The 2026-08-10 correction above is now confirmed by measurement, not only
by source.** `discovered` read **`nil`** immediately before the grant — exactly
the state in which `CheatResearchAll()` grants a Breakthrough **nothing**. The
parked batch-1 leg-5 instrument would have researched the entire rest of the
tree and produced no Frictionless Composites.

⚠️ **Type trap for the next probe:** `IsTechResearched` returned **`1`** and
`IsTechDiscovered` returned **`163`** — numbers, not booleans. Both truthy, but
an `== true` test reads false. (Two siblings found the same day: `el.broken` is
a **table**, `GetSpentTimeAverageInHours` is a **`T()` object**.)

**FIXTURE CONTENTS — `PT35FIXTURE.savegame.sav`**, the one deliberate survivor of
the sitting, named in its recording commit: `CB2SIT` lineage (Copy-Item of
`TEST2H TRAIN`, MD5 `103B320A…8958`), `FrictionlessComposites` researched=1
discovered=163, **one Large Wind Turbine** (working, Polymer Blades offered),
**one applied building upgrade** — Remote Medic on a Hospital — for the F03
leaked-upgrade-modifier half.

⛔ **This leg proves nothing about F35 or F03.** It builds a fixture. The tech
was granted *before* the turbine was placed, so this does NOT reproduce F35's
own scenario (a save that researched the tech pre-hotfix) — it is the
do-no-harm fixture PT-35 case A needs, and it unblocks that leg's turbine half,
UNSAMPLED since `unattended-1` for want of a Large Wind Turbine on the map.

## ✅ 2026-08-11 — THE FIXTURE IS RE-CONFIRMED GOOD on a fresh load (`unattended-2` run 1)

Read off a byte-identical `Copy-Item` of `PT35FIXTURE.savegame.sav`
(MD5 `D721329D1EE18604B3D6C89401F74738`, 54,424,001 bytes) on retail
`Mars.exe` 1.0.7.396349, map `BlankBigCanyonCMix_09`. Log
`docs/archive/u2run1void_Mars.exe-20260811-01.17.34.log`. ⚠️ **This run's
pack-dependent verdicts are all VOID** (the Community Fix Pack was disabled in
the Mod Manager — see [F48](F48.md)), **but these reads are not pack-dependent**:
they are the world's own state, and the pack being absent is exactly what makes
them a clean baseline.

| reading, verbatim | |
|---|---|
| `PT35 FrictionlessComposites researched=1 (type=number truthy=true ok=true) discovered=163 (type=number truthy=true ok=true) \| 1 Large, 6 plain, 12 Diffuser turbine(s)` | the fixture survived the save that made it |
| `READ PT35-turbine-half-fixture(a Large Wind Turbine exists) = 1 \| population=1 \| APPLICABLE=true` | ⭐ **APPLICABLE=true at last** — the population that read 0 for `unattended-1` and again for `corun-batch-1` |
| `PT35 upgraded buildings=144` | the F03 half has a real population too, found by walking every label of every city and reading `upgrades_built` rather than guessing a class name |
| `dome_upgrade_shaped_ids=3` | three live `<handle>_upgrade<N>_mod_<M>` ids across 13 domes — the exact id shape `RepairLeakedUpgradeModifiers` sweeps, so its do-no-harm read finally has something it could wrongly strip |
| `turbine_label_modifiers=3 turbine_percent_sum=300` | the tech's own three `+100%` label modifiers are present **with our pack absent** — the control this entry's 2026-08-01 measurement predicts |

⚠️ **EF-048 re-confirmed on a second occasion**: `researched` read `1` and
`discovered` read `163` — numbers, not booleans, six days after the first
sighting on this same fixture.

⛔ **What this does NOT say.** It does not sample the Remote Medic upgrade
specifically: the finder reports 144 upgraded buildings and printed four
`SmartApartmentsCCP1`/`SmartHome_Small` examples, so *"an applied upgrade
exists"* is measured and *"the Hospital's Remote Medic is among them"* is not.
And in THAT run PT-35 leg A's turbine half was still UNRUN — no sanitizer pass
executed, because the pack was not loaded. The chain's stop condition 3 (a
broken fixture) was **not** triggered: the fixture was fine; the pack was off.

## ✅✅ 2026-08-11 — PT-35 LEG A's TURBINE HALF IS SAMPLED AT LAST, and the do-no-harm claim now rests on a real population

Owner re-enabled the pack and the run was redone the same night. Retail
`Mars.exe` **1.0.7.396349**, pack **81/81 active AS READ**, staged copy of
`PT35FIXTURE`, **three loads, two save+reload round trips, six pass calls**,
**0 `[LUA ERROR]` in the whole final log**. Log
`docs/archive/u2run3_Mars.exe-20260811-02.01.06.log`. FORCED: everything —
staged copy, the calls, the saves. ORGANIC: nothing. ⛔ Not a `tested` grant.

| comparison | reading |
|---|---|
| both passes called, load 1 (PRE-RELOAD) | `RepairTurbineBuff returned 0` · `RepairLeakedUpgradeModifiers returned 0` · **0 of 14 readings changed** |
| **across save+reload #1 (R4)** | **0 of 14 readings changed** |
| both passes called, load 2 | both `returned 0` · **0 of 14 readings changed** |
| **across save+reload #2 (R4)** | **0 of 14 readings changed** |
| both passes called, load 3 | both `returned 0` · **0 of 14 readings changed** |
| **START → FINISH** (3 loads, 2 round trips, 6 calls) | **0 of 14 readings changed** |

⭐⭐ **`RepairTurbineBuff`'s zero is no longer trivially forced — this is the
whole point of the fixture.** `unattended-1`'s 0 came from the early return at
`90_SaveSanitizer.lua:58` on an unresearched tech, and was recorded as
UNSAMPLED. Here `IsTechResearched` read **`1`**, so the pass walked its full
body over all three of the tech's `Effect_ModifyLabel` entries, found a percent
modifier already present on each (`turbine_label_modifiers=3
turbine_percent_sum=300`), and skipped all three. **The "already buffed → skip"
guard is now measured doing its job on a researched save, which is the exact
scenario this pass exists for.** `APPLICABLE=true`, population 1 Large turbine.

⭐ **And `RepairLeakedUpgradeModifiers`'s zero is sampled on live state.**
`READ PT35-F03-do-no-harm-population(live upgrade-shaped modifier ids) = 3 |
population=3 | APPLICABLE=true` over the colony, every city and all 13 domes,
with **144 upgraded buildings** on the map. `unattended-1` could say *"it does
not strip live state"* only over a population whose upgrade-shaped id count was
never measured; here three real `<handle>_upgrade<N>_mod_<M>` ids sat in front of
the sweep across six calls and two round trips and **not one was removed**.

The 14 readings compared each time, so a future reader can check rather than
trust: `turbine_label_modifiers` · `turbine_percent_sum` ·
`large_turbine_electricity_production` (own and resolved, both `29008`) ·
`colony_label_modifiers` (131) · `colony_upgrade_shaped_ids` (0) ·
`city_label_modifiers` (8) · `city_upgrade_shaped_ids` (0) · `dome_count` (13) ·
`dome_label_modifiers` (48) · `dome_upgrade_shaped_ids` (3) · `turbines_large`
(1) · `turbines_plain` (6) · `turbines_diffuser` (12). The comparison names the
entry that moved, not just how many did — nothing moved.

⛔ **Scope, stated plainly.** One save lineage, one fixture, one build. This is
**MECHANISM**-grade evidence from an unattended run: PT-35 leg A case A is
complete, but the section does not become `tested` and cases B and C stay
parked. The Remote Medic upgrade specifically is still not identified by name —
144 upgraded buildings and 3 live upgrade-shaped ids are what was measured.

## ⚖️ 2026-08-11 — TERMINAL AUDIT (unattended-2 prompt 2): PT-35 LEG A CASE A SUSTAINED AS COMPLETE

Re-derived, not inherited: all six `0 of 14 readings changed` diffs located in
the archived log (byte-verified against its original) at their claimed
comparisons — after each of the three pass-call pairs, across both R4 round
trips, and start-to-finish; `APPLICABLE=true` with its population printed on
every read (1 Large turbine, 144 upgraded buildings, 3 live upgrade-shaped
ids); `researched=1 (type=number truthy=true)` on all three loads, so the
turbine zero is sampled, not an early return. The checklist's PT-35 status
token still read `unrun` and was corrected by this audit; the section stays on
the checklist because cases B/C are parked in it. No verdict changed.
