---
id: "C50"
seq: 130
row: 165
title: "SpaceY is the only sponsor in the game whose gameplay modifier has no matching description bullet: its `+20 CommandCenterMaxDrones` is granted and never mentioned, while all five other modifier-carrying sponsors describe every one of theirs"
status: "tested-attended"
status_source: "tag"
priority: "P3"
evidence: "SOURCE-VERIFIED (both `Effect_ModifyLabel` entries and the `effect` string, `Data/MissionSponsorPreset.lua:625` and `:694-703`) + SOURCE-VERIFIED CONTROL (whole-preset sweep: 16 sponsors, 6 carry `Effect_ModifyLabel`, 5 of 6 describe every modifier they carry — SpaceY is the only miss) + the effect target re-derived by symbol (`DroneHubBase:GetMaxDrones` is the ONLY reader of `g_Consts.CommandCenterMaxDrones`, so 'Drone Hubs' is the accurate noun) — ⛔ NOT measured in game, ⛔ no fix proposed, ⛔ not a release gate. ⚠️⚠️ THE OBVIOUS REMEDY IS A REGRESSION IN 8 LANGUAGES (`EF-039`): the vanilla string is a shipped, translated id and replacing it with a new literal renders English for every non-English player. ⇒ BUILT 2026-08-20 as `Code/Fix_SpaceYDroneCapBullet.lua` on route C (append at the UI, preset untouched), ⛔ UNOBSERVED. ⭐ THE RENDER-SITE LIST WAS RE-DERIVED FOR THE BUILD AND IT MOVED: **three LIVE sites, not two** — `GetSponsorSummary` (`PreGameMission.lua:574`), `GetSponsorDescr` (`:644`, the sponsor-picker rollover, previously unrecorded) and `LandingSiteObject:GetMissionSponsorEffect` (`PlanetUI.lua:314`, the challenge landing spot, previously unrecorded) — while `GetMissonProfileText` (`MissionProfileDlg.lua:25`), which this entry called the in-game mission profile, has **ZERO callers in all of Src** and is not wrapped (tier R4, `FIX_POLICY` §4a). Bullet text = shipped id `4706`, RE-VERIFIED across all NINE packs this session"
row_status: "filed 2026-08-16 by the `smr-community-fixes` coverage sweep (their fix #9 *Restore SpaceY Description*; prior art credited). **SOURCE-VERIFIED, both halves.** The SpaceY `MissionSponsorPreset` carries exactly two gameplay modifiers — `Effect_ModifyLabel{Label=\"Consts\", Prop=\"CommandCenterMaxDrones\", Amount=20}` and `Effect_ModifyLabel{Label=\"DroneHub\", Prop=\"starting_drones\", Amount=4}` (`Data/MissionSponsorPreset.lua:694-703`) — and its `effect` text (`:625`) names only the second, unquantified: *\"Drone Hubs start with additional Drones\"*. Nothing anywhere states the +20 command capacity. ⭐⭐ **THE CONTROL IS DECISIVE AND IT IS A WHOLE-TREE SWEEP, NOT AN IMPRESSION: 16 `MissionSponsorPreset` blocks, 6 carry an `Effect_ModifyLabel`, and 5 of those 6 give every modifier its own bullet** — IMM's `FoodPerRocketPassenger` → *\"Food supply from Passenger Rockets increased (x10)\"*, NASA's `SponsorFundingPerInterval` → *\"Periodic additional funding\"*, CNSA's `MaxColonistsPerRocket 10` → *\"Passenger Rockets carry 10 additional Colonists\"* (the exact number), Terra Initiative's `ForestationPlant.disable_electricity_consumption` → *\"Forestation Plants do not require Power\"*, Paradox's `AllRockets.FuelResourceAmount` → *\"Rockets require more fuel to launch (drawback)\"*. **SpaceY is the only sponsor in the game that grants a modifier it does not describe.** ⇒ this is a paired change with one half missing, the `C39`/`C47` shape, not a summary-writing style choice. ⭐ The noun is checked too: `g_Consts.CommandCenterMaxDrones` has exactly one behavioural reader, `DroneHubBase:GetMaxDrones` (`Lua/Buildings/DroneHub.lua:94-95`) — RC Rovers, rockets, pods and the artifact interface all override `GetMaxDrones` and never read it — so *'Drone Hubs'* is accurate, and it matches the const's own shipped help text *\"Maximum number of Drones a Drone Hub can control\"* (`Lua/__const.lua:53`). ✅ Player route is trivial and universal: the sponsor-selection screen of every new game. ⛔ Harm is informational only — the bonus is granted whether or not it is described; nobody loses a drone. ⚠️⚠️ **AND THE CHEAP FIX IS A TRAP THIS PROJECT HAS ALREADY PAID FOR ONCE.** The vanilla string is `T(880574954148, …)`, which ships translated in all nine language packs (verified in `Local\\German.fpk`). Under `EF-039` a replacement literal on a NEW id renders as the literal — i.e. English — so swapping the whole bullet list would hand every French, German, Polish, Russian, Chinese, Spanish, Turkish and Brazilian player an English sponsor description to gain one line. The only loss-free shape is `shipped_T .. Untranslated(\"…\")` (`EF-039` route 3), which can append but cannot edit the existing bullet, or a `ModItemLocTable` (route 4, post-release work). **fredware's module takes the replacement route** and therefore carries that regression"
updated: "2026-08-20"
copies: []
---
### C50 — SpaceY grants +20 Drone Hub command capacity and its description never says so; it is the only sponsor in the game that hides a modifier  `[tested-attended 2026-08-20: Code/Fix_SpaceYDroneCapBullet.lua appends one bullet at the three LIVE render sites and leaves preset.effect alone, so every existing bullet keeps its context and its translation — text borrowed from shipped id 4706, number from GetModifiedConsts. ⛔ AMENDED the same day, before the sitting: one of those three sites feeds a panel the IN-GAME Goals dialog rebuilds, where g_Consts already carries SpaceY's modifier and the number came out double-counted (60 for a cap of 40); the module now stands down while a game is running (owner-ruled, checklist 61) and the wave-13 probe, which had reproduced the same arithmetic and so passed, now tests the stand-down. Parse-clean, doccheck GREEN, upload_preflight 0 FAIL. ⭐ tested-attended 2026-08-20: the owner watched the bullet on BOTH pre-game sponsor screens in English — 'Maximum number of Drones a Drone Hub can control (40)', the predicted number — and again in German, 'Maximale Anzahl an Drohnen, die ein Drohnen-Hub kontrollieren kann (40)'; SpaceY's first bullet kept its (40,000) cargo figure every time. The two screens are provably different code paths: Starting Rockets read 5 on the rollover and 6 on the summary while our number stayed 40 on both, which is the live control on omitting the commander. ⛔ SKIPPED BY NAME: the challenge landing-spot site was never opened, and the in-game Mission Profile was never opened on a SpaceY colony. Evidence docs/archive/link4en_*, link4de_*; report reports/04_ATTENDED_SITTING.md. Prior art: fredware's *Restore SpaceY Description*]`

## The claim, and whose reading this is

`fredware`'s **SMR Community Fixes** #9 says SpaceY's sponsor description is missing its
+20 maximum Drone Hub capacity. That is a claim about Paradox's data; the reading below is
ours, taken at `ModTools\Src` by symbol.

## What SpaceY actually grants — SOURCE

`Data/MissionSponsorPreset.lua`, the SpaceY block (`:605-704`), ends with exactly two
gameplay modifiers:

```lua
	PlaceObj('Effect_ModifyLabel', { Amount = 20, Label = "Consts",   Prop = "CommandCenterMaxDrones" }),
	PlaceObj('Effect_ModifyLabel', { Amount =  4, Label = "DroneHub", Prop = "starting_drones"        }),
```
`:694-703`

## What SpaceY's description says — SOURCE

`:625`, the whole string:

> `<bullet> <em>Dragon Rocket</em> - has smaller cargo capacity (<cargo>) but is faster and requires less fuel`
> `<bullet> <em>Solar Array</em> - a large cluster of solar panels that have lower maintenance cost but are vulnerable to dust storms`
> `<bullet> Drone Hubs start with additional Drones`
> `<bullet> 50% cheaper advanced resources`

The third bullet is the `starting_drones` modifier, unquantified. **`CommandCenterMaxDrones`
appears nowhere in the string, and nowhere else on the preset.**

## Is that a defect or a design choice? — the control that settles it

An incomplete blurb is not automatically a bug; a sponsor blurb could legitimately be a
highlight reel. So the question is empirical: **does this game's data enumerate sponsor
modifiers, or summarise them?** Sweep of every `MissionSponsorPreset` block in
`Data/MissionSponsorPreset.lua` — 16 presets, of which 6 carry an `Effect_ModifyLabel`:

| sponsor | modifier(s) | matching bullet |
|---|---|---|
| IMM | `Consts.FoodPerRocketPassenger 9000` | *"Food supply from Passenger Rockets increased (x10)"* ✅ |
| NASA | `Consts.SponsorFundingPerInterval 500` | *"Periodic additional funding"* ✅ |
| CNSA | `Consts.MaxColonistsPerRocket 10` | *"Passenger Rockets carry 10 additional Colonists"* ✅ (exact number) |
| Terra Initiative | `ForestationPlant.disable_electricity_consumption 1` | *"Forestation Plants do not require Power"* ✅ |
| paradox | `AllRockets.FuelResourceAmount 30` | *"Rockets require more fuel to launch (drawback)"* ✅ |
| **SpaceY** | `DroneHub.starting_drones 4` | *"Drone Hubs start with additional Drones"* ✅ |
| **SpaceY** | **`Consts.CommandCenterMaxDrones 20`** | **— nothing —** ❌ |

**5 of 5 other modifier-carrying sponsors describe every modifier they carry, three of
them with the exact number.** SpaceY is the only sponsor in the game with a modifier that
has no bullet. Under `FIX_POLICY` §4 that is a stated intent the data misses — the same
one-half-of-a-paired-change shape as `C39` and `C47` — rather than an editorial choice.

## Does "Drone Hubs" name the right thing? — re-derived, not inherited

`g_Consts.CommandCenterMaxDrones` is read in exactly three places
(whole-tree grep): the const definition (`Lua/__const.lua:53-54`), a UI string
(`Lua/Buildings/Building.lua:2667`), and **`DroneHubBase:GetMaxDrones`**
(`Lua/Buildings/DroneHub.lua:94-95`). `DroneControl:GetMaxDrones` (`DroneControl.lua:905`)
returns it as the class default, but every other `DroneControl` descendant overrides the
method — `RCRover` (`Units/RCRover.lua:690`), `RocketBase` (`Buildings/RocketBase.lua:1593`),
`UniversalRocketBase` (`UniversalRocket.lua:1479`), `SupplyPodBase`
(`Buildings/SupplyPod.lua:32`), `UniversalSupplyPodBase` (`UniversalPod.lua:37`),
`AncientArtifactInterfaceBase` (`Buildings/AncientArtifactInterface.lua:20`) — and none of
those overrides reads the const. ⇒ the +20 lands on Drone Hubs and nothing else, which is
also what the const's own shipped help text says: *"Maximum number of Drones a Drone Hub
can control"*.

## Player route

Trivial and universal: the **sponsor-selection screen at the start of every new game**.
Nothing gates it, nothing hides it. The harm is informational — the capacity is granted
regardless — so a player comparing sponsors chooses on incomplete information. `P3`.

## ⚠️⚠️ The remedy is not the one-liner it looks like — EF-039

`fredware`'s module replaces `preset.effect` with `T(981267450064, "<four bullets, one of
them completed>")` — a **new** id carrying an English literal. Under `EF-039`
(source-read 2026-08-02, **and its live control was discharged the same day**: `type(T(8821,
"ZZZ"))` printed `userdata`, log `Mars.exe-20260802-20.28.19`), a `T()` on an id the shipped
`TranslationTable` does not know falls through and renders the embedded literal.

The vanilla id **is** in the shipped tables: `880574954148` appears in `ModTools\Game.csv`
and in the shipped language packs. So the trade is real and one-sided:

> ✅ every English player gains one accurate line
> ❌ every French, German, Polish, Russian, Chinese, Spanish, Turkish and Brazilian player
>    loses a fully translated sponsor description and gets English

⛔ **Do not build the replacement shape.** `EF-039`'s route 3 —
`shipped_T .. Untranslated("…")` — appends without loss in any language, and is the only
retail-safe edit that does not need a `ModItemLocTable`. It can add a bullet; it cannot
rewrite the existing one. Whether one appended English line beats nothing is a judgement
call, not an obvious win, and it is not made here.

## ⭐⭐ REPAIR-ROUTE ANALYSIS, 2026-08-20 — asked for by the owner, derived at Src and against the shipped German pack

⛔ **The append route this entry recommended above is UNSAFE FOR THIS FIELD, and that is
new.** `EF-039` route 3 is correct in general; what it does not know is how the sponsor
description is *consumed*. Both sites that render it wrap the field with a context so the
tags inside it resolve:

```lua
texts[#texts + 1] = sponsor.effect and T{sponsor.effect, context} or ""
```

(`Lua/MissionProfileDlg.lua:42`, the in-game mission profile; `Lua/PreGameMission.lua:623`,
the sponsor-selection screen. The context is `GetModifiedConsts(sponsor, commander)` plus
every sponsor property — `PreGameMission.lua:519-536`, `MissionProfileDlg.lua:37-41`.)

⛔ **And the `T` constructor short-circuits on a concatenation, discarding the context:**

```lua
T = function(T, Ttext, gender)
	if type(T) == "table" then
		if getmetatable(T[1]) == TConcatMeta then
			return T[1]
		end
```

(`CommonLua/Core/localization.lua:224-227`.) ⇒ setting `preset.effect` to
`shipped_T .. Untranslated(…)` makes `T[1]` a `TConcatMeta` table, so the call returns our
concatenation **without the context** — and SpaceY's FIRST bullet contains `(<cargo>)`,
which resolves out of that context (`cargo = 40000`, `Data/MissionSponsorPreset.lua:617`).
⚠️ The render outcome of an unresolved tag was NOT observed; what is source-certain is that
the resolver is dropped. **A repair that damages bullet 1 to complete bullet 3 is not a
repair.**

### The route that survives: append at the UI, not at the preset

Leave `preset.effect` untouched — so the game keeps applying its own context to it — and wrap
the two functions that assemble the description, appending our bullet as a separate `T`
after theirs. Same shape as the `C51` repair, two sites instead of one.

### ⭐ AND THE APPENDED BULLET CAN BE FULLY TRANSLATED — measured, not assumed

The reason this entry called every remedy lossy was the assumption that appended text must be
an English literal. **It does not have to be**: a shipped id can be reused for its own text,
and two enrolled records already say what we need. Looked up this session in
`Local\German.fpk` (extracted with `tools/flpk_extract.py`):

| id | English record | German | source column |
|---|---|---|---|
| `4706` | *Maximum number of Drones a Drone Hub can control* | *Maximale Anzahl an Drohnen, die ein Drohnen-Hub kontrollieren kann* | `Lua/__const.lua(00053)`, **`ConstDef Buildings CommandCenterMaxDrones help`** — the help text of the very const SpaceY modifies |
| `887279699046` | *Drone Hubs are able to operate additional Drones* | *Drohnen-Hubs können zusätzliche Drohnen steuern.* | `Data/PolicyDef.lua(00668)`, `PolicyDef DroneHubEfficiency description` |

⭐ **Better still, the number need not be a literal either.** `GetModifiedConsts` walks the
sponsor's own `Effect_ModifyLabel` entries with `Label == "Consts"` and writes
`t[mod.Prop] = base + Amount` (`PreGameMission.lua:522-528`) — so
**`CommandCenterMaxDrones` is already in the context, carrying the modified value**. A bullet
built as `T{our_text, context}` with a `<CommandCenterMaxDrones>` tag renders the real
number, in the game's own idiom, and stays correct if the base const ever changes.

⚠️ Both borrowed ids carry the same rider: they are *other objects'* text. `4706` is tied to
the const itself and is the more defensible of the two; `887279699046` is a policy's
description and would follow that policy's wording if a patch ever reworded it. Neither is
free of that; both are honest to disclose in the fix header.

### ⛔ CORRECTION, same day — this is a PLAIN REPAIR, not a judgment call

The paragraph that stood here called this a *content addition* in the
`— judgment call` category and said it would be the pack's first repair of text the
developers never wrote. ⛔ **Both halves are wrong, and the owner overturned them within the
hour.** Tested against our own published definition (`SMR-CommunityMods`
`content/fix-list.md:20-23`: *"each one required deciding what the game **meant**, and
reasonable people could decide differently"*; the worked examples read *"there is no coding
error here"* and *"we added a behaviour the game does not have … an absence, not a mistake"*):

- **Nothing here required deciding what the game meant.** The whole-preset control settles
  it — 16 sponsors, 6 carry `Effect_ModifyLabel`, **5 of the 6 describe every modifier they
  carry**, three with the exact number. A paired authored change with one half missing is the
  `C39`/`C47` shape, and both of those are filed as plain defects.
- **No behaviour is being added.** The `+20` is granted and working; the description
  contradicts the preset it is declared in. *The code says one thing and does another* — which
  is the shipped `metadata.lua` description's own test for what belongs in this pack.
- **And the precedent already exists:** `Fix_TechDescriptionBuilding` (F25) repairs a tech
  description that names the wrong building. It carries no judgment-call mark and never has.

⇒ **The store card's "Five of the fixes are judgment calls" count does NOT move if this
ships.** What survives of the old paragraph is one narrow point, and route C answers it: F25
restores wording the preset itself contradicts, so that wording is the game's, whereas here no
shipped string contains the missing bullet — so the wording would be ours. ⭐ Building it on
`4706` (the `CommandCenterMaxDrones` help text) removes even that: the sentence is the game's,
the number is the game's from `GetModifiedConsts`, and the only authored decision left is to
put the bullet where the other five sponsors already put theirs.

## ⭐⭐ BUILT 2026-08-20 — `Code/Fix_SpaceYDroneCapBullet.lua`, route C

Close-out chain link 2. The preset is **not touched**: `preset.effect` keeps binding its own
context, so `(<cargo>)` in bullet 1 still resolves, and one extra element is appended at each
function that ASSEMBLES the description. All of them already return a concatenation in
vanilla, so appending is structurally what that code does on every draw.

### ⛔ THE RENDER-SITE LIST ON RECORD ABOVE WAS WRONG IN BOTH DIRECTIONS

The analysis above named two sites. Re-enumerated for the build over the whole of
`ModTools\Src` — by grepping **every read of a `.effect` field**, not the one spelling
`sponsor.effect`, which is what hid the extra sites — the answer is **three live and one
dead**:

| site | where | callers | verdict |
|---|---|---|---|
| `GetSponsorSummary` | `Lua/PreGameMission.lua:574` | 1 — `Lua/XDef/PGMissionSummaryDlg.generated.lua:116`, the pre-game mission summary panel | **LIVE, wrapped** |
| `GetSponsorDescr` | `Lua/PreGameMission.lua:644` | 1 — `GetSponsorEntryRollover` (`:700`), itself called once from `Lua/X/XPGMission.lua:158`: the rollover on the sponsor picker | **LIVE, wrapped** — never recorded before |
| `LandingSiteObject:GetMissionSponsorEffect` | `Lua/UI/PlanetUI.lua:314` | 1 — the `<MissionSponsorEffect>` tag in `Lua/XDef/PGChallengeLandingSpotRemastered.generated.lua:611`; **three shipped Challenges set `sponsor = "SpaceY"`** (`Data/Challenge.lua`) | **LIVE, wrapped** — never recorded before |
| `GetMissonProfileText` | `Lua/MissionProfileDlg.lua:25` | **ZERO in all of Src** | ⛔ **DEAD — not wrapped** |

⛔ **`GetMissonProfileText` is not "the in-game mission profile", as the section above says.**
A whole-tree search for `MissonProfile` returns its own definition line and nothing else.
Wrapping a function with no shipped caller is what `FIX_POLICY` §4a bars (tier R4, the F28
lesson), so it is left alone and this is the record of why. No XDef binds an `<effect>` tag,
so there is no fourth *function*.

### ⛔⛔ AND THE SITE LIST WAS STILL WRONG — THE COUNT OF FUNCTIONS IS NOT THE COUNT OF SCREENS

**Found 2026-08-20 by link 4's preparation, before the sitting; owner-ruled the same day
(checklist 61); the code now stands down.** Two sessions re-derived this list, one of them
specifically to correct it, and **both counted callers and stopped there.**

The sentence struck above — *"the in-game profile dialog … does not render `sponsor.effect` at
all"* — was **FALSE**, and the mistake was one indirection deep. `MissionProfileDlgRemastered`
does not call `GetSponsorSummary`; it **instantiates the panel that does**:

| `PGMissionSummaryDlg` instantiated at | when | reaches `GetSponsorSummary`? |
|---|---|---|
| `Lua/XDef/PGMissionSponsorRemastered.generated.lua:609` | pre-game sponsor screen | ✅ yes — the site as recorded |
| `Lua/XDef/MissionProfileDlgRemastered.generated.lua:342` | ⛔ **IN-GAME** — the HUD Goals button, `OpenDialog("MissionProfileDlgRemastered", …)`, `Lua/X/HUD.lua:283-288` | ✅ **yes, and no record ever said so** |

The panel's context update calls `GetSponsorSummary(entry)` whenever
`visible = (… id == "idMissionSponsor" …) and (MainCity or not random_mission_params[id])`
(`PGMissionSummaryDlg.generated.lua:108-120`) — `MainCity` is truthy in-game — and
`g_CurrentMissionParams` is written to and restored from the savegame
(`PreGameMission.lua:316, :321`), so the running game's sponsor entry is live there.
⇒ **one caller, two screens.**

⛔ **And on the second screen the number was wrong.** In a running game the sponsor's own
modifier is already applied to `g_Consts`: `Colony.lua:76` adds `g_Consts` to the `"Consts"`
label and `Effect_ModifyLabel:OnApplyEffect` sets a `Modifier` on every object in it
(`MarsGameEffects.lua:161-172`). `GetModifiedConsts` then computes from that already-modified
value and adds `Amount` again:

| | `base` (`g_Consts[Prop]`) | `value` (`GetModifiedConsts`) | the `value <= base` guard | printed |
|---|---|---|---|---|
| pre-game | 20 | 20 + 20 = 40 | passes (40 > 20) | **40** ✅ the true cap |
| ⛔ in-game | **40** | 40 + 20 = **60** | ⛔ passes (60 > 40) | ⛔ **60**, cap is 40 |

⚠️ **The guard could not catch it: it is satisfied by the very double-count that causes it.**

⭐⭐ **AND NEITHER COULD THE PROBE, FOR A REASON WORTH KEEPING.** The wave-13 TestKit probe
derives its "independently derived number" as `g_Consts[PROP] + modifier.Amount`
(`63_Probes_Wave13.lua`, clause 3) — **the same arithmetic the fix uses.** Two implementations
of one mistake agree with each other, and the probe reported PASS. A probe must take its truth
from a source the fix does not use, or it can only ever confirm the fix is self-consistent.
The probe now carries that lesson in a comment and a running-game branch (clause 4b).

⇒ **The remedy, ruled by the owner over ship-and-file and over pulling `C50`:** the module
declines while a game is running (`in_running_game`, keyed on `MainCity` — the game's own
discriminator on this exact panel), so the in-game Goals dialog shows **vanilla** and the three
pre-game screens are unchanged. Fixing the number instead would mean re-implementing the game's
modifier arithmetic, which this module exists to avoid.

⚖️ **The site's fix-list entry needs no edit** — it says the bullet appears *"while you are
choosing a sponsor"* and quotes no number, which is exactly what is now true.

### What the bullet is made of

- **The sentence is the game's**: shipped id `4706`, the `ConstDef` help of
  `CommandCenterMaxDrones` itself. ⭐ RE-VERIFIED this session across **all nine packs**
  (`tools/flpk_extract.py`, `CurrentLanguage/Game.csv` read directly, 23,090 records each):
  a real translation in all eight non-English packs, and the empty English `Translation`
  column is the loader's English-source fallback, not a gap (`C51`'s *RE-DERIVED 2026-08-20*
  derivation). Because the id is ENROLLED, `EF-039`'s mechanism works **for** us — the
  literal we pass is discarded and the shipped translation prints.
- **The number is the game's**: `GetModifiedConsts(sponsor)` is called for the same preset
  the site is rendering and the `<CommandCenterMaxDrones>` tag resolves out of it. Never a
  literal. The commander is omitted on purpose — this const is in neither `modifiableConsts`
  nor `directlyModifiableConsts` (`PreGameMission.lua:469-482`), so the value is identical.
- **Our template carries no English**: `"\n<bullet> <drone_cap_help> (<CommandCenterMaxDrones>)"`
  is tags and punctuation only.
- ⚠️ **The disclosed cost:** the sentence belongs to another object — it is the const's help
  text, not a line written for this sponsor. `887279699046` was rejected as the alternative:
  it is the `DroneHubEfficiency` policy's description, carries no number, and would follow
  that policy's wording if a patch reworded it.

### Handles, and the one guard the others do not need

Every check re-runs per call (these panels redraw constantly, and a mid-session language
change must be able to change the answer): the sponsor must be SpaceY; its `effect` must
still carry id `880574954148`, read with `TGetID` rather than assumed; the value
`GetModifiedConsts` computes must still EXCEED the base const — the defect restated as a
test; and `4706` must be in the language table loaded right now. Each site stands down
independently, loudly in our log and silently on screen.

⚠️ `GetSponsorDescr` prints starting laws and flavour text **after** the effect (`:666-691`)
and we can only append at the end. Today it lands correctly by construction —
`FactionDefs.SpaceY` declares no `initial_laws` and the preset has no `flavor_text` — and
both are re-checked per call, so if either ever becomes non-empty that one site declines
rather than printing the bullet in the wrong place.

⚠️ What no check can catch: the id kept but its text rewritten upstream to already mention
the cap. No runtime surface sees that; the after-every-patch extraction diff is the real
re-verification.

### Save safety — layer 3, nothing of ours enters a save

No persisted class, no GameVar, no game-time thread, no `LoadGame` handler. The wrapper
bodies contain no blocking call, and the only things holding our functions are two entries in
`_G` and one class table — none persisted.

⚠️ **CORRECTED 2026-08-20:** this paragraph used to close *"All three sites are pre-game
screens, which do not exist while a savegame is loaded"* — the second clause was false (site
1's panel is rebuilt by the in-game Goals dialog; see the site-list section above). ⭐ **The
layer-3 conclusion is unaffected and does not rest on it:** the module writes nothing anywhere,
in-game or out, so it is still free to add to and remove from any existing save. The wrappers
run in a loaded game and, since the owner's ruling, do nothing there.

### Probe — TestKit wave 13, `Code/63_Probes_Wave13.lua`

⭐ It discriminates in **any** language, unlike wave 12: it calls the shipped original and the
live global on the same preset, translates both, and asserts
`patched == vanilla .. our_bullet` — failing if vanilla text changed at all, and failing if
the bullet never landed. The number it checks is derived independently, off the preset's own
modifier plus `g_Consts`, not from the fix. ⚠️ The challenge site is only checked for
installation; seeing it work needs a person starting one of the three SpaceY challenges.

⛔ **UNOBSERVED.** Nothing here has been seen on a screen.

## Prior art

`fredware`'s **SMR Community Fixes** #9, *Restore SpaceY Description*, identified the
omission first and states both benefits correctly; `FIX_POLICY` §8 credit applies if we ever
ship anything here. Their module is `beta`/default-off and explicitly leaves the
`Effect_ModifyLabel` values untouched, which is right. The localization consequence above is
ours and is not addressed in their spec.
