---
id: "F08"
seq: 8
row: 8
title: "Tourist star-rating applicant bonus inverted"
status: "tested"
status_source: "tag"
priority: "P1"
evidence: "high"
row_status: "tested"
updated: "2026-07-27"
copies: []
---
### F08 — Tourist star-rating applicant bonus inverted  `[tested: Code/Fix_TouristApplicants.lua — PT-06 PASS 2026-07-27, 5★ +23/$544.5M vs tanked ≤2★ +7/$94.5M]`
`Lua\HolidayRating.lua:77` — `if Random(0,100) > bonus_chance` grants the bonus with
probability ~(100 − chance); rewards table (:2-11) is plainly a monotonic progression.
As shipped, 2-star tourists yield fewer applicants than 1-star. Codebase idiom elsewhere:
`Random(100) >= chance then return` (MonumentOfMarsLiberty.lua:21, ToxicPool.lua:182).
Called from RocketBase.lua:818, UniversalRocket.lua:2014. **Fix:** override
`HolidayRating.RewardApplicants`, flip to `<`.

## ⭐ 2026-09-08 — game 1.1.0: what changed, and what hotfix 2 did about it

⛔ **Nothing above this line is edited.** The 1.0.7 record stands exactly as it was
written and every citation in it was true of **1.0.7.396349**. This section adds only
what game **1.1.0.403908** changed, read against the shipped 1.1.0 body on 2026-09-08.

The roll was fixed upstream as `Random(0, 99) < reward.bonus_chance` (`HolidayRating.lua:92`). ⚠️ Our `Random(0, 100)` copy is now marginally WORSE than vanilla -- one extra losing outcome. Removal restores vanilla's roll rather than merely dropping a no-op.

⇒ **Fix_TouristApplicants** — removed from the pack in hotfix 2 (re-verification row R-33;
owner decision 98, ruled 2026-09-08: delete rather than gate, because the live pack serves the
current patch period and 1.0.7 players are served a frozen v5 instead, decision 118).

⛔ **What may NOT be read into this.** Not that the fix was never needed — it was
correct on 1.0.7, and what is true is that 1.1.0 fixes it itself. Not that removal is
verified safe — it is verified **in source**; nothing has been run in a game. Not that no
player is affected — a player still on 1.0.7 who takes this update loses this fix.
No status word is moved here, because no boot log has been taken since the change.
