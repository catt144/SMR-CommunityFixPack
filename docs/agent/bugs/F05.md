---
id: "F05"
seq: 5
row: 5
title: "Milestone completion crashes (NoTerraforming/NoPolitics)"
status: "tested"
status_source: "tag"
priority: "P1"
evidence: "high"
row_status: "tested"
updated: "2026-07-26"
copies: []
---
### F05 — Milestone completion crashes (NoTerraforming/NoPolitics)  `[tested: Code/Fix_MilestoneCrash.lua — PT-05 PASS 2026-07-26 ("A dream fulfilled" popup at 18/18 with 9 terraforming milestones hidden; zero LUA errors in-log)]`
`Lua\Milestones.lua:87-100` — hidden-but-uncompleted milestones fall through to
`score_sum + milestone:GetScore()`; `GetScore()` returns nil when uncompleted (:23-28) →
arithmetic-on-nil inside `CompleteMilestone`. Hidden milestones are guaranteed under
NoTerraforming (9 milestones) / NoPolitics (Independence), so completing the last visible
milestone errors and the "AllMilestonesCompleted" popup is lost. **Fix:** the eval fn is a
local — override global `CompleteMilestone` with a copy using `(milestone:GetScore() or 0)`.

## ⭐ 2026-09-08 — game 1.1.0: what changed, and what hotfix 2 did about it

⛔ **Nothing above this line is edited.** The 1.0.7 record stands exactly as it was
written and every citation in it was true of **1.0.7.396349**. This section adds only
what game **1.1.0.403908** changed, read against the shipped 1.1.0 body on 2026-09-08.

Vanilla now writes `score_sum = score_sum + (milestone:GetScore() or 0)` (`Milestones.lua:116`) -- our copy is identical to it.

⇒ **Fix_MilestoneCrash** — removed from the pack in hotfix 2 (re-verification row R-28;
owner decision 98, ruled 2026-09-08: delete rather than gate, because the live pack serves the
current patch period and 1.0.7 players are served a frozen v5 instead, decision 118).

⛔ **What may NOT be read into this.** Not that the fix was never needed — it was
correct on 1.0.7, and what is true is that 1.1.0 fixes it itself. Not that removal is
verified safe — it is verified **in source**; nothing has been run in a game. Not that no
player is affected — a player still on 1.0.7 who takes this update loses this fix.
No status word is moved here, because no boot log has been taken since the change.
