---
id: "C109"
seq: 213
row: 248
title: "A colonist that once failed to enter its dome, standing within twenty hexes of it with no walkable path under the cap, loops Idle → Abandoned → a guard that sleeps without moving or counting a failure, until it suffocates"
status: "cand"
status_source: "tag"
priority: "P1"
evidence: "SOURCE archived 1.1.1.405907, cleared by two seats (audit MIGRATION_AUDIT_2026-09-24 N1): Idle sends an outside colonist with dome_enter_fails > 0 to Abandoned (Colonist.lua:2501-2503); Abandoned keeps self.dome and calls EnterBuilding (:1452, :1477); the Abandoned-only guard in Colonist:EnterBuilding sleeps HourDuration/10 and re-issues Idle without a Goto and without OnEnterDomeFail (ColonistTransport.lua:753-760); the teleport rescue needs 100 counted failures (:518-527). The loop holds only while HasLocalAccess(self.dome) passes, i.e. within DefaultOutsideWorkplacesRadius (20 hexes, :270-300); farther out the SetCommand wrapper's rescue chain takes over. NOT observed in play; trigger frequency unknown. Field match: Steam 2026-09-24 (TheGodUncle) colonists stuck near passages and production buildings, suffocating"
row_status: "cand 2026-09-24 - source-verified loop from the migration audit; never watched in play; the fix shape needs an owner decision"
updated: "2026-09-24"
copies: []
---
### C109 — a colonist close to home but off any walkable path stands still until it suffocates (P1) `[cand 2026-09-24: source-verified loop; unobserved in play]`

#### Report

Filed from the migration audit ([MIGRATION_AUDIT_2026-09-24.md](../reports/MIGRATION_AUDIT_2026-09-24.md), N1),
opened at the owner's ask after a Steam report on 2026-09-24 (TheGodUncle, on the Workshop
page): colonists that "move outside and get stuck near passages and certain production buildings
where they suffocate and die". The report is a match in shape, not a confirmed instance.

#### Source (archived 1.1.1.405907; every line below re-read by the coordinating seat)

1. Idle, for a colonist standing outside any dome: `if not self.dome or self.dome_enter_fails > 0
   then self:SetCommand("Abandoned")` (`Lua/Units/Colonist.lua:2501-2503`).
2. Abandoned with `self.dome` set skips dome choice, keeps `dome = self.dome` (:1452), sets
   `was_abandoned`, and calls `self:EnterBuilding(dome)` (:1477). Its own fallback (`SetDome(false)`,
   cancel reservations, wander) sits after that call (:1479-1486) and is reached only if
   `EnterBuilding` returns false.
3. `Colonist:EnterBuilding` carries a guard scoped to Abandoned: when the target is a Dome and
   `not IsInWalkingDist(self, building)`, it logs "abandoned far from dome", sleeps
   `const.HourDuration / 10`, and `SetCommand("Idle", true)` (`Lua/Units/ColonistTransport.lua:753-760`).
   `SetCommand` from the command's own thread ends that thread, so neither a Goto nor
   `OnEnterDomeFail` (:1416-1423, the only writer of `dome_enter_fails`) runs.
4. `IsInWalkingDist` for an outside unit is `CheckWalkableDistance` (`Lua/Buildings/Dome.lua:295-297`,
   `:209-231`): a straight-line pre-reject, then a pathfinder path length capped at
   `ColonistMaxDomeWalkDist`. It is false for a pocket with no path as well as for a long path.
5. Every `SetCommand("Idle")` passes the wrapper at `ColonistTransport.lua:382-503`, which checks
   `HasLocalAccess(self.dome)` (:270-300); within `DefaultOutsideWorkplacesRadius` of home it passes,
   so Idle runs again and step 1 repeats. Beyond that radius the wrapper books a rescue ride or
   issues Stranded instead, which is why the loop is confined to a colonist close to home.
6. The teleport rescue, `TeleportToDomeOnRepeatedEnterFails`, requires `dome_enter_fails >= 100`
   (:518-527). This loop never increments it.

Intent tell (FIX_POLICY §4, tell 2): a rescue guard that cannot fire, because the path that needs
it records no failure. The vendor comment above the guard says it is deliberately scoped to
Abandoned so other commands may walk the full distance; nothing in it addresses the case where
the distance is not walkable at all.

#### Harm

`OxygenMaxOutsideTime` is 120000 ms (`Lua/__const.lua:1753-1759`); the loop's period is
`HourDuration / 10`. A colonist in this state dies outside, and the only player remedy is to change
the geometry (demolish what encloses the pocket). Priced in the early game, where a colony has one
or two domes and every specialist counts, a single such death is a lost founder; the pack cannot
tell the player why it happened.

#### Reach

Tier U until observed. The precondition is a colonist outside, within twenty hexes of its dome,
with one recorded entry failure, standing where no path under the cap exists. Two concrete routes
to that state are recorded elsewhere: a colonist interrupted on a passage hub is dumped onto the
hub surface (C99's source note on `WouldStrandHubColonists`), and a passage hop whose result is
ignored can leave a colonist outside mid-route (audit N9, `Colonist.lua:2204-2209`, `:3842-3849`).
The pack raises exposure to both by routing more colonists through passages (F52/F125); it does not
create the loop.

#### Control — not yet run

On the phase 2b fixture (Tesla, Sacagawea, Fuller): place a colonist outside within twenty hexes
of its dome, enclosed so `CheckWalkableDistance` is false, give it one entry failure, and watch.

- **Supports:** the log alternates `abandoned far from dome` with Idle every `HourDuration/10`,
  `dome_enter_fails` stays constant, the colonist does not move, and it dies of suffocation.
- **Refutes:** the colonist moves, or `dome_enter_fails` rises, or another command takes over
  before death.
- **Vacuous if:** `HasLocalAccess` fails (the colonist is farther than twenty hexes), because the
  rescue chain then runs instead.

Read the state with a console line rather than a save: print `command`, `dome`, `dome_enter_fails`,
`IsInWalkingDist(c, c.dome)` and `c:HasLocalAccess(c.dome)` for the colonist each tick.

#### Fix shape (owner decides)

The guard is at the top of a method the pack can wrap. Breaking the loop on its first pass means
the wrapper handles that one case itself rather than calling the original, so it is a guarded
bypass, not a chain (FIX_POLICY §1.4 versus §1.5). The least invasive candidate: under the exact
guard condition, return false so Abandoned's own fallback (:1479-1486) runs and hands the colonist
to dome choice or Stranded. Not to be built before the control above has been watched.

#### Not opened

Whether the engine's `Goto` to a dome interior ever leaves a colonist in an unpathable pocket
without an entry failure · how often the hub-surface dump (C99) produces this geometry · whether
`pairs(self.failed_domes)` on a false default (audit N10, :1458) reaches the same standstill by
error · any interaction with `Fix_ShelterReflex`, whose Idle pre-wrapper runs on the same Idle pass.
