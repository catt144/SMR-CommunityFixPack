---
id: "C87"
seq: 183
row: 218
title: "'Landscaping excavation is too deep' blocks lakes on ordinary flat ground in 1.1.0; the check is unchanged from 1.0.7, so one of its inputs (lake-prefab depth data, the cursor's height, or generated terrain) changed"
status: "cand"
status_source: "tag"
priority: "P2"
evidence: "source-read + prefab-data decode; one field report (Steam, 1.1.0, new save, screenshot); a PDX developer asked for an in-game bug report"
row_status: "cand 2026-09-12 — FILE AND WATCH. The decisive runtime readout will NOT be run: owner ruling, checklist 162 (c) — \"a lead not a test … only ever had one report … not spending time to run it down\". ⛔ NOT a refutation and NOT a closure: the source read stands unchanged and a second report reopens it at once. The reply is re-drafted and postable without the check"
updated: "2026-09-12"
copies: []
---
### C87 — "excavation too deep" on ordinary ground (P2) `[cand 2026-09-11: source-read + data decode; not reproduced]`

#### Field report (INHERITED — owner-relayed)

- Steam "'Excavation too Deep' when making lakes" (09-08): the error at every spot on a brand-new save; a reboot +
  reload doesn't help; cycling shapes with `[`/`]`, one small variant works.
- The screenshot (the imgur album, relayed by the owner 09-11): the Small Lake cursor on flat, ordinary ground beside two
  domes and a rocket, the red ground label "Excavation too deep", Status "Landscaping ex…".
- A Paradox developer (PDX Nova, 09-08) asked the reporter for an in-game bug report (Ctrl+F1).
- **The owner (09-11): "not a normal terrain warning … a warning I have never seen before."** This entry exists because
  of that pushback; the first triage (`reports/FIELD_LEADS_2026-09-11.md` §4, first version) blamed very low ground, and
  the screenshot does not support it.

**Priority:** P2 if universal on 1.1.0 (lakes — terraforming content — unbuildable); lower if map-specific.

#### The check (SOURCE, game 1.1.0.403908)

- `LandscapeLake:GatherConstructionStatuses` (`Lua/Buildings/LandscapeLake.lua:347-358`):
  `prefab = PrefabMarkers["Gameplay.Any." .. self:GetEntity()]`, then `if z + z0 <= 0` with `z = prefab.min.z` and
  `z0` from `self:GetVisualPosXYZ()` → `ConstructionStatus.LandscapeLowTerrain`.
- Its only caller: `ConstructionController:UpdateConstructionStatuses`, on a table wrapping the cursor's C object
  (`[true] = cobj`, `__index = template_obj`), when `self.is_template` (`Lua/Construction/Construction.lua:3007-3011`;
  `is_template` set at `:1091`). So `z0` is the cursor object's visual Z.

#### Unchanged on 1.0.7 (SOURCE, per item)

- The check: `LandscapeLake.lua:295-305` @1.0.7, byte-identical.
- The call site: `Construction.lua:2214-2218` @1.0.7; `is_template` `:1016`; it is the only caller in both trees.
- The small-lake template: description markup only.
- Lakes use the ordinary construction path in both trees; `LandscapeConstructionController` has no lake reference.
- `CommonLua/Libs/MapGen/PrefabMarker.lua`'s 1.1.0 diff doesn't touch how a marker's `min` is built (object-def
  unpacking, a render flag, a test-map print).
- ROUTE (INFERRED): a warning that is new in 1.1.0 therefore needs an **input** change — (a) the prefab `min` data,
  (b) the cursor's Z, or (c) the heights of 1.1.0-generated terrain.

#### Evidence per input

- **(a) prefab data.** INHERITED decode (`reports/FIELD_LEADS_2026-09-11.md` §4): every lake variant ships a marker; small
  −1020 / −1008 / −947, mid −1420..−1523, big −2090..−2215, huge −3554. SOURCE hint that 1.1.0 re-exported prefab data: the
  new `UnpackPrefabObjDef` carries a "compatibility code" branch for the old def format. ⛔ No 1.0.7 `Packs/` is archived
  (`Data.fpk` absent under `C:\Dev`, glob 2026-09-11), so the 1.0.7 values cannot be compared at the desk.
- **(b) the cursor's Z.** SOURCE: `CursorBuilding:UpdateShapeHexes` lost a `terrain.GetHeight` line in 1.1.0 (hex
  markers; INFERRED not the cursor). `UpdateCursor` / `FixConstructPos` in 1.1.0 NOT read.
- **(c) generated terrain.** SOURCE: `terrain.QuantizeHeight(map, 8)` is new in `RandomMapGenerator` (`:3010`), a C++
  call. INHERITED: stock blank templates' lowest core ground is 5703–10000 raw, far above any lake; generated maps
  unmeasured.
- Units (INFERRED): the small lake's |min.z| ≈ 1000 matches the 10 m dig at `LandscapeLake.lua:311` at guim 100, so
  `min.z` and `z0` are probably both world units — the check should fire only when the cursor reads below ~10 m.

#### The pack (INHERITED, investigator grep)

No `Code/` hit on the check's path. `Fix_LakeEntombment` post-wraps `PlacePrefab`, which runs after placement.

#### Control — `[NEVER RUN, AND NOW RULED NOT TO BE]`

⛔ **2026-09-12, owner, checklist 162 (c): this check will not be run.** *"162 (c) is a lead not a test, we
have only ever had one report. So I am not spending time to run it down when we have so many other things to
work on."* ⇒ **C87 is file-and-watch.** The recipe below is kept because it is correct and costs nothing to
keep — **a second field report makes it live again**, and whoever revives it should know the console line was
repaired on 2026-09-12 before it was ever used. ⛔ Do not re-ask the owner for this sitting; it was declined
on cost with one report behind it, which is a pricing call, not a doubt about the lead.

In any 1.1.0 colony: Small Lake cursor over flat ground by the base.
- **Places fine** → map-specific; the reporter's save/map is the next input.
- **Shows the warning** → universal on 1.1.0; run the `LAKECHK` line (cursor entity, `cursor_z`, `ground`, `min_z`) and
  read the log:
  - `cursor_z` ≈ 0 while `ground` is large → (b), the cursor's height.
  - `min_z` far larger in magnitude than the decode (e.g. ×100) → (a), re-exported data in other units.
  - both ordinary and the warning still shows → the formula is not what fires; look for a second raiser.

⚠️ **The `LAKECHK` line was touched up 2026-09-12** (checklist 162 (c)): it now prints `NOCURSOR`
rather than throwing if the lake cursor is not active, and spells the ground read
`terrain.GetHeight(map, x, y)` (witnessed, `BottomlessPit.lua:23`).

⛔ **CORRECTION, same day, and it reverses this section's original claim.** I first recorded the
09-11 spelling `o:GetMap():GetHeight(x,y)` as broken — "a two-number form witnessed nowhere in the
shipped tree; it would have thrown and printed nothing, costing the sitting". **That is refuted.**
A peer session (`smr-bugfixpack-b2`) enumerated the presence side: the two-argument method form
appears in **19 shipped files** — `ResourceStockpile.lua:1358`, `TerrainDeposit.lua:297`,
`Construction.lua:1331` and `:1341`, `LevelPrefabBuilding.lua:196`, `ElectricityGrid.lua:865`,
`GridObject.lua:198`, `Meteors.lua:685`, `Pathfinding.lua:79` and more — **including the exact
`obj:GetMap():GetHeight(x, y)` receiver spelling at `Landscaping.lua:225` and `:243`.** It is an
overload the exported docs do not enumerate (`LuaExportedDocs/Game/Terrain.lua:97-99`), which is
probably why it read as absent. **The 09-11 line would have run.** My claim came from a grep read at
`head -5` whose five visible hits all passed a point — an absence stated from a truncated list, which
is the same one-sided-count failure the project has been burned on before. The replacement spelling
is attested and stays (no churn in re-changing it), but the only real gain over 09-11 is the
`NOCURSOR` guard, and this entry no longer claims a defect was repaired. ✅ Re-traced at the same time and sound: `GetConstructionController()` resolves from the
current interface mode (`ConstructionControllers.lua:29-37`), `cursor_obj` is the controller's field
(`Construction.lua:1133`), the key `"Gameplay.Any."..entity` matches `LandscapeLake:GetPrefabName`
(`LandscapeLake.lua:265-267`) verbatim, and `m.min:z()` is valid — the shipped check itself calls
`prefab.min:xyz()` (`:351`), so `min` is a point, not a plain table.

#### Fix sketch

None until the readout.

#### Not opened

1.1.0's `UpdateCursor`, `FixConstructPos` and `CursorBuilding` Z handling; the `PrefabMarkerEdit.lua` export diff
(+101/−84); the reporter's save, map and mods.
