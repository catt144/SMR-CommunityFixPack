---
id: "D01"
seq: 64
row: 64
title: "Rockets don't auto-refuel/auto-export rare metals"
status: "opt-in"
status_source: "tag"
priority: "dsgn"
evidence: "high"
row_status: "opt-in fix"
updated: "2026-08-12"
copies: []
moved_to: "C:\\Dev\\SMR-OptInPack\\docs\\agent\\bugs\\D01.md"
---
### D01 — Rockets don't auto-refuel / auto-export rare metals — INTENTIONAL REDESIGN (verdict)  `[opt-in fix — fuel half shipped: Code/Opt_ClassicRockets.lua (id ClassicRockets, off by default); the standing Rare Metals export half is deliberately unwritten; PT-55 closed in full 2026-07-30; the parked-rocket activation limitation is documented, not owed. Heading tag ADDED 2026-08-01 — this entry had none, so its status could not be cross-checked against the index row — ⛔ MOVED 2026-08-12 → `SMR-OptInPack/docs/agent/bugs/D01.md`, which is now the record]`

⛔ **MOVED 2026-08-12 — this entry's record now lives in the Community Opt-In Pack.**

`C:\Dev\SMR-OptInPack\docs\agent\bugs\D01.md` — moved whole and byte-preserved by
the `split-optins` chain when `Opt_ClassicRockets.lua` left this pack (commit `0efb87e`).

The file stays here as a signpost: `INDEX.md` is GENERATED and `seq` must run 1..N
contiguous, so a deletion would renumber a hundred unrelated entries, and hundreds of
historical references to **D01** would resolve to nothing instead of one hop away.
The front matter above is the row as it stood at the move; the status word is kept
because the record did not change — it relocated.

