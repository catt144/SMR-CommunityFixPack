---
id: "C93"
seq: 189
row: 224
title: "Open Domes swaps the Outside Ranch to an entity missing three of its nine stockpile spots; repair keeps nine anchors and was watched clearing stranded piles"
status: "tested-attended"
status_source: "tag"
priority: "P2"
evidence: "OWNER-ATTENDED REPRODUCTION on 1.1.0.403908, 2026-09-16: OpenPasture(4396) carried nine MixedPoolStockpiles — six reachable on Resourcepile1..6 and three unreachable at Origin, holding exactly the visible 81 units (46 Food, 35 Meat). OWNER TIMING + SOURCE JOIN: the ranch works before Open Domes; the law runs SetOpenAirBuildings, which changes every OpenAirBuilding's skin. OpenPastureBase inherits that class, so OpenPasture becomes OpenPasture_Open; the affected post-law entity exposes only six usable pile anchors while Lua still declares Resourcepile1..9. FIX BUILT, NOT YET RUN: Fix_OpenPastureStockpiles keeps this ranch on the nine-anchor entity and repairs already-open saves without rewriting resources."
row_status: "cand 2026-09-13 - triaged from a Steam report; not ours (no pack module touches pastures or stockpile spots); the discriminating control is one grep on the reporter's log"
updated: "2026-09-23"
copies: []
---
### C93 - Open Domes changes the Outside Ranch to a six-anchor entity and strands three piles (P2) `[tested-attended 2026-09-16: owner reloaded the affected save and watched it end to end, no food stuck]`

#### 2026-09-23 build response

**Retired from the 1.1.1 pack:** `Fix_OpenPastureStockpiles` and its load/editor registrations and TestKit probe requirements are removed. The status above records the historical fix, not a newly attended 1.1.1 result.

**SOURCE, archived 1.1.1.405907:** Lua/Buildings/Animals.lua:1382-1526 rebuilds the stockpile pool; :1569-1583 enroll the new save fixups. The settled retail asset gate and its bounded evidence remain as recorded by the adjudication. Native migration and prior repaired pile/entity state contain no mod function or field.

No new save cleanup is added. Ordinary state already repaired stays repaired; no new executable residue is introduced by deletion. Current controls are in `tools/desk_gamepatch_retirements.py` (F45 uses `tools/desk_f124_track.py`); historical tests below retain their original builds and limits.

See [build report](../reports/GAMEPATCH_1.1.1_BUILD_2026-09-23.md) for commands, controls, claim limits and owner play.

#### Historical filing and evidence


**The immediate cause is settled on the owner's reproducing save:** three live ranch piles
sit at `Origin` and cannot be approached, while six piles on named entity spots drain normally.
The owner's timing observation supplies the missing transition: the ranch works before Open
Domes, then the law changes every `OpenAirBuilding` to its `_Open` entity. The Outside Ranch
inherits that class, but its open entity has only six usable pile anchors while its Lua class
declares nine. ⛔ **That still does not settle either field reporter's install or attribution.**
Neither reporter supplied a log/save/mod list measured here. Do not project the owner's save
result onto either reporter.

#### ✅ TESTED ATTENDED 2026-09-16 — the owner watched the repair end to end

⚖️ Owner's words: *"c93 tested i re loaded it and watched it end to end no food stuck"*. **MEASURED in the file log:** `OpenPastureStockpiles: applied`, then on load `restored 2 open ranch(es), explicitly reattached 6 pile(s)` and `restored 4 open ranch(es), explicitly reattached 12 pile(s)` (`Mars.exe-20260916-16.47.23-6a91a190.log`, repeated on reload in `16.57.56`). Every restore reattached exactly three piles per ranch — the stranded set. The owner reloaded the affected save and watched drones clear the ranches with no food left stuck; the closed ranch visual is the declared tradeoff. ⛔ **Not claimed:** checklist 191's Leg B (a new ranch built after Open Domes) was not stated, and no numeric per-pile reconciliation was read — the acceptance rests on the owner's attended observation. The `16.57.56` log also carries 7,218 `HGE::l_HasSpot: The object given has no entity` lines on `UnpersistedMissingClass` objects in vanilla drone approach code. That run loaded the reporter's save past `LoadGame error: missing mods` with twelve third-party mods absent; ⚖️ **owner attribution 2026-09-16: those mods' missing classes, not ours** — not investigated.

#### REPAIR BUILT, 2026-09-16 — source/desk at build time; ✅ attended validation passed above

`Code/Fix_OpenPastureStockpiles.lua` wraps the shipped entity selector and changes behaviour
only for an `OpenPastureBase` resolving the exact `OpenPasture_Open`/`OpenPasture` pair. It
returns the closed entity for both sides, retaining all nine anchors; atmosphere and
life-support changes remain the global law's work. A synchronous load/reload sweep changes an
already-open ranch back and reattaches only any still-invalid piles to unoccupied
`Resourcepile7`–`Resourcepile9` anchors. It never changes pile contents, requests, producer
lists, storage caps or resource totals.

The apply-time behaviour probe reads the entities directly: it requires spots 7–9 on
`OpenPasture`, requires those same spots to be absent from `OpenPasture_Open`, and requires the
class's persisted nine-name list. A vendor asset correction therefore deactivates the module.
Body pins, Lua parse, load order, module-set agreement and doccheck are green. **No repaired
gameplay result is claimed yet; checklist 191 owns the affected-save and new-build legs.**

Build evidence: [C93 Open Domes repair](../reports/C93_RANCH_OPEN_DOMES_BUILD.md).

#### OWNER-SAVE REPRODUCTION, 2026-09-16 — placement, contents and pickup all reconcile

**MEASURED, attended, game 1.1.0.403908 / build `6a91a190`:** a temporary TestKit
probe enumerated the live piles on `OpenPasture(4396)` and observed the first pickup
approach to each across a completed production cycle. Durable evidence and command/filter:
[C93 ranch diagnosis report](../reports/C93_RANCH_ORIGIN_DIAGNOSIS.md); archived
[boot log](../../archive/logs/C93_ranch_probe_Mars.exe-20260916-11.52.38-6a91a190.log).

| live pile group | count | load-time contents | pickup result |
|---|---:|---:|---|
| `Resourcepile1`–`Resourcepile6` | 6 | empty | **6/6 `true`** after new Cheese/Food/Meat output arrived |
| `Origin` (`GetAttachSpot() == -1`) | 3 | 27 units each | **3/3 `false`**, contents and requests unchanged |

The three Origin piles total **81 units = 46 Food + 35 Meat**, exactly the toolkit's
previous centre-stack reading. This answers the owner's overflow/livestock question:
**the split is pile placement, not livestock or resource.** New cycle output can enter the
six reachable piles and leave for outside depots while the older Origin contents remain
stranded at the centre.

Every pile reports `entity=ResourcePlatform` and `HasEntity()==true`. Therefore the same
boot's `HGE::l_HasSpot: The object given has no entity` stream is **not emitted by the ranch
piles** and a generic entity guard cannot repair C93. The first probe named mall targets;
the owner identified a mall-related mod and instructed this investigation to ignore them.
No mall defect was filed. Owner decision and its condition: checklist 190.

**SOURCE JOIN:** `OpenPastureBase` declares nine names per producer
(`Animals.lua:1108-1114`). Both `StockpileController:CreateStockpiles`
(`StockpileController.lua:102-107`) and `SavegameFixups.SharePastureStockpilePools2`
(`Animals.lua:1430-1436`) replace a missing named spot with `Origin`; the latter emits no
diagnostic. The measured 6+3 state is exactly that fallback shape. The log cannot say
which creation route originally produced these persisted piles, so that narrower history
remains open.

#### The report (INHERITED, not verified)

Steam comment channel, read 2026-09-13, user **saYo**, posted ~6 h earlier, verbatim:

> *"Any chance you can fix the Outside Ranch? There seems to be a bug with it storing its
> products in the middle where drones cant reach it. They are circling the Ranch but cant
> pick anything up, because its inside."*

No log, no mod list, no save, no game version supplied. Everything below is either source
or our own measurement; nothing re-states the reporter's claim as fact.

#### SECOND REPORT, 2026-09-15 (INHERITED, not verified) — and it moves two things

Owner-relayed, a different player, explicitly referencing saYo's report, verbatim:

> *"Hello! I was looking around a way to fix the issue of outside ranch mentioned by @saYo,
> I have the same issue and using no mod so far (except this one). Before the dome opening,
> ranch were working good."*

⭐ **A SAVE HAS BEEN REQUESTED and the owner is waiting on it (2026-09-15).** ⛔ Pull-only still
applies (`WORKFLOW.md` Records and rulings, player replies; ck165): nothing is drafted and nothing is on anyone's owed list.

Three things are new, and **all three are the reporter's word, not measurements**:

1. **Independent corroboration.** Two unconnected players, same symptom. ⚠️ This weakens — it
   does not kill — the "one report is a poor fit for a vanilla defect" argument recorded below.
2. **A claimed CLEAN INSTALL except our pack.** If true, the control table's mod-list column is
   answered for this reporter, and a present log line would land on the **vanilla entity/spot
   defect** row. ⛔ Self-reported and unverified; the save settles it (`active_mods` is readable
   in the first ~2 KB of a savegame).
3. ⭐ **A TRIGGER: "before the dome opening, ranch were working good."** This was the first
   temporal claim on this defect. The owner's later timing observation and the shipped open-air
   transition now give it a concrete source path: enacting Open Domes changes the ranch entity.
   The reporter's own save remains unmeasured, so this explains the reported shape without
   attributing that individual report.

⚠️ **The trigger did not fit the creation-only mechanism as this entry first recorded it.** A
spot missing at `CreateStockpiles` predicts a ranch broken from construction. The missing path
was not another creation call but the later `OpenPasture` → `OpenPasture_Open` entity swap;
the save-load migration below remains a second way to create the same nine-pile pool.

#### NEWLY MEASURED 2026-09-15: a SECOND entry point to the same fallback, post-build

`CreateStockpiles` is not only build-time. The two entry points on the live 1.1.0.403908 tree:

- **Build time** — `StockpileController:GameInit()` (`Lua/Buildings/StockpileController.lua:88-90`).
- ⭐ **SAVE LOAD** — `SavegameFixups.AB_MigrateStockpileControllersToMixedPool()` (`:343`), which
  calls `controller:CreateStockpiles()` again at **`:381`** on buildings that already exist,
  after setting `stockpile_class = "MixedPoolStockpile"`.

The fixup's own comment states its purpose (`:334-336`): *"old saves carry per-spot
ResourceStockpile instances and a scalar `amount_stored` on every Farm"*. It is gated to fire
once, on controllers still holding the old class (`kStockpileMigrationSafeClasses`, `:338-340`;
`can_swap`, `:344-349`), and it **explicitly handles the pasture/farm family** — `:375-377`
resets `FarmBase.amount_stored`, and `:373-374` notes *"Pasture's amount_stored field is
declared but never written"*.

⇒ **Loading an older save re-runs the `HasSpot` check on an already-built ranch**, so the
`Origin` fallback can capture a building that was working before the load. ⭐ **That is the
shape the second reporter describes**, and it makes the awaited save far more valuable than a
log line alone: a save that has already been migrated may show the piles at `Origin` directly.

⛔ **For the two field reporters, this remains a mechanism that fits rather than an
attribution.** Nothing establishes that either reporter's ranch went through this fixup,
and the reporter said *dome*, not *save load* or *update*. The owner's separate save now
measures the same Origin-pile mechanism directly, but does not connect that reporter wording
to a particular creation path.

#### Our exposure: NONE

**MEASURED:** `grep -riE "\b(ranch|pasture|OpenPasture)\b" Code/` returns nothing. No pack
module touches pastures, ranches, stockpile spots, `HasSpot`, or stockpile creation.
⚠️ A first pass appeared to hit 27 modules; those were matches on the substring inside
**b-ranch**. The word-boundary search is the real one.

#### The mechanism (SOURCE, game 1.1.0.403908)

"Outside Ranch" is `OpenPasture`, `object_class = "OpenPastureBase"`, `entity = "OpenPasture"`
(`Lua/BuildingTemplate/OpenPasture.generated.lua:9,21,28`). Its produce is placed at **named
entity spots**, not at Lua-computed positions:

```lua
DefineClass.OpenPastureBase = {
    __parents = { "Pasture", "OpenAirBuilding" },
    stockpile_spots1 = { "Resourcepile1", ... "Resourcepile9" },   -- and 2, 3
}
```
(`Lua/Units/Animals.lua:1108-1114`; the generic default is
`ResourceProducer["stockpile_spots"..i] = {"Resourcepile"..i}`, `Lua/Buildings/BuildingComponents.lua:459`.)

⭐ **The fallback is the defect shape.** `StockpileController:CreateStockpiles`
(`Lua/Buildings/StockpileController.lua:102-107`):

```lua
for _, spot in ipairs(self.stockpile_spots) do
    if not self.parent:HasSpot(spot) then
        print("once", self.parent:GetEntity(), "doesn't have spot", spot)
        spot = "Origin"
    end
    local first, last = self.parent:GetSpotRange("idle", spot)
```

⇒ **If the entity lacks the named spot, the pile is attached at `Origin` - the building's
centre** - and the engine logs one line naming the entity and the missing spot.

#### Why that strands produce on a ranch but not everywhere

⭐ **Walkability decides whether an `Origin` pile is reachable.** `MicroGHabitatBase` declares
`flags = { efWalkable = true }` (`Lua/Buildings/MicroGHabitat.lua:3-6`), so a pile at its
centre still sits on passable ground. `OpenPastureBase` inherits `Pasture` + `OpenAirBuilding`
(`Lua/Buildings/Dome.lua:456`) and declares **no `efWalkable`**, so its footprint is solid and
a centre pile is enclosed by it. Drones path toward the pile, fail to reach it, and circle -
the reported behaviour.

#### MEASURED: the fallback path is LIVE, and it fires in vanilla

**Our own archived log**, 1.1.0, 2026-09-09, with only our three mods loaded (Fix Pack,
Opt-In Pack, Test Kit - none of which replace entities or touch spots):

```text
MicroGHabitat doesn't have spot Resourcepile
```

[sitting2play110 log, line 274](../../archive/logs/sitting2play110_Mars.exe-20260909-19.48.51-6a91a190.log),
emitted immediately after a Quick Build.

⇒ **A missing `Resourcepile` spot does NOT require a mod.** The shipped game already carries at
least one entity whose declared stockpile spot is absent. ⚠️ This refutes the tempting
inference *"products in the middle ⇒ a mod replaced the entity"*. It is exactly why the
attribution below is left open.

#### Corrected control — the creation print is insufficient; read the live piles

The old control treated absence of this line as refuting the mechanism:

```
OpenPasture doesn't have spot Resourcepile...
```

That was too strong. `StockpileController:CreateStockpiles` prints on its fallback, but
`SavegameFixups.SharePastureStockpilePools2` has the same `Origin` fallback with no print.
Zero lines proves only that the printing creation branch did not run in that session; it
says nothing about a persisted pile or the silent fixup.

The direct control is now measured: enumerate the ranch's piles, read `GetAttachSpot()` and
the parent's spot name, then observe a pickup approach. A named `ResourcepileN` plus
`result=true` falsifies stranding for that pile; `Origin` plus an unchanged request and
`result=false` confirms it. The owner's save returned both controls side by side: six of
the first kind and three of the second.

⛔ Reporter attribution still needs the reporter's own save/mod list. Replies remain
PULL-ONLY (`WORKFLOW.md` Records and rulings, player replies; ck165); this entry queues no reply and creates no owner debt.

#### The owner's original field-attribution hypothesis, still not settled for either reporter

**Owner, 2026-09-13:** *"that stinks of a conflicting outdated mod."* The shape fits - the
stockpile positions come from the 3D entity rather than from Lua, so a mod shipping its own or
an outdated `OpenPasture` entity would strip the spots while leaving every line of vanilla Lua
intact. Circumstantial support: `OpenPasture` is **not** new content - it exists in
1.0.7.396349 too (`Data/BuildingTemplate/OpenPasture.lua`) - so if vanilla shipped it without
spots the ranch would be broken for every player, and one report is a poor fit for that.
⛔ **But this is a shape argument, not evidence**, and the `MicroGHabitat` measurement above
shows the same symptom arising with no mod involved. INFERRED, leading candidate, unproven.

**Superseded as the leading explanation for the owner's save.** The new timing/source join is
more specific: the ordinary ranch entity supplies the nine anchors, while the entity selected
by Open Domes does not. That explains why this is not broken for every player from construction.
It does not retroactively establish either reporter's version or mod list, so their attribution
remains open without making the outdated-mod hypothesis the repair target.

#### Still open

The repaired-save and new-after-law gameplay legs · whether either field reporter is on 1.1.0
or used the same transition · whether either reporter's ranch also went through the stockpile
migration · whether `MicroGHabitat`'s missing spot is harmless or strands deliveries · a
tree-wide census of other declared-but-missing stockpile spots.

The built repair addresses both prevention and the already-affected save while retaining the
same nine pile objects and their contents. Checklist 191 is the evidence gate: until both legs
run, the status stays `fixed`, not `tested-attended`.
