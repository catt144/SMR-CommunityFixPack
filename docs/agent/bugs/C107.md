---
id: "C107"
seq: 205
row: 240
title: "Dry Farming halves crop water for the three base-game plant farms only; the four Feeding the Future plant farms (Small, Underground, Small Underground, Automated) are never reduced"
status: "fixed"
status_source: "tag"
priority: "P3"
evidence: "SOURCE-VERIFIED on 1.1.0.403908 (archived tree; Data/Tech.lua byte-identical to the installed copy); the same three effects sit in 1.0.7.396349 Data/TechPreset.lua. Field report: Steam comment (-=Maure=-), 2026-09-18. Tech text is unqualified (Data/Tech.lua:770, 782) while its effects name three class labels (:787-801); a building joins only its own class label (Lua/Buildings/Building.lua:460). The four DLC plant farms inherit FarmBase and take water from their crop (Lua/Buildings/Farm.lua:619). Built 2026-09-18 as Code/Fix_DryFarmingFarms.lua, desk-tested only; control not run"
row_status: "cand 2026-09-18 - Steam report; source settles the gap; scope set to plant farms by the base-game curation rule"
updated: "2026-09-18"
copies: []
---
### C107 — Dry Farming skips the Feeding the Future plant farms (P3) `[fixed 2026-09-18: built as Code/Fix_DryFarmingFarms.lua; desk-tested only, control not run in game]`

#### Report

Steam comment, 2026-09-18 (-=Maure=-): the breakthrough *"is supposed to reduce water consumption
of all farms by half"* but applies only to Farm, Hydroponic Farm and Open Farm by label; they propose
the `AllFarms` label, *"the same way the Agronomist Commander effect applies"*. The gap is real; the
proposed scope is not (see Intent).

#### Source (1.1.0.403908 unless stated)

- `Data/Tech.lua:769-802`, tech `DryFarming`. Description and short description say *"Water
  requirements of crops are reduced by <percent(param1)>"* (`:770`, `:782`), with no qualifier.
  Effects: three `Effect_ModifyLabel`, `water_consumption` −50%, on labels `Farm`, `HydroponicFarm`,
  `OpenFarm` (`:787-801`). 1.0.7.396349 carries the same three in `Data/TechPreset.lua`.
- `Building:AddToCityLabels` adds a building to `self.class` only (`Lua/Buildings/Building.lua:460`),
  and the class is the template id, so `FarmUnderground` never joins `Farm` despite
  `object_class = "FarmConventional"` and `prg_class = "Farm"`.
- Crop farms set their base water from the crop: `FarmBase:PlantNextCrop` →
  `SetBase("water_consumption", cropdef.WaterDemand)` (`Lua/Buildings/Farm.lua:619`); the getter
  applies modifiers (`:825`).
- Farm templates and whether each inherits `FarmBase` (script over the archived tree, template
  `object_class` walked through `DefineClass` parents):

| template | class | FarmBase | Dry Farming |
|---|---|---|---|
| Farm, HydroponicFarm | FarmConventional, FarmHydroponic | yes | paid |
| OpenFarm | OpenFarmBase (VegetationPlant) | no | paid |
| FungalFarm, FungalFarm_Asteroid | FungalFarmBase (ResourceProducer) | no | not paid, fixed 500 water |
| **FarmSmall, FarmSmallUnderground** (norman) | FarmSmallBase → FarmConventional | yes | **not paid** |
| **FarmUnderground** (norman) | FarmConventional | yes | **not paid** |
| **AutomatedFarm** (norman) | AutomatedFarmBase → FarmBase | yes | **not paid** |
| FarmInsect (norman) | FarmInsectBase → FarmBase | yes | not paid |

  Crop water demand in `DLC/norman/Presets/CropPreset.lua`, by `FarmClass`: FarmSmall 11 crops,
  800–1500; FarmUnderground 6, 500–2400; FarmSmallUnderground 5, 250–1200; AutomatedFarm 5, 3000
  each (coffee, basil, chili, sorghum…); FarmInsect 6, 0–500.

#### Intent

- **Tell 4 (self-contradiction):** the tech's own text promises every crop and its enumeration pays
  three buildings. Same shape as [F95](F95.md).
- **The curation rule is recoverable, and it is not `AllFarms`.** In base 1.0.7 the `AllFarms`
  label already existed (`Data/Label.lua:8`) and held exactly Farm, HydroponicFarm, OpenFarm,
  FungalFarm and FungalFarm_Asteroid (1.0.7 `Data/BuildingTemplate/*Farm*.lua`, `label3`/`label4`).
  The author enumerated that set minus the two Fungal Farms, which already drew 500 water. So the
  rule is *plant-growing farms*: the fungi were left out on purpose. The four norman plant farms fit
  that rule and are missed; FarmInsect follows the fungi and stays out. The reporter's `AllFarms`
  swap would also pay the Fungal and Insect Farms, contrary to the base author's choice (OpenFarm
  carries `label3 = "AllFarms"`, so it would not be lost).
- **Counter-reading, recorded:** the DLC authors could have left their farms out for balance, since
  the small farms already draw little water. Nothing in the source says so, and the same DLC wrote
  its Agronomist bonus on `AllFarms` (`DLC/norman/Presets/CommanderProfilePreset.lua:21`), which
  shows it expected farm-wide bonuses to reach its new farms. Veto point if the owner reads it the
  other way.

#### Repair (built 2026-09-18; desk-tested only, never run in a game)

`Code/Fix_DryFarmingFarms.lua` (Register id `DryFarmingFarms`), listed in `items.lua` and
`metadata.lua` `code` before `90_SaveSanitizer`. It follows F95's shape: a §1.1 additive preset
patch plus a load-time heal through vanilla's own `OnApplyEffect`.

- **Target, corrected from the plan above:** in 1.1.0 the effects live on the `Tech` preset
  `Techs.DryFarming` (GlobalMap `Techs`). `Presets.TechPreset` / `TechDef.DryFarming`
  (`Data/TechPreset.lua:205-209`) holds only `param1`. Research applies the tech with
  `preset:EffectsApply(UIColony)` (`Lua/TechTree.lua:1228`), so a new research pays the
  appended entries with no other code.
- **Preset pass** (`SMRFixPack.DataPatch`, after `DataLoaded`): appends one `Effect_ModifyLabel`
  for each of `FarmSmall`, `FarmSmallUnderground`, `FarmUnderground` and `AutomatedFarm`, built
  with `PlaceObj`. Each copies `Prop` and `Percent` from the shipped Farm entry. Guards:
  - An entry is appended only where that label's BuildingTemplate exists. Without the DLC
    nothing is appended, and the module stays active and idle.
  - The three shipped entries must share one negative Percent; otherwise the pass latches.
  - A §2a probe calls the shipped Farm entry's `OnApplyEffect` on a stub colony. It must file
    under its own Label, keyed by the effect object.
  - An existing entry with the same Label and Prop is adopted, never doubled.
  - A template that already joins a label the tech pays is skipped, so the reduction never
    stacks to −100%. If every template is covered, the pass latches benign as a RETIRE
    candidate.
- **Heal** (`OnMsg.LoadGame`, only if `IsTechResearched("DryFarming")`): applies any owned entry
  that `UIColony.label_modifiers` lacks. It tests presence by the stored key's class, Label,
  Prop, Percent and Amount, as vanilla's fixups do (`Lua/MarsGameEffects.lua:329-330`), never
  by identity, so the F95 1→2-per-load regression is excluded. It is exposed as
  `SMRFixPack.DryFarmingFarms.Heal` for the kit probe.
- **Manifest:** `SRC: none` plus `DEFECT@Data/Tech.lua`, which states the absence: DryFarming
  modifies `water_consumption` and names none of the DLC plant-farm labels.
- **§3a:** no thread, GameVar or wrapper frame. The persisted modifier is keyed by a vanilla
  `Effect_ModifyLabel` copy. After the pack is removed, a researched save keeps the −50% on the
  DLC farms: a non-harmful trace, and the intended behaviour.
- **Kit probe:** `DryFarmingFarms` in `SMR-BugFixPack-TestKit/Code/67_Probes_Wave16.lua`,
  kind `behavior`. It checks the preset, then drives the heal on a stub colony across a
  simulated save boundary, then reads the live save if it has researched the tech. It skips
  without the DLC.
- **Desk harness:** `tools/desk_c107_dry_farming.py`, run on the shipped 1.1.0 bodies and data.
  All 19 of its checks passed. Each of 7 one-guard-reverted scratch variants failed its named
  checks.

#### Control — not yet run

On a Feeding the Future save, build a Farm and an Underground Farm, each with a crop planted, and
note each panel's water consumption. Grant Dry Farming. **The claim holds** if the Farm's figure
halves and the Underground Farm's does not. **It is refuted** if both halve (some other route
covers the DLC farms) or neither does (the modifier path is not what this entry says). Run it without the pack
enabled; with the pack, **both** must halve. A save that researched Dry
Farming before the fix must show the Underground Farm halved after one load, and still halved (not
quartered) after a save and reload.

#### Reach

R1 for any owner of Feeding the Future (norman DLC, EF-078) who researches the breakthrough: all
four templates are in `build_category = "Farms"` or `"LifeSupport"` with no `hide_from_build_menu`.

#### Not opened

The norman `DLC/.../Code/BuildingTemplate/*.generated.lua` bodies beyond the lines cited; any
Fungal Farm's norman crop presets, which its non-FarmBase class does not appear to consume.
