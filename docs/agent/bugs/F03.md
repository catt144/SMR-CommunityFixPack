---
id: "F03"
seq: 3
row: 3
title: "Upgrade buffs leak & stack after salvage/demolish"
status: "closed"
status_source: "tag"
priority: "P1"
evidence: "high"
row_status: "⛔ DOWNGRADED FROM `tested` 2026-09-09 by owner ruling (sitting 2), on the F21 precedent: the pack ships NO repair for this any more. Link 02 deleted `Fix_UpgradeModifierLeak.lua` AND the matching F03 pass in `90_SaveSanitizer.lua` (`f707903`, ck117) because game 1.1.0 fixes BOTH ends itself — the leak source (`Building:StopUpgradeModifiers` now iterates with `pairs`, `Building.lua:1303-1311`) and the residue already in saves (`SavegameFixups.RemoveLeakedUpgradeModifiers`, `:1313-1345`). The 1.0.7 record below stands and was true of 1.0.7.396349; PT-02's PASS was real. What changed is that the CLAIM outlived the code. ⛔ The public surfaces were already clean before this flip: the site fix-list entry went with link 02's `7cef4f3` (82 → 46 entries, 09-08) and `metadata.lua` never carried it — only this entry, its heading tag and the generated INDEX row still said `tested`"
updated: "2026-09-09"
copies: []
---
### F03 — Upgrade buffs leak & stack after salvage/demolish  `[closed: ⛔ DOWNGRADED FROM `tested` 2026-09-09 (owner ruling, sitting 2) — the pack ships no repair for this any more. Game 1.1.0 fixes both ends itself, so link 02 deleted Code/Fix_UpgradeModifierLeak.lua AND the F03 pass in Code/90_SaveSanitizer.lua (f707903, ck117). The 1.0.7 record below stands and PT-02 PASS 2026-07-25 was real; the claim simply outlived the code]`
`Lua\Buildings\Building.lua:1268-1274` — `StopUpgradeModifiers` iterates `upgrade_modifiers`
with `ipairs`, but the table is string-keyed (`ApplyUpgrade`, lines 1168-1170) → `TurnOff()`
never runs. Self-targeted modifiers die with the building; **LabelModifiers on other
containers leak permanently and stack on rebuild**: MedicalCenter/HospitalCCP1 "Holographic
Scanner" (+30 birth comfort on parent_dome), AncientArtifactInterface "Full System
Integration" (+1 colony-wide DroneResourceCarryAmount). Call sites: `Building:Done` (:510),
`Building:SetDome` (:675). No other cleanup path exists (`LabelModifier:TurnOff`,
Modifiers.lua:277, is the only remover). **Fix:** replace `Building.StopUpgradeModifiers`
with corrected iteration (`pairs` outer, `ipairs` inner, honoring `only_for_object`).
Consider optional one-shot savegame sweep for already-leaked modifiers (id pattern
`"%d+_upgrade%d_mod_%d"` with no live building).
*Sweep implemented* in `Code/90_SaveSanitizer.lua` (LoadGame), on exactly that id pattern.
`ApplyUpgrade` mints the id as `string.format("%s_upgrade%d_mod_%d", self.handle, tier, i)`
(`Building.lua:1155`), so the handle in the id is the OWNING building's and nothing else in
the game writes ids of that shape — an entry whose handle no longer resolves to a live
object is by construction a leak. Containers swept: `UIColony`, every city, and every dome
(the three things `ApplyUpgrade` can target, `:1152`). Conservative: a handle that still
resolves to anything valid is left alone even though handles can in principle be recycled —
a missed leak is cheap, stripping a live building's bonus is not. Idempotent.
Probe: `SaveSanitizerUpgradeLeak` in `30_Probes_Wave3.lua`.

⭐ **`Fix_UpgradeModifierLeak` CONVERTED TO A §1.4 CHAINED WRAPPER 2026-08-02**
(chain prompt 8; `SAVE_SAFETY_REDESIGN.md` §5.4 group A). This is the shape
§1.4b names explicitly — **the broken original is a verified no-op**, so nothing
has to be prevented and a post-step doing the correct work is sufficient. The
no-op proof is mechanical, not inferred: `self.upgrade_modifiers` is created
empty in `Building:Init` (`:322`) and only ever written at
`self.upgrade_modifiers[id]` with a string upgrade id (`ApplyUpgrade`,
`:1168-1171`), so it holds no integer keys and `ipairs` over it visits nothing,
always. `orig` is still called so another mod's wrapper on the same method stays
in the chain; the corrective `pairs` loop survives unchanged as the post-step,
under the same `only_for_object` filter, so the set of modifiers turned off is
exactly the set the copy turned off. One documented consequence of delegating:
vanilla's `ipairs(self.upgrade_modifiers)` would raise if that field were still
the class default `false` (`:255`) — it cannot be at either call site
(`Building:Done` `:510`, `Building:SetDome` `:675`, both on constructed
buildings), and it is vanilla's own exposure in every unmodded game.
`90_SaveSanitizer`'s cleanup half is untouched.

---

## 2026-08-12 — first in-game execution of the §5.4 conversion (`corun-pt60`, PT-60 P3/P6/P7)

The 2026-08-02 conversion of `UpgradeModifierLeak` carried a byte-equivalence
argument, never an observation, until this leg. On the owner's pre-batch Sol-302
campaign: module `active`, empty detail, at the opening gate, at P3 and at the
close (81/81) · probe `PASS UpgradeModifierLeak [behavior] modifier turned off`
(`docs/archive/cp60sitting_Mars.exe-20260812-13.38.15.log:653`) · 0 `[LUA ERROR]`
in the 1,082-line log across ~50 min of owner play, two loads, mixed speeds.
Owner's P7 verdict (`:1033`) is a whole-session normalcy read, not per-system.
Execution evidence on this sitting's executed paths only; `tested` (pre-existing)
unchanged.

## ⭐ 2026-09-08 — game 1.1.0: what changed, and what hotfix 2 did about it

⛔ **Nothing above this line is edited.** The 1.0.7 record stands exactly as it was
written and every citation in it was true of **1.0.7.396349**. This section adds only
what game **1.1.0.403908** changed, read against the shipped 1.1.0 body on 2026-09-08.

Fixed upstream at BOTH ends. The leak source: `Building:StopUpgradeModifiers` now iterates with `pairs`, not `ipairs` (`Building.lua:1303-1311`), so it actually turns the modifiers off and our second `TurnOff` is idempotent and useless. The residue already in old saves: 1.1.0 ships `SavegameFixups.RemoveLeakedUpgradeModifiers` (`Building.lua:1313-1345`), which strips leaked `<handle>_upgrade<tier>_mod_<i>` entries keyed on live ownership -- a stricter test than our handle-resolution one. ⇒ `Fix_UpgradeModifierLeak.lua` was deleted AND the matching F03 pass was removed from `90_SaveSanitizer.lua`. ⛔ **A FINDING I FILED HERE AND THEN REFUTED THE SAME DAY — recorded so nobody re-raises it.** I read vanilla's new fixup ending on an unguarded `for _, entry in ipairs(leaked) do` with `leaked` left `nil` for a container with no leaks, and filed it as candidate `C54` on the ground that vanilla guards `ipairs(x or empty_table)` in 36 other places. **That was a one-sided count and the conclusion is wrong.** The unguarded form is the house idiom: the identical shape ships at 18 sites in `Lua/`+`CommonLua/` — `CommonLua/Cooldown.lua:239-252` and `CommonLua/Libs/Notifications/Notifications.lua:430-437` were read in full and both leave the local `nil` in the ORDINARY case — and `ipairs(self.<field>)` on a field defaulting to `false` ships at 28+ sites including `ElectricityGrid.lua:67` and `LifeSupportGrid.lua:67`. `EF-005` had already recorded that this engine tolerates nil-iteration and says in terms: *don't report/fix nil-iteration as crashes*. ⇒ **Vanilla's cleanup works; `C54` is closed as refuted.** Nothing about this entry's REMOVE verdict was ever contingent on it.

⇒ **Fix_UpgradeModifierLeak** — removed from the pack in hotfix 2 (re-verification row R-5;
owner decision 98, ruled 2026-09-08: delete rather than gate, because the live pack serves the
current patch period and 1.0.7 players are served a frozen v5 instead, decision 118).

⛔ **What may NOT be read into this.** Not that the fix was never needed — it was
correct on 1.0.7, and what is true is that 1.1.0 fixes it itself. Not that removal is
verified safe — it is verified **in source**; nothing has been run in a game. Not that no
player is affected — a player still on 1.0.7 who takes this update loses this fix.
No status word is moved here, because no boot log has been taken since the change.
