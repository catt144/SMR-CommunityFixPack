---
id: "C62"
seq: 157
row: 192
title: "Hostage Situation building explosion collects a local list of destroyed neighbors that is never consumed"
status: "cand"
status_source: "tag"
priority: "P3"
evidence: "source-read"
row_status: "cand 2026-09-10 — PASSING; Hostage Situation supplies a concrete R2 route; dead allocation cost remains unprofiled"
updated: "2026-09-10"
copies: []
---
### C62 — Hostage Situation explosion fills an unused buildings_hit table (P3, no measured cost) `[cand 2026-09-10: source-read; concrete authored route]`

**PASSING. SOURCE intent tell:** dead computation in both branches.
`Lua/Buildings/Building.lua` old `BlowUp :3405-3511`, new `:3588-3703`
allocates local `buildings_hit` (old `:3428`, new `:3611`) and inserts a
new `{pos, radius}` table for successfully destroyed neighboring buildings
(old `:3464`, new `:3656`). There is no read or capture of the local before
return. The stockpile/resource change that brought the body into this hunt
did not introduce the dead work.

**SOURCE ROUTE — R2, concrete authored base scenario:** in both trees,
`Data/StoryBit/HostageSituation_BlowUp.lua:3-23` selects one `CasinoComplex`
and authors `ExplodeBuilding` with radius 500; current adds its
`Tick_FollowUps` trigger at `:24`. Current generated
`Lua/ClassDefs/ClassDef-Effects.generated.lua:768-790` dispatches that effect
to `obj:BlowUp(...)`. A player who reaches the Hostage Situation's detonation
branch therefore executes the body. Dynamic/native dispatch remains outside
the literal search, but is not needed for this route.

**INFERRED severity / non-owner answer:** base code and the authored story need
no DLC. The local work allocates one table per successfully destroyed nearby
building during the scripted blast; frame-time effect is unmeasured and may be
negligible. It cannot be advertised as a stutter cause.

**Recipe / falsifier:** reach Hostage Situation's blow-up outcome with a Casino
and at least one destructible neighboring building inside 500 units, then
profile `Building:BlowUp` and count inserts into `buildings_hit`. Vacuous if no
neighbor is successfully destroyed or only dome/unit/rejected-building branches
execute. A demonstrated consumer of the local refutes the lexical tell;
negligible attributed allocation/time refutes any performance priority. No
gameplay change is proposed.
