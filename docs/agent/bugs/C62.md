---
id: "C62"
seq: 157
row: 192
title: "Building explosion body collects a local list of destroyed buildings that is never consumed"
status: "cand"
status_source: "tag"
priority: "P3"
evidence: "source-read"
row_status: "cand 2026-09-10 — PASSING; dead allocation, concrete scenario reach and cost not established"
updated: "2026-09-10"
copies: []
---
### C62 — explosion code fills an unused buildings_hit table (P3, no measured cost) `[cand 2026-09-10: source-read; instantiated scenario route unknown]`

**PASSING. SOURCE intent tell:** dead computation in both branches.
`Lua/Buildings/Building.lua` old `BlowUp :3405-3511`, new `:3588-3703`
allocates local `buildings_hit` (old `:3428`, new `:3611`) and inserts a
new `{pos, radius}` table for successfully destroyed neighboring buildings
(old `:3464`, new `:3656`). There is no read or capture of the local before
return. The stockpile/resource change that brought the body into this hunt
did not introduce the dead work.

**SOURCE ROUTE — U for a concrete instantiated scenario:** all four direct
base Lua calls are effect/script wrappers: new
`Lua/ClassDefs/ClassDef-Effects.generated.lua:606,790` and
`Lua/ScriptStatements.lua:723,743`. The method's own definition is the fifth
`BlowUp(` match. These provide dispatch capability, not a demonstrated player
trigger; no concrete authored scenario instance was established by this read.
TAKEABLE WHEN 03d reads those effect/script consumers and can establish one.
Dynamic/native dispatch is outside this literal search.

**INFERRED severity / non-owner answer:** base code requires no DLC, but no
ordinary owner or non-owner trigger was established here. The local work
could waste allocations during scripted explosions; frame-time effect is
unmeasured and may be negligible. It cannot be advertised as a stutter cause.

**Recipe / falsifier:** no independent natural recipe yet. First establish
an authored scenario that invokes the wrapper, then profile the whole body
with at least one successfully destroyed neighbor. Vacuous if only the dome,
unit or rejected-building branches execute, or if the wrapper is never used.
A demonstrated consumer of the local refutes the lexical tell; negligible
profiled cost refutes any performance priority. No gameplay change is proposed.
