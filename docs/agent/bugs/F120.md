---
id: "F120"
seq: 197
row: 232
title: "Legacy Wildfire saves lose the revealed cure when tech-point migration omits Mysteries"
status: "fixed"
status_source: "tag"
priority: "P2"
evidence: "shipped Lua desk A/B: load recovery, repeat load, disabled-fix persistence, research to final wait completion; platform gate exercised: non-Steam Load anyway can reach this, ordinary Steam legacy load is blocked; retail recovery and reporter cause unconfirmed"
row_status: "fixed 2026-09-16 - additive affected-save recovery built and desk-exercised; retail untested"
updated: "2026-09-16"
copies: []
---
### F120 — Tech-point conversion hides a previously revealed Wildfire cure `[fixed 2026-09-16: affected-save recovery desk-exercised; retail unexercised]`

**MEASURED:** the shipped discovery converter leaves the entire Wildfire family
hidden for a synthetic legacy player with a discovered cure and sufficient tech
points. A second PostLoadGame sweep does not recover it. The same converter
successfully restores a discovered breakthrough control. Command and source
hashes: [desk evidence](../../archive/wildfire_cure_desk_before_20260916.txt).

**INHERITED report:** Steam, Jäger, 2026-09-14, owner-relayed: “I seem to be
having an issue with the Wildfire Mystery, there is no option to research the
cure in the new tech tree.” No save, log, migration history, mod list or mystery
stage was supplied. **This mechanism can produce that symptom; the player's
specific cause remains unconfirmed.** No reporter save is being requested.

**MEASURED platform limit:** retail Steam refuses the archived 1.0.7 save
revision. Non-Steam retail offers **Load anyway**, which can reach conversion.
The harness executes `ValidateSaveMetadata` with shipped configuration
(`Lua/Config/config.lua:174-175`, minimum 402200; block on Steam, warn elsewhere).
Thus F120 does **not** explain this Steam report through ordinary old-save
loading. A fresh 1.1.0 colony also lacks the legacy state this fix requires.

#### Cause and reachability

**SOURCE, R2 / platform-conditional:** a non-Steam colony that revealed the
cure on 1.0.7 and accepts the old-save warning when crossing to 1.1.0 retains
`tech_status.WildfireCure.discovered`. The old creator stores
`field = "Mysteries"` (`Lua/Research.lua:668-682`, archived 1.0.7; preset at
`Data/TechPreset.lua:2677-2693`). The scenario's `Field = "Special"` is editor
metadata, not this saved field.

The current converter `SavegameFixups.TechPoints_MigrateDiscoveredSpecialTechs`
(`Lua/TechTree.lua:1486-1502`, 1.1.0.403908) accepts only BuriedWonders,
Storybits and Breakthroughs. It drops Mysteries without an error. Newly created
lock state hides the current cure presets (`CommonLua/Features/LockablePreset.lua`,
`ResetLockablePresetState` and `PreProcessLockablePresets`). A scenario already
past its reveal does not replay that action merely because the game was upgraded.
`SA_WaitResearch:SAExec` continues waiting on the final `WildfireCure`.

**SOURCE intent:** the converter explicitly preserves discovered event research
for sibling groups, and the Wildfire scenario promises an available research
chain. Losing an earned discovery while retaining the waiting scenario violates
both contracts. This benefits ordinary vanilla colonies and needs no other mod.

**MEASURED qualification:** a remaining medical-building grant force-researches
the first intermediate, which reopens the next node. This is a recovery route
only while the saved SpeedUpResearch loop still has a grant available. A save
that used up that loop before conversion has no remaining medical grant to
rescue its hidden chain. Loop exhaustion is a SOURCE condition, not sampled in
a retail colony.

#### Controls and repair contract

**MEASURED:** fresh reveal enables the chain head; sequential purchases reach
the final tech; zero points leave a revealed node visible but unaffordable;
medical grants advance intermediates without spending points; the final tech
still needs a point. `Field = "Special"` does not prevent the completed-tech
wait from returning. These are shipped-body desk results, not UI observations.

**Falsifier:** run the converter and PostLoadGame processing on an old colony
whose cure discovery predates conversion. If a shipped path already restores a
researchable family member, this hidden-family defect is absent for that state.
The harness must retain both that healthy control and the omitted-group case.

**Repair built:** [Fix_WildfireCureMigration.lua](../../../Code/Fix_WildfireCureMigration.lua)
restores only the existing scenario's chain entrance
when the legacy discovery is positive, the colony is Wildfire, and the entire
current family remains hidden. Follow `MysteryTechRevealRemapping`'s entrance;
do not award research, points, crops, or scenario completion. Existing affected
saves must recover on load; a prevention-only change is insufficient (owner,
2026-09-16). A second load must preserve recovery and skip the already healthy
state. No new persisted field or thread is used. This is an additive synchronous
PostLoadGame handler; it initialises any unprocessed presets first, preserving
their vanilla processed markers so a subsequent vanilla load does not hide the
entrance again. The runtime legacy-discovery and hidden-family conditions make
healthy saves no-ops. A closed-table behaviour probe checks the lock reader;
missing modern APIs and unknown probe results decline.

**MEASURED verification:** `python tools/desk_wildfire_cure.py` passes **37/37**
named demands on 1.1.0.403908 (sum of the PASS members in the
[after evidence](../../archive/wildfire_cure_desk_after_20260916.txt)). The
unpatched converter loses discovery; the actual module restores an already
converted fixture; another PostLoadGame is idempotent; vanilla PostLoadGame
with the fix disabled retains recovery; buying the chain releases an already
suspended `SA_WaitResearch`. Controls cover partial/completed progress, missing
discovery, foreign mystery/field, changed mapping, veto, inactive registry,
changed/throwing lock reader, early preset processing, and absent modern API.
Apply is exercised with game/presets absent and with presets present. These are
desk models of cold/enable paths, not retail boots. The persisted lock tables
and markers are carried through the load handlers; engine serialization itself
was not exercised. `python tools/parsecheck.py --quiet` and the module-scoped
bodycheck pass. No claim of attended testing, retail recovery or crop production
is made; crop-effect calls are sinks in the desk harness.

**Recovery use:** on a platform where the old save can be loaded, enable the
updated pack and load the affected Wildfire save. The previously revealed
entrance becomes available in the Special/event research section. Research
normally, then save; no fresh colony or scenario restart is required for this
mechanism. This usage follows the exercised source path; its retail UI and
save/write/load round trip remain untested. F120 adds no old-save loading bypass.

**SOURCE class / routing:** the omission is group-wide, so other migrated mystery
discoveries deserve the same question. This entry owns the established Wildfire
member. C79's discarded cost, C69's dead in-progress state, C92's unreachable
technology and C97's tutorial flags share the research rewrite as historical
context, not a demonstrated common repair. None is changed by this finding.

**Number allocation:** on `7d63900`, inline Python read front-matter id/seq/row
from `docs/agent/bugs/*.md` and `docs/archive/bugs/*.md`; maxima were F119,
C99, seq 196 and row 231. This entry takes F120 / 197 / 232.
