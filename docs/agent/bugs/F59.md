---
id: "F59"
seq: 59
row: 59
title: "Freed housing notification — HARMFUL: expedition-home loss (1.1.0) AND over-capacity residence on manual assign (both branches); ck151"
status: "fixed*"
status_source: "tag"
priority: "P2"
evidence: "med"
row_status: "fixed* historically; HARMFUL — expedition claim CONFIRMED by independent re-derivation (12/12) AND a second caller found: manual Set Residence on a full home overfills it, on BOTH branches (8/8, tools/desk_f59_interact.py); repair must cover both; ck151"
updated: "2026-09-11"
copies: []
---
### F59 — Freed housing never notifies homeless (P2, med)  `[fixed*: Code/Fix_FreedHousingNotice.lua — the RemoveResident half; the CancelResidenceReservation site is deliberately not hooked, see below]`
`RemoveResident` (`Residence.lua:83-90`) and `CancelResidenceReservation` (:353-365) never
call `CheckHomeForHomeless`; homeless rely on Idle heavy-update throttled to 12 game hours
at 3600+ pop (`City.lua:118-120`). **Fix:** post-hook `RemoveResident` →
`CheckHomeForHomeless()`.
*Implemented one level up, on better evidence:* the post-hook is on
`Colonist:SetResidence` (`Colonist.lua:2291-2307`), the only caller of `RemoveResident`.
Same event, but the notification then runs when the move is FINISHED. `RemoveResident` is
called from the MIDDLE of `SetResidence`, before `home:AddResident(self)` and before
`self.residence` is updated; waking the homeless there lets one of them take the slot the
colonist in hand is about to occupy, and the next shipped statement is
`assert(self:GetFreeSpace() > 0)` followed by an unconditional insert (`Residence.lua:
74-77`) — assert does not unwind in this engine, so that is an over-capacity residence.
The hook no-ops unless a home was actually left and `GetFreeSpace() > 0` in it.
*Open half:* `Residence:CancelResidenceReservation` is NOT hooked. `Residence:AddResident`
releases the incoming colonist's reservation (`:74`) one line before the same
`assert(self:GetFreeSpace() > 0)`, so a notification from there reintroduces exactly that
race. The reservation case is instead bounded by F58's daily stale-reservation sweep plus
the normal heavy update. Probe: `FreedHousingNotice` in `30_Probes_Wave3.lua`.

## 2026-09-11 — HARMFUL on the 1.1.0 expedition boarding sequence; audit stopped

The original 1.0.7 record above is preserved. **SOURCE**, game 1.1.0.403908:
`Colonist:EnterTransporter` saves `expedition_residence` before
`SetDome(false)` (`Lua/Units/Colonist.lua:5027-5040`). `SetDome` calls
`SetResidence(false)` (`:410-435`), invoking our immediate vacancy notification
after removing the resident (`:2898-2917`). `Residence:CheckHomeForHomeless`
(`Lua/Buildings/Residence.lua:160-168`) can synchronously assign the bed to a
suitable homeless neighbour through `UpdateResidence` (`Colonist.lua:2919-2926`).
Only later does `OnDisappear` try to reserve the expedition home (`:5003-5007`).
A full home fails `CanReserveResidence` (`Residence.lua:283-288`), clearing the hold.

**INFERRED ROUTE, R2:** a housed expedition crew member reaches boarding while
an eligible, command-changeable homeless colonist shares their full residence's
dome. The crew loses the intended return-home reservation because our notification
runs before the larger operation finishes. This is caused by our module, not a
new vanilla defect. Earlier transport may change the home; record it at boarding.

**DESK-CONTROLLED**, `tools/desk_f59_expedition.py`: 6/6 demands held with
extracted shipped `EnterTransporter`, `SetDome`, `SetResidence`, `OnDisappear`,
selection and reservation bodies and whole F59/F58 modules. Module absent:
crew retains its reservation. F59 applied: neighbour takes the bed and the hold
is lost. No competing neighbour: F59 retains the hold. Current F58 cannot
protect or restore a reservation that was never created. The Unit transport
boundary, labels, UI, comfort and geometry are stubs; no engine run or expedition
return was observed. Transcript: `reports/MIGRATION_DESK_RESULTS.txt`.

**Disposition:** HARMFUL on the controlled state; **no fix built**. The audit
stopped under its explicit harmful-module rule. Checklist **151** asks the owner
to prioritize a repair preserving expedition holds; temporary disable/removal is
a separate owner choice. Historical `status`/heading remain the 1.0.7 record;
the generated title exposes current harm alongside the historical status. Mechanism,
unrun numbered A/B recipe and vacuity conditions: `reports/MIGRATION_DEV_REPORT.md` §1.

## 2026-09-11 — resumed audit: repair, not retirement; candidate remains unbuilt

Owner authorized continued checks and findings after the stop. **SOURCE:** the
ordinary notification gap remains in `Residence.lua:119-126`,
`Building.lua:3276-3318`, `Community.lua:361-364`, and `Colonist.lua:2898-2917`.
These update occupancy/cache/own state, not the homeless neighbour. Periodic
housing assignment still runs at `Colonist.lua:2338-2358`; `City.lua:118` caps its
heavy interval at 12 hours at 3,600 colonists. Other events can shorten the delay.

**DESK-CONTROLLED:** expanded harness **12/12**. Ordinary vacancy: vanilla leaves
an eligible neighbour homeless until a later housing update; F59 assigns it
immediately. An **in-memory, unbuilt candidate** captures whether the departing
home equals `self.expedition_residence` before the original setter, then excludes
that exact home from notification. It preserves the expedition hold with F58
loaded, still notifies ordinary departures, and does not suppress an unrelated
home merely because an expedition pointer exists. No `Code/` changes.

**INFERRED recommendation:** repair F59 with a narrowly reviewed expedition
exclusion, rather than retire the ordinary notification. Before a build, review
failed boarding, cancellation, return, and stale expedition intent; skipping the
helper can delay notification about other free beds. Reservation cancellation
remains unhooked. Report §1/§1a contains separate numbered recipes and alternatives.
Audit complete; checklist **151** retains implementation and sitting decisions.

## 2026-09-11 — INDEPENDENT RE-DERIVATION (ck151 a): expedition claim CONFIRMED, and the hook has a SECOND, worse caller

Re-derived from the shipped 1.1.0 bodies without reusing the audit's reasoning,
both halves separately. `tools/bodycheck.py` reads 110 OK, so the live tree still
matches the 2026-09-08 pins ⇒ citations below are **game 1.1.0.403908**.

**Half (1) — does the window exist in vanilla's own order? SOURCE: YES.**
`Colonist:EnterTransporter` (`Lua/Units/Colonist.lua:5025-5044`) saves
`expedition_residence` at `:5029-5031`, calls `SetDome(false)` at `:5039`, and only
then `Unit.EnterTransporter` at `:5043`. `Colonist:SetDome` calls `self:SetResidence(false)`
at `:434` — a `self:` dispatch, so a class-level wrapper reaches it. The engine leg is
synchronous with no yield: `Unit:EnterTransporter` (`Lua/Units/Unit.lua:1292-1306`)
→ `Disappear` at `:1305` → `Unit:Disappear` sets `disappeared` at `:1223` and calls
`OnDisappear()` at `:1225`, before its `SetCommand` at `:1229`. `Colonist:OnDisappear`
(`:4992-5019`) takes the hold at `:5003-5008`. `Unit:OnPreDisappear` is empty
(`Unit.lua:1181-1182`) and `Colonist` does not override it. ⇒ the bed is unheld
between `Colonist.lua:434` and `:5005`.
**SOURCE: vanilla puts nothing in that window.** The only shipped callers of
`Residence:CheckHomeForHomeless` are `Residence:GameInit` (`:64`), `SetUIWorking`
(`:173`), `Residence:SetDome` (`:214`) and `Hotel.lua:23` — all player-caused.
`RemoveResident` (`:119-126`) ends at `ResetFreeSpace`; `Community:ResetFreeSpace`
(`Community.lua:361-364`) only nils a tally; `Msg("ColonistLeavesDome")`'s single
handler (`StatusEffects.lua:50-55`) touches starvation only.

**Half (2) — does OUR post-hook fire inside it on the shipped path? SOURCE: YES.**
`Colonist:SetResidence` is defined ONCE (`:2898-2917`) with no subclass and no
override anywhere in the tree, so `Code/Fix_FreedHousingNotice.lua:66-79` owns every
dispatch. Its guard passes in the expedition case: `left` = the home, `self.residence`
is `false` after `orig`, the home is valid and `ui_working`, and `GetFreeSpace()`
(`Residence.lua:232-234`) is 1 once the crew member is out. The crew member cannot
self-rescue: `SetDome` clears `self.dome` at `:411` before `:434`, so
`UpdateHomelessLabels` (`:2885-2896`) adds them to the CITY label only, while
`CheckHomeForHomeless` (`:160-168`) walks `parent_dome.labels.Homeless`. The
neighbour's `UpdateResidence` (`:2919-2926`) passes `CanChangeCommand` (`:2685-2688`)
for any ordinary idle homeless colonist and reaches `Dome:ChooseResidence`
(`Dome.lua:3554-3557`) → `ChooseResidence` (`Residence.lua:437-467`).
Then `OnDisappear:5004` fails `CanReserveResidence` (`:283-288`) and `:5007` clears
the hold. **VERDICT on the audit's claim: CONFIRMED at source.** ⛔ Still NEVER
reproduced in play; the in-play half is unchanged.

**Harness legs checked, not taken on trust.** `tools/desk_f59_expedition.py` re-runs
12/12 here. Its `Unit.EnterTransporter` stub is faithful to `Unit.lua:1223-1225`
(sets `disappeared`, then `OnDisappear`). Both principal legs are cross-sensitive,
which is the falsifier: the harm expression is FALSE with the module absent, and the
vanilla expression is FALSE with it present. `deskbench.body()` asserts exactly one
body match, so a moved target fails loudly. Named fixture gaps that remain: suitability
always passes (`ColonistFilterFunc` empty), comfort is constant, and no colony was run.

### ⛔ NEW — the same hook ALSO fires inside MANUAL ASSIGN, and that one OVERFILLS a residence

**SOURCE, and it is NOT 1.1.0-specific.** `Residence:ColonistInteract` — the player's
"Set Residence" command (`Residence.lua:330-354`, offered by `ColonistCanInteract`
`:309-328`) — when the target is full calls `KickOldestResident` at `:342`
(`:356-383`) → `KickResident` (`:156-158`) → `colonist:SetResidence(false)` at `:157`,
then assigns the forced colonist at `:348`. Here `self.dome` is INTACT, so
`UpdateHomelessLabels:2893` puts the just-kicked resident into the dome's Homeless
label inside that same call — and our hook then runs `CheckHomeForHomeless` over a
label that is guaranteed to hold at least one candidate for the slot in hand. The
next shipped statement is `assert(self:GetFreeSpace() > 0)` followed by an
unconditional insert (`Residence:AddResident`, `:111-112`), and by `EF-008` an
assert does not unwind ⇒ **an over-capacity residence.**
The identical bodies are in the 1.0.7 archive (`Residence.lua:310-316`, `:120-122`,
`:75-76`; `Colonist.lua:2291-2307`) ⇒ **this has shipped on BOTH branches in every
released version of the module**, unlike the expedition case.

**DESK-CONTROLLED — `tools/desk_f59_interact.py`, 8/8.** Same shipped
`ColonistInteract`/`KickOldestResident`/`KickResident`/`SetResidence`/`AddResident`/
`ChooseResidence` bodies, module off/on. Vanilla: the kicked resident is evicted, the
forced colonist moves in, 2/2, no assert. F59 applied: the kicked resident is re-homed
into the freed slot, the forced colonist is inserted anyway, **3 residents in a
capacity-2 home, infopanel reads 3/2** (`GetUIResidentsCount`/`GetUICapacity`,
`:408-413`), one assert fired. Negative leg: a better-comfort free bed elsewhere in
the dome draws the kicked resident away and the assignment stays within capacity —
so the precondition is "the freed slot is the best available choice for some woken
homeless colonist in that dome", which a full dome supplies. `assert` is shimmed to
record-and-continue per `EF-008`; labels, comfort and UI are fixtures.

**What this means for the module's stated design — SOURCE.** The header's claim that
`SetResidence` is the only caller of `RemoveResident` is TRUE (`:2905`, plus two
delegating subclass overrides in `Arcology.lua:66-67` and `MicroGHabitat.lua:80-81`).
The load-bearing inference is the one that fails: *"hooking one level up means the
notification runs when the move is FINISHED"*. `SetResidence` finishing is not the
enclosing operation finishing, and the callers of `SetResidence` were never
enumerated. There are **11 shipped call sites**: `Colonist.lua:435` (`SetDome` — the
expedition window), `:1255` (`Erase`), `:1297` (death), `:2926` (`UpdateResidence` —
the intended case), `:4995` (`OnDisappear`, a no-op by then);
`Residence.lua:85` (`OnDestroyed`), `:157` (`KickResident`), `:265`
(capacity shrink), `:348` (`ColonistInteract`'s own assignment);
`NaturalHabitat.lua:7`; `Data/TraitPreset.lua:772`. Of these, `:157` is the confirmed
mid-operation hazard above. `:265`'s shrink loop is SAFE by inspection — it evicts only
while `#reserved + #colonists > home_capacity`, so `GetFreeSpace()` is 0 at every hook
call and our guard declines (`Residence.lua:236-279`).

**UNRESOLVED lead, not a finding — `Residence:OnDestroyed` (`:81-92`).** Its eviction
loop frees slots one at a time, so our guard opens; whether a homeless colonist can be
assigned INTO the residence being destroyed depends on whether it is still in
`dome.labels.Residence` with `ui_working` true at that moment — NOT SAMPLED here.
⚠️ Vanilla's own `colonist:UpdateResidence()` at `:86` has the same exposure, so this
may be a vanilla shape rather than ours; `self.colonists = {}` at `:88` would drop any
such assignment and leave the colonist pointing at a dead home. Needs its own check
before anyone calls it either way.

**Disposition unchanged by this session: FILED, NOT FIXED.** Any repair must now cover
both callers, so the report's unbuilt expedition-home exclusion is NECESSARY BUT NOT
SUFFICIENT — it does nothing for the manual-assign route. Checklist **151** carries the
decision and the cheap in-play check this finding unlocks.
