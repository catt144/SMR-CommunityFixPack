---
id: "F27"
seq: 27
row: 27
title: "Storage charge/discharge rate modifiers ignored (latent)"
status: "fixed"
status_source: "tag"
priority: "P3"
evidence: "med"
row_status: "fixed"
updated: "2026-07-25"
copies: []
---
### F27 — Storage charge/discharge rate modifiers ignored (latent)  `[fixed: Code/Fix_StorageRateModifiers.lua]`
`Lua\ElectricityStorage.lua:47-63`, `Lua\LifeSupportStorage.lua:25-42,131-148` —
`OnModifiableValueChanged` fires for `max_*_charge/discharge` but never copies to
`element.max_charge/max_discharge` (read in SupplyGrid.lua:164-170). No vanilla modifier
touches rates; breaks the documented modding surface. **Fix:** post-hook the three
`OnModifiableValueChanged`s, copy both fields before `UpdateStorage()`.
*Implemented as sketched, with the copy AFTER the shipped body rather than before it* — the
shipped body ends with its own `element:UpdateStorage()`, so a post-wrapper that copies the
rates and calls `UpdateStorage()` again is the smallest change that gets the new rates into
the same pass. Exact fields confirmed: `NewSupplyGridStorage` (`SupplyGrid.lua:64-76`) seeds
`max_charge`/`max_discharge` once at creation and `SupplyGridElement:UpdateStorage`
(`:167-168`) is what reads them every tick. The wrapper no-ops unless the property that
changed is one of the two rates, and each of the three classes is wrapped independently, so
one missing target cannot take the other two down.
Probe: `StorageRateModifiers` in `40_Probes_Wave4.lua`.

## ⭐ 2026-09-08 — game 1.1.0: what changed, and what hotfix 2 did about it

⛔ **Nothing above this line is edited.** The 1.0.7 record stands exactly as it was
written and every citation in it was true of **1.0.7.396349**. This section adds only
what game **1.1.0.403908** changed, read against the shipped 1.1.0 body on 2026-09-08.

The electricity leg was fixed upstream (`ElectricityStorage.lua:55-56`, plus a sync fixup at `:240-249`), and the water/air legs can never fire because those rate properties are not declared `modifiable` (`LifeSupportStorage.lua:9-10`; `Modifiers.lua:106-109`). Nothing is left for the module to do.

⇒ **Fix_StorageRateModifiers** — removed from the pack in hotfix 2 (re-verification row R-31;
owner decision 98, ruled 2026-09-08: delete rather than gate, because the live pack serves the
current patch period and 1.0.7 players are served a frozen v5 instead, decision 118).

⛔ **What may NOT be read into this.** Not that the fix was never needed — it was
correct on 1.0.7, and what is true is that 1.1.0 fixes it itself. Not that removal is
verified safe — it is verified **in source**; nothing has been run in a game. Not that no
player is affected — a player still on 1.0.7 who takes this update loses this fix.
No status word is moved here, because no boot log has been taken since the change.
