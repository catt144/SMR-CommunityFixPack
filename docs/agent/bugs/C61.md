---
id: "C61"
seq: 156
row: 191
title: "Non-natural death popup still promises two lost applicants after the base death code removed that penalty"
status: "cand"
status_source: "tag"
priority: "P3"
evidence: "source-read"
row_status: "cand 2026-09-10 — DIFF-CAUSED; base death route and mutation enumeration, no game observation"
updated: "2026-09-10"
copies: []
---
### C61 — death popup advertises a penalty the base death route no longer applies (P3) `[cand 2026-09-10: source-read; no intended balance repair inferred]`

**DIFF-CAUSED. SOURCE intent tell:** player-facing text contradicts its producing
system. Old `Lua/Units/Colonist.lua` `Die :978-1036` removed up to two random
applicants on a non-natural death (`:1007-1015`); new `Die :1281-1333` omits
that loop while retaining death counters and notifications (`:1311-1317`).
New `OnDie :1355-1359` still passes `ApplicantsLostOnColonistDeath = 2` to
`Evaluation_Colonist_Deaths`; old `OnDie :1055-1059` did the same.
Base `Data/PopupNotifications/PopupNotificationPreset-Evaluation.lua:8` in
both trees says that many applicants leave per non-natural death.
The candidate is the contradiction; restoring the old penalty is not implied.

**SOURCE ROUTE — R2:** normal fatal health loss reaches new
`ChangeHealth :4710-4725` -> `Die` -> `OnDie`. IronColonists can prevent it.
The popup requires five non-natural deaths across the current/previous sol,
and `city.dead_notification_shown` still false. `Lua/City.lua:22-24,132-133`
initializes/rotates that state. Train crashes, suicide and script/building
destruction also invoke Die; one concrete health-loss route suffices for the
conditional popup trigger. Keep surviving colonists to avoid game over first.

**MEASURED source search / SOURCE replacement-path check:** all direct
`g_ApplicantPool` mentions across base Lua/Data/CommonLua were enumerated.
Mutations found: generated insertion (`ApplicantsPool.lua:62`), whole-pool clear
(`:148-150`, Mystery 8 caller), hourly expiry/replacement (`:152-188`, removal
`:161`), and passenger-rocket boarding (`Buildings/RocketUtilities.lua:185-193`).
Traits UI's nearby remove affects `matching_applicants`, not the shared pool.
All five direct `OnMsg.ColonistDied` handlers were read: `GameOver.lua:1-5`,
`MarsStoryBits.lua:163-186`, `ColonyViability.lua:170-214`,
`TrainDisasterHandling.lua:120-122` -> `RemoveDeadFromTransport :42-57`,
`Telemetry.lua:150-153`. None removes applicants. No base ColonistDie handler
was found. `Data/Trigger.lua:534-540` forwards to storybits; the matched base
death-triggered `DustSickness_Deaths` is obsolete and has no applicant action.
This does not exclude dynamically constructed access or uninspected DLC hooks.

**INFERRED severity / non-owner answer:** misleading consequence text for
non-owners; the same base route is present for owners and needs no DLC symbol,
but DLC overrides were not inspected.
No failure to remove applicants is called a balance defect, and no runtime
pool behavior or popup rendering was measured.

**Independent recipe [NEVER RUN]:** use an isolated fresh colony with more than
five colonists and a nonempty applicant pool; incur five non-natural deaths
within two sols, preserve survivors, and record pool membership around each
death plus the popup. Account separately for hourly expiry/generation and any
passenger launches. Vacuous if death is natural, the popup already fired, the
window/count is missed, the pool is empty, or game over interrupts the route.
Do not sacrifice the owner's campaign for this test.

**Falsifier:** a runtime death handler still removes the advertised two, or
the actual rendered popup no longer promises them. No executing death control
was run; a desk comparison would require command, scheduler, message and world
shims. TAKEABLE WHEN an isolated fresh-colony death fixture exists
(checklist seam rider); otherwise file and wait for field evidence.
