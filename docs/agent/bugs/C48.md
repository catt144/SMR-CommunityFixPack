---
id: "C48"
seq: 128
row: 163
title: "Wild vegetation offers Seeds to the drone network with NO distance penalty, while the only comparable scattered source in the game (SurfaceDeposit) carries a deliberate 50% one — so on a vegetated map hundreds of one-plant trickle offers compete with a full depot on equal terms"
status: "filed"
status_source: "tag"
priority: "?"
evidence: "SOURCE-VERIFIED (mechanism) + owner-observed (drone behaviour, attended sitting) + ⭐⭐ MEASURED-unattended 2026-08-15 (the ROUTING SIGNATURE: 134 deliveries, 100% under a full drone trip, 83.6% exactly 280 = one bush's yield, while 12.4M stored Seeds across 34 depots did not fall) — ⛔ the PREFERENCE is STILL not measured (the pairing is C-side), ⛔ the owner's density explanation is REFUTED as stated (533 vs 490 reachable offers) — ⭐⭐ INTERVENTION-measured 2026-08-15 (leg c48-brake, owner-authorized): the devs' own 150 distance brake applied live with ALL 3,390 requests rebuilt and provably rebaked — ROUTING UNCHANGED (280-wall stands at 86-88%/68%, ZERO bulk arrivals) ⇒ ⛔⛔ the one-field brake is REFUTED as a fix shape; the cause moves to priority=1 / the C-side pairing, both unread"
row_status: "filed 2026-08-15 out of the `C47` attended sitting, and ⭐ **THE OWNER FOUND IT** — they watched drones fetching single seed crates out of the landscape while a FULL 4,000/4,000 Seeds depot stood beside the starving farm, and guessed *'there is a hidden rate at which vegetation offers seeds'*. There is. **SOURCE-VERIFIED:** `Lua/Vegetation.lua` keeps a per-hex seed cooldown (`is_in_seed_cooldown = 128` at `:4`, `GameVar(\"seed_cooldowns\")` at `:32`) and places a **`VegetationTaskRequester`** on each grown, off-cooldown plant to offer its seeds to the drone network (`:1081`, `:1774-1783`; Lichen excluded). ⭐⭐ **THE ASYMMETRY IS THE FILING: that requester inherits `TaskRequester.supply_dist_modifier = 100` — the default, NO distance penalty — and the ONLY override anywhere in the shipped tree is `SurfaceDeposit.supply_dist_modifier = 150`, commented *'surface deposits are considered 50% further than storages'* (`SurfaceDeposit.lua:84`).** The developers identified exactly this problem for surface deposits and braked them; scattered vegetation seed offers got no brake, so one plant's yield competes with a 4,000-unit depot on equal terms — and on a carpeted terraforming map there are hundreds of them. Explains the owner's three observations at once: drones arriving with ONE crate (the pile only holds one plant's worth, not a carry-capacity bug), passing a full depot to do it, and why the sibling farm is always silent (*'a much more dense area... and the drone extenders in that area give the drones a much larger area to look for vegetation seeds'*) — which also fits the `C47` numbers, where the two farms **spent almost identically (9,425 vs 9,520) and differed in RECEIPT (7,320 vs 8,361)**. ⛔ **NOT ESTABLISHED: that drones actually PREFER these over a depot** — the mechanism permits it and the owner watched it, but nobody has measured it, and the owner's own report that drones travel FURTHER for ground seeds is an anomaly distance weighting alone does not explain (`priority = 1, --seeds have lowest prio` is unread). ⛔ Offer size and cooldown rate unread. ⛔ Not proven a defect — sweeping the landscape for free seeds is defensible design. ⛔ Nothing built, not a release gate. ⚖️ **If confirmed it is BIGGER than `C47`** (every consumer on a vegetated map, and a one-field data repair of the shape the game already applies to surface deposits) ⇒ ⛔⛔ **DO NOT BUILD A `C47` FIX WHILE THIS IS OPEN — a bigger buffer would MASK it.** Instrument BUILT and gate-proven: `prompts/c47-farm/98_C48Veg.lua.txt` + prompt `03_C48_VEGETATION_SUPPLY.md`. ⭐⭐ **MEASURED 2026-08-15, one unattended launch on a staged COPY of `C47FARM`, owner cost ZERO, 0 `[LUA ERROR]`** (`archive/c48veg_*`; predictions committed first in `94eb508` and pushed before the launch). **THE ROUTING SIGNATURE IS MEASURED AND IT IS THE SHARPEST READING THIS CHAIN HAS PRODUCED: 134 deliveries sampled at 200 ms, NOT ONE of them a full drone trip (3000 on this rig), 112 of 134 = 83.6% EXACTLY 280 — which is what a live requester reads (`preset=Bush`, `request actual amount=280` = Bush's `output_amount` 200 × the 1.4 soil bonus) — while the colony's 34 Seeds-accepting depots held 12,406,609 Seeds and their total ROSE by 2,498 across the window; 21 of the 34 did not move a unit.** ⇒ the two farms ate 45,480 seeds in 280-unit crumbs beside 12.4 million in storage. ✅ `supply_dist_modifier=100` and `priority=1` READ LIVE off an instance. ⛔⛔ **BUT THE OWNER'S DENSITY EXPLANATION IS REFUTED AS STATED, by this entry's own pre-registered falsifier: farm A's drones reach 533 distinct offers and farm B's 490 (1.09×, inside the ±20% band written down beforehand), and A stands in only ~10% denser vegetation (142 vs 127 within 35 hexes) — the extender COUNT is 6× but the extenders OVERLAP, and a wider net over the same plants is not more plants.** ⚠️ A real asymmetry is recorded as an observation with no control: A's six areas hold 5,459 TaskRequesters of every kind against B's 888. ⛔ **The owner's SPEED LADDER is UNSAMPLED, not refuted** — 0 stalls in 838 samples on both farms at every rung (minima 905 / 1681 of 5000), so the condition it ranks never occurred; ⭐ its companion column did read something, per-game-hour drain flat at 1x/3x/5x (2466/2306/2866) and COLLAPSING at 20x (0 and 300), which is the owner's direction — but the rungs are ordered in time and `C47`'s hex-saturation mechanism is perfectly confounded with speed, so the separating control is named instead of a conclusion: **run the ladder DESCENDING on a fresh copy**. ⚠️ Two instrument defects found at source BEFORE the launch and disclosed (the payload asked farms for a `work_radius` no building has; the arm script's strip pattern could not see its own C48 line, so two 'restored to shipped shape' kit commits each ADDED a stray). ⭐⭐ **INTERVENTION LEG `c48-brake` RUN 2026-08-15 ON OWNER AUTHORIZATION** (*'your test is a go'*; same conversation ruled *'I am not going to manipulate drone behavior on a bug fix mod'* — behavioral remedies live in the opt-in house): one unattended launch, 0 `[LUA ERROR]`, predictions pushed first (`d385ae2`), `archive/c48brake_*`. The brake TOOK at 100% (3,390/3,390 requests rebuilt, live reads 150, restored at close, save contract held whole) and **the routing did not move — the pre-registered falsifier fired**. ⛔ Still a CANDIDATE: nothing built, no defect ruled, the drone's DECISION remains unread — and the one-field fix shape is now dead by measurement"
updated: "2026-08-15"
copies: []
---
### C48 — vegetation seed offers get no distance brake, so a full depot loses to a trickle  `[filed 2026-08-15 from the C47 attended sitting — mechanism SOURCE-VERIFIED, owner-observed in play, ⭐⭐ the ROUTING SIGNATURE MEASURED-unattended (84% of deliveries = one bush's 280, zero full trips, 12.4M stored Seeds unmoved), ⭐⭐ INTERVENTION-measured 2026-08-15 owner-authorized: the devs' 150 brake applied at 100% and the routing DID NOT MOVE — the one-field fix shape is REFUTED, ⛔ the drone PREFERENCE is STILL not measured, ⛔ nothing built, ⛔ not a release gate]`

## Where this came from, and whose it is

⭐ **The owner found this by watching drones during the `C47` attended sitting**
(2026-08-15). The agent was counting notification events; the owner was watching
the screen, and asked why drones were fetching single seed crates out of the
landscape while a **4,000/4,000 Seeds depot stood next to the farm**. Their own
words, and they turned out to name the mechanism exactly:

> *"they are picking seeds to deliver to it from the environment even though
> there is a massive full seed storage right there… I am guessing there is a
> hidden rate at which vegetation offers seeds."*

There is. It is called `seed_cooldowns`.

## The mechanism, read at `ModTools\Src` on the pinned build

* `Lua/Vegetation.lua:4` — `is_in_seed_cooldown = 128`, a per-hex flag on the
  vegetation grid.
* `Lua/Vegetation.lua:32` — `GameVar("seed_cooldowns", {})`, the per-hex timer
  table, keyed `x + y*hex_width`.
* When a grown plant is off cooldown the game places a **`VegetationTaskRequester`**
  on that hex — an invisible `TaskRequester` whose job is to offer that plant's
  seeds to the drone network (`:1081`, `:1774-1795`). Lichen is excluded
  (`:867`); every other vegetation group participates.
* The farm has the matching reader: `VegetationPlant:GetSeedsInRange()` →
  `GetResourceInRange("Seeds")` (`TerraformingBuilding.lua:428-429`), and
  `GetGrownPlantsWithNoRequestorInRange` (`:405-421`) counts grown, non-withering,
  off-cooldown plants that do not yet carry a requester.

### ⭐⭐ The asymmetry, which is the whole filing

```lua
DefineClass.VegetationTaskRequester = {
    __parents = { "TaskRequester" },
    priority = 1, --seeds have lowest prio
}                                            -- Lua/Vegetation.lua:1774-1783
```

It inherits `TaskRequester.supply_dist_modifier = 100` — *"modifier (percentage)
of the distance when considering this supply (0-255)"*, `Lua/_TaskRequest.lua:96`.
**100 is the default: no penalty at all.**

And there is **exactly one** override of that field in the entire shipped tree:

```lua
supply_dist_modifier = 150, -- surface deposits are considered 50% further than storages
                                      -- Lua/Buildings/SurfaceDeposit.lua:84
```

⇒ **The developers identified this exact problem for surface deposits and applied
a 50% brake. Scattered vegetation seed offers got none.** A single plant's yield
is weighted identically to a 4,000-unit storage depot, so on a heavily terraformed
map hundreds of unbraked trickle sources compete with the depots.

## What the owner observed, and what it would explain

1. **Drones arrive at the farm carrying one crate** when the rig's carry dial is
   at +2 (3,000 per trip). Explained without any carry-capacity bug: *the pile it
   chose only contains one plant's worth*.
2. **They pass a full depot to do it.** Explained by the missing brake — with one
   caveat below.
3. ⭐ **The two farms' difference.** The owner: *"the farm that is complaining is
   in a much more dense area of the map… and the drone extenders in that area give
   the drones a much larger area to look for vegetation seeds."* This is a better
   explanation of the `C47` A/B split than the crop-cost one the agent proposed,
   and the `C47` numbers agree: the two farms **spent almost the same** (9,425 vs
   9,520) and differed in what they **received** (7,320 vs 8,361).
4. **Why it is a terraforming-speed-run symptom.** The offers only exist where
   vegetation is grown. A young colony has few; the owner's map is carpeted.

## ⛔ What is NOT established, stated plainly

* ⛔ **That drones actually PREFER these over a depot.** The mechanism permits it;
  the owner watched it; nobody has measured it. ⚠️ And the owner's own evidence
  contains an anomaly the distance rule does **not** explain: *"these drones are
  going after ground seeds that are further away than the big storage and far
  further away than the smaller depot."* If they are travelling FURTHER, distance
  weighting alone is not the whole story — `priority = 1, --seeds have lowest prio`
  may be involved, and the interaction between request priority and supply
  selection has not been read.
* ⛔ **The offer size and the cooldown rate.** Neither has been read out of the
  source or measured live.
* ⛔ **That this is a defect at all.** "Colonists should sweep the landscape for
  free seeds" is a defensible design, and the trickle is genuinely free resource.
  The complaint is only that it appears to out-compete a bulk source that would
  serve a consumer far better.
* ⛔ **Nothing is built. Not a release gate.**

## The measurement that settles it — designed, not yet run

Read-only, one unattended launch on a copy of `C47FARM`:

1. **Enumerate every live `VegetationTaskRequester`** on the map: count, position,
   the resource and amount each offers, and how many sit inside each farm's
   drone-coverage radius. ⭐ **That count, per farm, is the headline** — if the
   complaining farm's radius holds many and the silent farm's holds few, the
   owner's explanation is measured rather than argued.
2. **Track both Seeds depots' stored amounts** on the same clock. If they sit flat
   while the farm's buffer sawtooths, the farm is being fed from the landscape.
3. **Sample the farm buffer at 200 ms**, not 2 s, so each delivery lands in its
   own sample and the delivery-size distribution is real rather than smeared.
   ⚠️ The `C47` sitting's 2-second polling is why "how much does one delivery
   bring" is still unanswered.
4. Re-read `supply_dist_modifier` and `priority` **live** off the requester
   objects rather than trusting the class declaration.

⚖️ **If confirmed this is bigger than `C47`**: it would affect every consumer
building on a vegetated map, not farms alone, and the repair shape would be a
one-field data change of the kind the game already applies to surface deposits —
rather than `C47`'s balance-changing buffer resize.

## ⭐⭐ 2026-08-15 — PREDICTIONS, COMMITTED BEFORE THE RUN (chain `c47-farm`, prompt 02 Jobs 3 and 4)

⛔ **Written and committed before any game was launched.** The commit carrying
this section carries no log and no reading; the readings arrive in a later
commit, and the commit order is the evidence.

**The run being predicted.** ONE unattended launch of `98_C48Veg.lua` on
`C48STAGE.savegame.sav`, a byte copy of the owner's `C47FARM`
(MD5 `1B06B9BFAE8922562F5B8B64766F8584`, 56,382,611 bytes, verified on both
sides of the copy). Read-only apart from the game speed. The payload runs the
owner's **speed ladder** `1x → 3x → 5x → 20x` in one process on one colony,
**3.5 game hours per rung**, sampling every **200 real ms**.

### The arithmetic every prediction below is derived from

| term | value | where it comes from |
|---|---|---|
| a vegetation seed offer's size | `AddSupplyRequest(preset.output_resource, GetVegOutputAmount(preset, self), 0, 4)` | `Vegetation.lua:1848-1851` |
| `output_resource` default | **"Seeds"** | `ClassDef-PresetDefs.generated.lua:1917-1918` |
| `output_amount` default | **1000** | same, `:1919-1920` |
| the only presets that can raise an offer | **Bush 200 · Tree 1000 (unset ⇒ default) · Broadleaf 2000** | `Data/Vegetation.lua:36,154`; Grass and Lichen are **0** and are gated out by `preset.output_amount > 0` (`Vegetation.lua:1052`, `:1412`) |
| soil-quality bonus | `+ (sq − min_soil_quality)/2` percent | `GetVegOutputAmount`, `Vegetation.lua:1842-1846` |
| ⇒ **offer size ceiling at perfect soil** | Bush **280** · Tree **1300** · Broadleaf **2600** | min_soil 20 / 40 / 40 |
| a drone trip **on this rig** | **3000** | `Drone.lua:719` × `DroneResourceCarryAmount`, which our own D09 dial sets to **+2** |
| farm steady-state drain (measured, `C47`) | A ≈ **573**/tick · B ≈ **410**/tick | the 08-15 unattended leg |
| farm tick | **9000 game ms** = 0.3 h | `TerraformingBuilding.lua:371-374` |
| ⇒ ticks per 3.5-hour rung | 3.5 / 0.3 = **11.7** | |
| ⇒ predicted spend per rung | A ≈ **6,700** · B ≈ **4,800** | |
| ⇒ predicted spend over four rungs (14 game hours) | A ≈ **26,800** · B ≈ **19,200**, ≈ **46,000** together | |
| the whole game-time budget | ~16 h — the save starts at **day 384 hour 8** and its clock stops dead at **day 385 hour 0** | `C47` FreezeDiagnostic, measured three times |

### P1 — ⭐⭐ THE HEADLINE. The complaining farm's drones can reach many more seed offers than the silent farm's

The owner's explanation, in their words: *"the farm that is complaining is in a
much more dense area of the map… and the drone extenders in that area give the
drones a much larger area to look for vegetation seeds."* Measured as the
union — deduplicated by handle — of the `VegetationTaskRequester`s inside every
command centre serving the farm and every working extender hanging off those
centres, which is the game's own search (`FindTaskRequesters`,
`DroneControl.lua:315-324`).

⇒ **predicted: `OpenFarm#15779` (A, complains) reaches at least 1.5× as many
offers as `OpenFarm#15974` (B, silent)**, and the same direction holds on the
topology-independent local-density proxy.
*Falsifiers, all three written down now:*
* ⛔ **within ±20% of each other ⇒ the owner's density explanation is REFUTED**
  and the `C47` A/B difference is unexplained again. Say so; do not patch it.
* ⛔ **B > A ⇒ an outright reversal**, stronger than a refutation, and it must be
  reported as one.
* ⛔ **zero offers on the map, or `command_centers` resolving empty ⇒
  `UNSAMPLED`**, never "no offers exist". A zero is only a reading if the
  condition was sampled.
⚠️ **Band, stated honestly:** nobody has ever read this count, so the map-wide
figure is predicted only as *hundreds* (**200–3,000**). A number outside that
band is not a failure of the case, only of this guess.

### P2 — the depots stay flat while the farms eat ~46,000 seeds

If the farm is fed from storage, the Seeds depots must fall roughly in step with
what the farms receive.
⇒ **predicted: each Seeds depot's stored amount moves by less than one drone
trip (3,000) between the census and the close**, while the two farms consume
≈46,000 between them.
*Falsifier:* ⛔ **a depot draining by anything close to the farms' cumulative
receipt (say ≥ half of it) means the farm IS being fed from storage and this
entry's premise is wrong.**
⚠️ Boundary that does not go away whichever way this reads: flat depots are
**circumstantial**. They cannot show that a drone *chose* a plant over a depot.

### P3 — the delivery-size distribution is a wall of small values, not trips of 3,000

A drone on this rig carries **3,000**. A vegetation offer holds **200 / 1000 /
2000** plus a soil bonus, ceiling **280 / 1300 / 2600**. So a depot-fed farm
should show deliveries at or near 3,000 and a landscape-fed one should not.
⇒ **predicted: ≥ 60% of sampled deliveries land below 3,000, with the mode in
the 200–2,600 band**, and few or no exact-3,000 arrivals.
*Falsifier:* ⛔ **a wall at 3,000 refutes the routing story** — the farm is being
bulk-fed and `C48` has no symptom to explain.
⚠️ **Two ways a delivery can read small for reasons that are not `C48`,** named
before the numbers exist: a nearly full buffer caps what the drone can unload
(5,000 − stored), and **at 20x one 200 ms sample spans 4,000 game ms**, so
several arrivals merge. ⇒ **read the distribution off the 1x and 3x rungs**;
the 20x rung's sizes are smeared by construction.

### P4 — the requester's weighting reads on a live object exactly as the class declares

⇒ **predicted: `supply_dist_modifier` = 100 and `priority` = 1 on a live
instance.** *Falsifier:* anything else — and it would matter, because the whole
filing rests on the 100.

### P5 — ⭐⭐ THE OWNER'S PREDICTION, RECORDED AS THEIRS (Job 4)

> *"my guess is the speed… I noticed that the frequencys would change on how
> often it ran out of stock depending on the speed I was on, it seems like the
> faster the speed is the less it runs out of stock."* — owner, 2026-08-15

⇒ **predicted (theirs): the stalled-% column falls monotonically 1x > 3x > 5x >
20x.** The two existing readings agree in direction — 52% stalled at 1x
attended, 3.6% at 5x unattended — but were taken in different processes; this
ladder is one process, one colony, one drone fleet, so the only variable between
rungs is the time factor.

⇒ **companion arithmetic, not a new theory:** the same claim says supply keeps up
better at speed, so **received per GAME HOUR should rise with speed while drained
per game hour stays flat** (the drain is a game-time tick and nothing about it
knows the time factor). Reported as a separate column.

*Falsifier:* ⛔ **a flat or non-monotonic stalled-% column refutes it**, and that
outcome is worth more than a confirmation — it would mean the 52%-vs-3.6% gap
still has a cause nobody has found. ⛔ **This leg offers no sixth cause without a
control**; four are already refuted on `C47` and the owner has been right every
time.
⛔ **Rung bookkeeping is binding:** `took=false` ⇒ the rung is **VOID**;
`complete=false` ⇒ **PARTIAL**; a rung that never ran is **UNSAMPLED**, never
zero. ⛔ Read the per-game-hour columns, never the raw counts — the rungs are
equal in game time and wildly unequal in real time (a 3.5 h rung is ~105 real
seconds at 1x and ~5 at 20x).

### P6 — the run fits inside the day-385 freeze, and writes nothing

Four rungs × 3.5 game hours = **14 game hours** against a ~16-hour budget.
⇒ **predicted: all four rungs `complete=true`, the freeze diagnostic does not
fire, and the whole run costs ~5 real minutes.**
⇒ **predicted: `C48STAGE` MD5 identical before and after; the save directory
reconciles BY NAME; 0 `[LUA ERROR]` attributable to this payload.**
⚠️ The autosave rotation (`EF-056`) is unpredictable — all three autosaves were
byte-copied out before the launch (`Sol 376` `5175623063E4D02F332EBD485D67F043`,
`Sol 381` `2B5ED6E8A57FEAD23B9434AD1AD08C23`, `Sol 386`
`1E777CBDA46287BB352A52766FD7B2A9`) and any file the rotation eats is restored
from those copies.

### ⚠️ The confounds, named before the numbers exist

* **Cheats are enabled on this rig** and this colony is an oversized terraforming
  speed-run at sol ~384. The template numbers are playstyle-independent; ⛔ every
  rate is not.
* **The drone carry dial is at +2** — one of our own opt-in modules. A trip is
  **3,000**, not the vanilla 1,000, and P3 is stated against 3,000.
* **Both mods loaded is the rig's normal configuration**, and the one module on
  the measured path (`Opt_AcknowledgedWarnings`, D02) is read rather than assumed.
* **One save, one colony, two farms.** Nothing here generalises to another map.

### ⛔ Two defects in our own instruments, found at source BEFORE this launch and disclosed rather than quietly fixed

1. ⛔⛔ **The payload's headline reading could not have worked.** `98_C48Veg.lua`
   asked each farm for `work_radius or service_area_radius` and counted offers in
   a world-distance circle of that size. **A farm has neither field**:
   `work_radius` is declared on `DroneNode` — hubs, rovers and extenders
   (`DroneControl.lua:40`) — and `service_area_radius` appears **nowhere in the
   shipped tree**. Both farms would have logged *"no serving radius resolved,
   comparison NOT SAMPLED"* and the leg would have produced everything except the
   number it exists for. Repaired to walk `command_centers` and their working
   `linked_extenders` and count in **hexes**, mirroring `FindTaskRequesters`
   (`DroneControl.lua:315-324`) — which is also a closer read of the owner's own
   sentence than the farm-centred circle was. ⚠️ Same family as the three
   instrument defects `C47` disclosed: trusting *how* a value is read rather than
   checking *what* it is.
2. ⛔ **The arming script could not disarm this payload.** `C47_ARM.ps1`'s strip
   pattern was `Code/9[78]_C47`, which does not match `98_C48Veg` — the file is
   named after the case it measures. So each `arm-veg` added a `metadata.lua`
   line nothing could remove, the disarm gate (`Code/9[678]_(C47|AutoRunFlag)`)
   was blind to it too, and **two TestKit commits whose messages say the metadata
   returned to its shipped shape each ADDED a stray line** pointing at a Lua file
   that is not on disk (`a9d5c43`, `f4d3bb0`). Both patterns now match the family
   (`C4[78]`); the strays were stripped by the repaired script itself and the
   gate re-read the result off disk (TestKit `15cb9d4`).

## ⭐⭐ 2026-08-15 — THE READINGS. The farms ARE fed from the landscape, 280 seeds at a time, beside 12.4 MILLION seeds in storage — but the owner's density explanation is REFUTED as stated, and the speed ladder is UNSAMPLED because nothing stalled at any speed

**One unattended launch, owner cost zero.** Log
`archive/c48veg_Mars.exe-20260815-19.35.01.log` (912 lines, **0 `[LUA ERROR]`**,
re-counted from the archived file). Every figure below is re-emitted from that
archive; nothing is inherited from the run's own summary.

Read against the predictions committed in `94eb508`, pushed before the launch.

### The fixture, as the census found it

| | reading |
|---|---|
| clock at census | `GameTime()=275836964`, **day 384 hour 8**, speed 1x, map `BlankBigCanyonCMix_09` |
| farms | **2** (`APPLICABLE=true`) — `OpenFarm#15779` **3385**/5000, `consumption_amount=400`, tick 9000 ms · `OpenFarm#15974` **3700**/5000, `consumption_amount=200`, tick 9000 ms |
| ⭐ Seeds-accepting depots | **34**, not the two the entry assumed |
| ⭐⭐ Seeds in storage colony-wide | **12,406,609** — including two `MechanizedDepotSeeds` at **4,000,000** each and nine `StorageSeeds` at ~180,000 |
| live `VegetationTaskRequester`s on the map | **3,584** |
| run conditions | `fix pack 75/75` · `opt-in 8/8` · save-rescue absent · `Opt_AcknowledgedWarnings` ACTIVE — ⭐ but it wraps **notification add/remove only**, and this leg counts no notifications at all, so it is off the path of every reading here |

### Verdict per prediction

| # | prediction | verdict | the reading |
|---|---|---|---|
| **P1** | the complaining farm reaches **≥1.5×** as many seed offers as the silent one | ⛔⛔ **FAILED — and the pre-registered falsifier fires: the density explanation is REFUTED as stated** | A reaches **533** distinct offers at census and **545** at close; B reaches **490** and **512**. Ratio **1.09** and **1.06** — inside the ±20% band that this entry wrote down beforehand as refutation. Held in all six scans (533/490, 532/490, 498/460, 450/431, 556/520, 545/512) |
| **P1b** | map-wide count in the band **200–3,000** | ⛔ **FAILED (marginally)** | **3,584** at census, 19% above the top of the band. The band was a guess and is reported as one |
| **P2** | the depots stay flat | ✅ **HELD in aggregate, ⛔ FAILED as written per-depot** | Across 34 depots the total went **12,406,609 → 12,409,107 = +2,498, a RISE**, while the two farms consumed **45,480**. **21 of 34 depots did not move by a single unit.** But 7 moved by more than one drone trip in each direction (biggest fall −27,469, biggest rise +23,954), so the per-depot form of the prediction is wrong |
| **P3** | ⭐⭐ **≥60% of deliveries below 3,000, mode in the 200–2,600 band** | ✅✅ **HELD, and it is the finding** | **134 deliveries, 100% of them below 3,000. ZERO full drone trips. 112 of 134 = 83.6% are EXACTLY 280** — which is what a live requester's request reads: `preset=Bush`, `request actual amount=280`. Largest single delivery in the whole run: **1,519** |
| **P4** | `supply_dist_modifier`=100 and `priority`=1 on a live object | ✅ **HELD, now MEASURED not declared** | `VegetationTaskRequester#2000057731` — `supply_dist_modifier=100 (ok=true)`, `priority=1 (ok=true)` |
| **P5** | ⭐ **the owner's:** stalled-% falls monotonically 1x → 20x | ⛔ **UNSAMPLED — and it must NOT be read as refuted** | **stalled 0 of 838 samples, on BOTH farms, at every rung.** Buffer minima **905** (A) and **1681** (B) against 5000. The condition the prediction ranks never occurred at any speed, so the ladder cannot rank them. ⭐ Its companion arithmetic did read something — below |
| **P6** | four rungs complete, no freeze, nothing written | ✅ **HELD in full** | All four rungs `took=true complete=true`; span **438,702 game ms = 14 game hours**, day 384 h8 → h23, **838 samples**, `frozen at=not frozen`; real cost **168.8 s**. `C48STAGE` and `C47FARM` both MD5 `1B06B9BF…`; all three autosaves byte-identical to their pre-copies (the rotation did not fire); directory reconciled **by name** — the only new entry is our own `C48STAGE.savegame.sav` |

### ⭐⭐ What the delivery sizes actually show, and why 280 is the number that matters

`VegetationTaskRequester:CreateResourceRequests` posts one supply request for
`GetVegOutputAmount(preset, self)` (`Vegetation.lua:1848-1851`). Bush's
`output_amount` is **200** (`Data/Vegetation.lua:36`) and the soil bonus is
`(sq − min_soil_quality)/2` percent, so **280 = 200 × 1.4**, i.e. a bush standing
in soil of quality **100** — which a terraformed map is.

| | farm A `#15779` | farm B `#15974` |
|---|---|---|
| deliveries sampled at 200 ms | **71** totalling **23,765** | **63** totalling **22,921** |
| **exactly 280** | **59 (83.1%)** | **53 (84.1%)** |
| the rest | 80×1, 560×9, 725×1, 1400×1 | 80, 121, 180, 560×2, 1081, 1300, 1339, 1341, 1519 |
| **at 3,000 (a full trip on this rig)** | ⭐ **0** | ⭐ **0** |
| drained over the window | 22,920 | 22,560 |

⇒ **A drone on this rig carries 3,000. Not one delivery in the run was a full
trip, 84% were exactly one bush's yield, and the whole colony's 12.4 million
stored Seeds fell by nothing at all** — the aggregate rose. ⭐ **The 560s are
exactly 2 × 280**, consistent with two arrivals inside one sample rather than a
larger source.
⭐ **This is the number three legs failed to get**, and the 200 ms sampling is
why: at the 2-second polling of every earlier leg these crumbs merged.

⛔ **What is still NOT claimed, and the boundary has not moved:** none of this
reads a drone's decision. It shows what arrived, in what size, while storage did
not fall. **Flat storage plus 280-unit arrivals is strong circumstantial
evidence for the routing story; it is not a measurement of preference**, and the
engine's actual supply pairing is C-side (`Request_New`, `_TaskRequest.lua:154`)
and unreadable from Lua.
⚠️ **And one falsifier was tripped on a technicality that the delivery sizes then
answer.** P2's falsifier said a depot draining by ≥ half the farms' receipt would
mean the farm is fed from storage. `StationSmall#12472` fell **27,469** = 59% of
the 46,686 the farms received. But a station emptying into these farms would have
to arrive as ~9 trips of 3,000, and **zero deliveries above 1,519 were sampled**;
five other stations *rose* by up to 23,954 in the same window. ⇒ That falsifier
was written for a two-depot colony and is too loose for a 34-depot one with its
own inter-station traffic. Recorded as a defect in the prediction, not explained
away.

### ⛔ The owner's density explanation, measured — and it does not hold as stated

Their words: *"the farm that is complaining is in a much more dense area of the
map… and the drone extenders in that area give the drones a much larger area to
look for vegetation seeds."*

| | farm A (complains) | farm B (silent) | ratio |
|---|---|---|---|
| centres + working extenders serving it | **6** (`DroneHub#1078`, `DroneHubExtender#1360`, `DroneHub#1457`, `DroneHubExtender#2341`, `DroneHubExtender#2869`, `UniversalZeusRocket#1063`) | **1** (`DroneHub#8591`) | 6× |
| ⭐⭐ **distinct offers reachable** (deduplicated by handle) | **533** | **490** | **1.09×** |
| offers within a fixed 35 hexes of the farm | 142 | 127 | 1.12× |

⇒ ⛔ **The half of the sentence this leg can test is refuted: the two farms'
drones can reach almost the same number of seed offers, and the complaining farm
stands in only ~10% denser vegetation.** The extender COUNT is 6× — the coverage
in offers is not, because the extenders overlap. **A wider net over the same
plants is not more plants.**
⚠️ **One asymmetry is real and is recorded as an observation, NOT as a cause:**
those six areas contain **5,459 `TaskRequester`s of every kind** (summed, not
deduplicated) against B's **888**. That is a load difference, not a seed-density
difference, and this leg has no control for it. ⛔ **No sixth cause is offered**
— four are already refuted on `C47`, the owner has been right every time, and a
suggestion without a control is exactly what the brief forbids.

### ⭐ What the ladder DID read, and the one thing it cannot separate

Nothing stalled, so the owner's stall-frequency prediction is unsampled. The
per-game-hour columns underneath it are not:

| rung | A drained/h | A received/h | B drained/h | B received/h | samples |
|---|---|---|---|---|---|
| **1x** | 2,466 | 2,240 | 2,500 | 2,340 | 527 |
| **3x** | 2,306 | 2,520 | 2,306 | 2,173 | 176 |
| **5x** | 2,866 | 2,695 | 2,413 | 2,373 | 107 |
| ⭐ **20x** | **0** | 466 | **300** | 753 | 28 |

⇒ **Flat across 1x, 3x and 5x — and the drain collapses at 20x**, on both farms,
in the same process on the same colony. In direction that is the owner's
observation exactly: at speed the farm runs down its buffer far less per game
hour, so it would run out less often.
⛔⛔ **But this design cannot say why, and the reason is structural: the rungs are
ordered in time.** `C47` measured that a planting iteration only spends when it
finds a FREE or over-represented hex (`TerraformingBuilding.lua:943-961`), so by
the fourth rung the farms may simply have run out of hexes to fill. **Speed and
saturation are perfectly confounded by the ladder's own order**, and 28 samples
is a thin rung.
⭐ **The control that separates them, and it costs one unattended launch:** run
the same ladder **descending** — 20x → 5x → 3x → 1x — on a fresh copy of the same
save. If the collapse follows the **20x rung**, it is speed and the owner is
right; if it follows the **last** rung whatever its speed, it is hex saturation.
⇒ Proposed as a control, not as a theory.

### The log, read whole

* **`[LUA ERROR]`: 0.** Stated explicitly at zero, counted over the archived file.
* ⚠️ **Six `[Braze]` lines** at startup — `SessionStart error The server name or
  address could not be resolved`, `Failed to init`, `Failed sending launcher ev`.
  **Age: pre-existing and vanilla** — the 08-14 17:35, 08-15 15:44 and 08-15
  18:38 logs each carry **exactly six** of the same three signatures. Reported,
  not discounted: it is the publisher's analytics failing to resolve a host, it
  touches nothing this leg measures, and it is not filed because it belongs to no
  defect of ours.
* Every module of both packs logged `applied`; the two `NoHomeless` /
  `MultipleSuns` first-pass lines are the documented two-pass load, not new.

### ⚠️ Limits of this leg, stated rather than left to be discovered

* **The offer histogram is a 400-of-3,584 sample and contains only `Bush`.**
  Whether the map carries `Tree` (1,000) or `Broadleaf` (2,000) requesters is
  ⛔ **UNSAMPLED**, and the four deliveries between 1,081 and 1,519 are
  unattributed — they may be trees or merged arrivals.
* **The symptom did not reproduce**, in a fourth window on this save. Buffer
  minima 905 and 1,681; no stall at any speed. Everything above describes a farm
  that is coping.
* **Confounds unchanged:** cheats enabled, oversized terraforming speed-run
  colony, drone carry dial at **+2** (a trip is 3,000, not 1,000) — and P3 is
  stated against 3,000 for that reason.
* ⛔ **Nothing is built, nothing is proposed, and `C48` stays a CANDIDATE.**
  "Colonists sweep the landscape for free seeds" remains defensible design; what
  is now measured is that on this map they do it **instead of** touching 12.4
  million stored seeds, 280 at a time.

## Relationship to `C47`

`C47` says the Open Farm's buffer is too small for its cadence. This entry says
the supply feeding that buffer may be being routed badly. **They are compatible
and the second may be the cause of the first's symptom** — a bigger buffer would
mask a trickle-feed problem, which is exactly why `C47` must not be repaired
until this is settled. ⛔ **Do not build a `C47` fix while `C48` is open.**

## ✅✅ 2026-08-15 — TERMINAL AUDIT of chain `c47-farm`: ALL SIX VERDICTS SUSTAINED, nothing re-labelled

The audit re-counted `archive/c48veg_*` whole (912 lines, byte-verified
MD5-identical to the game's own log): the 34-depot census and close re-summed to
**12,406,609 → 12,409,107 (+2,498), 21 of 34 unmoved**, extremes −27,469
(`StationSmall#12472`) and +23,954 (`StationSmall#4792`) exactly as recorded;
the delivery distributions verbatim (A: 280×59 of 71; B: 280×53 of 63; largest
1,519; **zero at 3,000**); the six reach scans, the census density 142 vs 127,
the 5,459-vs-888 load observation, and all eight `LADDER ROW` per-game-hour
figures reproduced; all four rungs `took=true complete=true` at 105,000 game ms
each, so the ladder's game-time fairness held and P5's **UNSAMPLED** (0 stalls
in 838 samples) is the correct word, not "refuted". Routes re-derived at Src
independently of this entry's citations: `supply_dist_modifier = 100` default
(`_TaskRequest.lua:96`) with `SurfaceDeposit.lua:84 = 150` the ONLY override in
the tree (a whole-tree grep; `Elevator.lua:104` passes the field through, it
does not override it), `priority = 1` with its comment, `seed_cooldowns` /
`is_in_seed_cooldown`, and `GetVegOutputAmount` = `output_amount` × (1 +
(soil − min_soil)/2 %) ⇒ Bush 200 × 1.4 = **280** (`Vegetation.lua:1842-1851`,
`Data/Vegetation.lua:36,31`). Prediction commit `94eb508` (19:34:25) precedes
the launch (19:35:01) and carries no log. Both disclosed instrument defects
verified at their sources (the `work_radius` repair mirrors `FindTaskRequesters`,
`DroneControl.lua:315-324`; the arm script's family pattern now reads `C4[78]`
and the kit's metadata is clean at `15cb9d4`). Owner credit is on the entry
throughout; the drone-preference boundary, the defect boundary and the confound
sentence all held. ⛔ `C48` remains a CANDIDATE; the descending-ladder control
remains the named next reading.

## ⚖️ 2026-08-15 — TWO OWNER RULINGS, same conversation, recorded verbatim

1. **"I am not going to manipulate drone behavior on a bug fix mod."** ⇒ every
   BEHAVIORAL remedy for this family (the seeds-only cargo top-up "gleaner",
   and any shipped form of the distance brake) lives in the OPT-IN pack —
   recorded there in `agent/reports/DRONE_OVERHAUL_OPTIONS.md` §I+J and
   `FUTURE_IDEAS` item 7, parked under that repo's hard rule. The fix pack
   gets, at most, a data-shaped repair, and only after `C48` is ruled.
2. **"Your test is a go."** ⇒ the brake INTERVENTION leg is authorized — an
   unattended experiment on a staged copy, shipping nothing, described and
   predicted below. It exists to convert this entry's circumstantial routing
   evidence into causal evidence (or to refute the one-field brake as a fix
   shape before anyone trusts it).

## ⭐⭐ 2026-08-15 — INTERVENTION-LEG PREDICTIONS, COMMITTED BEFORE THE RUN (leg `c48-brake`)

⛔ **Written and committed before any game was launched; the commit order is the
evidence.** The readings arrive in a later commit.

**The run being predicted.** ONE unattended launch of `98_C48Brake.lua` on
`C48BRAKE.savegame.sav`, a fresh byte copy of the owner's `C47FARM`
(MD5 `1B06B9BFAE8922562F5B8B64766F8584`). At a constant **3x**: census → a
**5-game-hour BASELINE window** (200 ms sampling, per-window delivery-size
distributions and in-code depot totals) → ⛔ **the one FORCED mutation this
family has ever made**: `g_Classes.VegetationTaskRequester.supply_dist_modifier
= 150` (the developers' own scattered-source number, `SurfaceDeposit.lua:84`)
**plus a rebuild of every live requester's supply request** — required because
the modifier is baked into each request at creation (`Request_New`,
`_TaskRequest.lua:154`), executed per requester as `center:RemoveBuilding(o)` →
fresh `CreateResourceRequests()` → `center:AddBuilding(o)`, the exact
re-registration template `TaskRequester:SetPriority` uses
(`_TaskRequest.lua:157-181`) — → 0.5 game-hour settle → an identical
**5-game-hour BRAKED window** → class value restored → quit. Never saves;
budget ends ~5 game hours clear of the day-385 freeze.

### BR-P1 — ⭐⭐ the intervention takes, and the log proves it

⇒ **predicted: ≥90% of live requesters rebuilt** (bar printed in the log as
`INTERVENTION TOOK=`), the post-brake sample reads `supply_dist_modifier=150`
off live instances, and old-vs-new request amounts differ only by soil drift
(counted, small). *Falsifier:* a rebuild below the bar ⇒ the braked window is
**PARTIAL/VOID as an intervention reading** and no routing conclusion may be
drawn from it — the leg says so and stops there.

### BR-P2 — the routing discriminator, three-legged by design

If the missing brake is why drones choose the landscape, then in the braked
window against the baseline window:
* **(a) the 280-wall collapses** — the exactly-280 share of deliveries falls to
  **less than half** the baseline share (baseline expectation from `c48veg`:
  ~84%);
* **(b) bulk arrivals appear** — at least one delivery **≥ 2,000** (⚠️ named
  ambiguity: a 2,000–2,800 value can also be a Broadleaf offer at soil bonus,
  which is why (b) never carries the verdict alone);
* **(c) the depot total FALLS** — by at least half of the farms' summed receipt
  in that window, where the baseline window's total moves by less than the
  farms' receipt (the `c48veg` shape: +2,498 against 45,480 consumed).
⇒ **predicted: at least two of (a)/(b)/(c), including (a).**
*Falsifier, and it is the honest expectation on a carpeted map:* ⛔ **all three
null — 280-share within ±20% of baseline, no bulk arrival, depots flat — means
the developers' distance number does NOT redirect routing here**, i.e. distance
weighting is not what routes drones to the landscape (the C-side pairing, or
`priority = 1`, dominates) ⇒ **option J (the one-field brake) is REFUTED as a
fix shape** while C48's routing observation stands untouched. That outcome is
worth more than a confirmation and must be reported as a refutation of the
shape, never softened.

### BR-P3 — the farms keep working either way

⇒ **predicted: 0 stalled samples in BOTH windows** (this save has never stalled
unattended at ≥3x). ⛔ **The named surprise, worth more than the headline if it
fires:** stalls appearing ONLY in the braked window would mean the landscape
trickle was *feeding the farm better than the depots can* — which would flip
this entry's reading of the trickle from waste to load-bearing, and would kill
both fix shapes at once.

### BR-P4 — fairness across the ordered windows

⇒ **predicted: farm drain per game hour in the two windows within ±40%** of
each other (drain is demand-side and should not care about routing). A larger
divergence means the windows are not comparable (hex-saturation drift or a
freeze) and the comparison is reported as CONFOUNDED, not concluded.

### BR-P5 — nothing persists

⇒ **predicted: `C48BRAKE` MD5 identical before and after; `C47FARM` untouched;
all three autosaves byte-identical to their pre-copies or restored from them
(`EF-056` — they fire unpredictably); save directory reconciles BY NAME with
the staged copy the only new entry; 0 `[LUA ERROR]` attributable to this
payload; the class value reads restored at close.**

### ⚠️ The confounds, named before the numbers exist

Cheats enabled · oversized terraforming speed-run colony · drone carry dial at
+2 (a trip is 3,000) · one save, one map, one speed · ⛔ **windows ordered in
time, baseline first** — accepted deliberately (a two-launch A/B carries every
cross-run confound instead), bounded by BR-P4, and the settle gap absorbs the
rebuild's one-time drone idling (`DroneControl.lua:720-728`).
⛔ **What this leg cannot claim even if everything fires:** the drone's DECISION
(C-side, unread), any player-facing symptom relief (nothing stalls unattended
at this speed anyway), and any ship-worthiness call — where a repair lives and
whether one ships is the owner's, and rulings 1–2 above already frame it.

## ⭐⭐ 2026-08-15 — THE INTERVENTION READINGS. The brake TOOK, fully and provably — AND THE ROUTING DID NOT MOVE. The pre-registered falsifier fires: the developers' own distance number does NOT redirect seed routing on this map, and option J is REFUTED as a fix shape

**One unattended launch, owner cost zero, 0 `[LUA ERROR]`.** Log
`archive/c48brake_Mars.exe-20260815-22.27.30.log`, byte-verified identical to
the game's own (MD5 `FF7BD162…`). Predictions committed and pushed first in
`d385ae2` (22:0x), launch 22:27. Every figure below re-emitted from the archive.

### The fixture and the intervention, as the log proves them

Census at day 384 h8, speed 3x taken: 2 farms (`#15779` 3,665/5,000 ·
`#15974` 2,600/5,000), 34 Seeds-accepting depots totalling **12,400,609**,
**3,580** live `VegetationTaskRequester`s.
⭐⭐ **The rebuild was TOTAL: 3,390 of 3,390 live requesters rebuilt, 0 failed,
0 skipped, request identity changed on every one**, live instances read
`supply_dist_modifier=150` in every post-brake scan, and the class value reads
RESTORED to 100 at close. ⚠️ Disclosed side effect, counted not absorbed: 134
rebuilt requests re-derived their amount from current soil (net **+55,364
seeds** created across 3,390 offers, ~0.13%/offer mean) — on a staged copy that
was never saved.

### Verdict per prediction

| # | prediction | verdict | the reading |
|---|---|---|---|
| **BR-P1** | intervention takes, ≥90% rebuilt, proven in-log | ✅✅ **HELD, at 100%** | `INTERVENTION TOOK=true`, 3,390/3,390, live reads 150, restored at close |
| **BR-P2** | routing moves: ≥2 of (a) wall collapses / (b) bulk ≥2,000 appears / (c) depots fall, including (a) | ⛔⛔ **FAILED — THE PRE-REGISTERED FALSIFIER FIRES** | **(a) NULL**: exactly-280 share A **86% → 88%**, B **83% → 68%** — neither approaches the required halving. **(b) NULL: zero deliveries ≥2,000 in either window, either farm** (braked max: A 560, B 1,539). **(c) letter-triggered, mechanism-contradicted**: braked depot total fell 13,658 ≥ half the farms' 17,520 receipt — but not one bulk delivery reached a farm, and single-depot inter-station moves of ±23,696–37,093 dwarf the farms' receipt in BOTH windows, so totals cannot attribute on this colony. ⚠️ Same falsifier-too-loose-for-34-depots defect `c48veg`'s P2 already recorded; recorded again as a prediction defect, not as evidence |
| **BR-P3** | 0 stalls in both windows | ✅ **HELD** | 0 of 251 samples, both farms, both windows — and no braked-window regression, so the trickle is not load-bearing at 3x either |
| **BR-P4** | drain/game-hour within ±40% across windows | ⚠️ **MARGINAL BREACH on farm A** | A 1,924 → 1,120 (−41.8%, just past the bar — hex-saturation drift, the known mechanism); B 1,424 → 1,940 (+36%, within). ⇒ by the pre-registered rule A's window pair is CONFOUNDED. ⭐ **The refutation survives its own fairness discipline**: it is carried by farm B's clean pair (wall 83%→68%, no bulk) and by the size-shape evidence on both farms, which drain volume does not alter |
| **BR-P5** | nothing persists | ✅✅ **HELD in full** | `C48BRAKE` and `C47FARM` both MD5 `1B06B9BF…` after; all three autosaves byte-identical to their pre-copies (rotation did not fire); directory reconciled BY NAME — the staged copy is the only new entry; kit disarmed clean, wave-11 probe intact |

### ⇒ What this settles, and what it re-opens

* ⛔⛔ **Option J — the one-field `supply_dist_modifier = 150` brake — is DEAD as
  a fix shape for this symptom.** Applied fully and provably, it changed
  nothing: the 280-wall stands, no bulk trip appeared, the farms went on eating
  the landscape. On a carpeted map the offers are simply too close for a
  distance multiplier to matter — the honest expectation this leg wrote down
  first, now measured.
* ⭐ **The routing cause moves to the two things still unread**: `priority = 1`
  (how the C-side pairing weighs request priority against distance is unknown)
  and the pairing itself (`Request_FindSupply`, C-side). ⛔ No new theory is
  offered without a control — the standing rule, and the owner has been right
  every time.
* ✅ **What survives untouched:** C48's measured routing observation (three
  independent windows now show the 280-crumb signature), the gleaner (opt-in
  §I) — which attacks trip EFFICIENCY rather than source choice and is
  strengthened by this result, since it works regardless of why drones pick the
  landscape — and the `C47`-blocking rule, unchanged.
* ⭐ Incidental confirmation: requester churn 3,580 → 3,390 → 3,387 → 3,702
  across 10.5 game hours — the class-level-change-propagates-fast claim
  recorded in the opt-in §J notes is real, for whatever future knob needs it.
* ⚠️ Confounds unchanged and named: cheats, oversized speed-run colony, carry
  dial +2, one save, one speed, time-ordered windows.
