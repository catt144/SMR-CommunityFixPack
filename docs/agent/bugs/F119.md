---
id: "F119"
seq: 180
row: 215
title: "A landed Earth-sent trade rocket never re-sizes its fuel request when a fuel-cost modifier changes, so the Wildfire cure rocket can sit on the pad forever with surplus fuel nothing unloads (or a shortfall no drone fills)"
status: "tested-attended"
status_source: "tag"
priority: "P1"
evidence: "source-read; two field reports (r/SurvivingMars, 1.1.0, PC + PS5); attended A/B + load-heal in play 2026-09-11"
row_status: "tested-attended 2026-09-11 — Fix_TradeRocketFuelRefresh: fix-off leg reproduced the stuck rocket on the real Advanced Martian Engines trigger; the load heal refreshed it and it left; fix-on leg re-sized at once and it left; 0 mod errors"
updated: "2026-09-11"
copies: []
---
### F119 — a landed trade rocket keeps its landing-time fuel request; a fuel-cost change on the pad soft-locks it (P1) `[tested-attended 2026-09-11]`

- ✅ **STATUS: REPRODUCED AND REPAIRED IN PLAY 2026-09-11, owner at the keyboard (§Attended check).** The sections from
  "Field reports" to "Fix sketch" are the original desk read; the in-game reproduction followed its route.
- Triage record for all six leads of the day: `reports/FIELD_LEADS_2026-09-11.md`.

#### Field reports (INHERITED — player words, owner screenshots, r/SurvivingMars "Game-breaking bug for the Wildfire mystery on the new version", 2026-09-10/11)

- OP (PS5, Relaunched, Brazil, Wildfire): the cure shipped 2–3 times, then the latest Earth cargo rocket "refuses to
  leave the landing pad. All the crops are on board, but it says there's 20 fuel to unload." Nothing removes it; it is
  not the player's rocket. The OP has Advanced Martian Engines researched.
- A second player (PC, Brazil, Wildfire): "stuck with 20/30 required fuel and full of curetatoes"; manual drone control
  and an RC Transport both fail; drones still recharge batteries at it.

#### The mechanism (SOURCE, game 1.1.0.403908 unless marked)

1. The cure rocket is a `UniversalTradeRocket` with `RocketType = g_RocketTypes.Trade`
   (`Lua/Sequences/SA_Gameplay.lua:2851-2861`), spawned by Mystery 8 with `rocket_id = "CureRocket"`, 60 Wildfire Cure and
   no `fuel_amount` (`Lua/Scenario/Mystery 8.generated.lua:824-831`), so its launch fuel is the template's 50
   (`Data/BuildingTemplate/UniversalTradeRocket.lua:4`, `50000`) plus any `AllRockets` label modifiers.
2. On landing, the `our_colony` policy's Trade branch swaps the flight data and starts `CmdLoad`
   (`Data/FlightPolicyDef.lua:561-568`) → `SetCargoRequest` → `CargoTransporterNew:UpdateCargoResourceRequests`
   (`Lua/CargoTransporterNew.lua:1484`). That sizes the fuel requests ONCE: `requested + GetFuelResourceRequest() − amount`
   goes into the demand request if positive, the supply request if negative (`:1442-1458`).
3. `GetFuelResourceRequest` returns the LIVE `FuelResourceAmount` (`Lua/UniversalRocket.lua:1891-1908`). After the swap
   the arrival is Earth, and the `earth` policy has no `OnGetFuelResourceRequest`: overrides exist only for anomaly
   (`FlightPolicyDef.lua:44`), asteroid (`:247`), our_colony (`:596`) and rival (`:829`), and the policy is chosen by
   arrival type (`Lua/ClassDefs/ClassDef-Default.generated.lua:89-97`).
4. The ONLY re-size on a fuel-cost change is gated on player control: `OnModifiableValueChanged` calls
   `UpdateCargoResourceRequests` only `if prop == "FuelResourceAmount" and self:IsPlayerControlled() and self.cargo`
   (`UniversalRocket.lua:1916-1920`), and `IsPlayerControlled` excludes `Trade` (`:2631-2638`). Every other caller is
   one-shot or a button (`:580`, `:774`, `:2619` `AppendCargo`, `:3322` `ToggleRefuel`, `:3351` `ToggleReturnFuel`).
   `ToggleRefuel` is offered only on player-controlled rockets (`:3328`), and the trade rocket's own panel
   (`Lua/XDef/customUniversalTradeRocket.generated.lua`) has no buttons (0 `OnPress` / `func =` / `SetVisible` hits).
5. Readiness reads the LIVE requirement: `GetCargoResourcesStatus` returns `"unloading"` when fuel aboard exceeds
   `requested + GetFuelResourceRequest()` and `"loading"` when below (`CargoTransporterNew.lua:1288-1306`); `IsCargoReady`
   needs `"ready"` (`UniversalRocket.lua:558`) and `CmdLoad` spins on it (`:500-502`).

**ROUTE (INFERRED from 1–5):** a fuel-cost DROP while the rocket is landed leaves surplus aboard → status `"unloading"`,
but the supply request was sized at 0 → nothing ever unloads → no launch. A RISE leaves a shortfall → `"loading"` with
the demand already 0 → no drone brings fuel → no launch. Either way it waits forever. The panel lists the surplus as cargo
to unload (INHERITED: `XDef/customUniversalRocket.generated.lua:319-333`, investigator read).

#### What moves the fuel cost while a rocket sits on the pad (SOURCE, per row)

| trigger | change | where (1.1.0) | on 1.0.7? |
|---|---|---|---|
| Advanced Martian Engines researched | −20 | `Data/Tech.lua:8242-8246` | SOURCE yes (`Data/TechPreset.lua:2168-2171`) |
| Fuel Conservation law (`Policy_RocketSpeed_Efficient`) enacted / repealed | −5 | `Data/LawDef/LawDef-Technology.lua:791-798` | SOURCE **no** |
| the same law, while the Ministry of Technology works | −5 more | `:799-807` | SOURCE **no** |
| the fast-rocket law (the LawDef at `:911`) enacted / repealed | +10 | `:930-937` | SOURCE **no** |
| JunkInSpace story bits | varies | `Data/StoryBit/JunkInSpace_*.lua` | SOURCE yes |

- SOURCE: `MinistryBase:UpdateWorking` calls `ReapplyLawEffects()` on every working-state change
  (`Lua/Factions/FactionsBuildings.lua:383-388`), and `LawEffectModifyLabelMinistryWorking:OnReapply` swaps the modifier
  when the ministry's working flag differs (`Lua/Factions/LawDef.lua:637-643`).
- INFERRED: under Fuel Conservation, **every** Ministry of Technology stop or restart while a trade rocket is landed
  re-arms this — the one trigger in the table that repeats without the player choosing it.
- INFERRED fit: the reported "20 fuel to unload" is exactly Advanced Martian Engines (50 → 30) finishing while the rocket
  waited; the OP has it researched. Research finishing mid-flight should be harmless — the rocket is in `AllRockets` from
  spawn (INHERITED: `CityObject.lua:31-32`, `UniversalRocket.lua:1240-1241`), so landing sizes the request fresh.
- The 1.0.7 column was SAMPLED: the same `FuelResourceAmount` grep over the 1.0.7 `Data/` returned TechPreset, sponsor and
  StoryBit hits and no LawDef hit.

#### Consequence (SOURCE + INFERRED)

- SOURCE: Mystery 8 loops `while _InfectionLevel > 0 do` → `SA_CallTradeRocketWithCargo` (the CureRocket) → wait on
  `GetTradeRocket("CureRocket", "CmdOnEarth")` → `_InfectionLevel − 20` (`Mystery 8.generated.lua:822-836`). No timeout.
- INFERRED (`GetTradeRocket`'s body not read): a rocket that never takes off stalls the loop — Infection never falls, no
  epilogue or wonder tech, the pad stays occupied, the cure and fuel stay aboard. **A permanent mystery soft-lock: a
  player LOSS.**

#### Both trees

- SOURCE: the gate is identical on 1.0.7 (`Lua/UniversalRocket.lua:1658-1662`). The defect predates 1.1.0; 1.1.0 adds
  three new triggers (the laws). INFERRED: that is why reports appear after the update.

#### Reach (INFERRED, partly unverified)

- Every `Trade` rocket (`SA_CallTradeRocketWithCargo`; the effect route `Data/ClassDef-Effects.lua:~210-230`), not only
  Wildfire's.
- `TradePad` and `Rival` rockets are also outside `IsPlayerControlled` and inside `IsSpecialAutomode`
  (`UniversalRocket.lua:2020-2026`), but their fuel path was NOT traced. ⛔ Scope unverified.

#### The pack (SOURCE)

- Neither causes nor cures it. `Fix_RocketDroneChurn` replaces `CargoTransporterNew:UpdateCargoResourceRequests` with a
  copy of the same sizing and adds no refresh trigger. `Fix_LanderEmptyLaunch` only turns "ready" into "not ready", and
  only for player-controlled, non-special-automode rockets (`Code/Fix_LanderEmptyLaunch.lua:69-82`) — never a Trade
  rocket. No module wraps `OnModifiableValueChanged`.
- INHERITED: `Fix_RocketInteractGuard` (F74) refuses RC Transport interaction with trade rockets, so with the pack an RC
  attempt is refused outright; vanilla would find no open request anyway.

#### Player recovery today (route check)

- ⛔ No in-game route (SOURCE: the trade rocket's panel has no buttons; `ToggleRefuel` is player-rocket-only). PS5: none.
- Console re-size, rocket selected: `SelectedObj:UpdateCargoResourceRequests()` `[NEVER RUN]` — INFERRED from
  `CargoTransporterNew.lua:1454-1458`; whether a Relaunched player can open a console at all is unchecked.
- Prevention (INFERRED, not route-checked): an earlier save from before the rocket landed, then no Advanced Martian
  Engines completion and no Fuel Conservation change while a cure rocket is on the pad.

#### Fix sketch (PROPOSED at filing; implemented below — checklist 146)

- Post-wrap `UniversalRocketBase:OnModifiableValueChanged`: when `prop == "FuelResourceAmount"`, the rocket is landed
  with `self.cargo`, and it is NOT player-controlled, call `self:UpdateCargoResourceRequests()` — the game's own method,
  the same call the player branch already makes.
- A load-time heal for saves already stuck: each landed `Trade` rocket in `CmdLoad` whose fuel status is not ready gets
  one `UpdateCargoResourceRequests()`.
- `FIX_POLICY` §2a behaviour guard; §3a save-safety (calls a vanilla method, stores nothing). A Beta label until
  attended (the fixtoggles chain asked, 2026-09-11).

#### The repair (BUILT 2026-09-11 — desk-verified, NOT run in game)

- `Code/Fix_TradeRocketFuelRefresh.lua` post-wraps the declaring
  `UniversalRocketBase:OnModifiableValueChanged`. It delegates first and then,
  only for the exact shipped `UniversalTradeRocket` class in `CmdLoad`, calls
  vanilla `UpdateCargoResourceRequests()` after a `FuelResourceAmount` change.
  Both creation routes instantiate that class with `RocketType = Trade`
  (`SA_Gameplay.lua:2851-2861`; `ClassDef-Effects.lua:215-226`). `CmdLoad` is
  already the narrow landed/loading predicate: the Trade landing branch enters
  it after `SetFlightData` (`FlightPolicyDef.lua:561-568`), and
  `IsRocketLanded()` itself returns true for `CmdLoad`
  (`UniversalRocket.lua:1574-1577`). Separate `arrival_loc` and landed tests
  would be redundant.
- The load-time heal walks exact `UniversalTradeRocket` objects once on
  `LoadGame`. It refreshes only a rocket in `CmdLoad` whose fuel aboard differs
  from requested cargo plus the live trip-fuel requirement. A healthy rocket is
  a no-op. The shipped updater uses `SetAmount` to overwrite supply/demand
  targets (`CargoTransporterNew.lua:1454-1458`); it grants no resource and
  cannot double-grant anything.
- Inheritor/dispatch gate: the shipped tree has seven direct descendants
  (`UniversalRocket`, `UniversalTradeRocket`,
  `UniversalRefugeeRocket`, `UniversalLanderRocket`, `UniversalZeusRocket`,
  `UniversalDragonRocket`, `UniversalSupplyPodBase`) plus the latter's three
  leaves (`UniversalSupplyPod`, `UniversalDropPod`, `UniversalArkPod`); none
  redeclares `OnModifiableValueChanged`. The modifiable-property machinery marks
  this callback recursive and says parent implementations run first
  (`Lua/Modifiers.lua:1,16-18,82-85`), so the base wrapper is reached.
- The §2a guard is the defect itself: the shipped callback is invoked on a
  closed non-player stub and must make zero refresh calls. It is synchronous and
  side-effect-free on that stub. The defect/seam are identical on 1.0.7
  (`UniversalRocket.lua:1658-1662`), and the wrapper carries no copied
  branch-specific body, so applying there is branch-neutral rather than a
  1.1.0 body crossing branches.
- Desk control: `python tools/desk_f119_trade_fuel.py` loads six shipped bodies
  under their real paths/line offsets and the whole module through its registry
  seam. **11/11 demands held**: vanilla DROP stayed `unloading` with supply 0;
  module DROP requested 20,000 supply and reached `ready` after simulated
  unloading; vanilla RISE stayed `loading` with demand 0; module RISE requested
  10,000 demand; the pre-stuck heal fired once, the healthy heal zero times, and
  a player rocket retained one vanilla refresh. This is a desk result, not an
  in-game reproduction or proof that a real save heals.
- Built in `2c68bb1`. Attended check: below.

#### Attended check (2026-09-11, checklist 149 — owner at the keyboard)

Log `docs/archive/logs/f119sitting110_Mars.exe-20260911-14.02.05-6a91a190.log` (line numbers below; Lua uptime 2:28:59 at the
last reading). Game 1.1.0.403908; fix pack (v7 tree + these two modules), TestKit, opt-in pack and the FR-1 temp mod loaded. A
throwaway Terra Initiative colony (BlankBig_04, no mystery); setup cheats (Fill, drones, quick build) on depots only, never on a
rocket. PROBE SWEEP clean (0 hits).

- **Boot:** `TradeRocketFuelRefresh: applied` (:87), `ScanDowngrade: applied` (:119); 47 applied / 1 inactive (`SaintBlessing`,
  the ck130 disposition); **0 Lua errors from any mod** across the session.
- **Route:** the mystery's own spawn step, run from the console with no mystery active —
  `CreateGameTimeThread(function() SA_CallTradeRocketWithCargo:SARun{rocket_id=…, cargo=PlaceObj("ResourceCargoList", {Food=5000})} end)`
  (`SA_Gameplay.lua:2831-2868`; `SARun` needs a thread for its `Sleep(1)`). Food replaced the Cure so the rocket could finish.
  It landed at an ordinary pad (the Trade branch, `FlightPolicyDef.lua:561-568`). Trigger: the field trigger itself,
  `UIColony:SetTechResearched("AdvancedMartianEngines")` (force mode skips prerequisites and cost, `TechTree.lua:1200-1228`),
  with `AMECHK false` read first.
- **Timing (measured on two rockets):** a Trade rocket launches the instant its cargo reads ready — the take-off hold is
  player-only (`UniversalRocket.lua:507`, `EF-091`). The trigger must land while it is still fuelling; the first rocket left
  before it could be used.
- **A — fix off for one rocket:** a per-object `rawset` of `OnModifiableValueChanged` that returns for `FuelResourceAmount` and
  delegates every other prop (only `UniversalRocketBase` reacts to that prop in the parent chain; `F119 OFF true AMECHK false`,
  :676). Tech granted at fuel 41: `fuel 41000 need 30000 supply 0 demand 5000` (:678); once drones finished,
  `fuel 50000 need 30000 supply 0 demand 0` (:680): 20 over, no unload request, it did not leave. **The reported bug,
  reproduced.**
- **Heal:** the override removed before saving (`F119 ON true cmd CmdLoad fuel 50000`, :682); saved under a new name and
  loaded: `TradeRocketFuelRefresh: refreshed 1 landed trade rocket fuel request(s) after load` (:696), then
  `fuel 50000 need 30000 supply 20000 demand 0` (:725). The unmodded load path does not re-size (its only rocket `LoadGame`
  handler updates a notification, `UniversalRocket.lua:2131-2135`), so the refresh is the heal's. **The owner watched drones
  unload the 20 and the rocket leave.**
- **B — fix on:** a pre-grant save reloaded, a fresh rocket, `override false AMECHK false` (:771); tech granted at fuel 40:
  `fuel 40000 need 30000 supply 10000 demand -10000` (:773). **The owner watched drones remove the excess and the rocket leave.**
- ⚠️ **Prediction missed:** demand −10000, predicted 0. Both updaters clamp with `Max(0, …)` (`CargoTransporterNew.lua:1458`;
  our copy `Fix_RocketDroneChurn.lua:140`), so the read is the target net of fuel already claimed by drones in flight
  (INFERRED; A's 5000 against a 9000 gap fits the same reading). Three shipped guards stop an over-delivery (`EF-091`); the
  watched outcome agrees.
- Console-only errors (:670-672): my identity line run with nothing, then a non-rocket, selected — not a game or mod fault.
- ⛔ **Not covered:** the Wildfire mystery's own loop; a player's real stuck save; a fuel-cost RISE (the fast-rocket law);
  Fuel Conservation and Ministry of Technology flips.

#### Control (`[RAN 2026-09-11 in a stronger shape — see Attended check]`)

- Desk: load the shipped bodies under lupa (the `tools/desk_*` harness shape), land a Trade rocket, drop
  `FuelResourceAmount` by 20, read the status and the supply/demand amounts; then again with the wrap.
- In game: a Wildfire colony with the CureRocket landed; research Advanced Martian Engines while it waits → expect the
  surplus to show as fuel to unload and no take-off. ⛔ The fixture is heavy (`EF-079`: a fresh 1.1.0 colony, and the
  mystery must be running).

#### Not opened

`GetTradeRocket`'s body; `PlaceAndFlyRocketTo`; the TradePad/Rival fuel paths; the RC Transport transfer path; the
"cargo to unload" panel lines (INHERITED); the players' saves and mod lists.
