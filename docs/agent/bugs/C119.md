---
id: "C119"
seq: 224
row: 259
title: "Stale power-connection cells outlive the objects that wrote them, so every cable or building placed beside one throws while connecting, is left with no grid, and when removed leaves its own cells behind: an area becomes permanently unpowerable and the dead zone spreads with each attempt; the cells are saved, so reloading cannot help"
status: "cand"
status_source: "tag"
priority: "P2"
evidence: "MEASURED 2026-09-26 on the reporter's save (KKAG-4870, 'Europe Sol 280', started and saved on 1.1.0.403908, active_mods = {}), loaded on retail 1.1.1.405907 with the pack on; log archived docs/archive/logs/kkag4870_Mars.exe-20260926-10.33.35-6aad2d75.log. Fresh load, nothing built: 355 of 1094 electricity connection cells have no electricity object on their hex (739 matched), 2 of 216 water cells; all within q 296-322 x r 198-221 around a removed dome (lines 1237, 1246, 2363). Every cable, generator, habitat and dome placed there threw in SupplyGridConnectElement (SupplyGrid.lua:1820/1829/1872/1880/1890, 1.1.1), across two loads and a third session (98 errors from cable sites alone). Controls: two of the owner's colonies scanned 0 ghosts (4854 and 1211 power cells, all matched, lines 1441, 1917); refabbing a DomeHexa after removing its passage removed exactly its 48-cell shape from both grids with 0 ghosts (lines 2098-2130). SOURCE archived 1.1.1.405907 and 1.1.0.403908, identical here: cells are written before anything can throw (1.1.1 SupplyGrid.lua:1788), removal returns before SupplyGridRemoveBuilding for an element with no grid (:2089-2092 vs :2110), and the grid is a saved MapVar with no load rebuild (SupplyConnectionGrid.lua:2-4). Origin of the first stale cells NOT established; no pre-removal save exists. Vanilla."
row_status: "cand 2026-09-26 - Reddit field report + site form KKAG-4870 with save; measured on the save; owner-ruled fix shape v1 (load-time power-cell cleanup)"
updated: "2026-09-26"
copies: []
---
### C119 — Stale power-connection cells make an area unpowerable, and the dead zone spreads (P2) `[cand 2026-09-26: cleanup, clean save and topology witnessed; powered cable check open]`

#### Must_Read_Header

Candidate with an owner-ruled fix shape (v1, below), built and desk checked. Retail
loads witnessed cleanup, a clean-save control, powered topology, and the Excavator
and Open Farm shape readings. The newly built reporter cables have grids but did
not join a grid with producers; that power-path check remains open. The spreading
and the persistence are measured on the reporter's save and read in source.
What planted the first stale cells is NOT established, and the fix does not depend
on it. The new module repairs saved
electricity connection cells at load and then retries gridless power objects.
Line numbers without a build are 1.1.1.405907; 1.1.0.403908 is identical for every function cited
(SupplyGrid.lua lines shift by -114).

#### Report

Reddit, 2026-09-25, "Help with this bug": "There was a dome. I destroyed it and now i have this
problem. Anything i build near it or on top it will not get any power. Power cable will break
itself on multiple segments. Building dome again wont do anything. Clearing ground wont help.
Save/Load dose nothing." The owner contacted the reporter, who filed the site form (code
KKAG-4870, "deleted dome", mods "Not sure") and uploaded the save, now at
`saves/game/KKAG-4870_Europe Sol 280.savegame.sav`. The save header reads
`orig_lua_revision = 403908`, `lua_revision = 403908`, `active_mods = {}`: a 1.1.0 colony with no
mods. The reporter has no save from before the dome was removed: no manual save, and the
autosaves were overwritten while they tried to fix it (owner, 2026-09-26).

The reporter's screenshot showed a yellow dome-outline ring during placement. The owner could not
reproduce it on the save (a new dome builds there), so it is not part of this entry.

#### Mechanism

- **The cells.** `map.supply_connection_grid.electricity` holds one value per hex: low 6 bits are
  links, bits 8-13 are directions the hex can connect to, 128 marks a connector, 32768 a supply
  tunnel end (`GridObject.lua:230-235`; `GridTunnelConnector.lua:1`).
- **The throw.** `SupplyGridConnectElement` writes the new object's cells first (`:1788`). The
  engine's `SupplyGridApplyBuilding` then returns neighbour pairs from those cells. For each pair
  the Lua fetches the object at the neighbour hex (`:1819`, `:1871`) and indexes it. A stale cell
  has no object there: `adjacent` is nil (`:1820`, `:1872`). A neighbour that itself failed earlier
  has `element.grid == false` (`:1829`, `:1880`, `:1890`).
- **Left with no grid.** The throw leaves the element outside any grid, with its cells still
  written. The building gets no power, and cable lines break into separate pieces.
- **The spread.** Removing such an object calls `SupplyGridDisconnectElement`, which returns at
  `if not grid then return` (`:2089-2092`), before `SupplyGridRemoveBuilding` (`:2110`). Its cells
  become new stale cells. Every failed attempt that the player removes widens the zone.
- **Persistence.** `MapVar("supply_connection_grid", ...)` has no don't-save flag
  (`SupplyConnectionGrid.lua:2-4`), and no load hook rebuilds it. The only rebuild is a vendor
  one-shot fixup, pre-marked as applied on new 1.1.x colonies (`CommonLua/SavegameFixup.lua:10-16`).
- **Angle dependence (owner-observed; the pairing step is INFERRED).** A cable placed in a line
  first connects only along the line's own axis (`dir % 3 == connect_dir`, `:1818`), then to other
  neighbours only where the cells offer a pairing. Lines at some angles avoid every stale cell and
  build cleanly; others break.

#### Measured on the reporter's save (2026-09-26)

All figures come from the archived log above. The scan counted nonzero cells and looked up
`HexGridGetObject(object_hex_grid, q, r, nil, nil, function(o) return o[res] end)` on each.

| Reading | Result | Log line |
|---|---|---|
| Fresh load, electricity | 1094 cells: 739 matched, 355 stale, 0 tunnel-only | 1237 |
| Fresh load, water | 216 cells: 214 matched, 2 stale | 1246 |
| Stale power cells by value | 267 "can connect all six ways" (cable-type), 87 multi-hex-edge (ring-type), 1 other, within q 296-322 x r 198-221 | 2363 |
| Errors on building there | in the first session and again after a reload; every stack is `SetupElectricityElement` → `SupplyGridConnectElement` | 263-551, 669-979 |
| Owner's mapping session | 98 errors from `CableConstructionSite` alone: 44 at `:1820`, 44 at `:1829`, 1 at `:1872`, 1 at `:1880`, 8 at `:1890` | after 2342 |

The stale cells cover the removed dome's footprint and reach beyond it. The owner's overlay
screenshots show them as parallel runs of isolated cable nodes, consistent with the reporter's
repeated cable attempts. 267 + 87 + 1 = 355.

#### Controls (owner, 2026-09-26, retail 1.1.1.405907, pack on)

- **Clean colonies read clean.** Two of the owner's saves: 4854 power and 3855 water cells, then
  1211 and 547, every cell matched, 0 stale (lines 1441-1447, 1917-1923). The scan does not raise
  false alarms on ordinary play.
- **Ordinary removal is clean.** A DomeDiamond with 4 passages removed first, then refabbed: 0
  stale. A DomeHexa (`SHAPE` 48, line 2098): passage salvaged, then `CheatQuickRefab`. Power went
  1211 → 1163 cells and water 547 → 499, each exactly 48, with 0 stale (lines 2106-2130).
- **A dome cannot be refabbed with a passage attached.** The passage counts as a building inside
  the dome (`WHY 1 PassageGridElement(8098)`, line 2066), so `IsRefabable` is false
  (`Building.lua:3753-3756`). Domes show no Salvage button in this state; Refab is the removal
  players use.

#### Origin (open)

A plain dome removal cannot plant the cells, and the remove-passage-then-refab order is clean.
Candidates, none confirmed:

- **The dome's own power connection had already failed.** Power and water connect in separate
  protected `GameInit` calls (`CommonLua/Core/classes.lua:243`). If power threw, the dome kept its
  outline cells with no grid, and its later removal left them while water cleared normally. This
  needs an earlier stale cell beside it.
- **A passage restoring the dome's original cells.** `process_hacked_tile`
  (`GridTunnelConnector.lua:243-263`, 1.1.0) writes back a passage-edited hex's original value
  unless the owning dome is invalid or being destructed. No UI order that reaches this was found:
  refab is blocked while a passage is attached, and a dome goes straight to deletion.

The end state cannot separate these: 280 sols of attempts sit on the seed, and no earlier save
exists.

#### Harm

The player loses the area for anything powered: new buildings there get no power, cables break,
and each removed attempt widens the zone. Reloading does not help, and rebuilding a dome does not
cover it. The player sees no error, so it reads as "cables are glitchy here". Workarounds are
building elsewhere or cable angles that happen to avoid the stale cells.

**Game phase:** mid-to-late colonies. Refab needs the PrefabRefab tech, and rearranging domes is a
late-game act. Established colony with headroom; nothing early-game.

#### Reach

Rare in the field: this is the first report. The trigger for the seed is unknown, but spreading and
persistence apply to any stale power cell, whatever planted it.

#### Control

- **Refutes the mechanism:** on the reporter's save with the fix off, a fresh load showing 0 stale
  power cells, or a cable beside a listed stale cell connecting without an error.
- **Refutes the fix:** the reporter's save with the fix on still showing stale cells after load, or
  a new cable there still throwing; any clean save showing a nonzero change count.

#### Owner ruling on the fix shape (2026-09-26)

The owner, verbatim: "Should we just have a fix that only acts on load that does a basic ghost
cleanup. It would solve any on going or new problems of that shape, And likely wouldn't harm
anyone that doesn't have an issue?" Then: "We could also start small, only shape it for ones we have
witnessed and add to it as we witness and confirm the shape. Right now we have witnessed this on
tracks and electricle lines now, I don't recall witnessing it on anything else". The owner
approved filing with this shape and a build brief: "Yes to both".

**v1, as agreed:**

1. On load only, electricity connection grid only, on every loaded map.
2. Zero a cell only if it is nonzero, lacks the tunnel bit (32768), and no object with
   `.electricity` stands on the hex.
3. Rerun the connect for power objects left with no grid, counted across every electricity class.
4. One log line with counts: cleared, tunnel cells skipped, reconnected. A healthy save changes
   nothing.
5. Water is left alone. Its 2 stale cells were witnessed without observed harm.
6. Track leftovers stay with the existing track sweep (`Fix_TrackSalvageWipe`). The owner noted
   "we already sweep those".

**Signature checks behind step 2** (source, 1.1.1):

- Tracks mark only their two end hexes with the tunnel bit, and are not supply objects themselves
  (`Track.lua:113`; `GridTunnelConnector.lua:19-30`).
- Tunnels do the same on their registered hex (`Tunnel.lua:107-121`).
- Elevators pass power by merging grids with their twin on the other map (`Elevator.lua:499-532`),
  not through cells; each half owns its own cells.
- Passages and the dome hexes they edit sit under objects with `.electricity`.

**One check before shipping:** buildings whose connection shape may reach past their model:
Excavator, terraforming buildings, Regolith Extractor, Open City. Their cells must read as matched
in a save that has them. Any that do not are added to the claimed set from their own
`GetSupplyGridConnectionShapePoints`, per the owner's rule of adding shapes as they are witnessed.

#### Build and desk controls (`77c0fad`, timer correction `c79ddc6`, 2026-09-26)

`Code/Fix_GhostPowerCells.lua` registers `GhostPowerCells` and runs synchronously on
`PostLoadGame`, after shipped save fixups. It scans `LoadedMaps` and writes only the
electricity connection grid. A nonzero cell is retained if it carries bit 32768, if
`HexGridGetObject(..., function(o) return o.electricity end)` finds a power object,
or if it is in a live Excavator or Open Farm electricity connection shape. The
last two claims are conservative until their actual entity geometry is read in
game. After each successful map scan, the pass retries saved gridless power
elements through the declaring `SupplyGridObject` connect method, without
creating replacement elements. An unfinished passage is skipped; a completed
passage is retried through the vendor's power-only base call only when its tunnel,
shape and water grid are intact. A malformed built passage is counted as a retry
failure and left for diagnosis. Each retry is protected by `pcall`; an engine
failure can still leave partial native connections or duplicate visuals. No mod
fields or threads are persisted. The log has one count line, including elapsed
milliseconds and failures. The per-fix veto is checked on every invocation.

**SOURCE, archived build 1.1.1.405907:** `GridObject.lua:108-116` registers
`GetShapePoints` in the object grid, while `SupplyGrid.lua:1787-1788` writes
`GetSupplyGridConnectionShapePoints` to the connection grid. The Regolith
Extractor's power shape is a subset of its registered shape
(`Buildings/RegolithExtractor.lua:193-212`). Open City uses its computed
registered outline (`Buildings/OpenCity.lua:57-110,208-213`). Ordinary
terraforming buildings use the default shape; Open Farm instead derives its
power cell from a Tube spot (`Buildings/TerraformingBuilding.lua:1233-1242`).
The Excavator uses `QuarryExcavator`'s combined shape for power although its
entity is `Excavator` (`Buildings/TheExcavator.lua:153-158`,
`BuildingTemplate/TheExcavator.generated.lua:97`). Archived Lua does not expose
the latter two entity hex sets. A runtime membership reading on a fixture with
each building remains a **pre-ship gate**, whether or not the conservative shape
claim is exercised there.

**DESK, not a game run:** `python tools/desk_c119_ghost_cells.py` at `77c0fad`
commit exercised the whole module on synthetic loaded maps. Its PASS rows cover
clearing, tunnel and object preservation, the two extra shape claims, water
untouched, gridless cable/generator/dome/construction-site reconnection, a built
passage, per-object retry containment and the module-off control. Removing the
tunnel skip made its preservation demand fail. `python tools/parsecheck.py` and
the archived 1.1.1.405907 `bodycheck.py --module Fix_GhostPowerCells --all`
passed. The grid and object lookup implementations are synthetic; the fixture
does not establish native save/load, real connection visuals or performance.

#### First attended attempt (2026-09-26; controls not obtained)

The full closed-process retail log is
`docs/archive/logs/c119_Mars.exe-20260926-14.04.42-6aad2d75.log`, copied from
`%APPDATA%/Surviving Mars Relaunched/logs/Mars.exe-20260926-14.04.42-6aad2d75.log`.
`Get-FileHash -Algorithm SHA256` returned
`E54FEC2D41CF4732992B6CB6DD40B4A58544AB81353353E4B6B224C9AA25741D`
for source and archived working copy. The scoped command
`rg -n '^\[mod\] \[CommunityFixPack\] GhostPowerCells: maps=' <archived log>`
found one line, at 240: `maps=3 cleared=355 tunnel_skipped=2 shape_protected=0
reconnected=0 retry_failed=0 unbuilt_skipped=0 map_failed=0 elapsed_ms=0`.
`(rg -n '^\[LUA ERROR\]' <archived log> | Measure-Object -Line).Lines` returned
0. The positive pack line verifies the log input. The old `RealTime()` frame
clock made the 0 ms duration uninformative; `c79ddc6` uses the game's
`GetPreciseTicks()` millisecond clock for the next load. Neither connection
success nor visuals were witnessed by this count line.

The first Scratch read returned `taint=false` and `eligibility=UNAVAILABLE:sandbox`
(line 274). Slot 1 refused at lines 277 and 288 because it could not verify the
loaded filename as the test copy. The original and byte-identical copy share
the embedded display name and timestamp, so both appear as "Europe Sol 280"
in the Saves UI; a screenshot cannot identify which row is the copy. Slot 2
refused before its callback at line 282 because the dispatcher required a
selected cable, which the UI does not offer. The owner then pressed Slots
3–6 on the reporter map: Slot 3 required a clean owner save (line 291), Slot 4
required the track/passage/hub/tunnel fixture (line 294), and Slots 5–6 required
a selected Excavator and Open Farm respectively (lines 297 and 300).
`rg -n '^\[mod\] \[SMRTK\] SMRTK_ACTION action=slot_[1-6]' <archived log>`
returned seven refusal lines (two for Slot 1, one for each other slot).
These are refusals, not passing or failing control measurements.

TestKit `92271c7` binds Scratch to an owner-pressed native load by the copy's
exact filename, and Slot 2 now finds cables built after Slot 1 in the old
footprint without a selection. It reports connected only if every new cable
joins a grid with producers. Its local smoke covered exact-copy load,
baseline, powered, unpowered and gridless new cables, reload refusal, and the clean-save
guard; the native attended results are still owed. The reporter original and
test copy remain byte-identical (`Get-FileHash -Algorithm SHA256` on each:
`99F5FF06B75F7DB4E68F545189D0A7B3200492884063A4E1D1DDC47504573BE9`).
Neither file has been overwritten or deleted.

#### Second attended sitting (2026-09-26; power-path check open)

The closed-process retail log is
`docs/archive/logs/c119_Mars.exe-20260926-16.05.43-6aad2d75.log` (source and
archived working copy SHA-256 from `Get-FileHash -Algorithm SHA256`:
`7202E0FB6FF78AD94BEE84730017F366E199F94A35250E45CB3DBB1B86AF13DE`).
`rg -n '^\[mod\] \[CommunityFixPack\] GhostPowerCells: maps=' <archived log>`
returned four load lines: reporter `cleared=355` in 128 ms (line 239), then
clean/control loads `cleared=0` in 125, 125 and 136 ms (lines 358, 484, 733).
`(rg -n '^\[LUA ERROR\]' <archived log> | Measure-Object -Line).Lines`
returned 0; the four positive pack lines verify the input.

Slot 1 verified the exact test-copy filename and found `raw_unmatched=0`
across three maps (line 288). There is no
`^\[mod\] \[SMRTK\] SMRTK_ACTION action=slot_scratch` line in this log
(scoped `rg` presence count 0), so the Scratch press itself is unverified;
the exact-copy load is verified by Slot 1. The owner read taint `false` and
eligibility `UNAVAILABLE:sandbox` on that load (lines 262, 265).
Slot 2 found 15 cables built after Slot 1, all with grids and a follow-up
`raw_unmatched=0`, but `powered=0` and `connected=false` (lines 298, 307).
Whether the new run reached an existing powered network is not established
by the log. The connection-to-power and visual check remain open; the zero
gridless count alone is not a powered-cable verdict.

Slot 3 on `SpaceY Sol 20.savegame.sav` found `raw_unmatched=0`, with the pack
clearing 0 cells on that load (line 397 versus 358). Slot 4 refused on the
`trainhub post stuck test` selected map because it lacked the combined
track/passage/hub/tunnel fixture (lines 542, 547, 552). Slot 5 on that save
measured an Excavator: 34 nonzero shape cells, each owned by the selected
building, `missing_owner=0` (lines 605-673, 678). Slot 6 on
`Japan Sol 490.savegame.sav` measured one nonzero Open Farm shape cell,
owned by the selected building, `missing_owner=0` (lines 764, 770).
Those zero missing-owner fields are passing readings. The last load's
`shape_protected=39` counts cells preserved by special shape claims; it does
not by itself count unmatched cells.
For the shape totals, `(rg -n '^\[mod\] \[SMRTK\] SMRTK_DUMP.*leg=EXCAVATOR.*row=shape_cell'
<first log> | Measure-Object -Line).Lines` returned 34, and the analogous
`leg=OPEN_FARM` filter returned 1; these agree with the Slot 5 and 6 action
lines in the same log.

The owner then ran Slot 4 again on `Japan Sol 497 (Drone Hub Integrated).savegame.sav`.
The separate closed-process log
`docs/archive/logs/c119_Mars.exe-20260926-16.16.42-6aad2d75.log` has matching
source and archived working-copy SHA-256
`B79534EAAD0AEE12DFF14F72B48D441E743A7DC3E89FBBF69B3ED079A4CBDDC6`.
The pack cleared 0 cells on four maps (line 271). Slot 4 returned `status=OK`
with 13 of 13 track station pairs sharing a powered grid, 17 of 17 powered
passages, 3 of 3 powered hubs, 10 of 10 powered tunnels with linked grids and
tunnel bits, and `raw_unmatched=0` on the selected map (line 393).
`(rg -n '^\[mod\] \[SMRTK\] SMRTK_ACTION action=slot_4' <second log> |
Measure-Object -Line).Lines` returned 1, and the scoped
`^\[LUA ERROR\]` presence count returned 0. The per-object DUMPs are at
lines 298-384. No taint/eligibility read is recorded for this separate boot.
The scoped per-object DUMP filters `^\[mod\] \[SMRTK\] SMRTK_DUMP.*leg=track`,
`.*kind=passage `, `.*kind=hub ` and `.*kind=tunnel `, each passed to
`rg -n <filter> <second log> | Measure-Object -Line`, returned 13, 17, 3 and
10 respectively, matching Slot 4's action line.

#### Remaining attended check

TestKit `80_AgentSlots.lua` at `92271c7` remains preloaded. On a new load of
the reporter test copy, use Slot 1 before building. Extend a new cable through
the old footprint until it physically joins an existing powered network, allow
it to finish building, then use Slot 2 while paused. The prediction is
`gridless=0`, `powered=new_cables`, `connected=true`, `raw_unmatched=0`, no
`SupplyGrid.lua` errors and no duplicate connection visual. Record the
closed-process log and read taint/eligibility for that boot. Never overwrite
the reporter's original or test copy.

The status stays `cand` until the attended sitting and its control readings.
The cleanup runs on load; it does not prevent new mid-session spread or establish
the original seed of C119.

#### Not opened

- Engine internals of `SupplyGridApplyBuilding` and `SupplyGridRemoveBuilding` (C side).
- Whether the objects the reporter left with no grid survive in the save. The one probe counted
  `ElectricityGridObject` only and read 0, and cables may be another class.
- The water-side stale cells' harm, and any mid-session guard in the connect path (deferred by the
  ruling's load-only scope).
