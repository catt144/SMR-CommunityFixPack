---
id: "F50"
seq: 50
row: 50
title: "Auto-rockets kick approaching drones to Idle every hour"
status: "tested"
status_source: "tag"
priority: "P1"
evidence: "high"
row_status: "tested"
updated: "2026-07-26"
copies: []
---
### F50 — Auto-rockets kick approaching drones to Idle every hour (P1, high)  `[tested: Code/Fix_RocketDroneChurn.lua — PT-04 PASS 2026-07-25 (smooth loading/unloading, no churn)]`
`UniversalRocketBase:HourlyUpdate` (`UniversalRocket.lua:1357-1370`) → `CreateAutoCargoRequest`
→ `SetCargoRequest` → `UpdateCargoResourceRequests` (`CargoTransporterNew.lua:1238-1271`)
does `DisconnectFromCommandCenters()` + reconnect EVERY HOUR while landed;
`DroneControl:OnRemoveBuilding` (`DroneControl.lua:720-729`) sets every drone heading
there to `Idle`. Trips > 1 game hour can never complete; priority irrelevant. Aggravators:
`starting_drones = 0`, `exclude_from_lr_transportation = true` (shuttles never help).
Explains "drones ignore rocket cargo". **Fix:** wrap `UpdateCargoResourceRequests` to
suppress the disconnect/reconnect churn (requests mutate in place via
`TaskRequester:AddRequest`); one-time connect if never connected.

## ⭐ 2026-09-08 — game 1.1.0: what changed, and what hotfix 2 did about it

⛔ **Nothing above this line is edited.** The 1.0.7 record stands as written and every
citation in it was true of **1.0.7.396349**. This section adds only what game
**1.1.0.403908** changed, read on 2026-09-08 against BOTH shipped bodies — the live 1.1.0
tree and the archived 1.0.7 tree (`C:\Dev\SMR-SrcArchive\1.0.7.396349\Src`, restored the
same day, `ad5f93d`).

**The F50 defect persists on 1.1.0.** `CargoTransporterNew:UpdateCargoResourceRequests`
(now `CargoTransporterNew.lua:1430-1463`) still brackets its body with the unconditional
`DisconnectFromCommandCenters` / `ConnectToCommandCenters` pair (`:1431-1433`,
`:1460-1462`); the hourly path is intact (`UniversalRocket.lua:1557-1568` →
`CreateAutoCargoRequest` → `SetCargoRequest` `:2093` → `:1484`);
`DroneControl:OnRemoveBuilding` still idles drones whose goto target is the building
(`DroneControl.lua:784-793`). Re-verification row F-7, confirmed by `VANILLA_FIX_QA`
Reader A.

**What 1.1.0 changed in the body — one line, and our 1.0.7 copy reverted it.** A
two-sided diff of the two shipped bodies differs in exactly one line:
`local additional_amount = is_refuel_resource and not self.refuel_disabled and self:GetFuelResourceRequest()`
(`:1442`). `refuel_disabled` is the new "Accept fuel" infopanel toggle
(`UniversalRocket.lua:70` declares it, `:3319` `ToggleRefuel` flips it, `:774` clears it
when a parked rocket gets a trip). Our copy lacked the clause, so with the pack on, a
rocket the player had told to stop refuelling kept requesting fuel every hour. (Our
copy's non-FIX lines matched the 1.0.7 body exactly — two-sided, not argued from one
tree.)

⇒ **`Fix_RocketDroneChurn` — RE-COPIED on the 1.1.0 body in hotfix 2** (link
`hotfix2/04`). The F50 changes are re-applied on top; the `refuel_disabled` clause is
carried; nothing else differs between branches. Stamped with its `SRC:`/`DEFECT:`
manifest — `bodycheck.py` OK, and seen RED on a tampered hash (BODY-CHANGED) and on a
tampered defect expression (DEFECT-GONE) before the GREEN was trusted.

⛔ **The module now DECLINES on 1.0.7** (checklist 118, `FIX_POLICY` §2a): its `Require`
tests for the `refuel_disabled` property and `ToggleRefuel` on `UniversalRocketBase`,
which 1.0.7 does not declare (0 hits for either name in the archived file). A 1.0.7
player keeps the frozen v5 pack. It is a shape `test`, not a stub `probe`, because the
target loops over `TransportableResourceIds`, which is EMPTY at `ClassesBuilt` on a cold
boot (`PreProcessResources` fills it on `DataChanged`, `Resources.lua:404-449`,
`:492-500`) — a probe there would capture nothing, read UNKNOWN, and switch the module
off on every cold start.

**Desk control, not a game run** (2026-09-08, real Lua 5.4 via the `lupa` module on the
rig; harness in the link session's scratchpad, not committed): the shipped 1.1.0 body,
the shipped 1.0.7 body and our copy executed on a refuel-disabled stub. 1.1.0 and ours
file a fuel demand of 0; 1.0.7 files the full request; ours cycles the command centers 0
times on a steady state and once when a request is missing.

⛔ **What may NOT be read into this.** The status word does NOT move. The re-copy has
never run in a game; the refuel toggle has never been exercised on 1.1.0 with the pack
on. The in-play control (turn "Accept fuel" off on a landed rocket; no Fuel is requested,
and drones already en route are not idled on the hour) joins the post-99 consolidated
sitting (`PLAYTEST_CHECKLIST.md`, "Decisions waiting on you", LINK 04 block).

