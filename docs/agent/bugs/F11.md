---
id: "F11"
seq: 11
row: 11
title: "Train wedges at platform (`table.remove` misuse)"
status: "fixed"
status_source: "tag"
priority: "P1"
evidence: "high"
row_status: "fixed"
updated: "2026-08-03"
copies: []
---
### F11 — Train wedges at platform (`table.remove` misuse)  `[fixed: Code/Fix_TrainPlatformWedge.lua]`

`Lua\Units\ColonistTransport.lua:541-547` (`ExitVehicle` stale-passenger guard) —
`table.remove(vehicle.units, self)` needs an integer pos; intended API is
`table.remove_entry`. When the guard fires (dev comment: CargoTransporter abduction),
the error aborts before `DiscardTransportTicket`; `Train:UnloadTrain`
(`Units\Train.lua:443-453`) then spins forever → train permanently blocks platform.
**Fix:** PRE-wrapper on `Colonist.ExitVehicle` — repaired guard branch
(`table.remove_entry`), tail-call the captured original otherwise. Converted
2026-08-03 from a §1.5 full copy; see the conversion block below, including the
verification still owed.

### ⚖️ Settling observation DELIVERED 2026-08-03 (attended) — the abduction route does NOT produce the stale state

**The audit's open question is answered and its hypothesis is refuted.** The
2026-07-30 reachability block said the stale-passenger state "bottoms out in
engine-side `TransferToMap`… which source alone cannot settle". It has now been
settled at the keyboard, on both maps, and the answer is that **crew-gathering
abduction keeps `train.units` synced — there is no stale entry to guard.**

**Measured (owner's live campaign, session ~1h, pack ON — the fix only replaces
`Colonist.ExitVehicle`, which runs later at unload, so it cannot contaminate the
counter):**

| reading | value |
|---|---|
| rider's command while aboard | `BoardVehicle` |
| `table.find(train.units, col)` BEFORE | `1` |
| rider's index in a surface rocket's gather pool | `1513` of `1513`-plus pool of `1543` |
| `col:GetMap() == MainMap` / `rocket:GetMap() == MainMap` / `col.city == MainCity` | `false` / `true` / `false` |
| `col.holder == rocket` AFTER | `true` |
| `#train.units` AFTER | `6` |
| **`table.find(train.units, col)` AFTER** | **`nil`** |

The abduction was driven by invoking the shipped call site directly —
`col:SetCommand("EnterTransporter", rocket)`, which is verbatim the body of
`CargoTransporter:ExpeditionLoadCrew` (`Lua/Buildings/CargoTransporter.lua:300-302`)
— on a chosen target rather than one the gatherer picked. That removes the
lottery; it does not change the mechanism.

**Why the counter is honest.** `#train.units` = **6** after the abduction, so the
nil lookup is not an empty-or-nil-list artifact: the list is live and populated
and our colonist is simply not in it. `col.holder == rocket` = `true` proves the
transfer actually executed, so `nil` cannot be read as "the call did nothing".
The counter could have failed in either direction and did not.

**The mechanism, re-derived (route, not just citations).**
`EnterTransporter` (`Lua/Units/Unit.lua:1198-1211`) → `self:SetHolder(transporter)`
→ `SetHolderOnMap` (`Unit.lua:709-711`) → `holder:OnExitHolder(self)` on the OLD
holder, the train → `Holder:OnExitHolder` (`Lua/Buildings/Holder.lua:36-37`) →
`table.remove_entry(self.units, unit)` — the correct API. Both ends are real
Holders (`Train.__parents` includes `Holder`, `Train.lua:10-16`; every Building is
one, `BaseBuilding.lua:10`), so `SetHolderOnMap`'s `IsKindOf("Holder")` guard never
early-returns. The cross-map case runs `self:TransferToMap(transporter)` FIRST
(`Unit.lua:1201-1203`) — that was the suspected hole, and the measurement shows it
does not drop the holder link.
*(⛔ The last sentence is superseded 2026-08-03 — the measurement cannot show
that. See "Route claim narrowed" below.)*

⚖️ **What this does and does not establish.** It establishes that the ONE producer
the shipped dev comment names (`--abducted by CargoTransporter?`) does not produce
the state, same-map or cross-map. It does **not** establish that `train.units` can
never go stale by some other producer. The guard remains a correct repair of a
genuine `table.remove` misuse in reachable shipped code; what changed is that its
hypothesised trigger is now measured absent.

⭐ **Owner decision owed: does F11 keep `P1`?** A correct fix whose only
hypothesised producer is now measured absent is not obviously P1. Not changed
unilaterally — routed to `PLAYTEST_CHECKLIST.md` "Decisions waiting on you".

### ⛔ Route error corrected 2026-08-03 — the old citation was the wrong class in the wrong file

The 2026-07-30 block cited `CargoTransporterNew.lua:221-234` at path
`Lua\Units\`. Both wrong. The file is `Lua/CargoTransporterNew.lua` (no `Units/`),
and **`CargoTransporterNew` is a separate class that expedition rockets never
touch**: `RocketExpeditionBase.__parents = { "SupplyRocketBase", "CargoTransporter" }`
(`RocketExpedition.lua:1-2`) and `RocketExpeditionBase:GatherAvailableColonists`
statically calls `CargoTransporter.GatherAvailableColonists` (`RocketExpedition.lua:493`).
The live busy-colonist fallback is **`Lua/Buildings/CargoTransporter.lua:272-285`**.
The two implementations differ (the `New` one lacks `transfer_available`, adds an
`IsDead()` check, and prefers `self.cargo_request_passengers`). The old block's
*conclusion* — that crew-gathering takes busy colonists including riders — survives
in the correct file; the route to it did not. This is the failure mode the project
already has a name for, and it is recorded rather than quietly fixed.

### Owner witness 2026-08-03 — why expeditions APPEAR never to take busy colonists

Owner, unprompted, from their own campaign: *"Expedition waiting rockets have
never gathered busy colonists. It will leave the rocket sitting forever until you
either import a colonist that it needs from earth or train one in the university.
I have ran into this multiple times."*

**The observation is correct; the mechanism is not busy-refusal.** The busy
fallback exists (`CargoTransporter.lua:272-285`), but it is filtered by
`FilterColonistsByTrait` (`Lua/CargoTransporterNew.lua:163-194`) FIRST. When the
expedition asks for a specialization nobody on the map has, the busy pool
contributes nothing either, `GatherAvailableColonists` returns nil, and
`colonist_summon_fail` goes true (`RocketExpedition.lua:493-495`) — the rocket
sits. Cured by importing or by university training, which is exactly the remedy
the owner names. So the stall is **trait scarcity**, not busy-refusal.

Discriminator, if it is ever worth confirming: a *generic*-crew expedition
(`crew_specialization` unset → `"Colonist"`, `RocketExpedition.lua:522`) requesting
more crew than there are idle colonists should take working colonists. A generic
expedition stalling with busy colonists available would overturn this reading.

### The natural cross-map route is LIVE, and that half was settled by reading

An underground colonist cannot be in `MainCity.labels.Colonist`, so the rider's
index `1513` in a surface rocket's `1543`-entry pool can only have come from the
`GetConnectedCitiesForColonists` append (`CargoTransporter.lua:220-235`, `:244-251`),
which walks `city.labels.Elevator`. A surface rocket's crew gathering therefore
**does** see underground train riders and will take them as busy colonists once
idle ones run short. No expedition was consumed to establish this — it is a pool
reconstruction, not a launch.

*(⛔ The index argument above is STRUCK 2026-08-03 — see "Index argument struck"
below. The conclusion stands on a different route.)*

### ⛔ Corrections 2026-08-03 (f11-f99-review second-opinion chain — sealed derivation `28c253f`, adjudicated by the terminal prompt)

**Route claim narrowed.** "The measurement shows it does not drop the holder
link" was an over-claim: a post-hoc read (`holder == rocket`, `find == nil`)
cannot distinguish WHICH call cleaned `train.units`. Two candidate routes exist
in the cross-map case and both end in `Holder:OnExitHolder` →
`table.remove_entry` (`Holder.lua:36-41`): (a) `Unit:OnTransferToMapDone`
(`Unit.lua:846-851`) does `SetHolder(false)` when the holder was left on another
map — fired from C via `OnMsg.TransferToMapDone` (`_cobject.lua:166-170`); or
(b) `EnterTransporter`'s own `SetHolder(transporter)` at `Unit.lua:1209`. Route
(a) is better supported — it is what the function exists for, and
`Unit:DisembarkElevator` (`Unit.lua:930-933`) deliberately sets the destination
holder BEFORE transferring so Done won't clear it — but the Msg raise point is
inside the C-side `TransferToMap` and is not readable from Lua. **Neither route
is provable from source; the shared, settled fact is that whichever ran first
cleaned the list via the correct API.** Consequence either way: the SAME-MAP
removal path (`SetHolder` at `:1209` doing the work) remains unmeasured.

**Index argument struck.** "Index 1513 in a 1543 pool can only have come from
the append" requires `#MainCity.labels.Colonist < 1513`, and that number was
never printed. The conclusion survives via step 4 of the same sitting:
`col.city == MainCity` → `false`, plus the label invariant — `CityObject:Init`
asserts `city:GetMap() == map` (`CityObject.lua:11-17`) and both transfer edges
move label membership (`:70-81`); `Colonist:AddToCityLabels`/`RemoveFromCityLabels`
(`Colonist.lua:272-311`) act only on `self.city`. So `col ∉
MainCity.labels.Colonist`, no index needed. Caveats that make it "does not,
unless a known-buggy state exists" rather than "cannot":
`SavegameFixups.FixCityLabels2` (`CityObject.lua:89-116`) exists because shipped
saves have violated the invariant, and `ManualCityLabels` (`:83-87`) opts
classes out (Colonist is not one).

**Pool-rebuild scope.** The console rebuild reproduced only the raw
concatenation (`CargoTransporter.lua:237-251`). The shipped function then
filters `thread_running_destructors` (`:252-260`), partitions idle-before-busy
(`:262-285`), and runs `FilterColonistsByTrait` + truncation (`:270-291`).
Membership in the raw pool is established; SELECTABILITY is not, and the index
is a position in an ordering the shipped code never uses.

**Counter limits (for future counters of this shape).** `nil 6 true` also
prints if the `units` table was replaced between steps (`KickUnitsFromHolder`
nils it, `OnEnterHolder` rebuilds — `Holder.lua:11-32`) or if the colonist was
aboard a different train by the final read; `6` is not an identifier. Cheap
prophylactic next time: also print the container's identity and `#units`
immediately before the operation.

### ⚖️ Fix shape converted 2026-08-03 — §1.5 full copy → §1.4 PRE-wrapper

Decided and built by the f11-f99-review terminal prompt; the owner's "it ships"
decision was not re-opened — this changes shape, not disposition. Grounds:
FIX_POLICY §1.4b prefers a wrapper over a copy (a copy can silently undo an
official fix); §1.5's "mid-function and unhookable" justification is false here
(the defect is the method's first statement); the copy froze two live tuning
expressions (`ColonistTransport.lua:555-557` comfort, `:558-560` GreenView
sanity) and a recreated file-local `stat_scale` invisible to any `Require`
check. Wrapper facts, each verified against Src/loader rather than inherited:
pre-wrapper REQUIRED (EF-012 — sole call site is `SetCommand`, Train.lua:447;
normal path ends in destructor-tail `SetCommand`); apply-once safe (entry not
`optional`, so 00_Core's reconciliation never re-applies; a Lua reload rebuilds
game code before recapture); double evaluation of the condition is three pure
reads with no yield between. Declined: an `if vehicle.units then` guard around
the removal — the shipped kick path already proves nil-tolerance
(`KickUnitsFromHolder` nils `units` BEFORE the kick loop, so every kicked
unit's `OnExitHolder` runs `remove_entry(false, unit)` routinely), and the
guard would break byte-parity with the tested branch. `Require` now checks
`Colonist.ExitVehicle`, `table.remove_entry`, `IsSameMap`; the
`const.Scale.Stat` check was dropped with the copy that used it.

⛔ **Verification owed.** The conversion is behaviour-preserving **by
construction** — that is the strongest claim available. It has NOT run under a
probe suite or a keyboard leg; it earns `tested` only there. Falsifier: any
train unload behaving differently than before the conversion (comfort/sanity
deltas, ticket handling, or a train failing to leave a platform).

### Audit history (2026-07-30, superseded above)

**Audit 2026-07-30 (reachability): U — kept, settling observation recorded.**
The sole call site (Train.lua:447, UnloadTrain) is unconditional in train play,
but every Lua-visible path keeps `train.units` synced; the stale-passenger
state the guard exists for bottoms out in engine-side `TransferToMap`
(`Unit:EnterTransporter`, Unit.lua:1202-1209 — crew-gathering explicitly takes
BUSY colonists including riders, CargoTransporterNew.lua:221-234), which
source alone cannot settle. Observation that settles it: with a colonist
mid-ride, launch a crew expedition that dips into busy colonists, then inspect
`train.units` (or catch the shipped TrainsLogging "not in train" warn) — a
stale entry makes this R2. Playtest-item candidate. Full block in
REACHABILITY_AUDIT.md.
*(Superseded 2026-08-03: the observation was delivered, the citation was wrong,
and the answer is no stale entry — see the blocks above. The "not R2" verdict
stands, now on evidence rather than inference.)*
