---
id: "F127"
seq: 222
row: 257
title: "OUR OWN DEFECT — Fix_ArrivalDeaths' C83 reroute changes an arrival's destination but leaves its bed reserved in the rejected dome, which can keep the colonist off the Homeless list if the new dome is full"
status: "fixed"
status_source: "tag"
priority: "P3"
evidence: "DESK 2026-09-24, branch 795aefa: archived 1.1.1.405907 full-dome and full-habitat controls reproduce an orphan booking and missing Homeless label without the fix; repaired harness 12/12 and C83 regression harness 12/12. No attended play. SOURCE: RocketBase.lua:2072-2073, Colonist.lua:2223-2228 and :3089-3131, Dome.lua:3482-3493, Residence.lua:291-308, MicroGHabitat.lua:85-88. Owner's one-release hubset ruling still binds."
row_status: "cand 2026-09-24 - pack defect found by the migration audit; mechanism confirmed; full-destination branch to validate before the repair"
updated: "2026-09-24"
copies: []
---
### F127 — OUR OWN DEFECT: the C83 arrival reroute leaves the old bed reserved (P3) `[fixed 2026-09-24 on hubset: desk reproduced and repaired; pending play]`

#### Must_Read_Header

A defect in our shipped `Fix_ArrivalDeaths`, not in vanilla. Migration audit P2, confirmed by the
independent [cross-check](../reports/MIGRATION_CROSSCHECK_2026-09-24.md). Built on `hubset` at
`795aefa`, pending attended play and the set audit. The owner added it
to the hub release set on 2026-09-24.

#### Source

- Vanilla reserves a bed for each applicant in the dome the rocket chose
  (`Lua/Buildings/RocketBase.lua:2072-2073`, archived 1.1.1.405907).
- Our C83 path (`Code/Fix_ArrivalDeaths.lua:485-486`, pack at `21a16ec`) moves an arrival away from an
  unwelcoming dome by rewriting `emigration_dome` and `emigration_elevator`. It leaves
  `reserved_residence` in the rejected dome.
- On arrival, `UpdateResidence` (`Colonist.lua:3123-3130`) sees the reservation is in another dome and
  asks the new dome for a home. Settling into one cancels the old hold (`Residence.lua:111`).
- If the new dome is full, no home is found. `Colonist.lua:3095` then keeps the colonist off the
  Homeless list because its old reservation is still valid, and the bed in the rejected dome stays
  held until the stale-reservation sweep (`Fix_StaleReservations`) releases it.

#### Harm

HYPOTHESIS. A rerouted arrival in a full dome is homeless but not counted as homeless, so the
player's homeless figure and housing alerts undercount it, and a bed elsewhere is blocked for a
while. This is exactly the C83 fallback case: the working destination C83 picks may be full. Early
to mid game, when domes fill and rockets keep landing.

#### Control (desk first)

- **Supports:** after the reroute into a full dome, `reserved_residence.parent_dome` is the rejected
  dome and the colonist is missing from the Homeless label.
- **Refutes:** the reservation already moves or clears with the reroute, or the colonist is labelled
  Homeless anyway.
- The cross-check asks for the full-destination branch to be validated before any repair.

#### Fix shape

Cancel or move the reservation together with the reroute, inside the same C83 branch. Keep the
unrerouted path byte for byte native.

#### Release (owner ruling, 2026-09-24)

Joins the hub release set (C114, C115, C116, C117, C42, C111, F127 and the P3 pins in
`Fix_VacuumWalks`). The owner: "if p3 is just makeing something better we already ship or correcting
it and its ready to go we shoul,d just add it same with p2." The set ships in one release, never
alone; the ruling's words are in [C114](C114.md).

## 2026-09-24 — hubset 01 build on `795aefa`; pending play

- **DESK, archived 1.1.1.405907.** `python tools/desk_f127_arrival_booking.py --premise`
  reproduced the old booking and missing Homeless label in the full-destination branch. The
  repaired harness held 12/12 demands, including a full dome, a full Naturalist Habitat, free
  housing transfer, fix-removed controls and the unrerouted native path. The existing
  `python tools/desk_c83_arrivals.py` held 12/12. Both harnesses ran in the `hubset` worktree.
- **BUILD, branch `795aefa`.** The C83 branch of `Code/Fix_ArrivalDeaths.lua` cancels a valid
  booking in the rejected dome before asking the new destination to reserve, if it has that
  method. This stays before `orig_idle`; an unrerouted arrival still delegates unchanged. The
  cancellation uses a shipped colonist method and creates no new field or thread; the existing
  Idle wrapper's layer-2 save disposition remains. Four archived booking callee bodies are pinned.
- **SOURCE correction found during review.** `MicroGHabitatBase:ReserveResidence` delegates to
  `Residence:ReserveResidence` (`Lua/Buildings/MicroGHabitat.lua:85-88`), which cancels the old
  booking only after a free-space check succeeds (`Residence.lua:291-308`). The first draft's
  reserve-only call left a full habitat broken. The final harness includes that old shape as a
  mutant and requires it to fail. This drift is routed to link 99.
- **LIMIT.** The desk runs archived Lua with controlled engine seams. No real rocket arrival,
  save/reload, mod-off outcome or Homeless panel display has been observed. Link 07 owes the
  attended play result; `fixed` here means built and desk-verified, pending that result.
