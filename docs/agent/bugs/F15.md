---
id: "F15"
seq: 15
row: 15
title: "Mystery 11 wisp RP rewards double/silent"
status: "fixed*"
status_source: "tag"
priority: "P2"
evidence: "**MEASURED (batch half)**"
row_status: "fixed* — ⭐⭐ **the double-grant's removal is OBSERVED 2026-08-11** (`corun-pt15` sitting, F15 rider, log `cp15sitting_Mars.exe-20260811-15.09.30`): destroying a 15-wisp trapful posted **Δ=0 RP at the paused instant of the kill** (vanilla's batch `AddResearchPoints` would have posted 1,500 immediately) — the batch grant is gone, which is the shipped fix's exact half; notification figure 1,500 = wisps×100 per the leg's read. ⛔ The DELAYED reading (0 → 2,543 RP over ~7 game hours) is CONTAMINATED by the colony's own labs and the leg's printed `2543/1500` ratio is NOT a finding — per-destructor payment stays bounded, not measured (multipliers all read identity). The \"silent\" half stays open as before; the same rider found `C46`. NOT `tested` (entry; recorded by the terminal audit 2026-08-11 — the sitting banked the log but never updated this entry)"
updated: "2026-08-11"
copies: []
---
### F15 — Mystery 11 wisp RP rewards double/silent  `[fixed*: Code/Fix_WispRewards.lua — double-grant removed so display == granted; the "silent" half stays open, see below]`
`Lua\Mysteries\Fireflies.lua:466-469` — code after `SetCommand("Die")` unreachable
(`DoSetCommand` kills current thread, CommonLua\Classes\CommandObject.lua:340-378); actual
RP from Die destructor (:540-542). Batch destroy path (:676-688) grants again → trapped
wisps pay 200 RP each while notification says 100; later catches pay 100 silently. **Fix:**
patch `Firefly.Drain` to notify/grant before `SetCommand("Die")` and remove the destructor
double-grant (or drop batch grant) so display == granted.
*Implemented half:* the batch grant is dropped, leaving the Die destructor as the single
payer — every wisp is now worth exactly the 100 RP the notification claims. *Open half:*
wisps caught AFTER the mode was set to "destroy" are drained one at a time and still pay
silently (no per-wisp notification). That is a UI addition rather than a defect repair
(FIX_POLICY §4), so it is deliberately not shipped.

## ⭐⭐ 2026-08-11 — the destroy branch RAN and the double-grant's removal is OBSERVED (`corun-pt15` sitting, F15 rider; recorded by the terminal audit)

*(Integration note: the sitting banked the evidence and updated STATE/checklist
but never touched this entry; the terminal audit filled it 2026-08-11.)*

Retail `Mars.exe` **1.0.7.396349**, `CP15STAGE` lineage (the reloaded
`CP15PT15.savegame.sav`), pack **81/81 active AS READ**, log
`docs/archive/cp15sitting_Mars.exe-20260811-15.09.30.log` (L1493-L1616).
**FORCED:** `SetLightTrapMode("destroy")` on a **15-wisp** trapful — the
mystery's own payload function, first execution pcall-printed (`ok=true
result=destroy`). A tech was queued first (`MartianPatents`, `status.points=0`
baseline) after the leg's own guard refused the read with an empty queue.

* ⭐ **The clean discriminator: Δ = 0 at the paused instant of the kill.**
  `status.points` read **0** immediately after the destroy call, game paused,
  before any destructor could fire (L1558 vs L1529). Vanilla's batch path
  (`AddResearchPoints` inside `SetLightTrapMode`, the double-grant this fix
  removes) would have posted **1,500 immediately** (15 wisps × 100). Nothing
  posted ⇒ **the batch grant is gone — the shipped fix's exact half, observed
  at the defect site.**
* The notification figure was **1,500 = wisps × 100** (the leg's read), i.e.
  what the display claims matches what the destructors will pay.
* ⛔ **The delayed reading is CONTAMINATED and must not be quoted.**
  `status.points` 0 → 2,543,000 raw (~2,543 RP) over ~7 game hours includes
  the colony's own lab output with no baseline rate to subtract; the leg's own
  printed ratio `2543/1500` is **NOT a finding**. "Each destructor pays exactly
  100" therefore stays bounded, not measured — though every multiplier read
  identity (`BreakthroughResearchSpeedMod=100` etc.), so no scaling confound.
* `fireflies_destroyed` 0 → 15; the same rider's post-destroy trap read found
  **`C46`** (the emptied trap keeps its power modifier — separate entry) and
  the post-destroy state is saved as `CP15F15.savegame.sav`.
* ⛔ Not `tested` (no owner verdict on this reading); the "silent" open half is
  unchanged.

