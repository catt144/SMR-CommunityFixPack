---
id: "C39"
seq: 114
row: 149
title: "`Policy_Automation_ServiceAutomation` cuts `max_workers` by **label** while its performance compensation keys on **class** — the four Workshops are on the label and outside the class"
status: "tested-unattended"
status_source: "tag"
priority: "?"
evidence: "**OBSERVED**"
row_status: "filed 2026-08-02 by the D10 route re-derivation (chain prompt 9) — **VERIFIED vs Src, harm direction NOT determined.** The law's only effect is `LawEffectModifyLabel{Label=\"ServiceBuildings\", Prop=\"max_workers\", Percent=-50}` (`Data\\LawDef\\LawDef-Technology.lua:227-234`). **23 templates carry that label; all derive from `Service` except `ArtWorkshop`, `VRWorkshop`, `BioroboticsWorkshop` and `TVStudioWorkshopCCP1`**, which are `Workshop = {ElectricityConsumer, Workplace}`. The compensating branch in `Workplace:GetWorkshiftPerformance` is gated on `self:IsKindOf(\"Service\")` (`Workplace.lua:205-217`), so it cannot fire for those four. **Tells: self-contradiction** (one effect targeted by label, its compensation by class) **and an explicit dev comment** — `:209-210` says the code *\"assumes that there's a modification in max_workers matching the value on automation_workforce_reduction\"*, i.e. it assumes the two sets coincide. ⚠️ **NOT sized, and the sign is genuinely unclear**: `law_scale` appears to *raise* performance (a full reduced shift computes ~200 rather than 100), so the four may be missing an uplift rather than taking a penalty — and workshop `performance` feeds the **Comfort boost** (`ArtWorkshop.lua:24-27`), not resource output, so the player-visible consequence differs from every other member of the label. Next step is a keyboard observation with the law enacted, not more reading (entry). ⭐⭐ **THE OBSERVATION RAN 2026-08-11 (`corun-pt15` attended sitting) AND THE SIGN IS SETTLED: MISSING UPLIFT, not a penalty.** Game PAUSED for the whole bracket (both reads at `GameTime()=17766026`, `speed=0x`, so drift cannot explain the delta), subject `TVStudioWorkshopCCP1#1526` (`IsKindOf(Service)=false`, raw=nil) against control `Diner#1475` in the same dome. **Both took the identical `max_workers{percent=50}` cut; only the control was compensated** — control performance **114 → 268 → 124** (a ROUND TRIP that reverses on `:Deactivate()`), subject **127 → 131 → 129** while its shifts went 12/12/12 → 6/6/6 → 12/12/12. ⇒ the four Workshops lose ~half their staffing with no offsetting uplift, i.e. ~50% throughput. Owner verbatim: *more than double when I reverted it dropped to 124 for the dinner*. ⛔ Only `TVStudioWorkshopCCP1` existed in the colony — the other three are INFERRED, not measured. PRE-RELOAD ONLY; NOT `tested`; **the repair is a NEW owner decision** (checklist). ⚠️ Prep's *fired workers do not snap back* prediction did NOT hold — they returned immediately (entry). ⚖️ **REPAIR DECIDED 2026-08-12 (owner): EXTEND THE COMPENSATION**, scope widened to a sweep of all three automation labels (`FactoryBuildings` sibling law CONFIRMED in data; Research expected) for the same label-vs-class mismatch; delabel alternative declined. Queued into the next unattended build chain; verify = the same paused bracket on a `CP15PT15` staged copy (entry). ⭐⭐ **BUILT 2026-08-15** (`unattended-3` prompt 01) — `Code/Fix_AutomationLawCompensation.lua` + TestKit probe `59_Probes_Wave10.lua`. **The static sweep RAN and the mismatch list is EIGHT, not four**: the four Workshops (`ArtWorkshop`/`BioroboticsWorkshop`/`VRWorkshop`/`TVStudioWorkshopCCP1`) **plus `SecurityStation`+`SecurityPostCCP1` on the same `ServiceBuildings` label, plus `DroneFactory`+`BottomlessPitResearchCenter` on `FactoryBuildings`** — four families nobody had looked at. ⭐ The expected Research sibling EXISTS (`Policy_Automation_ResearchAutomation`, `LawDef-Technology.lua:149-223`, label `ResearchBuildings`) and has **NO** Workplace mismatch. Design settled as a **third** candidate the entry did not carry: a §1.4 post-wrapper adding the EXACT compensation delta (both entry candidates rejected, reasons on the entry — (a) is arithmetically inexact, worked example 152 vs 151). ⛔ NOT `fixed`: no suite run, no in-game reading yet — prompt 02 owns both. ⚖️ **Scope-growth confirm routed to the owner (checklist 30)**: the repair now also moves renegade suppression and drone build times, which the 08-12 ruling's picture did not include (entry). ⭐⭐ **THE PAUSED BRACKET RAN 2026-08-15 (`unattended-3` prompt 02) AND THE REPAIR IS MEASURED WORKING → `fixed`.** Staged `CP15PT15` copy (`U3C39BR`, MD5 `D2887D754C44134141B6CCC4C9020446`, byte-identical before AND after — this leg never saves), map `BlankBigTerraceCMix_10`, gate `76/76` + `8/8`, **0 `[LUA ERROR]` lines**, log `archive/u3c39_Mars.exe-20260815-01.36.41.log`. ⭐ **All four reads at `GameTime()=61108784`, `speed=0x` — INCLUDING THE REVERT**, which closes the 08-11 bracket's one recorded harness gap; drift cannot explain a number. **Subject `TVStudioWorkshopCCP1#1526`: performance 102 → 253 with the law active, our delta +127, and the log's own `vanilla-would-have-said` puts the uncompensated answer at 126 ⇒ 126 → 253 = 2.008×**, against `127 → 131` (1.03×) unfixed on 08-11. ⭐⭐ **Control `Diner#1475` rode 130 → 228 on vanilla's own compensation and OUR DELTA ON IT READ 0 AT ALL FOUR READS** — the double-pay failure mode this design was chosen to avoid did not occur on a live object; delta is also 0 before the enactment and 0 after the revert, so the repair is a correction to an active law, not a standing bonus. ⚠️ 127 not the probe's 100 because that constant assumes six IDENTICAL workers — predicted in writing before the run. ⛔ **STILL ONLY `TVStudioWorkshopCCP1` IS MEASURED**: this colony's `ServiceBuildings` label held 6 members (5 gated, 1 not), and `FactoryBuildings`/`ResearchBuildings` were BOTH EMPTY, so the other seven families stay SOURCE. ✅ One excluded-by-construction member WAS sampled and reads as predicted — `OpenAirGym#1476` returns `IsKindOf('Workplace') = false`. ⚠️ **Harness defect disclosed on the entry**: the payload's per-family tally used `rawget(b,'class')` and printed `0 present` for all eight families including the subject's own — those log lines are wrong by construction and must not be quoted; the population above comes from the scan's per-member lines. ⚖️ **RELABELLED `tested-unattended` 2026-08-15** — owner split the vocabulary the same day (checklist 26b): `tested-attended` = human at the keyboard, `tested-unattended` = real launches nobody watched, bare `tested` = legacy/unaudited and closed. ⛔ Still NOT `tested-attended`: law `Activate()`d directly, no keyboard witness, one family of eight. ⚖️✅ **SCOPE CONFIRMED BY THE OWNER 2026-08-15, same day, checklist 30 CLOSED: SHIP AS BUILT — all eight families, no list.** Their ruling: *\"go with whatever is supposed to be true to the code, which fits this mod as a true to code bugfix as much as possible\"* — and they challenged the framing first (*\"I thought we decided on this awhile ago?\"*), correctly: 08-12 had already ruled it and widened the sweep on their own question. The principle selects the shipped build because **the module carries NO building list** — it matches the active law's own effect object against the building's `max_workers` modifiers and re-evaluates vanilla's own three gates per call, so coverage IS \"whatever actually carries the defect\"; restricting to the four Workshops would have meant ADDING a hardcoded template list that does not exist today, purely to leave identically-broken buildings broken. It is also faithful to the law's printed text (*\"Service buildings require 50% less workers\"* — the trade is labour, there is no comfort clause; comfort is merely what the four Workshops spend `performance` on), which is what the declined delabel alternative would have contradicted. ⛔ The ruling settles SCOPE ONLY — it does NOT settle disclosure (the card must still say this moves gameplay-visible numbers) and does NOT upgrade the seven SOURCE families"
updated: "2026-08-15"
copies: []
---
### C39 — `Policy_Automation_ServiceAutomation` cuts `max_workers` by LABEL while its performance compensation keys on CLASS, and the four Workshops sit on one side of that line only  `[tested-unattended 2026-08-15: `Code/Fix_AutomationLawCompensation.lua` — the label sweep found **EIGHT** mismatched families across **TWO** labels, not four. Filed 2026-08-02 by chain prompt 9's D10 route re-derivation — VERIFIED vs Src; ⭐⭐ OBSERVED 2026-08-11 (corun-pt15 sitting, log cp15sitting_Mars.exe-20260811-15.09.30, audit-sustained): sign SETTLED = MISSING UPLIFT — control 114→268→124 round-trips on the law, subject 127→131→129 while its workers went 12→6→12; ⚖️ owner ruled EXTEND THE COMPENSATION 2026-08-12. ⭐⭐ **REPAIR MEASURED WORKING 2026-08-15** (paused bracket on a staged `CP15PT15` copy, all four reads at one GameTime and `speed=0x` including the revert): subject **126 → 253 = 2.008×**, control **unmoved by us, our delta 0 at every read**, repair stands down when the law does. ⚖️ **Relabelled `fixed` → `tested-unattended` 2026-08-15** under the owner's same-day vocabulary split (checklist 26b) — the evidence did not change, the word for it did. ⛔ What `tested-attended` would still buy, and what this label does NOT assert: only **one** family of eight is measured, the law was enacted directly rather than voted, and no keyboard witness exists]`

**How it was found.** Checking whether D10's proposed capacity dial would collide
with anything else that already modifies `max_workers` on the workshops. One
thing does, and the way it does it is asymmetric.

**The two halves that disagree.**

* **The effect targets a LABEL.** The law's sole effect is
  `LawEffectModifyLabel{ Label = "ServiceBuildings", Prop = "max_workers",
  Percent = -50 }` (`Data\LawDef\LawDef-Technology.lua:227-234`, parameter
  `automation_workforce_reduction`).
* **The compensation targets a CLASS.** `Workplace:GetWorkshiftPerformance`
  re-derives a `law_scale` from that same parameter, but only under
  `self:IsKindOf("Factory")` / `IsKindOf("ResearchBuilding")` /
  `IsKindOf("Service")` (`Lua\Buildings\Workplace.lua:205-217`).

**The sets do not coincide, and the mismatch is exactly four buildings.**
Twenty-three templates carry `label3 = "ServiceBuildings"`. Every one of them
resolves to a `Service` descendant — directly (`AmphitheaterBase`), via
`ServiceWorkplace = { "Service", "Workplace" }` (Diner, MegaMall, Spacebar,
CasinoComplex, the Shops, `MedicalBuilding` → Infirmary/Hospital/MedicalPost),
or via `FitService = { "Service" }` (OpenAirGym, TaiChiGarden) — **except**
`ArtWorkshop`, `VRWorkshop`, `BioroboticsWorkshop` and `TVStudioWorkshopCCP1`,
whose chain is `Workshop = { "ElectricityConsumer", "Workplace" }`
(`Lua\Buildings\ArtWorkshop.lua:3-6`). Those four take the staffing cut and
cannot reach the compensating branch.

**Intent — two tells, and the second is the strong one.**
1. **Self-contradiction:** one effect is aimed by label and its corrective by
   class, with no mechanism keeping the two lists in step.
2. **Explicit dev comment**, on the compensating block itself (`:209-210`):
   *"this code assumes that there's a modification in max_workers matching the
   value on automation_workforce_reduction, and tries to reverse its effect so
   that the overall performance is maintained; other modifications on
   max_workers are ignored."* The code says out loud that it assumes the two
   sets coincide. For these four they do not.

**⚠️ WHAT THIS ENTRY DOES NOT CLAIM — the sign is genuinely unresolved, and it
is recorded that way rather than guessed.** Reading the arithmetic,
`law_scale = MulDivRound(base_max_workers, 100, post_auto_workers)` comes out at
**200** for a −50% law, and `part_per_worker = law_scale / max_workers` then puts
a *full* reduced shift at ~200 performance where an unmodified full shift is 100.
If that reading is right, the compensation **raises** performance and the four
Workshops are missing an uplift rather than carrying a penalty — which is a much
smaller thing, and possibly a deliberate one. I could not settle it from source
without following `performance` through its consumers, which is beyond what this
sweep was for.

**And the consequence is a different KIND for these four.** For a Factory or a
Shop, `performance` scales resource output or service throughput. For a Workshop
it scales the **Comfort boost** the shift pays its workers
(`Min(MulDivRound(g_Consts.WorkInWorkshopComfortBoost, self.performance, 100),
100*const.Scale.Stat)`, `ArtWorkshop.lua:24-27`) — so whatever the sign turns out
to be, the player-visible effect lands somewhere no other member of the label
puts it.

**Reachability — R1 if the law is enactable in ordinary play, which is not yet
checked** (it is a `Policy_Automation_*` LawDef in the Technology group; whether
it needs a tech, a sponsor or an Assembly vote was not traced). Stating that as
open rather than assuming R1 from the file it lives in.

**Next step is a keyboard observation, not more reading:** enact Service
Automation on a colony with a staffed workshop and read the infopanel — workers
per shift, performance, and the Comfort the shift pays — against a Diner or
Spacebar in the same dome as the in-family control. Until that runs this stays
`cand`, and it is **not** a §4 package.

⚠️ **Do not fold this into D10.** D10 adds a `max_workers` modifier to the same
four buildings, so a session that builds D10 while this is open will be unable to
tell the two apart in a log. If D10 is ever un-parked, settle C39 first or hold
the dial at base for the leg.

## ⭐ 2026-08-11 — the observation is SCHEDULED, and prep answered the timing question it could not have answered from the keyboard (`corun-pt15` prompt 1)

**Nothing below is an observation.** It is the route, re-derived from Src rather
than carried from this entry, so the sitting knows *when* to look. Every
citation here was re-read at 1.0.7.396349.

**Citations checked, all four EXACT as this entry has them:** the sole effect
`LawEffectModifyLabel{ServiceBuildings, max_workers, −50%}` at
`Data\LawDef\LawDef-Technology.lua:227-234`; the compensating branch at
`Lua\Buildings\Workplace.lua:205-217` with the dev comment at `:209-210`; the
Comfort route at `Lua\Buildings\ArtWorkshop.lua:24-27`; `PolicyDef Automation`,
SortKey 900, at `Data\PolicyDef.lua:740-747`.

**⭐⭐ THE SHIFT-BOUNDARY QUESTION SPLITS IN TWO, and the split is what the
sitting's timeline needs.**

* **`performance` recomputes IMMEDIATELY — no shift change required.**
  `LawEffectModifyLabel:OnStart` calls `container:SetLabelModifier`
  (`Lua\ClassDefs\ClassDef-Factions.generated.lua:2039-2059`), which drives each
  member's `Workplace:OnModifiableValueChanged("max_workers", …)`. That handler
  ends with `SetWorkshift(current_shift)`, `CheckWorkForUnemployed()`,
  `UpdatePerformance()` **and `RebuildInfopanel(self)`**
  (`Workplace.lua:376-409`); `UpdatePerformance` is
  `SetBase("performance", GetWorkshiftPerformance(active or current shift))`
  (`:241-250`). The infopanel is explicitly rebuilt. Read it the moment the law
  lands.
* **⛔ The Comfort the shift PAYS is only observable at a shift boundary.** It is
  paid inside `Workshop:OnChangeWorkshift`, to the workers of the **outgoing**
  shift, and only `if working`. The arithmetic
  (`Min(MulDivRound(g_Consts.WorkInWorkshopComfortBoost, self.performance, 100),
  100*const.Scale.Stat)`, with `WorkInWorkshopComfortBoost = 5000`,
  `Lua\__const.lua:1730-1736`) is available instantly and the probe prints it;
  the **payment** needs one shift change with the law active.

**⚠️ AND ENACTING FIRES WORKERS — which is why the workshop must be FULL first.**
When `max_workers` drops, the same handler walks each shift and `FireWorker()`s
the overflow (`Workplace.lua:378-390`). A shift that was full therefore stays
full at the reduced max — which is exactly the "full reduced shift" this entry's
~200 arithmetic assumes. A half-staffed workshop settles nothing. On
`:Deactivate()` the fired workers do **not** snap back; `CheckWorkForUnemployed`
re-hires over game time, so an after-revert reading below the baseline is
expected and is not a second defect.

**Reachability, partially answered at the data level.** Neither the LawDef nor
`PolicyDef Automation` carries a tech prerequisite or condition field — the
gating, if any, is the ordinary Assembly policy flow, and the policy sorts 10th
of 11 in the Technology category, which is why organic play never surfaces it
early. The entry's "R1 if enactable in ordinary play" is therefore not
contradicted by the data; whether a *vote* is reachable on a given colony is
still unread.

**The population instrument.** The sitting walks
`UIColony.labels.ServiceBuildings` — the same container
`LawEffectModifyLabel:OnStart` writes to — and splits it live by
`IsKindOf("Service")`. That measures this entry's central claim (23 members, 4
outside the class) on the actual colony instead of counting templates in a data
file.

## ⭐⭐ 2026-08-11 — THE OBSERVATION RAN. THE SIGN IS **MISSING UPLIFT**, NOT A PENALTY (`corun-pt15` prompt 2, attended sitting)

**The fork this entry has carried since 2026-08-02 is closed.** Retail
`Mars.exe` **1.0.7.396349**, staged copy `CP15STAGE.savegame.sav` (`Copy-Item`
of the owner's `PT-15.savegame.sav`, MD5 `5D0D81A3D66CA7BABFCA85D6AC118C06`),
map `BlankBigTerraceCMix_10`, pack **81/81 active AS READ**, MODE=REAL.
Log `docs/archive/cp15sitting_Mars.exe-20260811-15.09.30.log`.

**⛔ THE GAME WAS PAUSED FOR THE WHOLE BRACKET.** `CLOCK C39 before` and
`CLOCK C39 after enact` both read `GameTime()=17766026 … speed=0x` — the two
readings are at the **same game instant**, so organic drift cannot explain the
delta. This mattered: three consecutive scans of the same colony had shown
`MegaMall` performance swing 115 → 117 → **0**, i.e. drift alone is larger than
the effect being measured. Pausing was chosen for that reason and prep's finding
made it safe (`performance` recomputes synchronously inside `:Activate()`).

**Subject and control** (`CP15.C39Set(6, 3)`), both in `DomeMegaTrigon#1069`:

* **subject** `TVStudioWorkshopCCP1#1526` — `IsKindOf("Service")=false`
  (**raw=nil**, ok=true), `IsKindOf("Workshop")=true`, `base_max_workers=12`
* **control** `Diner#1475` — `IsKindOf("Service")=true`, `base_max_workers=2`

### The four raw numbers, and the round trip

| | baseline | law ACTIVE | after `:Deactivate()` |
|---|---|---|---|
| **subject** `max_workers` | `nil` (base 12) | **6** | **12** |
| **subject** workers/shift | 12 / 12 / 12 | **6 / 6 / 6** | 12 / 12 / 12 |
| **subject** `performance` | **127** | **131** | 129 |
| **control** `max_workers` | `nil` (base 2) | **1** | **2** |
| **control** workers/shift | 2 / 2 / 2 | **1 / 1 / 1** | 2 / 2 / 2 |
| **control** `performance` | **114** | **268** | 124 |

**Both members took the identical cut** — `modifications.max_workers{amount=0
percent=50 min=0 max=20}` was read back on subject *and* control. Only the
control was paid back for it.

⇒ **The control's performance rose ×2.35 (114 → 268) while the subject moved
+3% (127 → 131).** By this entry's own arithmetic that is the **~200 uplift**
branch: the four Workshops are **MISSING AN UPLIFT**, not carrying a penalty.
The player-visible harm is the staffing cut with nothing to offset it — output
rides workers × performance and only the worker term moved, so those four lose
roughly **half their throughput** when the law passes.

**⭐ The revert is what makes this a measurement rather than a coincidence.**
`114 → 268 → 124`: the control tracks the law state and **reverses on command**.
Drift does not undo itself when a law is deactivated. Meanwhile the subject sat
inside a 4-point band (127 / 131 / 129) across the entire cycle while losing and
regaining half its workforce.

**A third, unplanned control the owner supplied.** Their infopanel screenshots
also caught `Metals Extractor` at **120** — a building **not on the
`ServiceBuildings` label at all** — sitting beside the Workshop's 131 while
`MegaMall` read 241 and the Diner 268. The Workshop reads like a building that
received *nothing*, which is exactly the claim.

**⚠️ The subject's +4 is not a partial uplift.** `OnModifiableValueChanged`
fires the *overflow* workers (`Workplace.lua:378-390`), so the survivors skew to
the stronger ones and per-worker performance ticks up slightly. Stated as
inference, not measurement.

### Owner verdict, verbatim (attended half)

> *"more than double when I reverted it dropped to 124 for the dinner"*

### Labels, disclosures and limits

* **FORCED:** `LawDefs.Policy_Automation_ServiceAutomation:Activate()` and
  `:Deactivate()` called directly — first execution for this project, both
  pcall-printed (`ok=true result=true`), liveness witnessed by reading
  `ActiveLaws` back (`table:…` truthy after enact, `nil` after revert). The
  Assembly vote flow was skipped; the effect path is identical
  (`Legislature:EnactLaw` → `LawDefs[law_id]:Activate`).
* **ORGANIC:** the colony, its staffing, and the owner's decision to activate
  all three shifts and fly in another passenger rocket.
* **Setup cheats disclosed** (rule 12, owner verbatim): `CheatFill` ×6 and
  `CheatRepair` — *"This is a minimal colony it will take significantly more
  time if I need to build infastructure (time) to feed, repair, build and
  maintant everything for the colony those are the reasons"*. Colony upkeep
  only. The 1:29 `CheatFill` is why the workshop held `Stored Electronics` and
  read `working=true` at the enact — a **precondition** of the reading, not the
  reading, which measured `max_workers` and `performance`.
* **PRE-RELOAD ONLY** (R4). This is a live in-memory effect, not a persistence
  claim, so no save/reload round trip was taken and none is owed.
* **⚠️ A prep prediction that did NOT hold.** The 08-11 section above says the
  fired workers "do **not** snap back". They did — both buildings read full
  worker counts immediately after `:Deactivate()`. Recorded rather than dropped.
  ⛔ Harness gap: `CP15.C39Revert` takes no `CP15.Now()` line, so the log cannot
  establish whether the game was still paused at that read; the pause state at
  revert is **not** evidenced.
* **⛔ Not claimed:** only **one** of the four named Workshops existed in this
  colony (`TVStudioWorkshopCCP1`). `ArtWorkshop`, `VRWorkshop` and
  `BioroboticsWorkshop` were **not** observed — they share the
  `Workshop = {ElectricityConsumer, Workplace}` chain, so the same result is
  *expected*, but it is inferred for those three, not measured. Reachability by
  an ordinary Assembly vote is still unread. NOT `tested`.
* **⚖️ The repair is a NEW owner decision** (chain scope fence: C39 is an
  OBSERVATION). Nothing is built on this entry.

## ⚖️⚖️ 2026-08-12 — REPAIR DECIDED: extend the compensation (owner ruling after a full walkthrough), scope widened by the owner's own questions

**Ruling: option (a) — extend the compensation to the mismatched buildings,**
after the owner probed three angles and each one sharpened the record:

1. *"Does it also affect other non-service buildings — food, extractors?"* —
   No: the law's only effect targets the `ServiceBuildings` label (their own
   Metals Extractor screenshot at an unchanged 120 was the live control).
   **But the question found a sibling**: `Policy_Automation_FactoryAutomation`
   exists with the identical shape (`LawEffectModifyLabel{FactoryBuildings,
   max_workers, −50%}`, `LawDef-Technology.lua:70-76`, Src-read 2026-08-12),
   and the compensation gate names three classes (Factory / ResearchBuilding /
   Service, `Workplace.lua:205-207`) so a Research sibling is expected. **The
   Factory and Research labels have NEVER been swept for out-of-class
   members** — the build must enumerate all three labels and cover every
   mismatch found, not just the four Workshops.
2. *"What does raising Workshop performance actually hit — consumption?
   balance?"* — `performance` appears exactly once in the Workshop class: the
   shift-end Comfort payment (`ArtWorkshop.lua:24`). Consumption scales with
   the STAFFING FRACTION (`#workers/max_workers × consumption_amount`, `:35-39`)
   and is untouched by performance — and effectively untouched by the law too
   (a full reduced shift is still 100% staffed). The fix restores exact
   Comfort-output conservation (12×~6.3 → 6×~12.7), i.e. the same treatment
   the Diner already gets. Balance exposure ≈ nil.
3. *"Weren't the Workshops exempted deliberately, to stay employment sinks?"*
   — The premise inverts the measurement: they are NOT exempt from the cut
   (12→6, measured); they only miss the payback. The theory would predict
   keeping them OFF the label. Recorded as the intent fork it is: the
   **delabel repair** (remove the four from the law's reach; requires
   enumerating every other `ServiceBuildings` consumer first) was put to the
   owner explicitly and **DECLINED** in favour of the dev comment's stated
   intent ("tries to reverse its effect so that the overall performance is
   maintained").

**Build shape (queued into the next unattended build chain):** design to be
settled there against FIX_POLICY §1 — the compensation is an inline
`law_scale` inside `Workplace:GetWorkshiftPerformance` (`:204-218`), so the
candidates are a chained post-wrapper scaling the result by the recomputed
`law_scale` for label-carrying/uncompensated buildings (degrades gracefully;
⚠️ deviates from the inline placement by scaling the overtime additive too —
disclose) vs a §1.5 body copy widening the `:205-207` gate (exact placement;
patch-rot exposure on a hot method). ⭐ The runtime discriminator for
"mismatched" is directly readable: such a building carries the law's
`modifications.max_workers{percent=50}` while failing all three `IsKindOf`
gates. **Verification:** re-run the exact paused C39 bracket on a staged copy
of `CP15PT15.savegame.sav` (it holds the measured `TVStudioWorkshopCCP1`) —
subject must now ride toward the ~2× uplift the control showed, and revert
must restore it. Static label sweep results land on this entry.

## ⭐⭐ 2026-08-15 — THE STATIC LABEL SWEEP RAN, AND THE MISMATCH LIST IS **EIGHT** FAMILIES ACROSS **TWO** LABELS (`unattended-3` prompt 01, Job 1)

**Job 0 re-derivation first — every citation this entry carries was re-read at
Src 1.0.7.396349 and all of them hold.** SOURCE: the gate
`Workplace.lua:205-207` with the dev comment at `:209-210` (verbatim as quoted
here), the `law_scale` block `:209-218`, the worker loop `:220-228`, the
Comfort route `ArtWorkshop.lua:24`. Two additions to the record, both SOURCE:

* ⭐ **The expected Research sibling EXISTS and is now named.**
  `Policy_Automation_ResearchAutomation`, `LawDef-Technology.lua:149-223` —
  `LawEffectModifyLabel{ Label = "ResearchBuildings", Prop = "max_workers",
  Percent = -50 }` at `:151-158`, `PresetParamPercent
  automation_workforce_reduction = -50` at `:212-217`. The 08-12 section
  recorded it as "expected"; it is now read. **All three laws share the
  `Automation` policy slot, so at most one can be active at a time.**
* ⚠️ **An asymmetry worth recording, harmless today.** The Service and Research
  effects carry `param_bindings { Percent = "automation_workforce_reduction" }`;
  **the Factory effect does NOT** (`:70-76`). All three still declare the
  `automation_workforce_reduction` Parameter, which is what
  `GetParameterValue` reads in the compensation, so the compensation arithmetic
  is identical for all three. (There is also a
  `SavegameFixups.Policy_Automation_FactoryAutomation_Effect`,
  `Laws.lua:722-734`, that re-instances that law's effects on old saves — see
  the discriminator note below, which is why it is harmless for us.)

### Method — a class-graph resolution, not a grep

Every `DefineClass` in `Lua/`, `CommonLua/`, `DLC/` and `Data/` (4,013 classes)
was parsed into a parent graph; every `label1..label5` in every
`Lua\BuildingTemplate\*.generated.lua` plus the one DLC template
(`DLC/thomas/.../LawOffice`, which carries none of the three) was collected; and
each label member's full ancestor chain was resolved against the three gate
classes. The script stayed in scratch; **every row below was then re-read by
hand at its declaring file**, which is what makes the table citable per rule R2
(per-row, never blanket).

### ⛔ THE COVERAGE CONTRACT — the eight the build must cover

Carries an automation label · IS a `Workplace` · fails **all three**
`IsKindOf` gates. Each row's chain re-read at source (SOURCE, per row).

| label | template | chain (verified at source) | what the missing uplift costs |
|---|---|---|---|
| ServiceBuildings | `ArtWorkshop` | `ArtWorkshopBase` → `Workshop = {ElectricityConsumer, Workplace}` (`ArtWorkshop.lua:3-6, :81-83`) | Workshop Comfort payment (`ArtWorkshop.lua:24`) |
| ServiceBuildings | `BioroboticsWorkshop` | `BioroboticsWorkshopBase` → `Workshop` (`BioroboticsWorkshop.lua:2`) | same |
| ServiceBuildings | `VRWorkshop` | `VRWorkshopBase` → `Workshop` (`VRWorkshop.lua:2`) | same |
| ServiceBuildings | `TVStudioWorkshopCCP1` | `TVStudioWorkshop` → `Workshop` (`TVStudioWorkshop.lua:6`) | same **+ TV-show progress**, `MulDivRound(self.performance * ProgressPerDay, dt, …)` (`TVStudioWorkshop.lua:100`) |
| ServiceBuildings | ⭐ **`SecurityStation`** | `SecurityStationBase = {ElectricityConsumer, Workplace}` (`SecurityStation.lua:1-7`) | **neutralised renegades**, `MulDivRound(self.performance, self.negated_renegades, 100)` (`SecurityStation.lua:13`) |
| ServiceBuildings | ⭐ **`SecurityPostCCP1`** | `SecurityPost` → `SecurityStationBase` (`SecurityPost.lua:4`) | same |
| FactoryBuildings | ⭐ **`DroneFactory`** | `DroneFactoryBase = {Building, ElectricityConsumer, Workplace}` (`DroneFactory.lua:3`) | **drone/android build time**, `perf_penalty`/`perf_bonus` (`DroneFactory.lua:73-78`) |
| FactoryBuildings | ⭐ **`BottomlessPitResearchCenter`** | `{Building, ElectricityConsumer, Workplace, WaypointsObj}` (`BottomlessPitResearchCenter.lua:1-7`) | **resources processed into RP** (`BottomlessPitResearchCenter.lua:45`) |

⭐ **Four of these were never on any list in this project.** This entry's "the
mismatch is exactly four buildings" was true only of the `ServiceBuildings`
label's Workshop chain — it had never looked at the Security family, and the
`FactoryBuildings` label had never been swept at all. The owner's own 08-12
question ("does it also affect other non-service buildings?") is what widened
the sweep, and it found them.

**`ResearchBuildings` has NO Workplace mismatch.** `LowGLab`, `ResearchLab` and
`ScienceInstitute` all resolve to `ResearchBuilding` (`ResearchLab.lua:1-3`).

### The other 26 members, and why none of them is a mismatch

* **22 are correctly compensated** — every remaining `ServiceBuildings` member
  reaches `Service`, every remaining `FactoryBuildings` member reaches
  `Factory` (`Factory.lua:1-3`), the three research labs reach
  `ResearchBuilding`.
* **4 are not `Workplace`s at all, and are excluded BY CONSTRUCTION rather than
  by a list**: `Amphitheater` (`AmphitheaterBase → Service`), `OpenAirGym` and
  `TaiChiGarden` (`FitService → Service`), and `MagneticFieldGenerator`
  (`MagneticFieldGeneratorBase → OutsideBuildingWithShifts → ShiftsBuilding`).
  **`max_workers` is declared ONLY on `Workplace`** (`Workplace.lua:5`) and
  `ShiftsBuilding` does not declare it (`ShiftsBuilding.lua:1-13`), so the law's
  label modifier lands on a property they do not have: no staffing cut to
  compensate, and `GetWorkshiftPerformance` never runs for them.
* **No runtime label additions exist.** Membership comes only from
  `label1..label5` via `ApplyCustomLabels` (`Building.lua:370-383`); a grep for
  the three label strings over `Lua/`+`Data/`+`DLC/` finds no `AddToLabel` with
  any of them. `EffectDeposit.lua:217` sets a *modifier* on the
  `ResearchBuildings` label (not membership) and `Laws.lua:104` derives a
  *different* label (`ResearchBuildings_NearSensorTower`).

⇒ **Nothing is ROUTED under the chain README's stop condition** — every
mismatch the sweep found is a `Workplace` the ruled shape reaches.

## ⚖️ 2026-08-15 — DESIGN SETTLED: a THIRD candidate, and it dominates both the ones this entry carried (prompt 01, Job 2)

Settled against `FIX_POLICY.md` §1. Both recorded candidates were rejected,
each for a reason this entry itself half-named:

* **(a) post-wrapper scaling the whole result by `law_scale`** — the entry
  flags that it also scales the overtime additive and says "disclose".
  Rejected because that is avoidable, **and because it is not exact even for
  the worker term**: vanilla's per-worker rounding is not linear in
  `law_scale`. Worked example, computed from the shipped arithmetic — six
  workers at performance 100/90/80/70/60/50 in a 12→6 building:
  vanilla-uncompensated **76**, vanilla-compensated **151**, but
  `76 × 2 =` **152**. Off by one, in the same direction, on every
  heterogeneous shift.
* **(b) §1.5 body copy widening the `:205-207` gate** — rejected on §1.4b's own
  warning, in exactly the shape it warns about: a copy of a hot method destroys
  any other mod's wrapper on it, and **if an official patch ever fixes the gate
  the copy silently reinstates the broken one**.
* ⭐ **(c) SHIPPED — a §1.4 chained post-wrapper that adds the EXACT
  compensation DELTA.** It reconstructs only vanilla's per-worker loop
  (`Workplace.lua:220-228`) twice — once at the `law_scale` the building should
  have had, once at the `law_scale = 100` it actually got — and returns
  `orig(…) + (compensated − uncompensated)`. The uncompensated term is
  arithmetically identical to the loop term already inside `orig`'s answer, so
  the sum **equals what vanilla would have produced had the gate been right —
  exactly, including its rounding**; and every additive that follows the loop
  (overtime, RemoteFarming, anything a future patch adds) rides through on
  `orig`'s result **untouched**. Both of (a)'s costs vanish and (b)'s
  compatibility cost is never paid.
  ⚠️ **§1.5 reconstruction disclosure:** the copied loop is ~6 lines and must be
  re-checked against `Workplace.lua:220-228` on every game update. The shipped
  method itself still runs in full on every call.

**The runtime discriminator is the game's OWN bookkeeping — no template list
ships.** SOURCE: `LawEffectModifyLabel:OnStart` registers the modifier under
**the effect object** as its id (`ClassDef-Factions.generated.lua:2039-2059`),
and `ContinuousEffectsPreset:CreateInstance` gives each activated law its **own**
effect instances (`CommonLua\Classes\ContinuousEffect.lua:152-163`), so the
authoritative id lives on `ActiveLaws[<id>].Effects`, **not** on
`LawDefs[<id>].Effects` — which is also what makes the Factory savegame fixup
harmless. A building is mismatched iff it carries one of those modifiers on
`max_workers` **and** fails all three gates. Two consequences, both deliberate:
a future patch that widens the gate or moves the law off labels makes the module
stop matching (it becomes a no-op instead of fighting the fix), and a building a
patch or DLC adds to one of these labels is covered without a code change.

**Guards, each mirroring one of vanilla's own early returns** (`:185-202`), so
the delta is never added to an answer that never contained a loop term:
`IsShroudedInRubble` · `automation > 0` · `max_workers == 0` · empty shift ·
`law_scale == 100`. Plus one shape the ruling does not cover — a building
carrying **two** active automation laws' cuts — where the module **declines
rather than guesses**. (No shipped template carries two of these labels and all
three laws share one policy slot, so that guard is defence in depth.)

**§3a save-safety tier: Layer 2 by construction, NOTHING PERSISTED.** The
wrapper is synchronous with no blocking call, so no frame of ours can sit below
a `Sleep`/`WaitMsg` on a game-time thread (route (a) closed by construction); it
allocates nothing, stores no function value on any object (route (c)), and
creates no modifier, GameVar or thread. It lives in a class table, which
savegames do not serialise. Removing the mod restores vanilla on the next load
with nothing to clean up.

### What was BUILT

* `Code/Fix_AutomationLawCompensation.lua` — id `AutomationLawCompensation`,
  registered in `metadata.lua` `code` immediately before `90_SaveSanitizer.lua`.
  Self-checks (`SMRFixPack.Require`): the declaring class for
  `Workplace.GetWorkshiftPerformance`; `Shroudable.IsShroudedInRubble` (checked
  on **Shroudable**, not Workplace — the F64 declaring-class lesson);
  `MulDivRound`; `Max`; and that `max_workers` is still a *modifiable* Workplace
  property, read off the classdef's own `properties` list (the
  `Fix_UniversityOvertraining` donor — property defaults are not on the class
  before flattening). The install is read back and the module returns a reason
  string if the class write did not land, so a `ListFixes()` line reading
  `active` **is** the install evidence.
* TestKit `Code/59_Probes_Wave10.lua`, probe `AutomationLawCompensation`
  (`[behavior]`), registered in the kit's `metadata.lua`. It drives the exposed
  pure halves (`SMRFixPack.defs.AutomationLawCompensation.compensation_delta` /
  `.find_automation_law` — the F97 `gate_descriptor` donor pattern, with
  `ActiveLaws` lifted out so no GameVar is stubbed and the probe runs even at
  the main menu). **Two hard numbers, derived by hand from the shipped
  arithmetic on the MEASURED subject's shape** (`base_max_workers` 12, cut to 6,
  full reduced shift): a six-worker shift at performance 100 gets **+100**
  (uncompensated 100 → compensated 200, i.e. the ~2× the Diner control was
  measured riding to), and a one-worker shift gets **+15** (35 − 20), which is
  what pins the remainder-to-the-first-worker rule. Nine negative controls must
  read 0. Both constants assume `/` truncates in engine Lua — re-derived here
  three ways (`EF-005`; the `%`-remainder construction is meaningless
  otherwise; and `MulDivRound` is declared `int MulDivRound(int, int, int)`,
  `LuaSharedLib.lua:501-507`) — and on a float-division build the probe would
  FAIL loudly rather than pass wrongly.

### ⛔ NOT CLAIMED, and what prompt 02 owes

* **No suite run and no in-game reading exist yet.** Status is `built`, not
  `fixed`. The verification the ruling itself specified — the paused bracket on
  a staged copy of `CP15PT15.savegame.sav`, subject riding toward the ~2× the
  control showed, revert restoring both — is prompt 02's job 2.
* **Only `TVStudioWorkshopCCP1` is MEASURED.** The other seven families are
  covered by the same discriminator and the same arithmetic, but the claim that
  each is currently harmed is SOURCE, not measured — as it already was for the
  three unbuilt Workshops, and now for four more.
* ⚖️ **The footprint grew past the ruling's picture, and that is ROUTED, not
  assumed** — checklist item 30. TAKEABLE-WHEN: any time, reading only; no
  colony, no launch.

## ⭐⭐ 2026-08-15 — THE PAUSED BRACKET RAN ON A STAGED `CP15PT15` COPY AND THE REPAIR IS MEASURED WORKING (`unattended-3` prompt 02, unattended)

The verification the 08-12 ruling itself specified, unchanged: stage a
`Copy-Item` of `CP15PT15.savegame.sav`, enact
`Policy_Automation_ServiceAutomation`, read paused before / active / after.
Retail `Mars.exe` **1.0.7.396349**, staged copy `U3C39BR.savegame.sav` (MD5
`D2887D754C44134141B6CCC4C9020446`, byte-identical to `CP15PT15` before AND
after the run — this leg never calls `SaveGame`), map
`BlankBigTerraceCMix_10`, colony day 86. Gate read **`fix pack present: 76/76`
+ `opt-in pack present: 8/8`**, both `DistressPopupPause` and
`AutomationLawCompensation` reading `active` with an empty `detail`.
**0 `[LUA ERROR]` lines in the whole log.** Log
`docs/archive/u3c39_Mars.exe-20260815-01.36.41.log` (byte-verified).

⭐ **The bracket is PAUSED at every point, and this time that includes the
revert** — the one recorded harness gap in the 08-11 observation was that
`CP15.C39Revert` took no clock line. All four reads land at
`GameTime()=61108784`, `speed=0x`, `UIColony.day=86 hour=2`. **Drift cannot
explain any number below.**

### The reading — subject rides to 2.0× where it rode to 1.03× unfixed

| | `TVStudioWorkshopCCP1#1526` (subject, outside the gates) | `Diner#1475` (control, inside them) |
|---|---|---|
| `IsKindOf` | Workplace **true**, Service **false** (raw `nil`), Factory false, ResearchBuilding false | Workplace true, Service **true** (raw `true`) |
| before | `max_workers` 12, shifts 10/10/10, **performance 102**, our delta **0** | `max_workers` 2, shifts 2/2/2, **performance 130**, our delta **0** |
| law active | `max_workers` **6**, shifts 6/6/6, **performance 253**, our delta **+127** | `max_workers` **1**, shifts 1/1/1, **performance 228**, our delta **0** |
| after revert | `max_workers` 12, shifts 6/6/6, performance 64, our delta **0** | `max_workers` 2, shifts 2/2/2, performance 115, our delta **0** |

Both `performance` (the cached field) and a direct
`GetWorkshiftPerformance(3)` call agree at every read, and the log carries
`vanilla-would-have-said` = the live answer minus our delta.

* ⭐ **THE HEADLINE: 126 → 253.** The subject's uncompensated answer under the
  law is **126** (the log's `vanilla-would-have-said`); with the repair it is
  **253**. That is **2.008×** — the missing uplift, paid. Against 2026-08-11's
  unfixed bracket on the same subject (`127 → 131`, **1.03×**) the sign, the
  size and the direction all match what the entry predicted.
* ⭐⭐ **THE CONTROL DID NOT MOVE, and that is the load-bearing negative
  result.** `Diner#1475` rode `130 → 228` — vanilla's own compensation, exactly
  as it did unfixed on 08-11 — and **our delta on it read 0 at all four
  reads**. The failure mode this build was chosen to avoid (paying twice a
  building the shipped gate already pays) did not occur on a live object.
* ⭐ **THE REPAIR STANDS DOWN WHEN THE LAW DOES.** Our delta is 0 before the
  enactment and 0 again after the revert, on every object read. It is a
  correction to an active law's effect, not a standing bonus.
* ⚠️ **The delta is 127, not the probe's 100, and that was predicted in
  writing before the run.** The +100 constant assumes six *identical* workers
  at performance 100; a real colony's workers differ, and
  `MulDivRound(Max(worker.performance, 25), …)` scales per worker — a shift
  averaging ~127 performance yields ~127. The prompt's own prediction said
  "expect the same shape, not that literal number".
* ⚠️ **After-revert is not the before reading, and is not a second defect.**
  Enacting fires the overflow workers (`Workplace.lua:378-390`); with the game
  paused `CheckWorkForUnemployed` cannot re-hire at all, so the subject sits at
  6 workers in 12 restored posts and reads 64. The 08-11 control showed the
  same shape (`114 → 268 → 124`). ⚠️ Noted, not explained: the *control's*
  shifts did return to 2/2/2 while the subject's stayed at 6/6/6 — recorded as
  an observation, no mechanism claimed.

### What this colony could and could not sample — an absence BY NAME

`UIColony.labels` walked this load: **`ServiceBuildings` = 6 members**,
**`FactoryBuildings` = 0**, **`ResearchBuildings` = 0**. Of the six, five reach
the compensating branch and **exactly one does not** — the subject. So:

* ⛔ **Only `TVStudioWorkshopCCP1` is MEASURED, exactly as the build section
  said.** `ArtWorkshop`, `BioroboticsWorkshop`, `VRWorkshop`, `SecurityStation`,
  `SecurityPostCCP1`, `DroneFactory` and `BottomlessPitResearchCenter` are
  absent from this colony; their coverage stays **SOURCE** (the static sweep)
  and is not upgraded by proximity. The two `FactoryBuildings` families could
  not be sampled at all — that label is empty here.
* ✅ **One of the four excluded-by-construction members WAS sampled**, and it
  reads as the entry predicts: `OpenAirGym#1476` returns
  **`IsKindOf("Workplace") = false`** with `base_max_workers`/`max_workers`/
  `performance` all `nil`. The exclusion is now measured for that member
  instead of derived.

### ⚠️ Harness defect found in this leg's own instrument — reported, not buried

The payload's per-family tally used `rawget(b, "class")`, which returns `nil`
for a class field that lives on the metatable, so **every one of the eight
`C39SCAN family … :: 0 present` lines in the log is wrong by construction** —
including the subject's own family, which the same log then reads in full three
lines later. The correct population is the one above, taken from the scan's
per-member lines and the mismatch list (both of which use `obj.class` and are
unaffected). ⛔ **Do not quote the `family ::` lines.** Same shape as the
unattended-1 ledger's "a count that reads 0 for a reason unrelated to the
population"; it changed no verdict here only because a second, independent
reading of the same fact was in the log.

### Status — `fixed`, and what that word does and does not carry

`built` → **`fixed`**: the suite ran in a real launch with the probe PASSing
(`80 PASS / 0 FAIL / 16 SKIP / 0 ERROR` of 96,
`archive/u3suite_Mars.exe-20260815-01.32.33.log`), the module's own
`Require` self-checks passed (`active`, empty `detail`, read three times in
this leg), and the §3a tier is **Layer 2 by construction, nothing persisted** —
this run wrote no savegame at all and the staged copy is byte-identical
afterwards.
⛔ **NOT `tested`.** No keyboard witness, no organic enactment (the law was
`Activate()`d directly — the vote flow was skipped, the effect path is
identical), one measured family of eight, and no save/reload of a colony that
had the repair active. The `fixed`→`tested` campaign is frozen pre-release by
the owner's 08-12 ruling; this is the frozen bar, met.
⚖️ **Checklist item 30 is still OPEN** — the owner's confirm that the widened
footprint ships as built. Nothing here settles it, and nothing here needed it.

## ⚖️ 2026-08-15 — SCOPE CONFIRMED BY THE OWNER: ship as built, all eight families, no list (checklist item 30, CLOSED the day it was raised)

The owner challenged the framing before answering it — *"I thought we decided
on this awhile ago?"* — and they were right: 2026-08-12 already ruled "extend
the compensation" and widened the sweep on their own question (*"does it also
affect other non-service buildings?"*). Item 30 was a confirm on the SIZE of
what that sweep returned, not a re-opening, and the entry records it that way.

**The ruling, verbatim:** *"Lets go with whatever is supposed to be true to the
code, which fits this mod as a true to code bugfix as much as possible."*

### Why that principle selects the shipped build rather than a narrower one

* **The module carries no building list.** `find_automation_law` matches the
  ACTIVE law's own effect object against the building's `max_workers`
  modifiers, and `compensation_delta` evaluates the same three `IsKindOf` gates
  vanilla evaluates. Coverage is "whatever actually carries the defect",
  decided per call, on the object. The eight-family table in the §2026-08-15
  sweep is **documentation of what that resolves to on 1.0.7.396349 — it is not
  logic**, and a patch that widens the shipped gate makes this module a no-op
  rather than a fighter.
* **Restricting to the four Workshops would mean ADDING a hardcoded template
  list that does not exist today**, whose only function is to leave
  identically-broken buildings broken. Under a true-to-code principle that is
  the less faithful option and the more brittle one, not the safer one.
* **It is also faithful to the law's own printed text**, which is the other half
  of the principle. `LawDef-Technology.lua` describes Service Automation as
  *"Service buildings require **50% less workers**"* — the trade is labour and
  nothing else; there is no comfort clause in the law. The compensation is what
  makes the unstated other half ("…at the same output") true, which is exactly
  what the dev comment at `Workplace.lua:209-210` says the block is for. The
  **delabel** alternative, declined 08-12, would have made the law quietly not
  apply to those buildings — contradicting its own description.
* ⚠️ **Recorded because it was a live misremembering worth pinning:** the law
  does not trade workers *for comfort*. Comfort is merely what the four
  Workshops happen to produce from `performance` (`ArtWorkshop.lua:24`), which
  is why every 08-11/08-12 conversation about this defect was phrased in
  comfort. The four families found on 08-15 spend their `performance` on
  renegades, drone build time and research throughput instead.

### What the ruling does and does not settle

* ✅ **Settles SCOPE.** No code change, no re-run; the build stands as verified.
* ⛔ **Does NOT settle disclosure.** The store card and site fix-list must still
  say that this moves gameplay-visible numbers, because it does — prompt 03's
  Job 2. Shipping as built is not permission to describe it quietly.
* ⛔ **Does NOT upgrade evidence.** Still one MEASURED family of eight; the
  other seven remain SOURCE and the entry must keep saying so.
* ⭐ **Blast radius, from the sweep and worth carrying into the card:** all three
  Automation laws share the `Automation` policy slot, so at most one is active
  at a time — four Workshops + two Security buildings, **or** the Drone
  Assembler + Bottomless Pit. Never all eight together.
