---
id: "C71"
seq: 166
row: 201
title: "Dredger mystery still reports an RC Explorer malfunction after removing the malfunction effect"
status: "cand"
status_source: "tag"
priority: "P3"
evidence: "source-read"
row_status: "cand 2026-09-10 — DIFF-CAUSED; effect/text and anomaly-register contradiction, runtime unobserved"
updated: "2026-09-10"
copies: []
---
### C71 — the damaged-expedition branch no longer malfunctions its rover `[cand 2026-09-10: source-read; runtime unobserved]`

**DIFF-CAUSED. SOURCE intent tell:** the Dredger mystery's unlucky close-
inspection message and voiced text still say the expedition rover malfunctioned
(`Lua/Scenario/Mystery 2.generated.lua:924-930`), but 1.1.0 removed the
sequence's `rover` register and the effect call. The old sequence accepted the
anomaly scanner through `SetRegisters` (`old :868-884`) and, after this exact
message, ran `rover:SetCommand("Malfunction")` when present (`old :927-938`).
The new sequence registers only choice/funding/research/random state
(`new :862-882`) and ends the branch after funding (`:932-935`).

**SOURCE ROUTE — R2, base/non-owner:** the first Dredger spawner attaches an
anomaly whose sequence is `Anomaly 1: First Digger` (`Mystery 2.generated.lua
:414-425`). `SA_SpawnAnomaly`'s generated scan route maps the scanner as the
`rover` argument (`Lua/Sequences/SA_Gameplay.lua:2480-2491`), but the new target
sequence no longer receives it. The base Dredger mystery and RC Explorer need
no DLC.

**INFERRED impact:** on the unlucky affirmative branch, the player is told the
Explorer suffered a malfunction without the corresponding rover command. The
runtime anomaly-register handoff and actual rover status remain unobserved.

**Independent recipe / falsifier:** in a fresh 1.1.0 Dredger-mystery colony,
scan the first Dredger encounter with an operational RC Explorer, choose close
inspection, and reach the randomized message that explicitly reports a
malfunction. Record that same scanner's command/status before dismissing the
message. Vacuous if the safe random branch fires or the scanner cannot be
identified. If another shipped route malfunctions that exact rover before the
message, the candidate is false. No fix is proposed.
