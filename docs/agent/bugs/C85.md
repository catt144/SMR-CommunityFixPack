---
id: "C85"
seq: 181
row: 216
title: "A producer 'Clogged after a Dust Storm' can stay disabled for good: the story event's 'fix it after the storm' reply waits for a LATER storm end, competes with other follow-ups for it, and has no fallback"
status: "cand"
status_source: "tag"
priority: "P2"
evidence: "source-read; field reports (Steam, 2 players, 1.1.0)"
row_status: "cand 2026-09-11 — both trees; the failing link for these players is NOT pinned (two hypotheses, falsifiers below); desk only"
updated: "2026-09-11"
copies: []
---
### C85 — "Clogged after a Dust Storm" has no fallback repair (P2) `[cand 2026-09-11: source-read; failing link not pinned]`

**Field reports (INHERITED — owner screenshots, Steam "Clogged Extractor", 2026-09-09):** a Rare Metals Extractor
"clogged after a Dust Storm. … I can't seem to fix it"; a second player's Polymer factory "never recovered", and destroy +
rebuild was the only way out; the first player confirms rebuilding too. Triage record:
`reports/FIELD_LEADS_2026-09-11.md` §3.

#### What "Clogged" is (SOURCE, game 1.1.0.403908)

Not a maintenance state: the story bit `BuildingClogged` (`Data/StoryBit/BuildingClogged.lua`).

- It fires at a dust storm's start (`trigger = "DustStormStart"`, category `DustStromStart`, `:9`, `:46-50`), only through
  sol 80 (`IsSolInRange` `Max 80`, `:21-23`), on one Water / Regolith / Metals / Rare Metals Extractor, Polymer Plant,
  Fuel Factory or Fungal Farm (`:24-39`). One-time: `OneTime` defaults to `true` (`Lua/ClassDef-StoryBits.lua:28-29`).
- Its ActivationEffects disable the building with the reason "Clogged after a Dust Storm." (`:4-8`), BEFORE the player
  answers; `Delay = 60000` (`:10`).
- The replies:
  - "Send a colonist" → re-enabled at once, 70% with a colonist lost (`:51-87`).
  - The inventor-commander reply → re-enabled (`:113-127`).
  - "Have the drones replace the entire component" → a 3-Electronics malfunction that a drone repair clears (`:98-112`).
  - **"We'll fix it after the storm" → only enables the follow-up `BuildingClogged_1_FixAfterStorm`** (`:88-97`).
- The follow-up re-enables the building (`Data/StoryBit/BuildingClogged_1_FixAfterStorm.lua:4-8`) on
  `trigger = "DustStormEnd_FollowUps"` with `Delay = 30000` (`:9`, `:16-19`), and inherits the building (`InheritsObject`
  defaults to `true`, `ClassDef-StoryBits.lua:23-24`).
- **One follow-up per firing:** each trigger first tries its follow-ups (`Lua/_StoryBits.lua:838-843`), and
  `StoryBitCategoryState:TryActivateStoryBit` shuffles them and activates only the first whose prerequisites pass, then
  `break`s (`:199-216`).

**ROUTE (INFERRED):** nothing else clears the reason. Ordinary maintenance doesn't (INHERITED:
`RequiresMaintenance.lua:413-417` needs the drone reply's flag), and the building's on/off button drives a separate flag
(INHERITED). After "fix it after the storm", the building waits for a storm-end firing that this follow-up wins.

#### Hypotheses for the reports (ranked; neither pinned)

- **H1 — the repair waits for a LATER storm end, possibly several (SOURCE shape, INFERRED timing).**
  - If the reply lands after the storm is over, the next storm end is a whole storm away. INHERITED: the popup can wait up
    to the 8-hour notification timeout (`__const.lua:1813`) and the shortest storm preset lasts about 10 hours.
  - Each storm end fires only one pending follow-up (SOURCE, above), so this one can lose the pick. INHERITED competitors:
    `DomeLeaks_3_Aftermath`, `Dust_Melancholy_End`, `MissingRover_4_DustStormEnd`.
  - If terraforming has stopped dust storms (INHERITED: `TerraformingDisasters.lua:15-19`), no storm ever ends again →
    **disabled for good**.
  - Falsifier: in a stuck save, `g_StoryBitStates["BuildingClogged_1_FixAfterStorm"]` is absent, or present with a
    different object.
- **H2 — the reply is lost across a save/load (INHERITED, unverified).**
  - Story-bit popups are shown non-saveable (`MarsStoryBits.lua:90`) and only saveable popups persist
    (`PopupNotification.lua:389-397`). A save taken while the popup is up may leave the reply unprocessed — building
    disabled, no follow-up armed.
  - This is F83's SHAPE, but F83's measured scope is the `WaitPopupNotification` callers only (`bugs/F83.md`, scope
    table), NOT story bits. ⛔ Do not cite F83 as proof.
  - Falsifier: a stuck save with `BuildingClogged` still in `g_StoryBitActive` and no popup on screen.

#### Both trees (INHERITED, investigator read)

Identical logic on 1.0.7; 1.1.0 only added the reason text (1.0.7 showed a generic "exceptional circumstances" line).
INFERRED: newly legible, which may be why reports appear now.

#### The pack (INHERITED, investigator grep)

Nothing wraps the story-bit engine, the triggers or `SetBuildingEnabledState`.

#### Player-visible effect

A producer dead until rebuilt — a LOSS if either hypothesis holds.

#### Fix sketch (NOT proposed yet — H1/H2 first)

A daily + on-load sweep: re-enable a building still carrying reason `789863173059` when no follow-up is pending for it,
no `BuildingClogged` is running and no storm is active. Optionally re-enable 1 hour after the reply when no storm is
active.

#### Cheapest next step

Ask the reporters which reply they chose and whether they saved/loaded with the popup open (draft in
`reports/FIELD_REPORT_REPLIES.md`). Their save's `g_StoryBitsLog` would show it (INHERITED: saved in release builds).

#### Not opened

`RandomEnumArray` / `ArrangeStoryBits`' ordering; engine persistence of game-time threads blocked on a popup (decides
H2); `PopPopupNotification`; the three competing follow-ups' prerequisites; storm-gap timings; the reporters' saves.
