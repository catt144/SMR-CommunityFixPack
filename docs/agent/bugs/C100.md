---
id: "C100"
seq: 198
row: 233
title: "A jobless habitat resident emigrates to any reachable dome offering a free apartment and a free job: the habitat's own no-connected-work rule makes its residents permanently need_work, which is exactly the emigration scorer's override trigger"
status: "cand"
status_source: "tag"
priority: "P3"
evidence: "⭐ OBSERVED IN PLAY 2026-09-15 (owner at the keyboard, same sitting as C95): Dermot Wilson rode the rail back to the Naturalist Habitat and moved in unaided, then LEFT AGAIN a few hours later for a different dome (Fuller #1) once he had an apartment and a job there. ⚠️ OWNER'S READING, 2026-09-16: people living in habitats are not supposed to want to leave. Mechanism SOURCE-VERIFIED on 1.1.0.403908 (Colonist:TryToEmigrate -> FindEmigrationDome -> GetBestReachableCommunities, Colonist.lua:1990/3497/3425): the scorer computes a stay-score for the current community and then accepts any reachable community where new_eval >= eval AND better_home_work, where better_home_work is satisfied by (need_work and better_work) alone - so a colonist with no workplace moves to the first reachable community with a free apartment and a free job REGARDLESS of score margin. ⛔ A habitat forbids connected work (EF-103), so its residents are structurally jobless, which is the trigger. ⛔ NOT REPRODUCED as a controlled test and the score terms were NOT computed numerically. ⚖️ WHETHER THIS IS A DEFECT OR INTENDED DESIGN IS THE OWNER'S RULING AND IS NOT MADE HERE - habitat residency is explicitly score-gated and re-evaluated (MicroGHabitatAutoResolve:IsSuitable). ⛔ Does NOT change C95's ruled repair shape"
row_status: "cand 2026-09-16 - raised by the owner from their own C95 sitting; source-verified mechanism, no controlled test; the defect-or-design question is unruled"
updated: "2026-09-16"
copies: []
---
### C100 — A jobless habitat resident emigrates to a dome offering an apartment and a job; the habitat's own isolation is what makes them jobless (P3) `[cand 2026-09-16: mechanism SOURCE-VERIFIED, NOT reproduced as a controlled test; ⚖️ defect-or-design is UNRULED and the owner's]`

⛔ **NOT A REOPENING OF [C95](C95.md).** C95's repair shape is ruled and scoped to the expedition
draft. This is a **second, continuous drain by a different route**, and recording it does not widen
that fix. ⚠️ C95's line *"a habitat is drained one resident per expedition"* is, on this reading,
only half the story — but the fix it names still does exactly what the owner ruled.

⚖️ **UNRULED, AND DELIBERATELY SO: is this a defect at all?** The owner's reading is that habitat
residents are not supposed to want to leave. Against that, habitat residency is **explicitly
score-gated and re-evaluated** — `MicroGHabitatAutoResolve:IsSuitable` is `GetScoreFor(colonist) > 0`
(`MicroGHabitat.lua:173-175`), so the design does contemplate people moving out. ⛔ **Do not author a
repair, and do not classify this, until the owner rules.**

#### The observation (owner at the keyboard, 2026-09-15)

From the same sitting that reproduced C95. **Dermot Wilson**, unemployed, *"Not looking for a job"*:
taken on the `Project Solar` expedition, re-homed into a dome on return, **rode the passenger rail
back to the Naturalist Habitat and moved in on his own** — then **left again a few hours later** for
a different dome (`Fuller #1`), once he had an apartment and a job there.

⇒ The habitat did not fail to hold him for want of reachability or space. **He re-qualified, moved
in, and the game then moved him out again.**

#### The mechanism, SOURCE-VERIFIED on 1.1.0.403908

`Colonist:TryToEmigrate` runs for every colonist on the heavy-update tick (`Colonist.lua:2349`,
gated by `next_heavy_update`), so this is **continuous**, not event-driven. It calls
`FindEmigrationDome` (`:3497`) → `GetBestReachableCommunities` (`:3425`).

That function builds a **stay-score** for the colonist's current community and then walks every
reachable one. The acceptance test is the part that matters:

```lua
local better_work = not work_available and new_work_available
local better_home_work = better_home or (need_work and better_work)
local better_eval = beat_threshold and new_eval >= eval or new_eval > eval
if better_eval or better_home_work then
```

with `need_work = can_work and not IsValid(self.workplace) and not self.user_forced_workplace`.

⭐ **`better_home_work` is an OVERRIDE, not a tiebreaker.** A destination needs only
`new_eval >= eval` (the outer guard) plus a free job the current community does not offer. **No
score margin is required.**

⇒ For a habitat resident:

| term | value, and why |
|---|---|
| `need_work` | **true** whenever they hold no workplace |
| `work_available` (stay side) | **false** unless a free *outside* workplace sits within `GetOutsideWorkplacesDist()` of the habitat — a habitat has `allow_work_in_connected = false`, so dome jobs are barred outright (`EF-103`, `Workplace.lua:1446-1462`) |
| `new_work_available` (dome side) | **true** for any dome with a free job |
| `new_home_available` | **true** when that dome has a free apartment |

⇒ ⭐⭐ **The habitat's defining rule is the thing that supplies the trigger.** It forbids connected
work in order to keep its residents apart from the colony; that keeps them jobless; and joblessness
is precisely what the emigration scorer treats as grounds to move them. ⚠️ **Whether that is an
oversight or the intended cost of the building is the question above, and it is not answered here.**

#### ⭐ Control — what would falsify this, in play

⛔ These are discriminators, not confirmations. Run them before treating the mechanism as live.

| observation | verdict |
|---|---|
| a habitat resident **with** a job (an outside workplace near the habitat) stays put, while a jobless one leaves | ⭐ the mechanism holds: `need_work` is the driver |
| a **working** habitat resident also leaves | ⛔ **refuted** — `better_work` is not what moved them; look at `better_eval` (the score margin) instead, and this entry's headline is wrong |
| a jobless resident stays while the only reachable dome has an apartment but **no free job** | ⭐ consistent: `better_work` needs a free job at the destination |
| a jobless resident leaves for a dome with **no** free apartment | ⛔ refuted — `new_home_available` gates the branch, so something else is moving them |

**Desk falsifier.** `GetBestReachableCommunities` (`Colonist.lua:3425-3494`) must still contain the
`better_home_work = better_home or (need_work and better_work)` short-circuit. **Stale if** a patch
makes the branch require a score margin, or gives `MicroGHabitatBase` its own override.

⚠️ **A fixture note that decides the test:** the habitat must be reachable from the dome for the
colonist to have come back at all, and the test colony must have a free apartment **and** a free job
in that dome — the owner's did. ⛔ A forced setup that intersects the mechanism (mass employment
reassignment) invalidates the reading; see `EF-104`.

#### Not opened

⛔ **Read this list before quoting anything above as measured.**

- **The score terms were never computed.** `Community:GetScoreFor` (`Community.lua:442-460`) is
  life support + `TraitFilterColonist(self.traits_filter, …)` + the best comfort among residences
  whose `filter_residents ~= "Everyone"`. `MicroGHabitatBase` sets **no** `traits_filter`, so that
  term is inferred to contribute nothing, and `max_comfort` for a habitat was **not read at all**.
  ⇒ "the habitat's stay-score is thin" is **INFERRED**, not measured.
- **That `self.dome` is the habitat for its residents is INFERRED**, from `Colonist:UpdateResidence`
  indexing `self.dome:ChooseResidence(self)` (`:2919-2926`) together with the habitat's own
  `ChooseResidence` override (`MicroGHabitat.lua:161-171`). ⛔ Not observed in play. If `dome` were
  false, `my_dome` would be nil, no stay-score would be computed at all, and the defect would be
  **worse** than described here, not absent.
- Whether `Workforce.HasFreeWorkplacesAroundForSpecialist` on a habitat honours
  `GetOutsideWorkplacesDist()` — the habitat declares no override, and `Dome`'s version
  (`Dome.lua:3521`) walks `GetResidentWorkDomes()`, which a habitat does not have. **Unread.**
- Whether a habitat appears in its own `labels.Residence`, which the comfort term iterates.
- `CanAcceptNewColonists()` on the destination, and whether `emigration_dome` ping-pong protection
  (`:3463-3465`) interacts with a habitat.
- Whether the same path explains **Oberon Cosmos**, who took a `Mega Mall` job and never returned —
  he never re-entered the habitat, so his case is C95's re-homing, not this.
- Frequency: `next_heavy_update` cadence was not read, so "continuous" is qualitative.
