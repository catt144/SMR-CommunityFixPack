---
id: "C102"
seq: 200
row: 235
title: "Expedition returnees are marched to a switched-off dome and suffocate: the return path hands ChooseDome the distance-only safety_dome fallback, and our F53/C83 repair of that fallback is keyed on new arrivals only"
status: "cand"
status_source: "tag"
priority: "P2"
evidence: "INHERITED 2026-09-16, the C95 reporter (Ordo_Liberal), verbatim in C95: 7 habitat residents returned from an expedition, 'tried to enter the closed dome and started suffocating'. No log, save, version or mod list held. SOURCE-VERIFIED the same day on 1.1.0.403908: CargoTransporterNew:UnloadPassengers (CargoTransporterNew.lua:1080-1094) and legacy RocketBase:Disembark (RocketBase.lua:1969-1988) call ChooseDome(unit, domes, safety_dome, dome_elevators); GetDomesReachableByColonists (_GameUtils.lua:390-412) picks safety_dome as the nearest can_be_safety_dome community by distance with NO is_welcoming_community test (that test gates only the domes list); ChooseDome (:486-501) starts from best_dome = safety_dome and replaces it only with a listed dome that has free living space; Colonist:ReturnFromExpedition (Colonist.lua:5076-5125) then issues TransportByFoot to it directly. OURS BY OMISSION: Code/Fix_ArrivalDeaths.lua repairs this exact fallback (F53, C83) inside a Colonist:Idle pre-wrapper gated on self.arriving. CORRECTED 2026-09-17, exploration 194f4c3: returnees DO set arriving in ColonistTransport.lua:204-207; the missing coverage is Idle-only rechecking. Direct return dispatch bypasses Idle and may book a native train (:381-502). The unshipped C95 home selector avoids the unsafe fallback for a valid reachable held habitat only; it does not close C102 generally. NOT REPRODUCED; which condition emptied the reporter's candidate list is unknown"
row_status: "cand 2026-09-16 - triaged from the C95 reporter's follow-up during the C95 audit; source-verified on both return receivers; not reproduced; the sibling path of F53/C83"
updated: "2026-09-17"
copies: []
---
### C102 — Expedition returnees are sent to a switched-off dome and suffocate (P2) `[cand 2026-09-16: SOURCE-VERIFIED on both return receivers; NOT reproduced; the reporter's trigger condition NOT isolated]`

⛔ **NOT A REOPENING OF [C95](C95.md).** C95 stops habitat residents being drafted. This is what
happens to **any** returnee — habitat resident or not — when the return path finds no welcoming
community with a free bed in walking range of the rocket. The reporter's words are in C95.

#### ⚖️ RULED 2026-09-17 — the fallback is the nearest SAFE dome

⚖️ **Owner's words**, asked what a returnee should do when their own home cannot be used:
*"Then it should be nearest safe dome. But with our fix extended so that they don't pick an
unpowered dome with no life support."*

⇒ **The repair shape is settled: extend the F53/C83 repair in `Fix_ArrivalDeaths` to the expedition
return path**, so the fallback destination is the nearest welcoming dome and never a switched-off or
lifeless one. No waiting aboard, no refused departure, no new shelter rule. ⚠️ Two constraints the
builder inherits from the [C95 exploration](../reports/C95_RETURN_HOME_EXPLORATION.md): the choice
must be corrected **before** the receiver reserves the fallback residence
(`CargoTransporterNew.lua:1091-1093`), and the return dispatch bypasses `Colonist:Idle`, where the
arrival repair lives. ⛔ **Still unruled:** a returnee with **no** welcoming dome anywhere (last
item under "Not opened"). Not built; status unchanged.

#### The mechanism (SOURCE, game 1.1.0.403908)

1. `CargoTransporterNew:UnloadPassengers` (`Lua/CargoTransporterNew.lua:1080-1094`; legacy twin
   `RocketBase:Disembark`, `Lua/Buildings/RocketBase.lua:1969-1988`) takes
   `domes, safety_dome, _, dome_elevators = GetDomesReachableByColonists(self.city, self:GetPos())`.
2. In `GetDomesReachableByColonists` (`Lua/_GameUtils.lua:390-412`) a community enters `domes` only
   if it is in walking distance **and** `is_welcoming_community` (`:382-384`: `accept_colonists`,
   `ui_working`, `HasLifeSupport`). **`safety_dome` gets neither test**: it is the nearest
   `can_be_safety_dome` community with `CanVisit()` (not destroyed) and a non-negative distance.
   A switched-off dome qualifies; so does one that is not walkable.
3. `unit:GetExpeditionReturnDome(domes)` returns the held home only if it is in `domes`
   (C95, `Colonist.lua:5047-5059`). Otherwise `ChooseDome` (`_GameUtils.lua:486-501`) starts from
   `best_dome = safety_dome` and replaces it only with a listed dome scoring above
   `CommunityEvalNone` that `HasFreeLivingSpaceFor` the colonist.
4. **Corrected 2026-09-17:** `Colonist:SetCommand` (`ColonistTransport.lua:381-502`)
   first runs ReturnFromExpedition_TransportDestination (:204-207), sets `arriving`,
   and may route by train via DisembarkOnArrival. The direct return body
   (`Colonist.lua:5076-5124`) ends in `SetCommand("TransportByFoot", dome)`.
   Neither dispatch path applies the missing welcoming test.

⇒ If held-home lookup fails and no qualifying candidate replaces the fallback,
the returnee is assigned the nearest safety dome even if it is switched off.
The command dispatcher may walk or use rail; neither adds the welcoming test.

#### Why ours does not catch it

`Fix_ArrivalDeaths` repairs this same fallback for F53 (unreachable safety dome) and C83 (dead
safety dome), in a pre-wrapper on `Colonist:Idle` gated on `local rocket = self.arriving`
(`Code/Fix_ArrivalDeaths.lua:381-383`), because Idle is the only issuer of `"Arrive"`.
**The earlier claim that a returnee never sets `arriving` was wrong.** The coverage
gap is the direct return dispatch: it bypasses Idle and the normal OnArrival
cleanup clears that field before a later Idle. The destination and its new
reservation were already chosen. See the [C95 exploration](../reports/C95_RETURN_HOME_EXPLORATION.md).
**MEASURED:** `grep -n "ReturnFromExpedition\|UnloadPassengers" Code/Fix_ArrivalDeaths.lua`
hits one line, a comment about the disembark snap (`:129`), which is a different repair.

#### Reach

Any colony that runs an expedition while the nearest dome to the pad is switched off or lifeless and
no welcoming community with a free bed is walkable from the pad. An all-habitat colony beside a
mothballed dome (the reporter's) is the natural case: habitats are `can_be_safety_dome = false` and
hold 5. Steam, Paradox and console alike; nothing platform-specific is involved.

#### ⭐ The control

- **Desk:** drive `UnloadPassengers` with `domes = {}` and a `safety_dome` whose `ui_working` is
  false; the unit must receive `ReturnFromExpedition` with that dome. Falsified if any shipped code
  between `ChooseDome` and `TransportByFoot` rejects an unwelcoming destination.
- **Play:** one rocket, one switched-off dome nearest the pad, every other community out of walking
  range or full; send a crew on a short expedition and watch where the returnees walk.
  ⚠️ Vacuous if a welcoming community with a free bed is walkable from the pad.
- **Stale if** `GetDomesReachableByColonists` applies `is_welcoming_community` to `safety_dome`, or
  `ChooseDome` stops seeding `best_dome` with it.

#### Not opened

- **Which condition emptied the reporter's list.** Their habitats could be out of
  walking range or full. The C95 exploration isolated a full nearby habitat being
  dropped even earlier by anonymous `CanVisit()`; its held resident passes the
  colonist-aware form. That narrows the possibilities without identifying this reporter's case.
- The general safe fallback repair. C95's unshipped selector recovers a valid reachable
  held habitat before the replacement reservation, but does not cover missing/invalid homes
  or ordinary dome returnees. A return-command-only wrapper is too late to preserve a
  home reference already cleared by that replacement reservation.
- The repair for a returnee with **no** welcoming destination at all. C83 preserves vanilla's
  assignment in that case; here vanilla's assignment is the lethal one.
- The reporter's game version, pack version and mod list. Not held.
