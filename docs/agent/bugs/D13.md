---
id: "D13"
seq: 110
row: 75
title: "Save-exit deliverables: uninstall procedure + standalone save-rescue artifact"
status: "tested"
status_source: "tag"
priority: "dsgn"
evidence: "high"
row_status: "⭐⭐ **`tested` GRANTED 2026-08-14 — THE ATTENDED AFTER-SWEEP RAN AND EVERY EYES-ONLY HALF PASSED** (the ONE combined sitting; owner at the keyboard; logs `archive/cs_*`). On a NATIVE witness (`SPCONRT` bytes, no manufactured residue) the automatic `PostLoadGame` pass removed **1566** entries by name — **matching a prediction committed BEFORE the sitting, row for row and skip for skip** (`6867552`) — kept the F48 latch, and healed NOTHING because nothing was broken: `heals: meteors 0, rains restarted 0, rains ended 0 | skipped 1`, the skip being the ambiguity rule declining a live `Meteors` thread. ⭐ **The two DIALOGS, ruled permanently unsampleable unattended by the terminal audit, are now WITNESSED:** the report dialog RAISED with text matching `report_text()` exactly; the owner's own save+reload of the cleaned save was **SILENT** with `save-rescue=1/1 active` beside it (so the silence means the pass found nothing, not that the mod was absent) and the second pass PRINTED its `removed 0`; and after the packs were re-enabled the stand-down dialog raised **exactly ONCE**, verbatim per spec §10.5, no duplicates. **0 `[LUA ERROR]` in every flushed cell.** ⚠️ Two things this does NOT grant: D10's KEEP condition was NOT re-sampled (no save we hold carries an F35 modifier — it stays sampled only on the 2026-08-13 declared-SYNTHETIC witness), and the rains/meteor heals were NOT exercised (this fixture has nothing to heal, which is the point). ⚠️ ONE GAP FOUND BY THE EYES, owned by step ③: **frozen spec §10.5 promises the drone-dial line carries \"(drone speed and carry capacity are back to the game's own values)\" and the built `report_text()` produces bare `N noun` pairs** — the headline residue is reported to the player as \"2 drone stat dials\" with no indication of what it means. Earlier: ✅✅ **AUDIT-SUSTAINED 2026-08-13, chain `d13-rescue` CLOSED** (prompt 5: all ten logs byte-compared + read whole, every tally recounted, exposed set re-derived a THIRD time — 27 sites, ZERO differences; corrections in `D13_VERIFICATION.md` §5, none moves a verdict; E5/E6/E7 recorded repaired-in-pack (`91fc5d0`); F103 filed; the two DIALOGS ruled unattended-UNSAMPLEABLE — they ride the attended after-sweep). **built 2026-08-13 — AND ⭐⭐ VERIFIED IN GAME 2026-08-13, THE ARTIFACT WORKS** (chain `d13-rescue` prompt 4; full record + predictions-before-the-run `agent/reports/D13_VERIFICATION.md`, 9 archived logs `archive/rs_*`). Nine unattended launches, owner cost ZERO minutes, zero `[LUA ERROR]` in any cell. On a witness carrying all 11 REMOVE names the AUTOMATIC `PostLoadGame` pass removed **1617** entries by name, **kept both KEEP names** (D10 F35 — condition SAMPLED for the first time, on a declared-SYNTHETIC witness — and D11 F48), healed a legacy rain loop and a dead `Meteors` thread, left both threads VALID, and a second consecutive load in a fresh process removed 0. Residue-zero MEASURED: 0 artifact-namespace names over 4510 objects + `UIColony` + rains entries + 440 `PersistableGlobals`. ⚠️ ONE spec clause corrected — §10.9(4)'s \"zero lines naming it\" is false: the ENGINE prints one savegame mod-reference line, a standing behaviour both packs share that self-clears on the next save. Earlier: **built 2026-08-13** (prompt 3). Derivation + disposition table + frozen spec: **`agent/reports/D13_EXPOSED_SET.md`** (27 sites over BOTH shipped trees, 12 capturable-code + 15 persisted-data, every one called; §10 is the build contract). The artifact is a THIRD MOD: `C:\\Dev\\SMR-CommunitySaveRescue` (`SMR_CommunitySaveRescue` / `SMRSaveRescue` / `[CommunitySaveRescue]`, public remote `catt144/SMR-CommunitySaveRescue`, junction installed, ⛔ **NOT enabled — one owner tick + full restart is needed before any verification leg**). 6 TestKit probes added (88→94 probes; the measured suite baseline is PENDING re-measurement). ⛔ Nothing has been run in a game: no residue-zero claim, no 'it works'. Verification is prompt 4. Earlier: directed 2026-07-31 (owner), prelaunch — **HARD LAUNCH DEPENDENCY (owner, 2026-08-01): the pack does not ship until this ships alongside it**; ⛔ **spec GATED on Tier 1/2 landing AND verifying** (target list = their output, never today's leak set — the gate is sequencing, not priority); also owns the **complete per-site disposition table** required before release (FIX_POLICY §3a) and **must DERIVE the exposed set itself rather than inherit any recorded count** (the docs carry an open lower bound, \"at least 13\", from a grep known blind to slot/global/preset assignments, and the builds have since changed the set) — **its derivation is authoritative and it updates every doc stating a count** (list on the entry); second-artifact costs + open player-story design question on the entry. ⛔⛔ **2026-08-10 (`corun-batch-2` leg T): \"OFF is three different things\" is now FOUR.** A Mod-Manager disable followed only by a return to the MAIN MENU leaves a mixed state nobody had named — `pack=81/81 active` (code and all 81 registrations live) while the mod's persisted permanent is ALREADY GONE (`Unpersist missing permanent: Mod/SMR_CommunityFixPack`, twice, in loads that follow the toggle and in none before it). **The disable needs a FULL PROCESS RESTART.** State (3), disable + restart, measured CLEAN: `pack=0/0`, 0 `[LUA ERROR]`, 0 new lines after a minute of sim. ⇒ any earlier uninstall-style reading taken without a restart measured the mixed state; **PT-20's 98-vs-98 is the named case to re-check** (owner decision, checklist) (entry)"
updated: "2026-08-13"
copies: []
---
### D13 — Save-exit deliverables: the uninstall procedure + the standalone save-rescue artifact (design, high)  `[tested 2026-08-14 — the attended after-sweep ran at the keyboard: removed 1566 by name on a NATIVE witness against a prediction committed beforehand, KEEP survived, heals 0/0/0, and all three dialog readings WITNESSED (report raised · cleaned reload silent · stand-down exactly once). 0 LUA ERROR in every cell. Logs archive/cs_*. Earlier: built 2026-08-13 — ⭐⭐ AND **VERIFIED IN GAME** the same day (chain prompt 4: 9 unattended launches, owner cost zero, `agent/reports/D13_VERIFICATION.md` + `archive/rs_*` logs). The derivation is authoritative and promoted (`agent/reports/D13_EXPOSED_SET.md`), the spec is FROZEN (§10 there) and the artifact EXISTS as a third mod at `C:\Dev\SMR-CommunitySaveRescue`. ✅ The one owner action it needed — ticking "Save Rescue" and restarting — was DONE by the owner 2026-08-13 11:16 (checklist 26, struck), and everything after it ran unattended. Earlier: directed 2026-07-31 (owner) — prelaunch deliverable. ⭐⭐ **SIZING INPUT PENDING, 2026-08-01: chain prompt 12 JOB 0.** If the "OFF is three different things" doctrine holds, this artifact's target population is NOT "players who used a module" but **every player who ever had the pack installed, whatever their toggles were** — because a toggled-off module still has its hooks installed and still seeds frames. That changes how much work this is. Owner-ordered re-verification against primary evidence before anything is scoped on it ("we cannot be wrong about this"); see `ENGINE_FACTS.md`, and note two of the four sub-claims are INFERRED, not measured. ⛔ SPEC GATED: not specced, not built, until Tier 1/2 land AND verify — its target list is their OUTPUT. **PROMOTED 2026-08-01 (owner) to a HARD LAUNCH DEPENDENCY: the pack does not ship until this ships alongside it** — "we won't launch till it does". The gate is a sequencing fact, not a priority judgement: the cleaner is specced LAST because it can only be designed against the residual set the builds leave behind. Also now the owner of the **complete per-site disposition table** required before release (FIX_POLICY §3a). Moved here from FUTURE_IDEAS entry 5 (2026-08-01) so that file's "nothing here is work" rule stays true]`

## ⭐⭐ 2026-08-14 — `tested` GRANTED: the attended after-sweep, at the keyboard

**The ONE combined sitting** (brief `agent/prompts/COMBINED_SITTING.md`). Six
archived logs, all byte-verified: `archive/cs_c1_*` · `cs_a1_*` · `cs_b1void_*` ·
`cs_b1_*` · `cs_b2_*` · `cs_zrestore_*`. **Attended cost: 34 minutes of parked
handover time** measured off the harness heartbeats (promise was 30–45), inside a
~67-minute wall clock whose difference is the owner's own landscaping lead and one
stalled launch (below).

### The pass, on a NATIVE witness — and the prediction it matched

⛔ **This is NOT a re-run of the `r2` matrix cell.** `r2` ran on `RESCUEDMG`, which
had three REMOVE rows, a dead `Meteors` thread and the D10 witness **manufactured**
into it. This ran on `CSSWEEP`, a byte copy of `SPCONRT` (MD5
`8A7639341C536816BFE18E35150AEDDE`) carrying only what the shipped packs really
wrote. **It is the first run of the artifact against what a real player's save
actually looks like.**

```
pass: removed 1566 entries (SMRFixPack_DroneCarryDial=1, SMRFixPack_DroneSpeedDial=1,
  SMRFixPack_ack_notworking=4, SMRFixPack_closed_to_new_residents=11,
  SMRFixPack_loop_version=1, SMRFixPack_no_homeless=11, SMRFixPack_payload_set=4,
  SMRFixPack_reserved_at=1533)
  | kept 1 track re-order latch
  | heals: meteors 0, rains restarted 0, rains ended 0 | skipped 1
```

⭐ **Every number, and the skip count, was predicted in `COMBINED_SITTING.md` §5 and
committed at `6867552` BEFORE the first attended launch.** The prediction was
derived, not guessed: it reconciles to the archived `r2` line to the unit —
`1533+4+4+11+11+1+1+1 = 1566`, and `r2`'s `1617` is exactly that plus its three
manufactured rows (`shelter_try=25`, `rocket_fuel_key=25`, `fixed_loop=1`).

⭐ **Zero heals is the correct answer here and was predicted as such:** the
`Meteors` thread is alive, so the ambiguity rule declined it —
*"skipped: meteor thread is alive and left alone — whether a live thread carries an
old body cannot be read from Lua, and restarting a healthy one would re-roll the
timer on every load"* — and the rains entry carried `loop_version` **without**
`fixed_loop`, i.e. already migrated by the pack: stamp removed, thread untouched.

### ⭐ The three dialog readings — the halves no log can ever carry

The terminal audit ruled these permanently unsampleable in unattended mode
(`WaitMessage` writes no line in either direction). All three are now witnessed:

1. **Report dialog RAISED**, text matching `report_text()`
   (`10_SaveRescue.lua:508-537`) exactly — *Removed* / *Kept on purpose* /
   *"it stores nothing in your save"*, and ⭐ **no "Repaired:" section**, which is
   correct: empty groups are omitted and there were no heals. Its own arithmetic
   reconciles to the pass line (`1 + 1533 + 2 + 22 + 4 + 4 = 1566`).
2. **The cleaned save's reload was SILENT** — and `save-rescue=1/1 active` sits
   beside it, so the silence means the pass found nothing rather than that the mod
   was absent. The second pass **printed** its `removed 0` (§10.9(3)). Every REMOVE
   name re-read absent from disk bytes after the round trip (R4).
3. **The stand-down raised exactly ONCE**, verbatim per §10.5, with both packs
   re-enabled — `stand_down_told` holding, no duplicate on any later load.

**0 `[LUA ERROR]` and 0 `[ERROR]` in every flushed cell; F99 `:805` and C45
`Quantum Comet` watches zero throughout.**

### ⛔ What this grant does NOT cover — stated so nothing quotes it wider

* **D10's KEEP condition was NOT re-sampled.** No save in the folder carries an
  `SMRFixPack_F35_*` modifier (measured again here: `D10 x0` on both fixtures). It
  remains sampled only on the 2026-08-13 **declared-SYNTHETIC** witness. `D11`
  carried the KEEP verdict here.
* **The rains and meteor heals were NOT exercised.** This fixture has nothing to
  heal. The manufactured-witness run of 2026-08-13 remains their only evidence.
* **A packs-present cell cannot judge the REMOVE contract.** `cs_b2_*` shows
  `REMOVE-list survivors = 4` — `reserved_at=13`, both Drone dial modifiers,
  `loop_version=1.0.1`. ⛔ **Those are the RE-INSTALLED PACKS writing their own
  state, not residue that survived the clean.** The control is the same save read
  pack-absent minutes earlier (`cs_b1_*` owner-load#2: every row `n=0`, both dials
  `present=false`), and `cs_b2_*` logs `[CommunityOptInPack] DroneStatDials:
  applied` doing it. This is §10.1's own argument — *the packs already do this work
  better* — and the reader that printed the alarm is a harness defect, recorded below.

### ⚠️ The gap the eyes found — owned by step ③

**Frozen spec §10.5 promises:** `Removed: 2 drone stat dials (drone speed and carry
capacity are back to the game's own values) · …`
**The build produces:** `"\nRemoved: " .. join(res.by_noun)` — bare `N noun` pairs,
no parenthetical (`10_SaveRescue.lua:512-514`).

So the player is told **"2 drone stat dials"** and nothing else — for the one piece
of residue that keeps *changing their game* after uninstall, which is the reason the
artifact exists. Nothing is wrong; the count and the removal are right. But step ③
inherits §10.5 as "the frozen player-facing dialog texts" and would have shipped a
text the code does not produce. **Fix the spec or the code before ③ quotes either.**

#### ✅ RESOLVED 2026-08-14 (chain `release-3` prompt 1, Job 0) — spec-amended, PROVISIONAL, owner call routed

Both sides re-derived at their sources. **The gap is real, and it is four gaps,
not one** — the eyes found the gloss; re-derivation found three more, recorded
with their evidence in `D13_EXPOSED_SET.md` §10.5's correction block:

1. the missing drone-dial gloss (the one the eyes found);
2. ⛔ **ordering** — `join()` sorts the already-formatted `"N noun"` strings as
   TEXT, so groups order by the *digits of their counts read as characters*.
   ⭐ **This one is confirmed by the witnessed dialog itself:** reading 1's
   `1 + 1533 + 2 + 22 + 4 + 4 = 1566` is exactly that sort over this save's noun
   sums, so on the night, **"2 drone stat dials" printed THIRD of six** —
   between 1533 reservation timestamps and 22 dome flags;
3. `35–115` (spec, en dash) vs `35-115` (build, hyphen);
4. `(one re-roll)` vs `(one re-roll each)` — here the **build is right** and the
   design text is wrong, because only the build's is correct in the plural.

⚠️ The **stand-down** text is unaffected: word-for-word identical to the code
(`10_SaveRescue.lua:712-713`), so "verbatim per §10.5" in reading 3 above holds
as recorded.

**Resolved by amending the spec, not the code**, on the no-cost branch this
prompt's brief names: a code change leaves the *new* string unsampled on a
gamepad-native `WaitMessage` whose wrapping no log can adjudicate — and the
gloss makes exactly the line that would wrap ~60 characters longer — so it
prices an owner re-witness (floor: one visit, per `EF-055`'s measured limit),
against an artifact whose publish is itself conditional (checklist 17).
⛔ **No re-witness is owed while the code is untouched, and `tested` is not
disturbed** — the reading that carries it was *"text matching `report_text()`
exactly"*, which is still true of the build as it stands.

⚖️ **Routed to the owner as checklist item 28**, bundled with item 17 (publish
or not), because the value of better dialog text is downstream of whether the
dialog ever reaches a player. ⭐ The *substance* does not wait on that answer:
③ carries "a non-base Drone speed/carry dial keeps working forever after
uninstall" in words on the rescue description and in the uninstall story —
surfaces ③ owns, no code, no visit.

### Harness defects from this sitting (mine, not the game's)

1. ⛔ **The Test Kit's on-screen output covered the dialog it existed to witness.**
   `print()` goes on-screen (`EF-015`) and drew over the report text. It did not
   cost this measurement — the owner screenshotted it and the wording was confirmed
   from source — but an attended cell whose instrument obscures its own subject
   would have cost the next one.
2. ⛔ **`CS.AssertCleared` ran the §10.9 REMOVE contract in a packs-present cell**,
   where that contract has no meaning, and printed `REMOVE-list survivors = 4`.
   Unchallenged that reads as "the artifact failed". Same class as the
   `corun-batch-2` reader defects: a reading taken outside the configuration its
   clause was written for.

## ✅✅ 2026-08-13 — TERMINAL AUDIT: chain CLOSED, every verdict SUSTAINED (prompt 5)

**All ten archived `rs_*` logs byte-identical to their on-disk originals and
read whole; every tally recounted from the verdict lines; every matrix verdict
re-derived; both residue-zero halves re-run from the archived bytes; the
exposed set re-derived a THIRD time from the shipped trees at audit time —
membership 27 sites, ZERO differences.** Corrections (none moves a verdict) in
`agent/reports/D13_VERIFICATION.md` §5. The three 6f-routed sites (E5/E6/E7)
are now recorded as **repaired-in-pack** in the disposition table — the owner
answered (checklist 19) and prompt 2b built the orphan gates (`91fc5d0`); the
audit re-read all three gate lines in shipped source. The Crystals repeater
disclosure that had passed three prompts homeless is FILED as **F103**.

⛔ **One ruling that narrows what "verified" covers: the two DIALOGS are
UNSAMPLED in unattended mode.** `WaitMessage` UI produces no log line in either
direction, so "the report dialog raised" (r2) and "no dialog" (r2b) have no
witness in any archived log — the no-`[LUA ERROR]` halves held; the raise
halves ride the ATTENDED after-sweep below. This is exactly the "untested UI"
prompt 4 flagged; the audit's addition is that no unattended run can EVER
adjudicate it, so it is named as the after-sweep's job, not re-queued.

### The uninstall procedure — player-facing DRAFT, frozen for step ③

⚠️ Draft tier: step ③ (MOD_DESCRIPTION rebuild) owns final wording and MUST
route-check every "you can/do X" line per the walked-not-derived rule
(`PUBLIC_DOCS_DESIGN.md` §7's lesson, re-learned by §10.9(4) this chain).
⛔ **SUPERSEDED 2026-08-14 — `RELEASE_UNINSTALL_ASSEMBLY.md` §3 is the
reference text; this draft failed its own route-check FOUR ways** (§2 there:
no restart step, tool-as-only-remedy, inert update step, and the rescue flow
never saying SAVE). ⛔ **And step 4 below is WRONG as written — owner-ruled
checklist 29:** the "notice" is a LOG line only; the screen warning skips
`optional_mod` mods, so the player sees nothing. Struck from all surfaces.
Do not quote this draft.

> **Uninstalling the Community Fix Pack (or its Opt-In Modules)**
> 1. Back up your saves first — always, for any mod change.
> 2. With the mod still installed: update it to the latest version, load your
>    colony, let the load finish, save, exit.
> 3. Uninstall the mod(s).
> 4. ⭐ On your next load of that save the game will show one notice per
>    removed mod: *"Savegame references Mod … which is not present."* **That
>    line is the game engine's own bookkeeping, not damage** — every mod that
>    was loaded when a save was written is recorded in the save's mod list —
>    and it disappears the first time you save without the mod. (Measured:
>    `D13_VERIFICATION.md` §4.5 — both packs produce the identical line, and
>    it self-cleared on the very next save.)
>
> What stays behind: a small amount of inert leftover data, documented
> name-by-name, plus **two entries that REPAIR your save and are kept on
> purpose** (the Large Wind Turbine buff repair and a one-shot track-repair
> latch). One thing does NOT stay inert: **a non-base Drone speed/carry dial
> keeps working forever** after uninstall. If that applies to you — or you
> removed the pack without step 2 — install **Save Rescue**, load the save
> once, read what it removed, and delete it again. It stores nothing in your
> save (measured about state; you will still see the one engine notice above
> after removing Save Rescue itself, once).

### Version skew — the statement this entry owns (from the frozen spec §10.6)

Save Rescue 0.1.0's list is **derived over fix-pack `cdbcd9d` / opt-pack
`e17586b` (2026-08-13)** and covers every persisted name either shipped pack
has written up to those builds, INCLUDING the two legacy names only older
builds wrote. Older unknown names are left untouched (it removes only what it
can name); a pack NEWER than the tool may write names it does not know — the
tool logs its own version and derivation stamp on every run (measured: the
`list derived over …` line in every pass log), and its description says to
check for a newer Save Rescue if the pack is newer. Detection cannot be
sniffed from the save: the only version-carrying stamp (D1) is a
mod-registered GameVar and is dropped on the first pack-less load.

### What step ③ (MOD_DESCRIPTION ×2(+1)) finds waiting — NAMED

1. The uninstall-procedure draft above (this entry).
2. The engine-notice sentence, measured, with its citation (§4.5 / above).
3. The version-skew statement above (spec §10.6).
4. The frozen player-facing dialog texts (spec §10.5 — report + stand-down).
5. The complete per-site disposition table (`D13_EXPOSED_SET.md` §7, all 27
   sites dispositioned, E5/E6/E7 now repaired-in-pack).
6. The rescue mod's README (`C:\Dev\SMR-CommunitySaveRescue\README.md`) —
   already written in player register, status line updated by the audit.
7. F102's mod-page disclaimer draft (F102 entry, per owner ruling 2026-08-12).
8. The Paradox-Mods discovery observation (below, 2026-08-01 — one browse,
   re-check before anything leans on it): searching `bug`/`fix` returned zero
   hits there; keyword-undiscoverability is a listing problem step ③ owns.
9. Suite numbers to quote: 94 probes; both-mods 78/0/16/0; with rescue loaded
   84/0/10/0 (logs `archive/rs_r0_*` / `rs_r1_*`).

## ⭐⭐ 2026-08-13 — VERIFIED IN GAME (chain `d13-rescue` prompt 4)

**Full record, with the predictions committed BEFORE the first launch:**
`agent/reports/D13_VERIFICATION.md`. **Nine archived logs:** `archive/rs_r0_*`,
`rs_r1void_*`, `rs_r1_*`, `rs_r2pre_*`, `rs_r2_*`, `rs_r2b_*`, `rs_r3_*`,
`rs_r3z_*`, `rs_rrestore_*` (plus `rs_ownertick_*`, the owner's enabling
restart). Every cell's configuration was set by JUNCTION presence (`EF-055`)
and proved by a config gate that STOPS before measuring. **Owner cost: zero
minutes.** ⛔ **Zero `[LUA ERROR]` in any cell.**

**The rescue case (r2), on a witness carrying all 11 REMOVE names, through the
AUTOMATIC `OnMsg.PostLoadGame` path rather than `force = true`:**

```
pass: removed 1617 entries (DroneCarryDial=1, DroneSpeedDial=1, ack_notworking=4,
  closed_to_new_residents=11, fixed_loop=1, loop_version=1, no_homeless=11,
  payload_set=4, reserved_at=1533, rocket_fuel_key=25, shelter_try=25)
  | kept 1 track re-order latch · 1 wind turbine repair
  | heals: meteors 1, rains restarted 1, rains ended 0 | skipped 0
```

* ⭐ **KEEP survives** — `KEEP-list present = 2`. **D10's condition is SAMPLED
  for the first time in this project's history**; ⚠️ its witness is
  **SYNTHETIC** (no save in the folder carried an F35 modifier, so one was
  planted with the exact id and shape `90_SaveSanitizer:84` writes). Absent is
  not refuted, and it is no longer absent.
* ⭐ **REMOVE goes** — `REMOVE-list survivors = 0`, by name, re-read absent from
  disk bytes in two later cells (`EF-049`).
* ⭐ **The one-shot bound proved itself:** the second pass reported the meteor
  skip *"alive and left alone"* — the ambiguity rule declining to touch the very
  thread the first pass had just restarted. A fresh process (r2b) removed 0.
* **The no-op (r1):** with both packs installed the automatic pass printed
  `removed 0`, and the suite went `78/0/16/0` → `84/0/10/0` of 94 with all six
  rescue probes PASS. The probes' forced passes moved **nothing** on the live
  colony (`RESIDUEDIFF … IDENTICAL`, on handle sets).
* ⭐ **D1/D2 promoted SOURCE → MEASURED:** `SMRFixPack_MeteorLatch` read `1.0.1`
  with the packs loaded and `nil` with them pulled, **before the artifact acted**
  — so their absence is `persist.lua`'s GameVar mechanism and must never be
  reported as a removal the tool performed.
* ⚠️ **§10.9(4) CORRECTED** (`D13_VERIFICATION.md` §4.5): "zero lines naming it"
  is false — the engine prints one savegame mod-reference line. **Both shipped
  packs produce the identical line** in the same situation, and it self-clears on
  the next save. Belongs in the uninstall text, which this entry owns.

⚠️ **What this does NOT grant.** `tested` still means a pass at the keyboard
(WORKFLOW); this is unattended, probe-and-instrument evidence, which is the
ceiling that mode can reach. The rains/meteor heals were exercised against
**manufactured** pre-rewrite residue on a byte copy — the derivation's claim was
only ever that this population exists in the wild, never that we hold one.

## ⭐⭐ 2026-08-13 — THE SPEC IS FROZEN AND THE ARTIFACT EXISTS (chain `d13-rescue` prompt 3)

⛔ **UNVERIFIED. Nothing below was run in a game** — every claim is source or
design tier. "It works" and "residue-zero" are prompt 4's words, not these.

* **The derivation is promoted and authoritative:** `agent/reports/D13_EXPOSED_SET.md`
  — 27 sites over BOTH shipped trees (12 capturable-code + 15 persisted-data),
  each with a disposition (§7), each with its own provenance tag. It supersedes
  every count in these docs; §4.1 reconciles them in both directions.
* **The three owner questions are answered** (checklist 17/18/19, 2026-08-13):
  (c) the packs are their own cleaner and the artifact serves ONLY the
  already-uninstalled · mod-shaped CONFIRMED · the three orphan gates were built
  in-pack rather than handed to the cleaner (prompt 2b).
* **The artifact is a THIRD MOD** — `C:\Dev\SMR-CommunitySaveRescue`
  (`SMR_CommunitySaveRescue` / global `SMRSaveRescue` / log tag
  `[CommunitySaveRescue]`), public remote `catt144/SMR-CommunitySaveRescue`
  (owner pre-created), junction installed. Two Lua files: a core a fraction of
  the pack's (no options page, no toggle machinery) and one cleaner module whose
  detection is the curated 16-name table, embedded as data.
* **What it does:** removes 11 named entries (headline: the two Drone stat dial
  modifiers, the only residue that keeps CHANGING the game after uninstall),
  KEEPS the two that repair the save (F35 turbine modifiers, F48 latch), does
  NOTHING about the two GameVars (unreachable by construction — reaching one
  would mean re-registering the name, which its own bar forbids), and carries
  two one-shot bounded heals for pre-rewrite-lineage saves (a dead `Meteors`
  thread; a rain loop still on the pack's old body, detected by the legacy
  stamp it then consumes). Each heal states its cost to the player: one re-roll.
* **It stands down per mod** while the pack that wrote a row is still installed
  — that is what (c) means in code, and it makes installing the rescue without
  uninstalling first a safe no-op with an explanatory dialog.
* **6 TestKit probes** (88 → 94). ⛔ The measured suite baseline `78/0/10/0 of
  88` is therefore **PENDING RE-MEASUREMENT** `[✅ SATISFIED the same day —
  prompt 4 measured `78/0/16/0 of 94` (r0) / `84/0/10/0` with the rescue mod
  loaded (r1); see the 2026-08-13 VERIFIED block above]`; the queued
  `FactionFundingCheck` PASS→SKIP repair was NOT taken and stays queued.
* ⛔ **ONE OWNER ACTION BLOCKS PROMPT 4:** the mod is installed but **not
  enabled**, and enabling needs a Mod-Manager tick plus a full game restart
  (this entry's own measured finding). Routed to the checklist.

**✅ CONSOLE-CHANNEL INPUT LANDED 2026-08-01 (owner ran the Paradox Mods check;
audit §7.1 item (b), recorded in `BUG_LIST_AUDIT.md` §10.4).** The audit booked
this because "whether fredware/GromGor mirror there matters for D13". Answers:

- **The channel is live and a Relaunched fix author already uses it.** GromGor
  mirrors to Paradox Mods — an **exact mirror** of his Steam workshop, same
  titles. **fredware does not; he is Steam-only** (so his removed mod has no
  console-side copy either, and our archived FPK stays the only recoverable
  form of it).
- **The sharpened question for D13, which nobody has answered yet:** if this
  pack ever publishes to Paradox Mods, the uninstall story is **not** the Steam
  one. That audience has no console, so any rescue path that assumes typing
  into a console does not exist for them — which bears directly on whether the
  "standalone save-rescue artifact" can be a console procedure at all, or has
  to be something a console player can actually run. **Decide the channel
  before speccing the artifact**, because the answer changes what the artifact
  is. Not a decision this entry makes.
- ⚠️ **Discovery problem, recorded as an observation and not yet re-checked:**
  the owner found that searching Paradox Mods for `bug` or `fix` returns **zero
  hits**, while searching the author name `gromgor` surfaces his bug-fix-titled
  mods immediately. If that holds, **a pack named for the words "bug" and "fix"
  is undiscoverable by its own keywords on that channel** — a naming and
  listing problem for `MOD_DESCRIPTION.md`, not a code one. One browse; re-check
  before anything leans on it.

**⛔ D13 DERIVES THE EXPOSED SET ITSELF — IT MAY NOT INHERIT ANY COUNT FROM
THESE DOCS (owner, 2026-08-01).** ✅ **DISCHARGED 2026-08-13: it did, and the
result is `agent/reports/D13_EXPOSED_SET.md` — rebuilt from the shipped `Code/`
of both mods over all five assignment shapes, then independently re-derived by a
fresh adversarial session which reproduced the membership with ZERO
differences.** The reasoning that made the demand is preserved below; the
recorded numbers it warned about are **not** a closed answer and were not
treated as one:

- The count is written as an **open lower bound** — *"at least 13"*, *"≥13"* —
  never as a determination. It was **12** on 2026-07-31, corrected to **13**
  the same day, and the membership was corrected **both ways** in the process
  (`Fix_DroneUnreachableForever` added, `Fix_TrainCargoDumping` removed).
- **The enumeration grep is known blind** to slot/global/preset assignments
  (`F86_ADJUDICATION.md` §3.1) — that blindness is *why* the bound is open.
- Tiers 1 and 2 then **changed the set from underneath the numbers** by
  repairing modules, so even a correct pre-build count is stale by
  construction.

**So D13 runs its own enumeration from source, over the current `Code/`, and
that result is authoritative.** It supersedes every number below on sight —
if D13's derivation disagrees with a recorded count, the recorded count is
wrong, not D13. Do the derivation FIRST; do not spec the cleaner against an
inherited list.

**Then update every place that states a count or a per-module denominator.**
✅ **DONE 2026-08-13 (prompt 3).** The table below is the 2026-08-01 starting
point with PRE-restructure paths, kept as the record of what was believed then.
The sweep that actually ran was independent of it: current-path patterns
(`≥13` · `at least 13` · `12 exposed` · `five of the twelve` · `5 of 12` ·
`12 modules` · `12 in total` · `exposure list` · `exposed set`) over `docs/` of
**all three repos**, archive excluded, plus a re-read of every hit. It confirmed
all nine files, corrected each in place with a citation to
`agent/reports/D13_EXPOSED_SET.md`, and found **one file the QA sweep had
called clean: the OPT-IN pack carries its own copy of `EF-023` and of
`FIX_POLICY`, both stating the superseded "13"** — corrected in that repo too.

Known locations as of 2026-08-01 (superseded by the sweep above; pre-restructure
paths):

| file | what is there |
|---|---|
| `BUGS.md` F86 index row + `:4474`, `:4525`, `:4527`, `:4578` | "13", "at least 13", "12 exposed", "five of the twelve", the 13-not-12 correction |
| `BUGS.md:4593` (Phase-1 re-run block) + `:4747` | the five-shape re-derivation; **"Exposure list (12 modules)"** — a stale denominator |
| `STATUS.md:321`, `:336`, `:348`, `:368`, `:369`, `:385` | "≥13", "13 plus one inert route-(c)", **"12 in total"**, "five of the twelve", "the exposure list grew: 13, not 12" |
| `agent/FIX_POLICY.md:291` | "the 13 and one additional inert route-(c) site" |
| `agent/ENGINE_FACTS.md:259` | "**13** after two same-day membership corrections" |
| `reports/SAVE_SAFETY_REDESIGN.md:15`, `:58`, **`:188` (§3 "12 exposed")**, `:226`, `:352` (§5.2) | the per-module disposition table and its denominator |
| `reports/F86_ADJUDICATION.md:290`, `:543` | "at least 13 by its own definition", "≥13 with CaveIns compliant" |
| `reports/F86_DISCOVERY_POSITION.md:113`, `:119`, **`:131` (§4 "The exposure list (12 modules)")** | the original 12-module framing |
| `reports/F86_SESSION_FINDINGS.md:201` | **"Exposed set — 5 of 12 have a route out"** |

The four bolded rows are the stale **12**-denominators; the rest are already
reconciled to 13 but still carry an open bound. **D13 closes the bound and
makes all of them agree with its own derivation**, and records the disposition
table (FIX_POLICY §3a) against that same list.

**The owner's directive (2026-07-31):** the pack ships with its exit paved —
prelaunch, ready for the community post-launch. Two deliverables:

1. **The uninstall procedure** (player-facing, MOD_DESCRIPTION): "update to
   the latest pack, load your colony, save, then uninstall" — true because
   the Tier-1 latched heal + rains migration clear our threads from the save.
   Backup-first advice, honest residual disclosure (inert layer-2 residue;
   irreversible history). `[FAQ]`
2. **The standalone save-rescue artifact** for saves that already lost the
   pack — the only remedy that works on console (Paradox Mods reaches
   Xbox/PS5; no logs, no console commands, no hex-editing there).

**⛔ THE SPEC GATE (owner concern, 2026-08-01): scope it against what is LEFT
after Tier 1/2 — never against today's leak set.** The F86 build exists to
stop creating residue; a cleaner specced now would be built to clean things
that won't exist when it ships, and would grow to fit. Sequencing therefore:
Tier 1/2 land → verify (their PT-20-method uninstall leg) → THEN freeze this
spec against the measured leftover classes (expected: pre-rewrite-lineage
saves — dead `Meteors` threads, old-body rains loops; `SMRFixPack_*` instance
fields — fixture-measured inventory 919× `reserved_at` + 8 others; GameVars
from any era). Building in parallel with Tier 1/2 means building it twice.

**Second-artifact costs (all owed by the release checklist, WORKFLOW):** own
metadata, preview image, description, PDX portal pass, console cert — plus
**version-skew management**: the cleaner must state which pack versions'
residue it handles, and its own residue must be ZERO (a cleaner that marks
saves is a farce — so: purely synchronous, no threads, no GameVars).

**⚠️ OPEN DESIGN QUESTION (owner, deliberately not yet answered): what does
the player actually DO?** (a) Run-after-removal — the promise depends on the
player remembering a second step at the exact moment they've decided to stop
using our mods (though the broken-save case is self-motivating: they arrive
because something is wrong). (b) Keep-installed — then it is not a cleaner
but a permanent runtime. (c) The pack is its own cleaner and the standalone
artifact serves ONLY the already-removed case. The choice shapes the whole
artifact; it is taken when the spec gate opens, not before.

**Primitives inventory (moved from FUTURE_IDEAS entry 5, still current):**
- Global GT threads: `RestartGlobalGameTimeThread(name)` swept over
  `GlobalGameTimeThreads` rebuilds each from `GlobalGameTimeThreadFuncs` —
  with the pack absent, that is **vanilla's body**. Fully generic across mods.
- Command threads: re-issuing `SetCommand` discards a stale stack carrying a
  vanished mod's frame (rarely needed — they self-clean per the LuaSavegame
  doc's "new invocations" rule).
- Named fields/GameVars: removable where identifiably named.
- **Trap 1 — some residue must SURVIVE:** `90_SaveSanitizer` writes
  `SMRFixPack_F35_<label>` modifiers that ARE the repair; a delete-everything
  cleaner re-breaks the save. Curated keep/remove list required.
- **Trap 2 — restarting a thread resets its interval** (35-115 h for
  Meteors); bounded as a one-shot, never blanket-repeated.
- Detection is the hard part: the 2026-07-31 `rawget` sweep false-positived
  on 192 buildings twice. Enumerate by curated list, never by pattern-guess.

**Playtest owed when built:** its own PT — damaged pre-rewrite fixture save →
run artifact → verify vanilla threads alive, fields/GameVars per keep-list,
zero artifact residue after ITS removal, on a save then loaded with no mods.

Cross-refs: F86, F88, `F86_EXECUTION_PLAN.md` Phase 5, WORKFLOW save-exit
gates, D06 (the beta-response-channel framing this composes with).

## ⛔⛔ 2026-08-10 — "OFF IS THREE DIFFERENT THINGS" IS NOW **FOUR** (`corun-batch-2` sitting, leg T)

Measured, not reasoned. Logs `docs/archive/cb2sitting_Mars.exe-20260810-15.30.16.log` and `docs/archive/cb2uninstall_Mars.exe-20260810-17.20.20.log`.

A **Mod-Manager disable followed only by a return to the MAIN MENU** does not
uninstall the mod and does not leave it intact either. It produces a **mixed
state nobody had named**:

- `CONDITIONS pack=81/81 active | speed=3 | loads_this_process=2 | note=leg T:
  after the Mod-Manager disable` — the pack's code and **all 81 registrations
  are still live**, with the Mod Manager showing "Disabled".
- **But its persisted permanent is already gone.**
  `Unpersist missing permanent: Mod/SMR_CommunityFixPack | Fallback permanent:
  … [7]` appears **twice** in that same pack-loaded process (sitting log lines
  **2787** and **2829**) and in **none** of the six loads taken before the
  Mod-Manager round trip.

⇒ **The four states:** (1) the pack's own per-module toggle off; (2) a
Mod-Manager disable **without** a process restart — permanent gone, code and
registrations live; (3) a Mod-Manager disable **with** a full restart; (4) a
real uninstall.

**State (3), measured the same day, is clean:** `CONDITIONS pack=0/0 active`,
`Loaded mod items for: SMR_CommunityFixPackTestKit` (the kit only), zero
`[LUA ERROR]` across the whole process, and zero new log lines after ~1 minute
of sim at speed 3. Six lines name the pack and all six are accounted for: two
mod-def loads, one items-load, the save's own recorded mod list, one unpersist
fallback, and `This savegame tries to load Mod Community Fix Pack … which is
present, but not loaded`.

⛔ **CONSEQUENCE TO ROUTE, and it reaches backwards.** Nothing in PT-53 E, the
co-run rig envelope, or any prior brief said a Mod-Manager disable needs a
**full process restart** to take effect. **Any earlier uninstall-style reading
taken without one was measuring state (2), not an uninstall.** PT-20's 98-vs-98
comparison is the named case to re-check — owner decision, on the checklist.

⚠️ The `Unpersist missing permanent` line is therefore **not diagnostic of an
uninstall**: it occurs in state (2) with the pack fully loaded. It should not be
read as evidence either way without the `pack=n/n` gate beside it.
