# C95 sittings, 2026-09-16 — the inert first build and the successful re-point

## Follow-up verdict — re-pointed repair passes attended

**MEASURED, owner at the keyboard**, game 1.1.0.403908 build `6a91a190`, repo
`733bed2`, TestKit `7b57b8a`. Clean log:
[`c95_repoint_Mars.exe-20260916-12.45.56-6a91a190.log`](../../archive/logs/c95_repoint_Mars.exe-20260916-12.45.56-6a91a190.log).
Fixture unchanged: save `lD1jaGcMJOxaiFcU`, one Naturalist Habitat with 5 residents,
`UniversalZeusRocket(1053)`, crew request 5 with specialisation Any.

1. **Boot and instrument live.** `HabitatExpeditionDraft: applied`; 49/49 modules
   applied. `CrewDraft` armed through the kit and wrapped 15 built classes including
   `UniversalZeusRocket` (`:261-266`).
2. **The new receiver fired.** `GatherAvailableColonists[new] UniversalZeusRocket(1053)`
   emitted at `:283-284`. The pool line reports 170 candidates, 18 transient-destructor
   drops, 0 liveness rejects, and buckets `0 / 30 / 0 / 122` (`:285-286`).
3. **The vacuity guard holds.** All five Naturalist Habitat residents appear in the
   eligible busy/unemployed bucket (`:354-371`). The logger reconstructs the shipped
   pre-fix pool before this pack's residence filter.
4. **The repair excludes and fills.** The picker returned 5/5 ordinary dome residents
   (`:393-404`); none of the five habitat handles appears in the returned crew.
5. **Direct behavior witness.** The owner pinned all five residents and read the habitat
   count before and after departure: **5 → 5**. The rocket launched normally.
6. **Whole-log result.** `python tools/logscan.py <archived-log>` reports zero
   error-shaped lines. Taint was already true on the prepared fixture; kit arm/copy
   actions are attributed and do not intersect residence filtering.

**FAILED INSTRUMENT ATTEMPT, not acceptance evidence:** the immediately preceding run
showed the same 5 → 5 behavior but the newly extended TestKit logger passed a Lua
multi-return through an `and/or` expression, collapsed two values to nil, and produced a
formatting popup before calling the real picker. TestKit commits `944616e` and `7b57b8a`
add the New receiver and correct that logger-only error; the clean rerun above is the
accepted run.

**OWNER RULING, after the pass:** lander/elevator player-choice behavior remains
desk-verified and is not scheduled for live testing absent a reported issue. The fixture
contains no Micro-G habitat. Live pack removal and the deliberate-shortage EF-104 panel
reading were not run and are not claimed.

Executed model for the re-point and close-out session: **GPT-5 / Codex**. No delegated
agents were used.

## Original sitting — failed target

**Attended, owner at the keyboard, game 1.1.0.403908 build `6a91a190`, repo `5fb34b4`.**
Log `Mars.exe-20260916-12.01.37-6a91a190.log`. Fixture: the owner's prepared save
`lD1jaGcMJOxaiFcU`, map 16N120E, one Naturalist Habitat with 5 residents, a rocket on the
pad and an expedition ready. Taint: `SMRTK_TAINT ... before=true used=true` — cheats used
on this fixture before the sitting, expected on a prepared save, and it does not intersect
the reading (the draft reads residence and employment, which cheat-built colonists carry
normally). ⛔ **No clean-save claim is made.**

### Original verdict

⛔ **At the original sitting's revision, `Fix_HabitatExpeditionDraft` was a no-op in
normal 1.1.0 play.** It was correctly built and desk-verified, but attached to a function
the live game did not call for expeditions. Checklist 189 was not satisfied and C95 was
not repaired at that point; the follow-up above supersedes this historical verdict.

⚠️ **What is NOT claimed:** the module is not defective, its desk suite is not invalidated,
and nothing here says the design is wrong. The guard shape, the exact-restore, the error
fallback and all 19 desk legs transfer unchanged to the correct receiver. This is a wrong
target, not a wrong repair.

## The measurement chain

1. **Boot clean.** `HabitatExpeditionDraft: applied` (`:127`), 47 modules seen and 47
   applied, `Game Loaded in 4782 ms`, and **zero error-shaped lines in the whole log**
   (`python tools/logscan.py <log>`).
2. **The instrument and the fix share a target.** `SMRTest.Log.CrewDraft` wraps
   `CargoTransporter.GatherAvailableColonists`
   (TestKit `Code/90_Loggers.lua:471-479`) — the same function
   `Fix_HabitatExpeditionDraft` hooks. It armed through the kit:
   `SMRTK_ARM action=logger_CrewDraft enabled=true logger=CrewDraft status=OK` (`:284`),
   preceded by `logger CrewDraft ON` (`:282`), so the kit owned the arm and quiet was off.
3. **The owner then ran the draft end to end**: the expedition was crewed and departed
   (owner, verbatim: *"yes crewed and took off"*).
4. ⭐ **The logger emitted nothing.** Not a partial trace — no `CrewDraft` output line exists
   in the log at all. A crew was gathered while the hooked function was never called.
5. **The rocket explains it.** The fixture's rocket is `UniversalZeusRocket(1053)`
   (`SMRTK_SECTION ... object=UniversalZeusRocket(1053)`, `:289`) →
   `UniversalRocketBase`, whose `__parents` are `PinnableObject, UniversalStorageDepotBase,
   DroneControl, WaypointsObj, Renamable, Recharger, CommandObject, CargoTransporterNew,
   ComponentCurvature, TemperatureVisualizationInvariant, ComponentInterpolation,
   ComponentAnim, AutoMode` (`Lua/UniversalRocket.lua:28-42`). **Neither `CargoTransporter`
   nor `RocketExpeditionBase` is among them**, so the module's receiver guard
   `IsKindOf(self, "RocketExpeditionBase")` can never be true for it.

## Why this is the normal path, not an exotic fixture

**SOURCE (1.1.0.403908):** the Universal rocket runs an expedition by switching its
**rocket type**, not by being a different class — `["Expedition"] = "RocketExpedition"` in
the type map (`Lua/UniversalRocket.lua:6`), `if new_type == g_RocketTypes.Expedition`
(`:1688`), `UniversalRocketBase:KillExpedition` (`:1729`), and the expedition entries in
its details and UI paths (`:2635`, `:2701`, `:3874`). The **return** half of C95's own
defect also lives on this path: `CargoTransporterNew` owns `GetExpeditionReturnDome` and
`SetCommand("ReturnFromExpedition", ...)` (`Lua/CargoTransporterNew.lua:1084-1094`).

⇒ `CargoTransporter` + `RocketExpeditionBase` is the **legacy** expedition path.
`CargoTransporterNew` + `UniversalRocketBase` is the one 1.1.0 uses.

## The uncovered path carries the same defect

`CargoTransporterNew:GatherAvailableColonists` (`Lua/CargoTransporterNew.lua:235-280`) is a
near-copy of the hooked body: same `label` normalisation, same four buckets
(`idle_unemployed, busy_unemployed, idle_employed, busy_employed`), same per-bucket
`FilterColonistsByTrait(pool, label, amount - #list)` at **`:272`**, same trimming tail. It
carries **no residence exclusion**, exactly like the legacy body. The differences are its
source pool (`self.cargo_request_passengers` filtered by `is_colonist_reachable`, else
`GetCityLabelWithConnected`) and an extra liveness test
(`idle and not unit:IsDead() or not idle and unit:CanChangeCommand()`).

⇒ **The same per-bucket filter design applies**, against a different receiver.

## How the build missed it, recorded without blame

The build report counted this call site — *"Filter calls **4** = CargoTransporter:278 +
CargoTransporterNew:272 + LanderRocket:1171 + LanderRocket:1182"* — and ruled
*"CargoTransporterNew is independent."* **That statement is true about the source and does
not settle the question that mattered:** which class the live game's expedition rocket
actually is. The inheritor census was run from `CargoTransporter` downward, so a sibling
transporter could not appear in it however far it was extended.

⭐ **This is the `fe6df03` reach check pointed the other way.** That rule asks whether a
player can reach the state a fix repairs; here the fix could not reach the state the player
was already in. ⇒ **A build brief should require naming the concrete class of the object the
fix will act on in a live save, not only the class that declares the body.**

## What the sitting could not cover, and stays unwitnessed

- **Micro-G habitat** — the fixture has a Naturalist Habitat only.
- **Lander / elevator player-choice leg** — not exercised.
- **Removal leg** — not reached; the run stopped when the no-op was diagnosed.
- ⚠️ **Whether habitat residents were among the departed crew** was not read off the
  habitat's resident count before the rocket left. It would have confirmed the defect
  reproduces on the New path directly; the class evidence above stands without it.

### Resolution

Resolved by code commit `4ec3e32`, desk commit `b055210`, and the attended rerun above.
The legacy receiver was retained for old-save compatibility; the one-off brief was consumed.
