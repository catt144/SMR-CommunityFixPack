# 04 drill-downs — ranked flags

Banner: plan brief `41672f1`; units: 14 ranked file/registry leads; date: 2026-09-10.

## Verdicts

1. **FR-1 MapGen/render — LEAD, not FILE.** Current `CommonLua/Libs/MapGen/MapGen.lua:1153-1161` adds native-facing pass/collision work on the new-game path. The apparent one-suspend/two-resume contradiction is REJECTED: current `CommonLua/Core/map.lua:641-652` stores reasons as set keys, so the second removal is idempotent. The related MapGen data files are new/changed relative to 1.0.7. Render/config changes include TAA capability probing, NRD/REBLUR tables, FilmGrain, SkyObject shader setup and shadow `hr` values, but no Lua writer/value contradiction proves a process crash. Affected players crashing with AA Off/FXAA refute the temporal-upscaler trigger. Source cannot observe Proton/native failure; `prompts/FR1_LINUX_SITTING.md` remains the discriminating test.

2. **`GetTransportRoute` F117 shapes — REJECT.** Current `Lua/Units/ColonistTransport.lua:114,123,136,141` retains the same booleans after slot 4 changed from `find_nearest` to `allow_reachable`; reading the complete caller shows a deliberate direct-first then reachable-fallback sequence. Old/current caller behavior is consistent with each stage.

3. **`ActionFX.GetLocObj` F117 shapes — REJECT for shipped base data.** The callee gained `action_pos` and `Source == "ActionObj"` (old `CommonLua/Classes/ActionFX.lua:1245`, current `:1318`); current destroy/follow callers at `:2052/:2821/:3967` omit it. `Data/FXPreset` and base DLC-excluded data contain no authored `ActionObj` source, so no shipped route needs the new argument. This remains a mod-compatibility lead, not a base defect.

4. **R08311 legacy rocket funding — REJECT.** The contract mismatch is real: old `Lua/Funding.lua:28-33` accepted missing `res_id`, current `:28-34` asserts and returns 0, while old/current `RocketBase:FlyToEarth` omit it (`:968` / `:966`). But current `Lua/RocketCompatibility.lua:950-975` converts every shipped legacy rocket class—including in-flight rockets—to Universal classes, and `RocketUtilities.lua:430-433` creates Universal rockets in new games. Universal export siblings pass resource ids. No current base route reaches the bad caller.

5. **F95 Astrogeologist migration — FILE C78, DIFF-CAUSED.** Old `Data/CommanderProfilePreset.lua:329-390` installs ten per-class `Percent = 10` effects. Current profile `:329-354` replaces them with `Extractors` effects, and added `Lua/MarsGameEffects.lua:309-343` tries to remove the stored old objects before reapplying the new profile. Its match at `:329-331` requires `Percent == 20`, so every actual old 10% object survives and the new effects stack. SOURCE route: load a supported pre-1.1 save using Astrogeologist on a non-Steam build; the named save fixup runs. Steam blocks this save generation, so the owner cannot reproduce it on the pinned Steam install. Falsifier: the loaded save has no old 10% label-modifier objects before this fixup, or another earlier fixup removes them. Recipe vacuity: no old modifier/object or no matching extractor makes the comparison vacuous. Player effect: silent overproduction/performance after migration; P2, no DLC required.

6. **R09089/R09475 research costs — FILE C79, DIFF-CAUSED.** Old `Lua/Research.lua:353-360` stores the supplied exact cost; current `:225-227` discards `points` and calls `BoostTech(tech_id, 20)`, whose current documentation/body `:245-257` makes it a percentage. `SA_RevealTech:SAExec` still forwards `self.cost` (old `Lua/Sequences/SA_Gameplay.lua:1187-1203`, current `:1196-1214`). Current authored examples vary from 3,000 (`Data/Scenario/Mystery 10.lua:47-50`) to 90,000 (`Mystery 8.lua:112-115`), proving a constant 20 is not an equivalent migration. SOURCE route: ordinary shipped mystery/buried-wonder sequence. Falsifier: displayed/queried cost after reveal equals the authored value because another layer consumes `SA_RevealTech.cost`. Recipe vacuity: the action must actually reveal an undiscovered tech and `cost ~= -1`. Player effect: materially wrong research requirement; P2, no DLC required.

7. **C62 authored explosion route — amend existing candidate.** Both trees' `Data/StoryBit/HostageSituation_BlowUp.lua:3-23` select one `CasinoComplex` and author `ExplodeBuilding` radius 500; current adds `trigger = "Tick_FollowUps"` at `:24`. Current generated effect `Lua/ClassDefs/ClassDef-Effects.generated.lua:768-790` calls `obj:BlowUp`. In both `Building:BlowUp` bodies, `buildings_hit` is allocated/filled and never read (old `Lua/Buildings/Building.lua:3428,3464`; current `:3611,3656`). The route is now R2, but cost remains unprofiled and P3.

8. **R11004 Seeds — REJECT.** Current `Lua/X/ColonyControlCenter.lua:894-953` gates the MultiResourceDepot loop through `DoesAcceptResource`. Current base MultiResourceDepot templates store only Meat/Mushrooms/LeafyGreens groups; none accepts Seeds. Universal-storage Seeds are handled in their separate availability-gated branch. No current base route enters the suspected branch for Seeds.

9. **F118 layout deactivation — REJECT.** Controller activate/deactivate moved from old `Lua/Construction/LayoutConstruction.lua:217/310` to current `:313/411`; current sets/clears `s_ConstructionControllerDeleteOnLoad` at `:385/:412`. The inherited/controller deactivation clears the slot, and no vanilla caller deactivates only the child controller. The pack-copy question does not reproduce in vanilla.

10. **EF-083 landscaping drones — REJECT.** Current `LandscapeConstructionSiteBase:ShouldAddRequestToCommandCenter` returns false (`Lua/Landscape/LandscapeConstructionSiteBase.lua:40-42`). Drone interaction test exists at `Drone.lua:2276`, but check returns false at `:2301-2303` and handler is a no-op at `:2337-2339`. This is an explicit RC Dozer-only redesign, not an accidental missing drone route.

11. **R05943 elevator pair destruction — FILE C80, PASSING.** Both trees validate `self.other` only before the asynchronous `LeadIn`, then call `self.other:GetPos()` before the next validity check (old `Lua/Buildings/Elevator.lua:844-870`, especially `:857-858`; current `:939-965`, especially `:952-953`). Current `ElevatorBase:Done:625-638` destroys the paired elevator, and normal colonist/unit routes call through `Unit:UseElevator` (`Lua/Units/Unit.lua:1037-1054`). SOURCE route: order a unit through, then complete demolition/destruction of either elevator during lead-in. Falsifier: engine permits `GetPos` on a DoneObject without error, or demolition cannot complete during the lead-in. Recipe vacuity: if transfer passes `:952` before destruction, nothing is sampled. Player effect: command-thread error/stuck traveler; P2, no DLC required.

12. **R11086 Infobar visibility scans — FILE C81, DIFF-CAUSED PERF.** The pre-existing one-second loop is old `Lua/X/Infobar.lua:35-40`, current `:38-43`. Current `UpdateContext:30-35` adds three `RefreshMostVisibleGrid` calls absent from old `:30-33`. Each current refresh reaches `Lua/GameOverlays.lua:235-263`, where `CountVisibleBuildings:205-226` walks producer, consumer and storage lists for every grid and projects every distinct building. SOURCE route: ordinary in-game HUD open, once per real-time second. Falsifier: profiler attribution is negligible or grid list/element counts are bounded independently of colony size. Recipe vacuity: a tiny colony/no populated grids cannot answer scaling. Player effect is unmeasured periodic CPU/GC work only; P3, no DLC required.

13. **Paradox mod-manager undefined globals — REJECT for shipped reach.** Current `CommonLua/Libs/Paradox/ParadoxModManager.lua:220-230` reads undefined `mod_details`; `:383-400` reads undefined `self` and later `mod_details`. Literal current-tree search finds no caller of the first, and the only resolver invocation is inside the commented block at `:460-468`. Broken dead API surface is not a player route.

14. **Infobar `GetEnvironment(city)` — REJECT.** The call is present in both trees (old `Lua/X/Infobar.lua:520-521`, current `:581-582`). Current `GetEnvironment` routes through `ResolveMap` (`Lua/MapData.lua:86-90`); `City` inherits `Object`, and `Object` inherits `CObject`, which satisfies the documented game-object input. The caller is not stranded by the map migration.

## Budget and residue

- Drilled: 14 ranked flags (4 FILE, 1 existing-entry amendment, 8 REJECT, 1 native LEAD; the FR-1 bundle counts as one ranked drill).
- Remaining file/registry flags: none. Lower-ranked SMELL tells not promoted from the skim are named KEEP/DROP in 04-A/B/C reports and were not independently drilled.
- Nothing here is runtime-tested. C78-C81 remain `cand` / `source-read`; C62 remains `cand` / `source-read`.
