# Fix_TrackTunnelPowerBridge review

Task/agent: `/root/module_a`, ONE module `Code/Fix_TrackTunnelPowerBridge.lua` (F65). Requested anchor: `2983fac`. Primary source: game `1.1.0.403908`.

No SOURCE disagreement with the current row's mechanism. **KEEP.** The row already identifies the missing grid work on a very short station-to-tunnel or station-to-station track. It does not promise continuous maintenance after later cable removal. The source still has that shortcut and current buildings still inherit its consumers. An optional wording improvement would name the current Universal Tunnel and distinguish the repaired station-to-entrance leg from the tunnel's already working entrance-to-exit capability; this is editorial precision, not a new filed defect or proof the row is false.

| module | entry | applies? | consumer still reads it? | row true? | bullet true? | verdict | evidence file:line on 1.1.0.403908 | basis | what I did NOT check |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Fix_TrackTunnelPowerBridge.lua | F65 | yes: final fresh menu registry active at line 252; captured module hash matches current file | yes: connection caller, supply tunnel masks/adjacency, live grid membership, connection flags, inherited Done cleanup, and post-fixup load pass remain used | yes: fix-list.md:378 names the short-track gap, including another station at :383; the repair is conditional on valid endpoints on different live electricity grids | n/a: metadata.lua:3 has no F65-specific headline | KEEP | 1.1.0.403908 Lua/Buildings/Track.lua:669; Lua/Buildings/StationsLink.lua:79; Lua/GridTunnelConnector.lua:56; Lua/SupplyGrid.lua:2209; Lua/TrainTransport.lua:26; CommonLua/Core/classes.lua:2004 | SOURCE | Current colony merge/split/reload, binary connector geometry, compiled grid traversal, later cable removal, actual instantiated subclass census, other mods |

Primary evidence (all game citations below are to read-only `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src`):

- **Installation only.** `docs/archive/logs/stillneeded_registry_Mars.exe-20260912-00.29.01-6a91a190.log:53` identifies build `1.1.0.403908`; `:108` records this module applied and `:252` directly reads `STATUS TrackTunnelPowerBridge active`. `CENSUS.json` records SHA-256 `b39db8721912956b9bcfcc7b73dbad3a62112276240b60ea02ce8cfc438826ef`, matching the current assigned source. Both packs and TestKit were enabled; no colony or cure was run. `BODYCHECK.txt:310` and `:313` accept the current target, but body agreement alone would not establish consumer reach.
- **The current call still reaches the skipped work.** `Lua/Buildings/Track.lua:532` calls `StationsLink.ConnectStations`; `Lua/Buildings/StationsLink.lua:75` through `:79` resolves two distinct endpoints and emits `StationsConnected`. The current shipped handler at `Lua/Buildings/Track.lua:668` through `:674` calls `ConnectToGrids` only for more than two elements and otherwise sets `stations_connected`. `TrackBase.ApplyToGrids` is still empty at `:662`; this is not a track whose normal grid-object initialization already supplies the missing power link. The source comment that short-track buildings already connect without the track is a premise, not independent geometry evidence.
- **What the fix changes.** `Code/Fix_TrackTunnelPowerBridge.lua:115` accepts only nonempty tracks with at most two elements; `:123`, `:129` and `:133` require surviving endpoints with electricity elements on different live grids. It calls the existing grid connector at `:137`, then clears the alternate connection flag at `:140`. This is a short track's endpoint coupling, not a new tunnel feature. Vanilla `Lua/Buildings/Tunnel.lua:87` through `:89` already merges the tunnel entrance and exit and applies their mask; `:160` through `:170` still implements that merge. `Lua/Buildings/TrackTunnel.lua:12` through `:16` reasserts the tunnel's own electricity/water merge after a station reconnection. The module connects the otherwise missing station-to-entrance leg; the same helper also covers station-to-station short tracks.
- **The repaired values have live consumers.** `Lua/Buildings/Track.lua:113` creates the supply tunnel and `:122` merges endpoint grids. `Lua/Buildings/Track.lua:572` through `:578` supplies the first/last track elements and their owner stations. `Lua/GridTunnelConnector.lua:48` through `:59` writes endpoint resource fields, the connection masks, both adjacency directions and `supply_tunnel_set`. `Lua/Buildings/Tunnel.lua:57` stores the adjacency; `:60` through `:65` exposes its reader, explicitly documented as called by C. The compiled walk was not inspected. `Lua/SupplyGrid.lua:2208` and `:2209` move elements between grids; `:540` changes each element's grid, and `:543` through `:551` incorporate its production and consumption into the new grid. These values still feed the game's current supply accounting. `Lua/Buildings/Track.lua:511` reads both connection flags to avoid reconnecting; `Lua/Buildings/Station.lua:1386` through `:1387` uses the supply flag to disconnect, and `Lua/Buildings/Track.lua:682` clears the alternate flag when stations disconnect.
- **Inherited reach is still present.** The currently exposed `Lua/BuildingTemplate/UniversalTunnel.generated.lua:5` inherits `TrackTunnelBase`, with build category `Tunnels` at `:21`; its base at `Lua/Buildings/TrackTunnel.lua:2` inherits both `TunnelBase` and `TrackConnectedObjBase`. `Lua/Buildings/BaseBuilding.lua:1113` also redirects the Copy action to UniversalTunnel. The original TrackTunnel data is hidden (`Data/BuildingTemplate/TrackTunnel.lua:4`), but that does not retire the inherited gap: the helper has no tunnel-class restriction. The playable Track class inherits TrackBase at `Lua/BuildingTemplate/Track.generated.lua:5` and declares no Done override in that generated body. The current module captures and wraps TrackBase's own Done at `Code/Fix_TrackTunnelPowerBridge.lua:152` through `:157`. `Code/00_Core.lua:516` invokes apply inline through `:452`; `CommonLua/PropertyObject.lua:1744` declares Done combined, while `CommonLua/Core/classes.lua:1971`, `:2004` and `:2082` collect parent methods and write the composition into descendants. This is SOURCE reach, not a runtime identity check or proof every possible descendant is instantiated. EF-036 and EF-066 inform this interpretation; their older line numbers were not reused as current primary citations.
- **The sibling inverse still matters.** `Lua/TrainTransport.lua:24` through `:26` deletes an entire two-element track when an endpoint is removed, now via a delayed `DoneObject`. `CommonLua/Core/classes.lua:1627` calls delete and `CommonLua/PropertyObject.lua:1754` calls the composed Done. `Lua/Buildings/Track.lua:69` through `:75` itself still lacks grid disconnect. The module's pre-wrap at `Code/Fix_TrackTunnelPowerBridge.lua:154` through `:155` disconnects bridged tracks before their elements disappear, except map teardown (`done_map`). `Lua/Buildings/Track.lua:130` removes the tunnel; `:141` through `:149` reconnects a surviving endpoint and reasserts its tunnel power, with `:153` through `:155` restoring other affected bridged tracks. `Lua/GridTunnelConnector.lua:72` through `:84` removes masks, adjacency and resource fields and clears the supply flag. Normal track demolition already disconnects at `Lua/Buildings/Track.lua:274` through `:275`, so the additional Done path then declines. No current consumer removal was found.
- **Existing saves still have a separate entry point.** `CommonLua/Savegame.lua:808`, `:809` and `:811` establish LoadGame, fixup, then PostLoadGame order. `Lua/Buildings/Station.lua:1553` still defines ConvertTrackPowerLinks; its final rebuilding loop at `:1581` through `:1586` bridges only tracks longer than two elements. The module's `Code/Fix_TrackTunnelPowerBridge.lua:166` through `:174` checks each city's TrackBase label after those fixups. The repaired counter at `:177` is observational bookkeeping; it is not the power consumer.
- **Public text and limits.** `C:/Dev/SMR-CommunityMods/content/fix-list.md:382` through `:386` describes the skipped very-short-track connection and repair. The original Train Tunnel promise remains in `Data/BuildingTemplate/TrackTunnel.lua:12`; the generated UniversalTunnel promise appears at `Lua/BuildingTemplate/UniversalTunnel.generated.lua:20`, although `Lua/Buildings/TrackTunnel.lua:88` through `:92` copies the ordinary Tunnel description after templates are created. That current ordinary Tunnel text still promises grid connection (`Data/BuildingTemplate/Tunnel.lua:12`). No screen text was checked. The same-grid guard deliberately declines a short track already powered via cables; later cable removal has no dedicated recheck until a subsequent load sweep. This accepted scope limit does not contradict the row's stated repair at connection time. F65's `docs/agent/bugs/F65.md:48` through `:58` remains an older PT-40 receipt, not a newly attended `1.1.0` result. No F65-specific bullet appears in `metadata.lua:3`; its destroyed-tunnel shortcut and track-deletion bullets describe other modules.

What I did NOT check:

- PT-40 or any current colony: no functional power merge, far-grid consumer, salvage split, station/tunnel deletion, long-track control or save/reload was fired.
- Binary entity connector spots and actual one/two-element placement geometry; the repair asks live grid identity instead of proving that geometry from Lua.
- Compiled SupplyGridExpand traversal or its C callback dispatch beyond the current Lua interface.
- The accepted same-grid-at-connection case followed by later cable removal, including its eventual next-load mitigation.
- Runtime subclass instantiation and dispatch counters, platform differences, option/veto variations or third-party replacement handlers.
- Other modules, historical migration decisions, code/header/entry/public edits, builds or git operations.
