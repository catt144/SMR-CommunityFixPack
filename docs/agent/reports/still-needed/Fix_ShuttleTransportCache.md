# Fix_ShuttleTransportCache — one-module still-needed review

Task/agent: `/root/module_a` · assigned module `Code/Fix_ShuttleTransportCache.lua` · entry F51 · anchor `2983fac`.
Captured module sha256: `ac350299e81b29d50d45feb5df787d0a1b2a72452c976c791eefa3eeaa6de4fd`; current bytes match CENSUS. Starting records: `MIGRATION_DEV_REPORT.md` §2 and `MIGRATIONFIX_AUDIT.md:116`; both correctly retain the cache mechanism and leave the current player symptom unverified.

No disagreement with the narrowed site row established. **KEEP is a recommendation**, with who-benefits explicitly separated from installation. The old automatic destination-skip gate moved, but the returned mode still selects transport behavior. Independent primary tracing found a conditional passage-route consumer that narrows the migration reports' open beneficiary question; it is **INFERRED player impact, not a filed defect or an organic reproduction**. A concrete current colony with the necessary geography is still owed before claiming that symptom.

| module | entry | applies? | consumer still reads it? | row true? | bullet true? | verdict | evidence file:line on 1.1.0.403908 | basis | what I did NOT check |
|---|---|---|---|---|---|---|---|---|---|
| Fix_ShuttleTransportCache.lua | F51 | yes — archived settled boot line 89 | yes — chosen destination's tuple still routes train/walk/shuttle; conditional passage-mode change remains read | yes — narrowed stale-value claim, no current refusal-to-move cure promised | n/a — no dedicated headline bullet | KEEP | C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Colonist.lua:3525; C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Buildings/Dome.lua:316 | SOURCE | organic 1.1.0 passage recipe; runtime transition/movement; pathfinder/geography; fixed-false rocket residual harm; save/uninstall and enable path; whole-card/counts |

Primary evidence (game paths below are pinned to **1.1.0.403908**):

- Cache defect and repair: `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Colonist.lua:3197` keys by community, `:3199` tests only the position entry, `:3200` computes with `shuttles_available`, and `:3203` unpacks the tuple. `FindTransportationModeToCommunity_BeforeTrains` at `:3113` checks walking and `:3116`–`:3117` checks the shuttle argument. The module at `Code/Fix_ShuttleTransportCache.lua:90`–`:100` stamps a normalized Boolean on a non-array entry key and recomputes the same `(mode, dist, elevator)` tuple on a flag change. No primary game code reads the added stamp; game callers read the tuple it causes to be refreshed.
- Four literal shipped calls to the patched global were found across the entire archived Src tree, including Data/DLC: `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Colonist.lua:3525`, `Lua/UniversalRocket.lua:2223`, `Lua/UniversalRocket.lua:2245`, and `Lua/Buildings/SupplyRocket.lua:68`. The last three always pass false and query rocket destinations. In the first, `Colonist.lua:3524` derives the flag from current availability for a non-forced destination in the same city.
- Destination selection has moved: `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Colonist.lua:3514` builds a fresh graph, `:3515` scores it, and only `:3525` reads the cache after selection. `BuildReachableGraph` independently checks `IsLRTransportAvailable` at `:3408`. The historical `if mode then` candidate gate is not here.
- The cached output is still consumed: `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Colonist.lua:2006` receives destination/mode/distance/elevator; `:2014` stores the elevator, `:2015` branches for train, and `:2018` passes mode and distance to `TryToEmigrateToDome`. Its walk branch starts at `:1898`. Its shuttle-task path at `:1959`–`:1970` does not require mode to equal shuttle, confirming that stale false alone does not recreate July's permanent destination skip.
- **Conditional current beneficiary — INFERRED:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Buildings/Dome.lua:315`–`:316` allows a passage-connected pair to count as walkable when outside walking is false and either open-air buildings apply, shuttles are unavailable, or the outside path distance is below the passage threshold. For an outside path distance **above** the threshold and with open-air buildings off, available shuttles can produce shuttle mode; losing availability produces walk mode via the existing passage. `Lua/_GameConst.lua:150` defines the default threshold as 1200m; this is external route distance, not passage length or dome separation.
- In that conditional same-city pair, with no train mode and no outstanding transport task, a previously cached shuttle mode would bypass `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Colonist.lua:1898` after shuttle availability becomes false. `:1969` checks `IsTransportAvailableBetween`, whose current availability gate at `:3597` returns false, so no new shuttle task is created. The repaired query can instead return walk; `:1910` finds the passage path and `:1916` permits foot transport when shuttles are unavailable. This is a real mode-consumer distinction in shipped bodies, but no actual colony satisfying all these conditions was examined.
- A normal player action can change the flag without the named cache events: `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Buildings/ShuttleHub.lua:1562` cycles hub transport mode; `:414` excludes cargo-only hubs from colonist transport availability. The function at `:1559`–`:1564` only changes the mode and rebuilds the panel. Existing cache invalidations are in `Colonist.lua:3124`–`:3153`, station/elevator validation at `:3184`–`:3195`, and the other cache writes found in the source grep. No shuttle-mode cache clear appears.
- **Do not overstate the recipe:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Colonist.lua:3377` expands `GetClusterDomes`, which `Lua/Buildings/Dome.lua:745`–`:746` returns as direct `connected_domes`; `Colonist.lua:3375` suppresses that expansion when arriving by walk. `Lua/Construction/GridConstruction.lua:156` defaults passage construction to 20 hexes, with the step limit applied at `:1193`. A remote multi-dome passage-chain destination is therefore not automatically proven to be scored by this graph. The candidate needs a genuinely selected pair, for example a short direct passage with a sufficiently long outside detour. Establishing that geography remains unverified.
- Named residual: the fixed-false rocket callers cannot trigger recomputation solely from an actual hub availability change, because their stored argument remains false. Nevertheless, `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Buildings/Dome.lua:316` internally reads current global availability. Whether that separate dependency causes departure harm was not checked. This module keys its explicit argument; it is not a complete invalidation scheme for every state its callees read.
- Known legacy beneficiary: `C:/Dev/SMR-SrcArchive/1.0.7.396349/Src/Lua/Units/Colonist.lua:2662` queries the cache before `:2663` gates candidate scoring on mode. July's PT-12 is recorded in F51, and the owner keeps 1.0.7 players on the live portals as a real constituency. This is additional keep rationale; it does not substitute for proving a 1.1.0 symptom.
- Runtime installation: `docs/archive/logs/stillneeded_Mars.exe-20260912-00.25.42-6a91a190.log:55` identifies 1.1.0.403908 and `:89` logs `ShuttleTransportCache: applied`. No later module status/heal message appears in that archived settled boot. Both packs and TestKit were enabled; no colony or suite ran.
- Public claims: `C:/Dev/SMR-CommunityMods/content/fix-list.md:82`–`:92` now says the cache notices a change and expressly says this is a stale-value repair rather than a current refusal-to-move cure. That bounded row is supported. `metadata.lua:3` has no F51-specific headline bullet; the bed-vacancy bullet concerns F59.

What I did **NOT** check, by name:

- Organic 1.1.0 reproduction of a selected passage-connected pair with outside path length above 1200m and no competing train route.
- A 1.1.0 runtime false-to-shuttle or shuttle-to-walk cache transition or resulting colonist movement.
- Geometry/pathfinder behavior and passage construction establishing that candidate geography in an actual colony.
- Whether a fixed-false rocket query's separate dependence on current global shuttle availability causes player harm.
- Full save/uninstall or main-menu enable-path verification on 1.1.0.
- Whole-card assertions, repair counts, other modules, or retirement decisions.

