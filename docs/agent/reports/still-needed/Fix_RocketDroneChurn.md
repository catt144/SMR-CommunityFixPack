# Fix_RocketDroneChurn — one-module still-needed review

Task/agent: `/root/module_a`; assigned module `Code/Fix_RocketDroneChurn.lua`; entry F50; anchor `2983fac`. EF-001, EF-058, EF-066 and EF-078 apply to timing/reach and measurement boundaries. Review accepts hotfix-2 F-7's cleared 1.1.0 re-copy/refuel clause; it does not redo that work.

Disagreements first:

- **Hourly interruption remains; universal permanent starvation is not established.** The header at `Code/Fix_RocketDroneChurn.lua:16`–`:19` and site row at `fix-list.md:224`–`:225` infer that any trip over one hour can never finish. Current Idle retains carried cargo and invokes Deliver again; Deliver can find another demand and approach its destination. The primary source proves repeated cancellation, not a universal reset of physical progress or permanent noncompletion. The categorical starvation claim needs qualification; this is an inference limit, not a newly filed defect.
- **The benefit has caller/state bounds.** Normal automatic landed rockets with valid locations enter the hourly request path; special automode and missing locations return early. Only connected command centers' drones whose goto target resolves to the requester are idled by this disconnect. Routine existing-request refresh avoids cycling after the fix; request creation still cycles, and non-auto requests still have vanilla's explicit interruption. The broad registry title should not imply every landed rocket stops every interruption.
- **UniversalRocket's override does not defeat the automatic fix.** Its ForceInterruptIncomingDrones runs only when auto mode is off, then it explicitly calls CargoTransporterNew.UpdateCargoResourceRequests. Normal automatic calls therefore reach the changed body. No vanished consumer or need for a fresh re-copy was found.

**KEEP-BUT-FIX-CLAIM is a recommendation.** The steady-state automatic interruption defect and the current primary consumers remain. The source-based qualification above does not erase the old PT-04 smooth-loading receipt, and that receipt is not a 1.1.0 cure measurement.

| module | entry | applies? | consumer still reads it? | row true? | bullet true? | verdict | evidence file:line on 1.1.0.403908 | basis | what I did NOT check |
|---|---|---|---|---|---|---|---|---|---|
| Fix_RocketDroneChurn.lua | F50 | yes — final direct registry active at line 233; installation only | yes — ordinary auto hourly request calls delegate to patched body; connected hub removal idles matching inbound drones; retained request objects/amounts feed current task selection and delivery | partial — repeated hourly cancellation and steady-state fix are supported; any trip over an hour can never finish and unconditional arrival are stronger than source establishes | n/a — no F50-specific card headline | KEEP-BUT-FIX-CLAIM | C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/UniversalRocket.lua:1943; C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/CommonLua/TaskRequest.lua:167; C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Buildings/DroneControl.lua:790 | SOURCE | current long-trip reproduction/cure; C task-selection routing; refuel-toggle play; live instance wiring; foreign overrides; every other transporter/template; whole-card guarantees |

Primary evidence (game paths pinned to **1.1.0.403908**):

- **Installation and accepted build binding:** `docs/archive/logs/stillneeded_registry_Mars.exe-20260912-00.29.01-6a91a190.log:53` identifies 1.1.0.403908, `:85` records RocketDroneChurn applied and `:233` directly reads it active. Current module SHA-256 matches `CENSUS.json`: `5cc705627f42e16439095ec7b29a2b0131e29445c276a2baf452d2496573c8f2`. Both packs and TestKit were enabled; no colony loaded and no suite, hourly trigger or cure fired. `docs/agent/reports/HOTFIX_2_AUDIT.md:236`–`:240` clears the re-copy and branch shape. The current primary `Lua/CargoTransporterNew.lua:1442` and module `Code/Fix_RocketDroneChurn.lua:124` retain not refuel_disabled; that cleared correction is accepted rather than re-tested on a stub.
- **Actual hourly caller:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/UniversalRocket.lua:1557` requires landed state; `:1562`–`:1563` calls CreateAutoCargoRequest only when auto mode is enabled. `:2029` excludes special automode, disabled auto and missing arrival/departure locations. Ordinary requests reach SetCargoRequest at `:2093`; `Lua/CargoTransporterNew.lua:1472` updates requested values and `:1484` dynamically calls the request updater. HourlyUpdate alone is not proof every automatic mode reaches the bracket.
- **Override/delegation reach:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/UniversalRocket.lua:1939`–`:1940` conditionally interrupts **non-auto** incoming drones, then `:1943` directly invokes CargoTransporterNew.UpdateCargoResourceRequests. It does not call a saved pre-patch function. The overridden method's refuel notification and ready-state work at `:1945`–`:1952` still follows delegation. `UniversalRocket.lua:37` includes CargoTransporterNew as a parent; the decisive evidence here is the explicit call, not an assumption that a parent patch replaces every override. `Code/00_Core.lua:516` applies registration inline before class flattening (EF-058); no live instance wiring was sampled.
- **Unnecessary cycle persists upstream:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/CargoTransporterNew.lua:1431`–`:1433` disconnects whenever working, even when every request already exists; `:1457`–`:1458` then only changes existing request amounts, and `:1460`–`:1462` reconnects. The fix at `Code/Fix_RocketDroneChurn.lua:106`–`:112` checks missing demand objects first. It still creates the same request pairs at `:129`–`:130`, changes their amounts at `:139`–`:140`, and reconnects at `:144`–`:145` after creation or when a working requester has no centers. This suppresses the routine bracket, not all deliberate interruption paths.
- **Primary disconnect-to-Idle chain:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/CommonLua/TaskRequest.lua:214`–`:217` removes every connected command center; `:166`–`:167` calls each center's RemoveBuilding with no old/new priority arguments. `Lua/Buildings/DroneControl.lua:796` invokes OnRemoveBuilding. That handler's priority-increase exception at `:785` does not apply to this removal; `:788`–`:790` scans that center's drones, resolves BuildingFromGotoTarget and issues Idle only for a matching building. `Lua/Units/RCRover.lua:734`–`:737` has the same matching-target Idle behavior with a siege-state condition. This is not every drone on the map or every drone whose resource happens to be cargo.
- **Requests still reach current consumers:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/CommonLua/TaskRequest.lua:126` creates a request, `:131` stores it on the requester and `:135`–`:136` registers that object with connected centers. `Lua/Buildings/DroneControl.lua:803`–`:806` removes those objects from supply/demand queues during a cycle; `Lua/_TaskRequest.lua:75`–`:78` passes the live queues to C task selection. `:58`–`:61` passes demand queues to FindDemandRequest. `Lua/Units/Drone.lua:1383` selects a demand, `:1401` obtains its current source, `:1446` approaches it and `:1478` unloads. `Lua/CargoTransporterNew.lua:1530`–`:1537` recognizes its demand object, updates actual cargo and offsets supply. Current amount and identity consumers have not been replaced by a different cargo model.
- **Why the starvation inference needs a limit:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/Units/Drone.lua:677`–`:678` dispatches Deliver when an idled drone still carries resources. A delivery interrupted before done does not clear resource/amount in the completion-only cleanup at `:1424`–`:1427`; it unassigns the old request at `:1421` and clears d_request at `:1439`. A new Deliver can select a destination at `:1383` and approach at `:1446`. No relocation back to the original departure is performed by the cited OnRemoveBuilding/Idle path. Request choice, changed destination, charging and approach behavior determine actual completion. **Permanent starvation of every longer-than-hour trip is INFERRED, not a source-established universal or a new filed bug.**
- **Non-auto and other bounds:** `C:/Dev/SMR-SrcArchive/1.1.0.403908/Src/Lua/UniversalRocket.lua:1565` takes the manual Earth-export branch. That routine requires manual export at `:2883`, compares targets at `:2995`, and only calls SetCargoRequest when changed at `:3000`–`:3001`; vanilla already avoids unchanged hourly manual requests. If a non-auto update does occur, ForceInterruptIncomingDrones remains at `:1940`; its filter at `:1776`–`:1778` uses current target/demand/supply source and `Lua/_TaskRequest.lua:345` issues Reset. F50 leaves that behavior intact. UniversalRocket's base default starting_drones is zero at `:82` and exclude_from_lr_transportation true at `:194`; those defaults do not prove every placed rocket has no drones or every possible cargo route starves.
- **Claim and receipt boundaries:** `C:/Dev/SMR-CommunityMods/content/fix-list.md:219`–`:227` correctly identifies the ordinary automatic-rocket cancellation, but `:224`–`:225` and the blanket arrival statement need the conditions/inference limit above. `metadata.lua:3` has no F50-specific headline; the empty-launch bullet belongs to a separate fix. `docs/agent/bugs/F50.md:14` records PT-04's old smooth-loading/unloading receipt; `:73`–`:77` explicitly excludes a current 1.1.0 game run/refuel check. This fresh menu status does not discharge that exclusion.

What I did **NOT** check:

- Reproduce a current longer-than-hour trip, observe repeated cancellation/starvation, or watch the repaired drone arrive/load/unload on 1.1.0.
- Resolve C request-selection outcomes, physical path progress across every interruption/entrance, charging, competing destinations or the full priority ecology.
- Play the Accept fuel toggle or redo hotfix-2's cleared body/refuel-control work; test all special/manual automodes.
- Sample current live rocket/drone instance method identity, every other CargoTransporterNew descendant/template, foreign subclass overrides or mod conflicts.
- Change the old PT-04 receipt, claim the registry measures a cure, or audit unrelated card headlines/counts/general guarantees.
