# Opt-In distribution prototype — shared playtest routing, 2026-09-25

ck218 routes the native request/claim test to the owner's next available game
session, after drone testing. The owner plays one game with both mods loaded;
this is an Opt-In dev-mod experiment, not a Fix Pack runtime change.

The [prototype report and console script](B:/Dev/SMR/SMR-OptInPack/docs/agent/reports/TRAIN_DISTRIBUTION_PROTOTYPE_20260925.md)
hold the procedure, offline evidence and limits. Native claims remain untested.
The delivered build leaves drone baselines untouched between calls. Drone response
and `accept` with a consumer need the Opt-In OI-29 ruling, a follow-up build and
local drone coverage; an uncovered spoke can test only train-side requests.

No game ran in this session. Executed model: GPT-6 as exposed in the transcript;
no subagents. This local receipt supplies the checklist's pull-only Home.
