# Everything but the fix pack moved to `B:\Dev\SMR` — second pass

Coordinator seat, 2026-09-21, from `MOVE_REST_high.md` (authored `ed86ed1`), fired at fix pack `ed86ed1`,
rooted at `C:\Dev\SMR-BugFixPack`. **One audit grades the whole move** (`MOVE_AUDIT_high.md`); this report
is only the witness of the moment of this pass.

```
C:\Dev\SMR-SrcArchive, workshop_fpk_archive        ->  B:\Dev\SMR\SMR-Shared\<same name>   NEW repo, local-only
C:\Dev\SMR-FR1-TempMod-…, -CacheRoute-V2-…,
        -DevPackage, -DevPackage.zip               ->  B:\Dev\SMR\SMR-FR1\<same name>      NEW repo, local-only
C:\Dev\SMR-CommunityMods                           ->  B:\Dev\SMR\SMR-CommunityMods        moved as a repo
C:\Dev\SMR-CommunitySaveRescue                     ->  B:\Dev\SMR\SMR-CommunitySaveRescue  moved as a repo
```

⛔ **Nothing was deleted.** Every original is renamed in place to `C:\Dev\<name>__MOVED_20260921`
(eight renames, all succeeded). **The owner deletes them after the whole-move audit.**

## 1 · Which originals are safe to delete, and why

| tree | redundancy once its `__MOVED_` original goes |
|---|---|
| `SMR-CommunityMods` | ✅ **GitHub remote** `catt144/SMR-CommunityMods`; `main` was even with `origin/main` at the copy |
| `SMR-CommunitySaveRescue` | ✅ **GitHub remote** `catt144/SMR-CommunitySaveRescue`; even with `origin/main` |
| `SMR-Shared` (SrcArchive + workshop) | ⚠️ **none** — no remote by owner decision; the `__MOVED_` copies are the only second copy |
| `SMR-FR1` | ⚠️ **none** — same |

Nothing proposed for the two without a remote. Stated for the owner's deletion decision only.

## 2 · Proof of an intact copy

`capture.py` ran identically on both sides of every tree: file, dir and byte counts, plus a sha256 over
every file's sha256, size and relative path (working tree and `.git` separately), plus the per-file rows
themselves, which were diffed. **All seven pairs matched on every field and every row.** Copy method:
`robocopy /MIR /COPY:DAT /DCOPY:DAT /XJ`, exit 1 (its success code), 0 FAILED, 0 Mismatch on all seven.
No tree held a junction or symlink (`Get-ChildItem -Recurse -Attributes ReparsePoint`, 0 each).

| tree | files | dirs | bytes | working-tree digest | covered by |
|---|---|---|---|---|---|
| `SMR-SrcArchive` | 9168 | 286 | 74,255,890 | `4d85f6b6…` | **its own manifests** (below) **and** my per-file hash |
| `workshop_fpk_archive` | 6 | 6 | 10,969,750 | `40a10b53…` | my per-file hash only — no manifest exists |
| `SMR-FR1-TempMod-2026-09-11` | 25 | 5 | 264,521 | `228d6ce9…` | my per-file hash only |
| `SMR-FR1-CacheRoute-V2-2026-09-11` | 547 | 11 | 15,643,263 | `0a021f9c…` | my per-file hash only |
| `SMR-FR1-DevPackage` | 12 | 4 | 194,345 | `43bfe83c…` | my per-file hash only |
| `SMR-FR1-DevPackage.zip` | 1 | — | 47,446 | `216c313b…` | `sha256sum` both sides, identical |
| `SMR-CommunityMods` | 2036 (1765 + 271 `.git`) | 441 | 188,669,145 | `85c0db15…` / `.git` `08d3195c…` | my per-file hash |
| `SMR-CommunitySaveRescue` | 79 (8 + 71 `.git`) | 59 | 184,912 | `5fae8567…` / `.git` `0718fde3…` | my per-file hash |

The brief's "93 MB / 11 MB / 17 MB" are `du` (block-rounded); the byte counts above are the ones that
compare. Sizes, plain-vs-repo and remotes otherwise **did not move** since the brief.

### `SMR-SrcArchive`'s own manifests, re-verified at the new location

There is no top-level manifest. Each build has `<version>\MANIFEST.sha256` over `<version>\Src`, and
those two cover every file in the tree except the two `MANIFEST.sha256` files and `README.md`
(4448 + 4717 = 9165 of 9168). `verify_manifest.py` at `B:\Dev\SMR\SMR-Shared\SMR-SrcArchive`, both
directions (each row hashes right; no file under `Src` is missing from the manifest):

```
1.0.7.396349 rows=4448 files_under_Src=4448 hash_mismatch=0 missing=0 not_in_manifest=0
  manifest body sha256 09d95e3448573dc378fa0bed5fc987fead3aafb70bf2ecf6a2cddef3f1ff9921  README states it: True
1.1.0.403908 rows=4717 files_under_Src=4717 hash_mismatch=0 missing=0 not_in_manifest=0
  manifest body sha256 a4577da25cb3fe8586bb7388b9b557d3dfd343938e453382e65d066f1945b3b2  README states it: True
```

The same script on the original returned the identical two blocks.

### The two repos that moved as repos

| | `SMR-CommunityMods` | `SMR-CommunitySaveRescue` |
|---|---|---|
| HEAD | `beaee4739f8879ca9f456b9ed631bab1d5f3066f` | `9c912b36e77cf51af6db596b8d60d5aac6da029d` |
| branch | `main` | `main` |
| remote | `origin` → `github.com/catt144/SMR-CommunityMods.git` | `origin` → `github.com/catt144/SMR-CommunitySaveRescue.git` |
| `git fsck` | exit 0 | exit 0 |
| `status --porcelain` | 0 entries | 0 entries |
| tags | **0** (nothing to resolve — say so rather than "all resolve") | **0** |
| `for-each-ref` digest | identical both sides (`67bb51d8…`) | identical (`d86cc265…`) |

### The two new repos

| | `SMR-Shared` | `SMR-FR1` |
|---|---|---|
| commit | `bd5036b` (one initial commit) | `369601d` |
| tracked files | 9174 = 9168 + 6 | 585 = 25 + 547 + 12 + 1 |
| `git fsck` / status | exit 0 / clean | exit 0 / clean |
| remote | **none** | **none** |

The only local config is `core.autocrlf=false` in each (the machine's system config is `true`), so the
committed bytes equal the working-tree bytes the hashes covered. No `.gitignore`, README policy or
per-repo structure was added. **Layout:** the moved folders keep their own names inside `SMR-Shared` and
`SMR-FR1` (not flattened) — the rule for every consumer is then one mechanical substitution
(`C:\Dev\` → `B:\Dev\SMR\SMR-Shared\` or `…\SMR-FR1\`), the archive's manifests and README stay
self-contained, and flattening `1.0.7.396349\` next to Workshop ids would have mixed two corpora at one
level for no gain.

## 3 · Consumers, re-derived

The two recorded lists disagreed and both were partly right. Grepped both repos for every form
(`C:\Dev\…`, `C:/Dev/…`, JSON-escaped `C:\\Dev\\…`, sibling-relative `dirname(ROOT)`, env-var reads):

| consumer | how it reaches the tree | disposition |
|---|---|---|
| `tools/treediff.py:128` | hard-coded `ARCHIVE` | repointed |
| `tools/patchcheck.py:72` | env `SMR_SRC_ARCHIVE` (archive **root**), default | default repointed |
| `tools/deskbench.py:73` | env `SMR_SRC_ARCHIVE` (the 1.0.7 **`Src`**), default | default repointed |
| `tools/presetdiff.py`, `desk_*.py` (4) | import `treediff`/`deskbench` defaults | follow transitively; nothing edited |
| `tools/l6_promise_map.py:32` | **sibling-relative** `dirname(ROOT)\SMR-CommunityMods` — on the list of neither | ⚠️ would have broken (fix pack is still on `C:`); now the absolute `B:\Dev\SMR\SMR-CommunityMods` |
| `tools/bodycheck.py` | reads the **live game install**, not the archive | not a consumer of the moved trees |
| `tools/TESTKIT.md:50,68` | names `SMR-CommunitySaveRescue` with no path | not a pointer |
| `doccheck.py` (fix pack) | **reads no `SMR_SRCARCHIVE` and names no archive path** | the brief's claim is refuted; nothing to change |
| fork `tools/sync_from_fixpack.py:333` | env `SMR_SRCARCHIVE`, default | repointed (mine, per the brief) |
| fork `tools/doccheck.py:1544,1659` | two message strings naming the archive; **the fork's `tools/README.md:82` is generated from the second** | repointed and `--regen`'d — see judgement 1 |
| fork `tools/devmods/train_hub/tests/traffic_smoke.py:20` + two Lua comments | the train seat's lane | **not touched by me**; the train seat repointed it itself in `4126be3` while this pass ran |

⚠️ **`SMR_SRC_ARCHIVE` means two different things** — the archive root in `patchcheck.py`, the 1.0.7
`Src` folder in `deskbench.py`. Setting it would fix one and break the other. Nothing sets it; I did not
unify them (not a move).

**Live docs repointed — fix pack (21 files, `+49 −181`, the −145 of that is the deleted prompt):**
`docs/README.md` (map + Path translation), `UPLOAD_WORKFLOW.md`, `agent/WORKFLOW.md` (3), `STATE.md`,
`LINUX_DISPATCH.md` (8), `GAME_PATCH_PROMPT.md` (2), `DLC_DEEP_CHECK.md`, `fixtoggles/08…` and its
`README.md`, `smrcf-verify/C35_DETECTOR.md`, `support/LIVE_SITE_READ.md`, `support/RELEASE_SURFACES.md`,
`facts/EF-083.md`, `facts/EF-100.md`, plus the four tools above. **Fork (commit `e6c7013`, pushed):**
the map + a new Path translation block, `WORKFLOW.md` (3), `STATE.md`, `DISPATCH.md`, `gamepatch/README.md`,
`KNOWLEDGE_SYNC_PASS.md`, the two live one-off prompts, the prompt map row, `EF-083`/`EF-100`.
`EF-083` and `EF-100` are **byte-identical between the two repos** after the edit (`cmp`, both).
Applied by a script with a per-file expected count; it caught one drift (`LINUX_DISPATCH.md`, 8 not 7 —
one line names two folders) and refused to write it until the count was corrected.

### What is left, by class (`count_refs.py`, all text files, both slash forms and the JSON-escaped form; counted before this report was written, so it is not in them)

| class | fix pack | fork | verdict |
|---|---|---|---|
| **live pointer** | **5** — all inside `docs/README.md`'s Path translation block, deliberately | **4** — same, in the fork's new block | intended |
| records: `docs/agent/reports/` | 1702 | 11 | untouched — source-line citations; the build beside the path finds the file |
| records: `docs/archive/` | 213 | 6 | append-only |
| records: `bugs/` | 14 | 11 | untouched |
| records: `facts/` | 4 | 4 | untouched (`EF-084/085/089/090` cite a line read on a build) |
| `Code/Fix_*.lua` comments | 5 | — | untouched; rewriting them is the defect (brief item 7) and would change shipped bytes |
| train seat: `tools/devmods/` | — | 2 (two Lua comments) | theirs |
| git-ignored, local | 11 (`.claude` 7, `local` 3, `scratch` 1) | — | see below |
| git-ignored `zz-owner/` | 29,301 | — | owner-held snapshots, not touched |
| **total** | **31,255** | **38** | |

Presence side, for the same regex family inverted: `B:\Dev\SMR\(SMR-Shared|SMR-FR1|SMR-CommunityMods|SMR-CommunitySaveRescue)`
occurs **33** times in the fix pack (excluding `zz-owner/`) and **22** in the fork.

Local git-ignored files I left and why: `.claude/HANDOFF_PROMPT.md` is the coordinator's own handoff and
**line 51 still says `MOVE_REST_high.md` is next and ONLY — it is now done and deleted**; `SEAT_WORKLIST.md`,
`OPTIN_INVENTORY.md`, `DECISIONS.md` (dated records). I did repoint `.claude/gamepatch_scratch/backtest.py`
(2, a runnable local script).

## 4 · The tools work, not just the strings

Run from the fix pack against the archive at its new home, all with the archive missing from `C:\Dev`:

- `python tools/treediff.py --out <scratch>` → `old B:\Dev\SMR\SMR-Shared\SMR-SrcArchive\1.0.7.396349\Src`,
  `new …\1.1.0.403908\Src`, **rows: 11742** (added 5351, body 3990, body+sig 373, removed 2010, sig 18),
  all five TSVs written, 27 s.
- `python tools/patchcheck.py --old 1.0.7.396349 --new 1.1.0.403908 --no-notes --no-parity --no-d5-list` →
  `A archived: 1.1.0.403908 (digest a4577da25cb3fe85…)`, `index: old 30063 declarations, new 33404`,
  `files (non-DLC): changed 2437 · identical 1963 · added 166 · removed 36`,
  `T 4710 changed hand declarations`, `B 39 file(s) …`. Its selftest, `patchcheck_selftest.py`: **GREEN, every leg passed**.
- `python tools/presetdiff.py --selftest` → `SELFTEST: PASS`.
- `python tools/deskbench.py` → **31 harnesses, all HELD** (they read the 1.0.7 tree through the new default).
- Fork: `python tools/sync_from_fixpack.py` → `38 shipped-game source file(s) under
  B:\Dev\SMR\SMR-Shared\SMR-SrcArchive — counted, not listed`, **no SKIPPED line**.

(The `treediff` split reconciles against its own members: 5351 + 3990 + 373 + 2010 + 18 = 11742. The
`patchcheck` figures are the tool's own and were not re-reconciled against an older run.)

## 5 · doccheck, and the `PARENT FILES` gate

- **Fix pack: GREEN**, with all selftests, `TOOLS COMPILE: PASS (70 …)`, `PACK IGNORE PARITY`, `LOCAL`, EOL.
- **Fork: GREEN** after one REDs-then-fixed step (below), with `SYNC FROM FIXPACK SELFTEST: PASS`, `TOOLS COMPILE: PASS (27 …)`.
- **`PARENT FILES (c:\Dev): 1 loose file(s) — SMR-FR1-DevPackage.zip__MOVED_20260921`.** Moving the zip
  into `SMR-FR1` did not clear the gate: the `__MOVED_` rename leaves the original as a loose file in
  `C:\Dev`. It clears when the owner deletes that original. (The fork's gate reads `PARENT FILES
  (B:\Dev\SMR): none`.)

## 6 · Judgement calls, and three things the brief did not say

1. **Fork `doccheck.py` strings, and the `RED`.** The brief names one fork file as mine. I edited a
   second: `tools/doccheck.py:1544,1659`, two message strings. Reason: my first pass edited
   `tools/README.md:82` for the same path, and the fork went **RED** — that line is *generated* from the
   `doccheck.py:1659` string. `doccheck.py` is in `TOOLS_ADAPTED` ("this repo's gate set, **paths**…"),
   the strings exist in the fork alone (the donor's `doccheck.py` has neither), so no sync pass could
   carry them, and leaving them would have kept the fork's own gate telling people the old path. Path
   substitution only, then `--regen`. Revert if the fork's ledger disagrees.
2. **A sync pass is owed, but only nominally.** The donor's `l6_promise_map.py` now carries an absolute
   `SITE`; the fork's copy is `dirname(ROOT)`-relative, which **already resolves correctly** because the
   fork sits at `B:\Dev\SMR`. `KNOWLEDGE_SYNC_PASS.md` will list it as adapted; nothing is broken. No other
   mirrored tool changed (`patchcheck`, `treediff`, `deskbench` are donor-only).
3. **`MOVE_FIXPACK_high.md` had two now-false lines**, which I corrected in place (line 161: FR-1 folders
   "still on `C:`" → in `SMR-FR1`; line 201: `SMR_SRCARCHIVE` "still on `C:`" → already at the new
   address). Its own advice that "a sibling-relative default never breaks on the next move" is true at
   rest, and the fix-pack pass may want `l6_promise_map.py` back on that form once it has moved.
4. **The push carried other seats' commits.** `git push` in the fork sent `d51ff88..e6c7013`, which
   included the four commits pass one deliberately left unpushed (train seat's). Pushing is standing-allowed
   here, but pass one's report treated them as not mine to publish. Now published.
5. **`SMR-Assets` still has no remote** (pass one's finding; unchanged, not proposed).

## 7 · The environment variables

⚠️ **I ran none of these; none of the variables was set before or is set now.** Every script resolves
through its in-code default, which is the change that matters, so the block is optional. **Only
`SMR_SRCARCHIVE` changed this pass** (pass one's block gave it the `C:` value; this supersedes it).
`SMR_SRC_ARCHIVE` is deliberately absent — it means two different things (§3).

```bat
setx SMR_SRCARCHIVE "B:\Dev\SMR\SMR-Shared\SMR-SrcArchive"
```

`setx` writes user scope and affects **new** processes only.

## 8 · Report, do not act: `SMR_FR1TempWorkaround` in `%APPDATA%`

`%APPDATA%\Surviving Mars Relaunched\Mods\SMR_FR1TempWorkaround` is a real directory (`Attributes:
Directory`, no `ReparsePoint`); the five other entries there are junctions. It is **22 files, 255,908
bytes**, `version 3` in its `metadata.lua`, last written 2026-09-11.
`diff -rq` against `B:\Dev\SMR\SMR-FR1\SMR-FR1-TempMod-2026-09-11\SMR_FR1TempWorkaround` reports **no
difference — the installed folder and the repo copy are byte-identical.** As a junction it would make the
repo copy the running mod and remove the sync step `LINUX_DISPATCH.md` §4 asks for after an owner upload;
the cost is that a stray edit inside the repo would ship to Linux players on the next upload. Left as
found — the owner's call, and FR-1 is winding down.

## 9 · Not done

- The fix pack's own move, the TestKit and `SMR-ScreenCaptures` (out of this pass).
- `SMR-CommunityMods` has an in-tree `site/` (built MkDocs output, tracked); not examined beyond the copy.
- No Claude memory store existed for any of the moved trees (`~\.claude\projects` has no entry for them),
  so none was copied.
