# P3 — VacuumWalks source-pin build, 2026-09-24

## Must_Read_Header

P3 is the hubset's metadata-only member, not a defect entry. This dated build record gives link 99 the branch SHA, archived source control and limit. It is not a runtime composition or play verdict.

## Build and control

- **BUILD, `hubset` at `795aefa`.** `Code/Fix_VacuumWalks.lua` now has `SRC:` pins for
  `Colonist:GetNextMigrationLeg` and `IsInWalkingDistDome`, the two bodies it wraps but had
  left unpinned. The module's behavior is unchanged.
- **SOURCE, archived 1.1.1.405907.** The pinning instrument read
  `Lua/Units/Colonist.lua:3709-3717` and `Lua/Buildings/Dome.lua:299-319` and emitted
  hashes `deba3d7951de6c358a04a316457c748641255bbd00040eeb0c061cf8d240ecb7` and
  `3c297c411ecc954fbcb052610a6ea76e8728d3c3de6961c7a7887510245b54f0`.
- **MEASURED.** `python tools/bodycheck.py --selftest` passed its verdict controls.
  `python tools/bodycheck.py --src
  B:\Dev\SMR\SMR-Shared\SMR-SrcArchive\1.1.1.405907\Src --module VacuumWalks --all`
  returned 6 OK, including both new pins. This command ran on branch `795aefa` after the
  branch commit.
- **LIMIT.** A pin catches an archived body change. It cannot prove that the wrapper still
  composes with other mods or that vacuum migration behaves correctly in game; link 07 and
  link 99 own those checks.
