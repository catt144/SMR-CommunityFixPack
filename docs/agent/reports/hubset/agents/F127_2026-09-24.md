# Hubset 01 F127 subagent report — 2026-09-24

## Must_Read_Header

Verbatim completion report and revision note from the F127 subagent. The revision followed a coordinator review of the full Naturalist Habitat branch. A desk result is not attended play.

## Verbatim subagent report

F127 now cancels the rejected dome’s booking before attempting to reserve in the new destination. This covers full MicroGHabitats, whose shipped reservation method leaves an old booking untouched when no slot is free.

The archived 1.1.1.405907 desk harness passed **12/12**, including full-habitat, fix-removed, and previous reserve-only controls. The existing C83 harness passed **12/12**; Lua parsecheck found **0 errors in 39 files**. Only [Fix_ArrivalDeaths.lua](B:/Dev/SMR/SMR-BugFixPack-hubset/Code/Fix_ArrivalDeaths.lua:482) and [desk_f127_arrival_booking.py](B:/Dev/SMR/SMR-BugFixPack-hubset/tools/desk_f127_arrival_booking.py:1) changed. No commit or documentation work was done.

## Verbatim revision note

F127 habitat refinement complete in my two owned files, still uncommitted. Archived 1.1.1.405907 `MicroGHabitatBase:ReserveResidence` (MicroGHabitat.lua:85-88) delegates to `Residence.ReserveResidence`; the latter cancels the old booking only after `CanReserveResidence` succeeds (Residence.lua:291-308), so a full habitat preserved it. `Code/Fix_ArrivalDeaths.lua:482-491` now calls `self:CancelResidenceReservation()` first when the valid booking belongs to rejected dome, then invokes `new_dome:ReserveResidence(self)` only if it is a function. `tools/desk_f127_arrival_booking.py` now extracts shipped MicroGHabitat ReserveResidence/ChooseResidence and checks full habitat, habitat with room, fix-removed full habitat, and an in-memory old reserve-only mutant. Commands/results: `python tools/desk_f127_arrival_booking.py` 12/12 demands held; `python tools/desk_c83_arrivals.py` 12/12; `python tools/parsecheck.py --quiet` PARSE 39 files, 0 errors [Lua 5.5]; `python -m py_compile tools/desk_f127_arrival_booking.py` exit 0; `git diff --check -- Code/Fix_ArrivalDeaths.lua` empty, exit 0. `git status --short --` owned paths shows M Code/Fix_ArrivalDeaths.lua and ?? tools/desk_f127_arrival_booking.py. No docs, metadata/items, other files, git writes, doccheck, hooks, or commits.

## Coordinator note

The coordinator added archived source pins for the four booking callees in `Fix_ArrivalDeaths.lua` after the subagent report. The full-habitat omission in the initial build is a drift instance for link 99's audit inbox. Branch SHA and re-run evidence follow in F127's entry.
