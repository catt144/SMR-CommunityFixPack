# Hubset 01 fan-out plan — 2026-09-24

## Must_Read_Header

Execution record for `01_WORKTREE_AND_SMALL_FIXES_fanout_level_3.md`. Code and desk harnesses go to the `hubset` worktree; entries, agent reports and chain handoff stay on `main`. This plan records assignments and progress, not a play verdict.

## Live work list

| Item | Writer | Files owned | State |
|---|---|---|---|
| Worktree | coordinator | branch and worktree setup | DONE: `git worktree add ../SMR-BugFixPack-hubset -b hubset` from `d6667dc` |
| P3 | coordinator | `Code/Fix_VacuumWalks.lua` | BUILT: two archived 1.1.1.405907 pins; branch commit and record pending |
| C42 | `c42` subagent | C42 module and desk harness only | RUNNING |
| F127 | `f127` subagent | `Code/Fix_ArrivalDeaths.lua` and F127 desk harness only | RUNNING |
| C111 | `c111` subagent | C111 module and desk harness only | RUNNING |
| Close-out | coordinator | `metadata.lua`, `items.lua`, entries, reports, checklist, chain inboxes and README | PENDING |

The coordinator alone registers modules, reviews each result against its cited evidence, runs repo gates and commits. Each subagent reports its commands and output verbatim; the report lands here even if its member stops. The branch is not a release or playtest result.

## Setup evidence

- `git pull --ff-only` on `main`: Already up to date.
- `git status --short --branch` on `main`: `## main...origin/main [ahead 5]`, no changed paths.
- `git branch --list hubset`: empty before creation; `Test-Path B:\Dev\SMR\SMR-BugFixPack-hubset`: `False`.
- Fresh worktrees lack the ignored `local/c92-placement/` and `local/c95-place/` folders named by `local/README.md`. The coordinator created empty worktree-local folders for that gate; the original evidence remains in `main`'s `local/`.
