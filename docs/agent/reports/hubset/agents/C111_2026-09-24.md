# Hubset 01 C111 subagent report — 2026-09-24

## Must_Read_Header

Verbatim completion report from the C111 subagent. The proposed wording remains open for the owner; a desk pass does not establish retail rendering or play behavior.

## Verbatim subagent report

Implemented C111 in the hubset worktree: [Fix_RescueReturnText.lua](B:/Dev/SMR/SMR-BugFixPack-hubset/Code/Fix_RescueReturnText.lua) and [desk_c111_rescue_text.py](B:/Dev/SMR/SMR-BugFixPack-hubset/tools/desk_c111_rescue_text.py).

**Proposed wording for owner approval:** `Returning to Dome: <h SelectEmigrationDome InfopanelSelect><em><EmigrationDomeDisplayName></em></h>`. Real relocations retain the shipped localized “Moving to a new Dome” text and destination link.

The fix changes only a synchronous UI result for an own-home rescue task. It adds no saved state or blocking frame; the desk reload check passes. The harness passes all 8 checks, while `--without-fix` fails the own-home wording check. The scoped source pin has 4 OK results, and the Lua parser reports 0 errors. Retail rendering, save serialization, and in-game play remain untested. No docs, load lists, staging, or commits were changed.
