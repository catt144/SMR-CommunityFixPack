# Hubset 01 C42 subagent report — 2026-09-24

## Must_Read_Header

Verbatim completion report from the C42 subagent. Its claims are checked by the coordinator against the branch diff and desk control before the build entry is updated. This is not an attended play verdict.

## Verbatim subagent report

C42 is implemented in [Fix_PassageStaleHolder.lua](B:/Dev/SMR/SMR-BugFixPack-hubset/Code/Fix_PassageStaleHolder.lua) with a desk control in [desk_c42_passage_stale.py](B:/Dev/SMR/SMR-BugFixPack-hubset/tools/desk_c42_passage_stale.py).

The fix synchronously removes invalid or mismatched members from a passage element’s holder list immediately before vanilla teardown. It prevents stale members from being kicked, preserves genuine occupants and chained return values, and adds no persisted state or blocking mod frame. Vanilla traversal still creates the stale entry until teardown.

Verification: desk harness **11/11 held**, including a stale kick when the fix is absent; archived 1.1.1 bodycheck **4 OK**; Lua parsecheck **39 files, 0 errors**; wrap-target check **0 outside Require**. No in-game test was run. I made no shared-file or documentation edits and no commit.

## Coordinator note

The coordinator narrowed the `DEFECT:` regex to the raw `unit.holder = nil` expression, removing the source comment from the discriminator. No behavior changed. Branch SHA and re-run evidence follow in C42's entry.
