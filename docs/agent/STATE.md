# Project State — the one mandatory read

Current only, rewritten in place; history newest-first in
`docs/archive/SESSION_LOG.md`. Defect truth `agent/bugs/INDEX.md` · engine facts
`agent/facts/INDEX.md` · doc map `docs/README.md` · authoring `agent/WORKFLOW.md`
· code `agent/FIX_POLICY.md` · chains `agent/reports/CHAIN_METHOD.md`.

## Where the project is

✅✅ Build chain COMPLETE 08-03; every closed co-run's audit sustained every
verdict. ⭐⭐ **SHIP LINE RULED 08-12 (owner — checklist 14): evidence campaign
FROZEN pre-release** (`fixed` + suite + self-checks + verified save-safety tier
IS the bar) · **trains = OPTION A, queue CLOSED** · **D13 BLOCKING** · riders
C42/F99/F80/F96-R2 → post-release WATCH. ⇒ **NEXT — the release front only:**
⓪ **THE OPT-IN SPLIT IS BUILT (08-12, prompt 3). A SECOND MOD EXISTS:
`C:\Dev\SMR-OptInPack`** (id `SMR_CommunityOptInPack`, global `SMROptInPack`,
prefix `[CommunityOptInPack]`, junction installed, LOCAL git — no remote
unasked). The 8 `Opt_` files, their entries (D01-D07/D09/D12 → tombstones here)
and this pack's ENTIRE Mod Options surface moved; ⛔ **this pack no longer
appears in Options → Mod Options at all** — correct, not a defect.
⇒ **NEXT = `agent/prompts/split-optins/04_OPUS_VERIFY.md`** (three-cell matrix +
save witness; unattended, needs the game) → prompt 5's audit. ∥ `unattended-3`.
① **D13 chain**: exposed set over BOTH trees (NO inherited count) → spec
(⚠️ owner design Q) → build + probe + curated keep/remove list. ② **ONE combined
sitting**: PT-20 redo + D13 after-sweep + both-mods load/suite smoke + F102's
1-min check (`Sylmacaink BH25`). ③ MOD_DESCRIPTION **×2** + disposition table +
metadata → ④ owner launch tasks.

## Build state — `python tools/doccheck.py --emit-counts`, never hand-typed

```
BUILD STATE (emitted by tools/doccheck.py)
- modules: 74 registered (74 default-active, 0 optional-gated files)
- Code/*.lua files: 75
- TestKit probes: 88
- BUGS index rows: 102 F + 12 D + 46 C
```

⛔ Post-split; pre-split `82/75/8` and the `81/81` gate read are ERA-STALE — the
gate reads `74/74` in every config now. Re-emit after any change (red refuses);
pinned **1.0.7.396349** (`agent/facts/EF-014`). F76 → `C41` · F11 settled P2 ·
F99/C42 rate-only · ⛔ **SKIPs BY NAME, never as a total.**

## Gates and holds

- ⭐ **BOTH MODS LOADED is the rig's NORMAL config** (owner 08-12, WORKFLOW).
  Two gate lines, two log prefixes — grep the FULL bracketed token.
- ⛔ **Owner owes ~1 min AFTER prompt 4 reports clean**: enable the new mod
  (full restart) + re-tick 7 toggles & 2 dials (new mod id = fresh account key;
  predicted). Saves untouched — persisted names kept their `SMRFixPack_` bytes.
- **PT-62 remainder** → D12 (record now in the opt-in repo); ⛔ NOT a release
  gate. ⚠️ Its P13 lever renamed: `SMROptInPack_Disabled.NoHomeless`.
- ⭐ **F02/F78/F81 ORGANIC watch.** F42 wontfix; D08 speced, unbuilt. ⚠️ **Rig
  has CHEATS ENABLED** — name it beside "retail" claims.
- ⚠️ **EF-051 ON HOLD — Steam Cloud ON (owner 08-12, temporary).** Say "deleted,
  listing verified", NEVER "gone". `CP60RT` + `Autosave Sol 306/311` HELD.
- ⛔ **Mod-Manager disable needs a FULL RESTART** (D13); re-enable is owner-only.
- **Owner decisions open: 3** — relabel/judgment-calls WORDING · D13 design Q ·
  the opt-in mod's DISPLAY NAME (placeholder sites: its `PROVENANCE.md` §3).
- **Release** — now **TWO** products, each with its own metadata/description/preview/portal pass; `archive/MOD_DESCRIPTION.md` FROZEN, rebuilt at prep.
