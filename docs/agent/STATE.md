# Project State — the one mandatory read

Current only, rewritten in place; history newest-first in
`docs/archive/SESSION_LOG.md`. Defect truth `agent/bugs/INDEX.md` · engine facts
`agent/facts/INDEX.md` · doc map `docs/README.md` · authoring `agent/WORKFLOW.md`
· code `agent/FIX_POLICY.md` · chains `agent/reports/CHAIN_METHOD.md`.

## Where the project is

✅✅ 18-prompt chain COMPLETE 2026-08-03; playtest campaign is the main line.
⭐⭐ corun-rig + `unattended-1` CLOSED 2026-08-04 — SAVE primitive PROVEN, tiers
ADOPTED, ledger → Co-runs guardrails. ⭐⭐ **`corun-batch-1` CLOSED 2026-08-05**,
every verdict SUSTAINED (SESSION_LOG; log `archive/cb1sitting_…14.28.49.log`):
**PT-37 case A PASS** (559→558, survived save+reload), **case B UNSAMPLED** (hex
grid ⇒ F48's blocking premise contradicted for its cited scenario, decision 1).
D07 **4-of-5**, E's precedence ROUTED (2). Rig misses scored, not owner leads.
⭐⭐ **`corun-batch-2` PREP DONE 2026-08-10** — confirm clean (0 errors, log
`archive/cb2confirm_…10.20.03.log`), every leg's subject FOUND and named;
⛔ **C42 = SKIP** (90-try witness, `units` empty every sweep — 3rd empty
denominator; needs a new FIXTURE, not a better poller); ⭐ **F21 reader gap
CLOSED** (`spent_time` is on no class — use `spent_time_sum`/`_values` + the
`ticket.start_wait` restamp); ⛔ leg S's `CheatResearchAll` premise MEASURED
false (`discovered=nil`). ⇒ **NEXT: the ATTENDED sitting, prompt 2 of
`agent/prompts/corun-batch-2/` — est. ≈33–36 owner-min (8 of them console, S6).**

## Build state — `python tools/doccheck.py --emit-counts`, never hand-typed

```
BUILD STATE (emitted by tools/doccheck.py)
- modules: 81 registered (74 default-active, 8 optional-gated files)
- Code/*.lua files: 82
- TestKit probes: 87
- BUGS index rows: 101 F + 12 D + 44 C
```

Re-emit after any module/entry change (red refuses); pinned **1.0.7.396349** (fpk parity — `agent/facts/EF-014`).

⚖️ **F76 CLOSED-REFUTED**, residue **`C41`**. ✅ **F11** route (a) SETTLED, ⛔ NOT
`tested`. ⚠️ **F99 (`cand`)** cheat-only — 0-in-4 organic + 0-in-2 cheat-driven,
rate bounds NOT refutation; untested cell on the entry. ⚠️ **C42 (`cand`)** 3
empty denominators — this save cannot sample it. ⭐ **PROBE COVERAGE** —
`[install]` 87/0 (EXECUTED-ONCE); ⛔ **retail coverage is 78/87.**

## Gates and holds

- **PT-62 remainder** (P12/P13/P14 + split loop counter through a landing) → D12;
  P4/P6 PASSED 2026-08-03, ⛔ NOT a release gate (opt-in). ⚠️ D12's preflight
  names the wrong class (F64 shape) — self-deactivates, recovers.
- ⭐ **F02 · F78 · F81 ORGANIC 2026-08-03 — ALL THREE again 2026-08-05** (watchdog
  restart · storm-wedge heal, 11 strays · rains re-roll; on entries).
- **F42** `blocked`, wontfix recommended. **D08 extender overhaul** speced in
  `agent/reports/DRONE_OVERHAUL_OPTIONS.md`, unbuilt; D06's dials wait on B2.
- ⛔ **Close-out gate hole (2026-08-10):** it checks both git trees, NEVER the save
  dir — 4 staged copies outlived their commits. Deleted; check it in close-out.
- **Owner decisions (15 open** — ⭐ **F101 DECIDED 2026-08-10 `wontfix`/out of
  scope:** impossible on a cheats build, retail needs `AreModdingToolsActive()`,
  ⛔ NOT our TestKit; toolkit → FUTURE_IDEAS #6) → checklist, never only agent
  docs. Relabel wording OWED.
- **Release** — `docs/archive/MOD_DESCRIPTION.md` is FROZEN, rebuilt from
  `agent/bugs/` at launch prep; four `[DRAFT NOTE]` markers die first.
