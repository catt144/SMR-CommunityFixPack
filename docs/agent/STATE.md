# Project State — the one mandatory read

Current only, rewritten in place; history newest-first in
`docs/archive/SESSION_LOG.md`. Defect truth `agent/bugs/INDEX.md` · engine facts
`agent/facts/INDEX.md` · doc map `docs/README.md` · authoring `agent/WORKFLOW.md` ·
code `agent/FIX_POLICY.md` · chains `agent/reports/CHAIN_METHOD.md`.

## Where the project is
✅✅ Build chain COMPLETE 08-03; every closed co-run's audit sustained every
verdict. ⭐⭐ **SHIP LINE RULED 08-12 (owner — checklist 14): evidence campaign
FROZEN pre-release** (`fixed` + suite + self-checks + verified save-safety tier IS
the bar) · **trains = OPTION A, queue CLOSED** · ✅ **D13 UNBLOCKED 08-14 (`tested`)** · riders
C42/F99/F80/F96-R2 → post-release WATCH. ⇒ **NEXT — release front only:**
⓪ ✅✅ **OPT-IN SPLIT CLOSED — built, verified, audit-SUSTAINED (08-12; chain consumed; every matrix
verdict re-derived from archived logs, save contract held, no-retraining test PASSED). A SECOND MOD
EXISTS: `C:\Dev\SMR-OptInPack`** (`SMR_CommunityOptInPack` / `SMROptInPack` / `[CommunityOptInPack]`;
junction in, public remote 08-13; D01-D07/D09/D12 → tombstones here; ⛔ NO Mod Options page — nor the fix pack).
⇒ ✅✅ D13 chain closed 08-13, audit sustained; ✅✅✅ **② THE SITTING RAN 08-14 AND IS DONE** ⇒ ✅✅ **③ CLOSED 08-14 — release-3 chain consumed, folder EMPTY.** 5 deliverables in `agent/reports/`: `RELEASE_DESCRIPTION_{FIXPACK,OPTIN,RESCUE}` (FIXPACK/OPTIN diff-proven VERBATIM to the audited cards, re-proven by the audit; 9 fill-in markers, all deletable) + `RELEASE_UNINSTALL_ASSEMBLY` + `RELEASE_PORTAL_PREP` (the ④ sheet; its rescue metadata-string cells were TRANSPOSED — corrected + re-measured). Terminal audit 08-14: all counts re-emitted CLEAN (§10.5 stays RESOLVED-provisional, 28 bundled w/ 17; 23 DONE 90→78/22→12/4→4), both hedges verified still open at source, firewalled control caught **3**: 2 FIXED agent-side (⛔ the rescue "load once…delete" flow NEVER SAID SAVE — the pass edits the loaded colony, only the player's save writes the file; and "does nothing while either mod is installed" contradicted the PER-MOD stand-down — rescue card + rescue `metadata.lua` `4d816b6` + opt-in FILL-IN 2) · 1 ROUTED = **checklist 29, decide BEFORE any paste**: the cards' "you will see a notice" claim is LOG-only — `GetMissingMods` skips `optional_mod` mods (all three), §10.9(4)'s "sees one notice" aside was a rig-console inference, no retail screen event exists ⇒ ✅✅ **ALL 5 ④ CALLS RULED+APPLIED 08-14 (29 STRIKE — swept off both cards, rescue card, assembly, site FAQ, source records · 17 HOLD-OFF — rescue = contingency only, fill-ins deleted per defaults · 28 ship-as-built, closes w/ 17, re-open w/ save-step line if contingency fires · cleanliness = Reading A, no text change · opt-in version 1.0.0 SET; bodies re-measured, VERBATIM re-proven post-strike) ⇒ ⭐ **OWNER ORDERED 08-14 ("finish the f85 first"), and ⑥ PROMPT 01 IS CONSUMED 08-15: BOTH MODULES ARE BUILT** — `Fix_DistressPopupPause` (F85, §1.4 wrapper in §3a Layer-3 form; `dont_pause` has ONE setter, re-derived) + `Fix_AutomationLawCompensation` (C39, §1.4 post-wrapper adding the EXACT delta — a THIRD design candidate that beats both the entry's; the label sweep found **EIGHT** mismatched families over **TWO** labels, incl. Security Station/Post, Drone Assembler, Bottomless Pit; Research label clean) + 2 probes (`59_Probes_Wave10.lua`). ⭐⭐ **② PROMPT 02 IS CONSUMED 2026-08-15 — BOTH ENTRIES ARE NOW `fixed`, ON TWO UNATTENDED LAUNCHES, OWNER COST ZERO.** Suite **`80/0/16/0` of 96** (`archive/u3suite_*`; baseline was `78/0/16/0` of 94 — delta is EXACTLY the two new probes, SKIP set byte-identical to baseline, gate `76/76`+`8/8`). ⭐ **C39's paused bracket MEASURED THE REPAIR WORKING** on a staged `CP15PT15` copy (`archive/u3c39_*`, **0 `[LUA ERROR]`**, all four reads at ONE `GameTime` and `speed=0x` **including the revert** — the 08-11 gap closed): subject **126 → 253 = 2.008×** (unfixed it was 1.03×), **control unmoved by us, our delta 0 at every read** (no double-pay), repair stands down when the law does. ⛔ Only **1 of 8** families sampled — `FactoryBuildings`/`ResearchBuildings` were EMPTY in that colony, the other seven stay SOURCE. F85: flag **true→false**, already-pausing popup untouched, no-flag popup **stays nil**, idempotent, install witness `active`/empty detail ×3 — ⛔⛔ **NO SCREEN CLAIM, by construction**; §3.6 rider RETIRED on its own condition. ⚠️ 2 findings routed to 03: a harness defect in this leg's own family tally (`rawget(b,'class')` → eight bogus `0 present` lines, entry-disclosed, changed no verdict) and an **EF-051 close-out miss — the extensionless `U2RT1` (54 MB) survived the 08-14 sweep** because it has no `.sav` extension; its `CreationTime` is the 08-12 restore window, so NEITHER launch tonight restored it and the falsifier did NOT fire. ✅✅ **③ = `unattended-3` TERMINAL AUDIT CONSUMED 08-15 — CHAIN CLOSED, folder EMPTY, BOTH BUILDS SUSTAINED** (routes re-derived at Src; an INDEPENDENT label sweep reproduced the 8-family list exactly; both logs re-counted — suite verdicts deduped to 80/0/16/0 of 96, SKIP set identical BY NAME; bracket + flip readings verbatim; screen-claim boundary held everywhere). Surfaces re-trued: judgment calls **SIX**, settled ONE way (card + `metadata.lua` description 843 chars + site FAQ ×3 + landing + fix list) · "96 checks" · the owed C39 disclosure ¶ (DISPLAY names — ⛔ `BottomlessPitResearchCenter` renders as **"Experiment 1: Big Drop"**, never "Bottomless Pit" on a player surface) · STORE↔RELEASE VERBATIM re-proven post-edit · site +2 entries (75-of-76 coverage), `mkdocs --strict` GREEN, ledger noted · packaging re-simulated **80 = 77+3** · portal §2 11,581 chars / §3 843 / §4 80. ⛔⛔ **MID-AUDIT the owner sitting filed item 31 AND RULED IT THE SAME DAY: THE F85 MODULE IS REMOVED AND SHELVED** (its dialog is DEAD-CODED on retail — sole caller behind `local cond = false`, `POIAdditionalContent.generated.lua:89-111`; audit re-verified at Src). ⇒ **the audit's OWN 08-15 numbers above are SUPERSEDED**: judgment calls SIX → **FIVE**, "96 checks" → **95**, site +2 entries → **+1** (coverage **74 of 75**, entries **77**), packaging 80 = 77+3 → **79 = 76+3**, portal §3 843 → **844**, §2 11,581 → **11,542**, §4 80 → **79**. All re-emitted, none hand-adjusted; card+site+`metadata.lua` swept, `mkdocs --strict` + doccheck GREEN. ⛔ Suite baseline NOT re-measured — predicted `79/0/16/0` of 95, gate `75/75`. ⇒ ✅ **NEXT = ④ OWNER, decision-free**: upload TWO mods (previews `reports/preview_art/FINAL_*.png`) → links → Pages.
① **D13** — ⭐⭐ **BUILT *AND VERIFIED IN GAME* 08-13, AUDIT-SUSTAINED** (chain consumed). Spec `D13_EXPOSED_SET.md` §10 ·
verification + predictions-written-first `agent/reports/D13_VERIFICATION.md` (**§5 = audit corrections**) · 10 logs `archive/rs_*`, all byte-verified.
⭐ **THIRD MOD `C:\Dev\SMR-CommunitySaveRescue`** (`SMR_CommunitySaveRescue`/`SMRSaveRescue`/`[CommunitySaveRescue]`;
remote public; ⛔ junction PULLED — NOT a standing rig mod; owner tick SPENT, 26 STRUCK). **9 launches, owner cost
ZERO, 0 `[LUA ERROR]`: auto pass removed 1617 BY NAME, BOTH KEEP kept (D10 SAMPLED at last — ⚠️ witness SYNTHETIC),
heals 1+1, threads valid, 2nd load 0, residue-zero measured.** ⚠️ §10.9(4) CORRECTED: one ENGINE savegame-mod-ref
line (both packs same, self-clears next save) → uninstall text. D1/D2 SOURCE→MEASURED · `EF-055` holds for mod 3 · ⛔ `tested` NOT granted.
② ✅✅✅ **THE COMBINED SITTING RAN 08-14, ALL THREE MOMENTS PASSED, ⭐⭐ D13 IS `tested`** (owner cost **34 min** parked vs a 30–45 promise; 6 logs `archive/cs_*` byte-verified; 0 `[LUA ERROR]` in every cell). **PT-20 redo CLEAN in state 3** (`pack=0/0`+`opt-in=0/0`, kit alone, 8 pack-lines accounted) — **supersedes** the 98-vs-98, does not confirm it. **After-sweep on a NATIVE witness: `removed 1566` matching a prediction committed beforehand row-for-row**, F48 kept, `heals 0,0,0`; all three DIALOG readings witnessed. **F102 local half CLOSED** (one already-placed deposit, sign renders + selectable) ⭐ and its uninstall claim promoted SOURCE→MEASURED (entity reverts, 0 re-sign lines, packs off). ⚠️ 3 findings: **`EF-055` LIMIT — the rescue tick did NOT survive the junction round trip** (2 causes, undistinguished, cost 1 owner visit) · **spec §10.5 vs `report_text()`** — the drone-dial gloss the build never prints (③ owns it) · 2 own-instrument defects. Rider: a vanilla stale landscaping overlay (owner lead).
③ ✅✅ CLOSED 08-14 — descriptions ×2(+1) + uninstall assembly + ④ sheet; audit: 2 fixed, 1 routed (item 29) → ④ owner launch tasks.
⑤ ✅✅ **public-docs chain CLOSED 08-14 — folder EMPTY, terminal audit consumed** — descriptions ×2 BUILT+audited (22d applied, 22b struck, `metadata.lua` strings `1ac1187`; ⭐ **R1 APPLIED 08-14** — card's patch-retirement claim narrowed to shape-of-the-code, record in `STORE_FIXPACK.md` notes) ⇒ ⭐⭐ **SITE BUILT 08-13 + TERMINAL-AUDITED 08-14 (shipped-module control CLEAN · exposure gate INTACT · trackers live-verified open): `C:\Dev\SMR-CommunityMods\content\` 5 pages (fix list 76 entries · FAQ incl. 22c's hostile-reader section · for-modders w/ hole 3 answered · landing · installing); ledger `reports/SITE_BUILD_AUDIT.md` incl. prompt-6 dispositions**. ⛔ Nothing on the web (`workflow_dispatch` only, Pages OFF; build GREEN `--strict`). ⛔ **Fix list covers 73 of 74 modules DELIBERATELY** (`F98`: the tech-description fix is a retail no-op). Feeds ③. ⭐ FOUR repos + shared TestKit. `MOD_DESCRIPTION.md` FROZEN, **≥6 known-false claims** (+`F24`/`F28` bullets promise DELETED fixes; sensor-tower causation is inverted) — treat every claim in it as unverified.

## Build state — `python tools/doccheck.py --emit-counts`, never hand-typed
```
BUILD STATE (emitted by tools/doccheck.py)
- modules: 75 registered (75 default-active, 0 optional-gated files)
- Code/*.lua files: 76
- TestKit probes: 96
- BUGS index rows: 103 F + 12 D + 48 C
```
Re-emit after any change; pinned **1.0.7.396349** (`EF-014`). F76 → `C41` ·
F11 settled P2 · F99/C42 rate-only · ⛔ **SKIPs BY NAME, never a total.**

## Gates and holds
- ⭐ **BOTH MODS LOADED is the rig's NORMAL config** (owner 08-12; WORKFLOW clause
  ACTIVE). Two gate lines — grep the FULL bracketed token. ⭐ **BASELINE (cell a2,
  audit-recounted): `74/74` + `8/8` · ⭐ **RE-MEASURED 08-13: `78/0/16/0` of 94** (`archive/rs_r0_*`;
  `84/0/10/0` with the rescue mod loaded, `rs_r1_*`; `FactionFundingCheck` still PASS, repair still queued)** — ⭐ **RE-MEASURED 2026-08-15 at 96 probes: `80/0/16/0`** (`archive/u3suite_*`; +2 PASS = the two new probes, SKIP set unchanged BY NAME) — ⛔ superseded same day by the item-31 removal and predicted at `79/0/16/0` of 95 — ✅✅ **THAT PREDICTION IS NOW RETIRED BY A REAL READING: `80/0/16/0` of 96, MEASURED 2026-08-15 15:20 (`archive/c47suite4_*`), gates `75/75` + `8/8`, SKIP set 16 names IDENTICAL BY NAME to the u3suite baseline** (the +1 over the predicted 95/79 is the c47-farm wave-11 probe). ⚠️ Its first two launches FAILED on defects in that probe, not the game (`c47suite1/2_*`, both disclosed on `C47`); order TestKit→fix→opt-in
  (opt-in wrappers OUTERMOST, `EF-054`). ✅ Owner re-tick SPENT 08-12 (`5x`/`+2`).
- ⛔ **`EF-056`: loading a COPY of a real campaign still runs that campaign's
  AUTOSAVE, whose rotation deletes the owner's autosaves** — it ate `Autosave Sol
  306` (unrecoverable); pre-copy every autosave first. ⭐ **`EF-055`: a junction
  can be pulled/restored agent-side** — real uninstall, zero owner cost.
- **PT-62 remainder** → D12 (record now in the opt-in repo); ⛔ NOT a release gate.
  ⚠️ Its P13 lever renamed: `SMROptInPack_Disabled.NoHomeless`. ⭐ **F02/F78/F81
  ORGANIC watch**; F42 wontfix; D08 speced, unbuilt. ⚠️ **Rig has CHEATS ENABLED**
  — name it beside "retail" claims. ⛔ **Mod-Manager disable needs a FULL RESTART**
  (D13); re-enable is owner-only. ✅ **EF-051 HOLD LIFTED 08-14 (owner: cloud OFF):**
  all 17 strays the hold restored are deleted (888 MB), 76 `.sav` listed by name; "gone —
  NAMED listing" allowed again; falsifier = any stray at next launch. `CP60RT`+`Sol 311` intact.
- **Owner decisions open: 0** ✅ — item 33 ANSWERED 08-15 in the attended sitting (*"nothing but normal maintenance"*), so the free-hex transient is NOT the story; nothing is owed anywhere, release front included. ✅ 08-15 **item 32 RULED + DONE: `U2RT1` DELETED** (owner: *"You can delete that save"*); save dir verified at 77 entries, only non-`.sav` left = `account.dat`/`.bak`/`steam_autocloud.vdf`, `EF-051` falsifier stands. ⭐ 08-15 **OWNER RIDER FILED — `C47`, Open Farm seed starvation, from an ORGANIC speed-run observation** (endless *waiting for Seeds* popups + VO next to a FULL `180/180` depot, 24 drones `Low` load). SOURCE-VERIFIED both halves: `OpenFarm.lua` **never sets `consumption_max_storage`** (editor default `5*ResourceScale` = the `0/5` on screen, `HasConsumption.lua:44`) but DOES override `vegetation_interval` 20→3 (**2.0 h → 0.3 h tick**, `TerraformingBuilding.lua:365`/`:373-374`), plants **3+Random(3)** hexes/tick (`:918`) each costing the crop's `placement_consumption_amount` (Potato **600**) ⇒ **up to 3000/tick against a 5000 buffer, under TWO ticks of runway**, and a drone carries only **1000** (`Drone.lua:719`, `DroneResourceCarryAmount=1`). ⭐ **COMPARISON IS DECISIVE**: the only other Seeds consumer / other `VegetationPlant`, **`ForestationPlant`, SETS storage 10000**, keeps the 2.0 h tick and plants **1** hex (2 under `Policy_ForestationEffort`) ⇒ **~40 in-game hours runway vs ~0.5 — ~80×**; Open Farm's cheapest crop still only ~3 h. Whole-tree sweep: **287 templates, 29 consumers, 10 at default storage — Open Farm the ONLY one of those that also tuned its cadence** (one half of a paired change, `C39` shape). ⛔ **NOT a proven defect** (unset field, not a self-contradiction; no comment ties the two), ⛔ **NOT measured in game by us**, ⛔ **not a release gate, nothing built.** Three unranked shapes recorded on the entry. ⚠️ **OWNER IS TESTING MORE THINGS THEMSELVES BEFORE LAUNCH** — expect further riders; do not treat ④ as imminent without their word. ⭐⭐ **CHAIN `agent/prompts/c47-farm/` — PROMPT 01 IS CONSUMED 2026-08-15, 7 UNATTENDED LAUNCHES, OWNER COST ZERO, 0 `[LUA ERROR]` in all 3 sampler runs.** Job 1 SHIPPED: `Code/61_Probes_Wave11.lua`, one permanent probe asserting the template pair **as RATIOS** (storage 2:1, cadence 20:3) and re-running the engine's own tick derivation, so a rebalance cannot silently pass; it tests no fix and PASSes with the pack on or off, by design. Job 2/4 = a temporary sampler counting flaps via **`OnMsg.AddNotificationObject`/`RemoveNotificationObject`** (no wrapper, no patched path) on a staged COPY of `C47FARM`; predictions committed FIRST in `3f1856f`, pushed before launch 1. ⭐⭐ **THE CONTROL WAS SAMPLED IN FORCE — 36 `ForestationPlant`s, 0 adds in all 3 runs, buffers 85–100%** — so the `UNSAMPLED` failure mode did not fire. ⛔⛔ **BUT THE SYMPTOM DID NOT REPRODUCE AND THE ENTRY'S DRAIN RATE IS REFUTED at ~40%**: over 15.3 game hours (≈51 ticks) neither farm's buffer reached 0 (minima 305–605 / 1661–2041 of 5000), farm B raised ZERO adds, farm A 2/0/4. ⭐ Cause MEASURED not reasoned: a planting iteration only spends when it finds a FREE or over-represented hex (`TerraformingBuilding.lua:943-961`) ⇒ **~2.2 hexes per draining tick vs the assumed 4, ~28% of ticks spending nothing** ⇒ the entry's 3–5/tick is the **CEILING of a farm with hexes to fill** (after building, enlarging, re-cropping, wither) — which is what a speed-run does, so the owner's sighting is BOUNDED, not refuted, and item 33 asks the one question that settles it. ✅ Template asymmetry CONFIRMED live. ✅ Voice cooldown proven **text-keyed + RealTime** by one live `VoicesCooldown` key (`769547039933`), correcting this entry's per-object claim. ⚠️ Rig facts found: drones carry **3000** not 1000 (our own D09 dial at +2), and **this save's clock STOPS at day 385** under a `PopupNotification`'s `XPauseLayer` (`IsPaused()=1` at factor 5x), capping every unattended run at 0.64 sols. ⚠️ 3 defects in OUR OWN instruments disclosed on the entry (probe asserted a live const our dial raises; `rawget` on a built class sees flattened inherits, `classes.lua:988`; sampler mis-read 3 fields). ⛔ Still forbidden: generalising to the other 9 default-storage templates, any rate without the cheats+speed-run confound. ⭐⭐ **THEN AN ATTENDED SITTING RAN THE SAME EVENING ON THE OWNER'S OWN `C47FARM` (their ruling: live campaign, not a copy; `Opt_AcknowledgedWarnings` kept ON for fidelity — census proved the ack-flag nil on all 38 objects, so it was a pass-through). Log `archive/c47ride_*`, 0 `[LUA ERROR]`. THE SYMPTOM REPRODUCED AND THE BANNER WAS WITNESSED** (owner-typed into the log: *"Buildings Not Working = 2, Insufficient resources + Seeds icon"*) ⇒ `C47` may take a SCREEN claim at last; ⛔ no voice line was witnessed. Readings: subject **52% of samples stopped (63/121), buffer min 0, 7-8 adds**; the other farm **0% and 0 adds**; 36 controls **0% and 0 adds**. ⛔⛔ **FOUR OF THE UNATTENDED LEG'S OWN CONCLUSIONS ARE OVERTURNED, THREE BY THE OWNER**: (1) "buffer never reached 0" REVERSED — `reason=Consumption` fires only when `CanConsume()` is false i.e. stored==0 (`Building.lua:611-613`), so a SAMPLED minimum is not a minimum; (2) "the rate gap is variance" refuted (~13x, far outside the 2/0/4 spread); (3) "drone contention from player construction" refuted by the owner (*"nothing but normal maintenance"*); (4) ⭐ **"the two farms are a clean crop-cost control" WITHDRAWN — they SPENT ALMOST THE SAME (9,425 vs 9,520) and differed in RECEIPT (7,320 vs 8,361)**; the owner supplied the better reading — different vegetation density and drone-extender coverage, i.e. different SUPPLY environments, never a clean A/B. ⇒ ⭐⭐ **`C48` FILED, AND IT IS THE OWNER'S FIND**: wild vegetation offers Seeds via `VegetationTaskRequester` (`Vegetation.lua:1774-1783`, per-hex `seed_cooldowns`) inheriting `supply_dist_modifier = 100` — **no distance brake** — while the ONLY override in the tree is `SurfaceDeposit = 150` (*"considered 50% further than storages"*). Hundreds of one-plant trickle offers compete with a full 4,000 depot on equal terms on a carpeted map. ⛔⛔ **DO NOT BUILD A `C47` FIX WHILE `C48` IS OPEN — a bigger buffer would MASK it.** ⚠️ **`EF-056` FIRED: the sitting's rotation DELETED `Autosave Sol 376`; restored byte-exact from this leg's pre-copy** (first time the pre-copy rule saved a file in this chain). ⇒ ✅✅ **PROMPT 02 IS CONSUMED 2026-08-15 — ONE UNATTENDED LAUNCH, OWNER COST ZERO, 0 `[LUA ERROR]`, PREDICTIONS COMMITTED AND PUSHED FIRST (`94eb508`), LOG `archive/c48veg_*`. ⭐⭐ THE ROUTING SIGNATURE IS MEASURED AT LAST — 134 deliveries at 200 ms sampling, NOT ONE a full drone trip (3000 on this rig), 112 of 134 = 83.6% EXACTLY 280, which is a live requester's own reading (`preset=Bush`, amount 280 = Bush's 200 × the 1.4 soil bonus), while the colony's 34 Seeds-accepting depots held 12,406,609 Seeds and the total ROSE 2,498 (21 of 34 unmoved).** The two farms ate 45,480 in 280-crumbs beside 12.4M in storage. ✅ `supply_dist_modifier=100`/`priority=1` read LIVE. ⛔⛔ **THE OWNER'S DENSITY EXPLANATION IS REFUTED AS STATED by the entry's own pre-registered falsifier — A reaches 533 offers, B 490 (1.09×); A is ~10% denser locally (142 vs 127). 6× the extenders, same plants: they overlap.** ⛔ **The owner's SPEED LADDER is UNSAMPLED, NOT refuted** — 0 stalls in 838 samples on both farms at all four rungs (`took=true complete=true` ×4, 14 game h, no freeze); ⭐ but per-game-hour drain is flat at 1x/3x/5x (2466/2306/2866) and COLLAPSES at 20x (0 · 300) = their direction, ⛔ perfectly confounded with `C47`'s hex-saturation because the rungs are ordered in time ⇒ **the named control is a DESCENDING ladder on a fresh copy, one unattended launch.** ⚠️ 2 own-instrument defects found at source BEFORE launch, disclosed: the payload asked farms for a `work_radius` NO building has (`DroneNode`'s, `DroneControl.lua:40`) so the headline would have been NOT SAMPLED — repaired to the game's own `FindTaskRequesters` shape; and `C47_ARM.ps1`'s strip pattern named `C47` not the family, so it could never remove its own C48 line and two kit commits saying "returns to its shipped shape" each ADDED a stray (fixed, kit `15cb9d4`). ⇒ **NEXT: `03_AUDIT.md` LAST** (renumbered 08-15 on the owner's catch — it deletes the whole folder; rescoped to audit all three legs + the sitting). ✅ **FIXTURE DELIVERED 08-15: named save `C47FARM`**, taken while the symptom was live (named, not autosave ⇒ `EF-056` sidestepped), game closed. **TWO farms**: A = Potato·Cover Crops·Wheat, B = Cover Crops·Leaf Crops·Wheat photographed at `3.7/5` (partial buffer, first fractional reading). Owner **could not tell which farm voices** — now a named deliverable (per-object counters). ⚠️⚠️ **C47's OWN ARITHMETIC CORRECTED THE SAME DAY, off that screenshot**: `GetCurrentHarvestType` ROTATES round-robin per placement (`ThreeHarvestTypesBuilding.lua:40-60`), so the per-hex cost is the **MEAN of the selected crops, NOT the dearest** ⇒ the `5×600=3000`/≈2 ticks/≈0.5 h headline was the **Potato-ONLY worst case** and is retracted as the operating figure; the owner's farms run **~1467 / ~933 per tick = ~3.4 / ~5.4 ticks = ~1.0 / ~1.6 in-game hours**. ✅ The FINDING survives — `ForestationPlant` still holds ~40–80 h ⇒ gap **~25–80×**, and 'cadence tuned, buffer not' is untouched. ⭐ Display names pinned: **Cover Crops = `Rapeseed` (100), Leaf Crops = `Spinach` (200)** — preset ids appear on NO player surface (the *Experiment 1: Big Drop* trap). ⚠️ **With only 2 farms the `VoicePerObject` cap is 2 voices/60 s real**, so 'endless' must come from the BANNER (no per-object cooldown) — the leg must report banner churn and voice-eligible adds SEPARATELY, and check whether `QueueVoice`'s cooldown is real or game time (they diverge hard at high speed, which is the owner's condition). ⛔⛔ ✅✅ 08-15 **item 31 RULED + APPLIED THE SAME DAY: THE F85 MODULE IS REMOVED AND SHELVED** (owner: *"we remove it but document it, so we can easily re apply it as a fix"*). Cause: `DistressCallPopup`'s SOLE caller in all of Src is the `DISTRESS CALL` XAction, which the shipped executable compiles behind a literal `local cond = false` (`POIAdditionalContent.generated.lua:89-111`; `__condition` returns literal `false` at `Data/XDef/POIAdditionalContent.lua:97`) — feature DEAD-CODED WHOLE (`DistressCallAllowed`/`Enabled`/`WaitIssueDistressCall`/`CooldownEnd` all trace to that one button), so the fix could never fire for a player while its wrapper sat on EVERY popup's `Init`. ⭐ **SHELF RECORD `agent/reports/SHELVED_F85_DISTRESS_PAUSE.md`** = module + probe **VERBATIM** (byte-compared pre-delete) + re-arm trigger + a 30-s source check that answers it + 6-step re-apply checklist + already-proven table — ⛔ deliberately NOT "it's in git history". APPLIED: module + its `metadata.lua` file-list row + probe DELETED · entry → `wontfix` · card judgment bullet + site fix-list entry DELETED (the latter opened *"What you saw:"* about an unopenable window — item-29 class) · judgment calls five→six→**FIVE** in all 6 places · suite **96→95** · modules **76→75** · files **77→76** · packaging **80→79 (76+3)** · portal §3 843→**844**, §2 11,581→**11,542** · `SITE_BUILD_AUDIT` reversed (coverage **74 of 75**, entries **77**) · doccheck + `mkdocs --strict` GREEN. ✅ **item 5 CLOSED with it — F85 stays `P3`/LATENT/tier U** (real, reproduced, unreachable by any player route). ⚠️ Third instance of *player-route ≠ source citation*, second in this ONE entry after `idQuickSave`. ⭐⭐ 08-15: **26b RULED THEN AMENDED THE SAME DAY — THE STATUS VOCABULARY IS SPLIT AND BUILT.** Owner's amendment (they backtracked on their own first answer once its consequence was named): *"labeling things tested - unattended / tested attended… allows the attended tested to have more serious weight if we are troubleshooting."* ⇒ **`tested-attended`** (human at the keyboard — strongest word, lean on it when debugging) · **`tested-unattended`** (real launches, nobody watching; ⛔ NEVER for a screen event) · ⛔ **bare `tested` = LEGACY/attendance-UNAUDITED, closed to new work** — 46 entries hold it, only 29 cite a sitting, **17 carry the bare word and no narrative** (`F03`/`F44`/`F66`+14), so it must NOT be read as "attended". APPLIED: `doccheck.py` `STATUS_WORDS` (+2, longest-first so `startswith` cannot mis-parse), `WORKFLOW.md` §step-5 picker, **F85 + C39 `fixed` → `tested-unattended`** (front matter + heading tag + row_status), bugs `INDEX.md` regenerated, doccheck GREEN. ✅ **RETRO-PASS RULED OUT 08-15 (owner: "No to the retro pass")** — 46 legacy labels frozen as-is, never bulk-upgraded; cite one as *"recorded `tested`, attendance unknown"*, re-derive a single entry only if it becomes load-bearing. ✅ 08-15: **26's leftover RULED — rescue display name becomes "Community Fix Pack: Save Rescue", PRE-APPROVED but applied only IF it publishes**; recorded as pre-upload step 1 in `reports/RELEASE_DESCRIPTION_RESCUE.md` beside the item-28 dialog text + save-step line. ✅ 08-15: **item 30 RULED THE DAY IT WAS RAISED — C39 SHIPS AS BUILT, all 8 families, no list** (owner: *"true to the code… a true to code bugfix as much as possible"*; the module carries no building list, so restricting it would have meant ADDING one). ⛔ Settles SCOPE only — the card must still disclose that it moves gameplay-visible numbers, and the 7 SOURCE families are NOT upgraded. ✅ 08-13: **item 27 bug-report route DECIDED + WRITTEN** (comments first, tracker for save/log; issue trackers open on both mod repos = a watched inbox). ✅ 08-13: 22d APPROVED+APPLIED (9a) · 22c YES → prompt 5 · relabel CLOSED via item 22. ✅ 08-13: **26 STRUCK — tick
  DONE 11:16, matrix ran off it**; the optional family-prefix rename is all that survives it (release prep). ✅ 08-13: D13 17-19 ((c) · mod-shaped · gates GO; launch dep REAFFIRMED, build ≠ publish) · opt-in remote PUBLIC · display name **"Community Fix Pack: Opt-In Modules"** (swept, `e17586b`) · default-OFF RATIFIED · rescue naming RATIFIED + remote pre-created (`catt144/SMR-CommunitySaveRescue`, empty).
- **Release** — now **THREE** products (rescue publishes = a release-time call, checklist 17), each with its own metadata/description/preview/portal pass; `archive/MOD_DESCRIPTION.md` FROZEN, rebuilt at prep.
