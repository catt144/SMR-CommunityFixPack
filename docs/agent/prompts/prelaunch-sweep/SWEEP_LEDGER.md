# Sweep ledger — COVERAGE ONLY

⛔⛔ **VERDICTS DO NOT GO IN THIS FILE.** Findings go in `SWEEP_FINDINGS.md`,
which the next link ⛔ **may not read**. This file is what the next link ⛔
**must** read, and it is the only reason the chain can converge instead of
looping: five blind sweeps can each cover the comfortable 60% and never touch the
awkward 40%.

**Write your row BEFORE you start work** (claims the lens) and complete it at
close-out.

⭐ **The `NOT reached` column is the most valuable column in the chain.** It is
what the stopping rule reads, and it is what the terminal audit reports to the
owner as the honest product of the whole effort. An empty `NOT reached` cell is
almost always a lie — say what you could not see.

⚠️ **Write that column as TERRITORY, not as findings.** *"Whether two shelter
preconditions can co-occur on one colonist — needs a running game"* says
everything the next link needs; *"whether **L1-F1**'s two preconditions can
co-occur"* names a finding in a file every link is required to read. Link 1's row
does the second, which is part of why the fence was found to be partial
(`00_CHAIN_SPEC.md` §2, 2026-08-18 amendment). ⛔ This is a light preference, not
a disclosure rule — do not file a contamination report over it.

---

## Lenses (`00_CHAIN_SPEC.md` §3) — take the first one not yet claimed

`L1` structure & collision · `L2` lifecycle & idempotency · `L3` save & exit ·
`L4` player experience · `L5` failure & containment · `L6` promise vs behaviour ·
`L7` environment & namespace · `L8` adversarial / hostile modder

---

| link | lens | scope actually covered | depth | ⭐ NOT reached | config | commit |
|---|---|---|---|---|---|---|
| *(seed — not a link)* | — | `metadata.lua` upload guards; the shipped `.fpk` file list; `00_Core.lua` status/`order`/`update_suspect` paths | mechanical: preflight tool (20 guards) + real archive listing + every `status`/`update_suspect`/`order` site enumerated | ⛔ **everything else.** No module was read. Nothing was run in a game. No collision map. No save-footprint measurement. No packed-install test. No player-experience pass. | dev tree, unpacked, all three mods | `2f077e8`, `2e47e3a` |
| **1** | **L1 — structure & collision** | All 76 `Code/*.lua` for patch sites, **alias-resolved**: 197 rows / 134 symbols / **50 class-method targets** / 16 global replacements / 21 `OnMsg` registrations. All five exposure shapes of `02_LENS_NOTES.md` L1: class method · table slot · global assignment · preset field · own thread. Cross-checked against `ModTools\Src` (4,446 files, 3,708 `DefineClass`): **C1** every subclass in Src declaring a method we patch on a parent (8 hits, each adjudicated by reading the override); **C2** every method patched twice on one class/chain; **C3** shared class surfaces (16 pairs). `AutoResolveMethods` enumerated in full (58). | **mechanical for code symbols** — extractor-emitted, every row cites file:line, artifact `reports/L1_COLLISION_MAP.md`. **Medium for preset fields** — enumerated by reading the 13 preset-touching modules, ⛔ NOT extractor-backed. Engine routes (`OnMsg` additivity, `classes.lua:608` declare-wins, `Done` not auto-resolved) **re-derived at Src**, not inherited. | ⛔ **Nothing was run in a game** — every verdict is source-derived, and neither 2026-08-17 core fix has yet executed. ⛔ **`OnMsg` state interference**: additivity proven, but whether the 11 `LoadGame` / 7 `PostLoadGame` handlers read+write overlapping state is UNCHECKED. ⛔ **Preset-field map is not mechanical** — a write behind an indirection I did not read would not appear. ⛔ **Second-apply / `ReloadLua` behaviour** (L2). ⛔ **Aggregate save footprint, `90_SaveSanitizer` coverage, uninstall of all 75 at once** (L3). ⛔ **Anything a player sees or reads** (L4). ⛔ **Runtime failure containment in aggregate** (L5). ⛔ **Registry↔package↔card agreement, dead-coded targets, the veto route over all 75** (L6). ⛔ **Globals we leak, packed-vs-unpacked, what the TestKit's `_G` mutation hides** (L7). ⛔ **Any foreign mod** (L8). ⛔ **TestKit tree not included** (`Code/` only). ⛔ **Runtime cost of 75 modules together** — never measured by anything. ⛔ **Whether L1-F1's two preconditions can co-occur on one colonist** — needs a running game. | dev tree, unpacked, source-derived; no launch | *(this commit)* |
| **2** | **L2 — lifecycle & idempotency** | The three routes by which pack code can run twice, enumerated and each adjudicated at Src (`ReloadLua` via the Mod Manager · `run_apply` twice in one load · a DataPatch pass several times per load). A **survives-a-`ReloadLua`** table for all 9 surface kinds a fix can land on, re-derived by symbol (`lib.lua:353-382`, `autorun.lua:423-424`, `classes.lua:37-38`+`:980-1099`, `cthreads.lua:6`+`:82-113`, `Preset.lua:1339-1340`, `Dlc.lua:636-668`, `const.lua:1-3`, `lib.lua:1049-1051`+`:1532`+`:1568-1594`, `Mod.lua:1546-1611`). Mechanical census of all 76 `Code/*.lua`: **284** field-assignment sites classified; **13/13** global replacements looked up at their Src declaration; **23** module `OnMsg` registrations (+8 in core) cross-checked against `ModMsgBlacklist`; the 1 `PeriodicRepeatInfo` slot, the 1 `GameVar` once-flag, the 3 class/template writes, the 1 file-scope thread, and **9 in-place preset edits in 7 modules** each adjudicated. ⭐ Then the real thing: all **58** pack-carrying archived logs counted for `applied` lines, the **2** that show **148 = 2×74** identified as genuine two-Lua-load sessions, and their two load blocks **diffed**. Falsifier harness `tools/l2_reload_sim.py` runs the shipped source twice in one process under Lua 5.4. | **mechanical + MEASURED.** Engine routes re-derived at Src by symbol, not inherited. The second-load behaviour is a **measurement from two independent retail sessions**, not an inference; the harness reproduces all 5 of their load-1 lines verbatim before being used on anything. Artifact `reports/L2_LIFECYCLE_MAP.md`; census script `tools/l2_reload_sim.py`. Preset-edit idempotency was read module by module, ⛔ not extractor-proven. | ⛔ **Nothing was run in a game** — no launch this link; the fix and the two `2f077e8` core fixes remain unexecuted in Surviving Mars. ⛔ **Third and later Lua loads** — both archived sessions stop at two; load 3 = load 2 is a PREDICTION. ⛔ **Mid-game reload** — every caller found is main-menu or Ged; whether a reload can happen with a colony loaded, and what our wrappers do to live objects then, is UNMEASURED. ⛔ **The predicted 2 false suite FAILs on a reloaded session were never observed** — neither two-load session ran the suite. ⛔ **TestKit tree excluded again** (`Code/` only) — its own second-load behaviour is unswept. ⛔ **The `update report:` dialog has 0 occurrences in all 58 archived logs**, so every claim about its behaviour (including the duplicate-thread finding) is source-derived. ⛔ **Runtime cost of 75 modules re-applying** — still never measured by anything. ⛔ Uninstall / save footprint / what a reload leaves behind (L3). ⛔ Anything a player sees (L4). ⛔ Aggregate failure containment (L5). ⛔ Registry↔package↔card, dead-coded targets, the veto route (L6). ⛔ Globals we leak, packed-vs-unpacked, what the TestKit's `_G` mutation hides (L7). ⛔ Any foreign mod (L8). | dev tree, unpacked, source-derived + archived retail logs (all-three-mods rig) + Lua 5.4 harness; **no launch** | *(this commit)* |
| **3** | **L3 — save & exit** | The aggregate footprint, censused mechanically over all **76** `Code/*.lua` in `metadata.lua` order (`tools/l3_save_footprint.py`): **294** non-local assignment sites bucketed by receiver kind, **6** game-time + **2** real-time thread constructors, **2** own `GameVar`s, **13** `SMRFixPack_*` tokens (11 persisted + 2 veto surfaces), **18** load-time passes tabulated **in run order**, and **0** `SaveGameStart`/`SaveGameDone` registrations. Then the census the naming convention cannot do: all **66** field names the pack writes on non-local carriers tested against the whole shipped tree (**4,446** Src files / **131,363** tokens) to find keys of ours that do not use the `SMRFixPack_*` prefix — 9 absent from Src, each read at source. §3a's orphan gate answered over **all six** game-time thread bodies, not per module. The exit re-derived from the persistence mechanism itself (`persist.lua:119-143` read this session, not cited from record) into a per-name survives-uninstall table for all 12 persisted keys. `SavegameFixups` gating re-derived at Src (`SavegameFixup.lua:10`,`:24-50`; **237** shipped fixups, alphabetical, `AppliedSavegameFixups` is a `GameVar`). Track cluster (5 modules mutating track state at load) cross-read against 4 shipped track fixups. `90_SaveSanitizer`'s scoping header checked against the tree module by module. | **mechanical for the census** — extractor-emitted, every row cites file:line, artifact `reports/L3_SAVE_FOOTPRINT.md`; the Src-membership test is exhaustive over the whole shipped tree, not a sample. **Source-read for every adjudication** — the 9 foreign keys, the 6 thread bodies, the 3 track intersections and the persistence route were each read at their lines. ⛔ **Two own-instrument defects found and disclosed in the artifact** (the `function OnMsg.X()` form was missed, hiding 2 passes; a string-blanked scan lost 4 of the 13 tokens) — both fixed before any count above was taken. Engine routes re-derived by symbol, **not inherited**: one recorded claim was re-derived and came out the other way. | ⛔ **Nothing was run in a game.** No save was opened, no footprint was weighed, no object counted — every measured figure in this territory still belongs to the D13 chain and was not re-taken. ⛔ **The simultaneous liveness of the six game-time threads has never been measured by anything**, so "how much is in a save at once" has no upper bound a reader can point to. ⛔ **13 of the 18 load-time passes were NOT cross-read against the 237 shipped savegame fixups** — the meteors/storm pair, the rains/disaster pair and the three preset-patch heals are unswept for load-order interference; only the 5-module track cluster was. ⛔ **Whether two load passes can contend for the same object in a save that needs both** is untested — it needs a save that carries both kinds of damage. ⛔ **The uninstall was not walked**, only derived; and the **reinstall** route (uninstall → play → save → reinstall) has never been exercised by anything in this project. ⛔ **Autosave-path behaviour** of the load passes is unexamined (`SaveGameStart`/`Done` fire on autosaves per `EF-030`, but the pack registers neither). ⛔ **TestKit tree excluded a third time** (`Code/` only). ⛔ **The opt-in pack's own save footprint** was not re-derived; D12–D15 stand as the D13 chain recorded them. ⛔ Anything a player sees or reads (L4). ⛔ Aggregate failure containment (L5). ⛔ Registry↔package↔card, dead-coded targets, the veto route (L6). ⛔ Globals we leak, packed-vs-unpacked, what the TestKit's `_G` mutation hides (L7). ⛔ Any foreign mod (L8). | dev tree, unpacked, source-derived + Src read by symbol; **no launch** | *(this commit)* |
| **4** | **L4 — player experience** | Every surface a player can see or read, censused mechanically over all **76** `Code/*.lua` in `metadata.lua` order (`tools/l4_player_surfaces.py`), five tables: **17** screen call sites (the API set re-derived from Src, each name confirmed a global `function` there, grouped modal / popup / notification / voice), **6** text sites (`T` / `Untranslated`), **59** log sites in 13 files, **181** strings that can become an entry `detail`, and those 181 crossed with `UpdateSuspects`' four substring tests (**72** match). Then the adjudications the census cannot do: the pack's single designed screen surface re-derived end to end at Src (call signature, nil-parent resolution, the engine's own re-wrap of our text, the gamepad action) · both of its player-read strings read as a player who arrived from a store link · the whole `docs/archive/*.log` corpus counted for what the pack writes (**57** pack-carrying logs; per-load composition broken down) · the two blanket claims in `metadata.lua`'s `description` checked **per row** across all 75 modules, not asserted · the site's five player pages and the store card searched for the identifiers and counts the mod's own surfaces emit · one in-game text site diffed byte-for-byte against its shipped original at Src. | **mechanical for the five censuses** — extractor-emitted, every row cites file:line, artifact `reports/L4_PLAYER_SURFACES.md`. **MEASURED for the log corpus** — line counts and a millisecond-level intra-load window taken from archived retail logs, not reasoned. **Source-read at Src by symbol for every screen adjudication**, not inherited; the dialog route had never been derived before and one recorded assumption about it was checked rather than carried. ⛔ Every screen verdict is nevertheless **source-derived**: see the next column. | ⛔ **Nothing was run in a game — fourth link running. No dialog drew, no notification was observed, no first run was walked.** ⛔ **The pack's one designed screen surface has never executed in any archived session** (its pre-dialog log line has 0 occurrences in all 57), so how it looks, where it lands, and whether it opens over the Mod Manager on the enable path are UNMEASURED. ⛔ **No console platform has ever been touched by this project**, and `FIX_POLICY` §7 makes that dialog the only player-facing surface there — the gamepad route is re-derived, never observed. ⛔ **No non-English run, ever**: whether the two re-used translation ids resolve in a shipped language pack was not tested against `Local\*.fpk`, though the route exists (`tools/flpk_extract.py`). ⛔ **The aggregate notification RATE is unanswered** — call sites were enumerated, but whether the pack makes a colony noisier needs a running colony, and one module deliberately revives a warning class vanilla's arithmetic could never fire. ⛔ **The store and Mod Manager entries as RENDERED** — truncation, layout, character limits — stay check-at-paste and have never been seen by anyone. ⛔ One markup-emitting site was not confirmed to render. ⛔ **TestKit tree excluded a FOURTH time** (`Code/` only) — its UI action and console are supposed to be absent in configuration B and were not swept. ⛔ L5 (failure & containment), L6 (promise vs behaviour), L7 (environment & namespace), L8 (adversarial) — **entire**. | dev tree, unpacked, source-derived + Src read by symbol + archived retail logs (all-three-mods rig); **no launch** | *(this commit)* |
| *(interlude — ⛔ NOT a link, no lens taken, rotation undisturbed)* | — (`97_VERIFICATION_LAUNCH.md`, spec §6.5) | ⭐ **The first territory in this chain that is a RUNNING GAME.** Three unattended retail launches on build 1.0.7.396349 via the TestKit autorun harness (`archive/vl97a/b/c_Mars.exe-20260819-*.log`), 66–69 s each, `armed → BEGIN → END → done`. Covered: the whole **load-time surface of the shipping configuration** — 75 registrations and applies, every data-patch/heal pass that fires on a save-less cold boot, the pack's own log output (81 lines at load, 90 per session), and the apply-phase wall clock (**≈0.57 s for 75 modules; ≈1.3 s including the heal work**, off the engine's `Lua` clock markers). The **whole 96-probe suite in the opt-in-ABSENT configuration** (never measured before), taken **by name** and diffed probe-for-probe against `c47suite4` (2026-08-15, same harness, same build, the last suite before the core-file change). The opt-in reached by **junction pull** (`EF-055`), which is itself now measured territory: pull, restore, and two relaunches after the restore. `EF-056` discipline exercised end to end — tag-driven (not name-driven) pre-copy, MD5s, and a by-name reconcile after **every** launch. | **MEASURED, and single-configuration.** Every number is emitted by the running game and carries its configuration; the one comparison drawn is against a baseline taken with the *same* harness on the *same* build differing in *one* mod, and it is stated as re-derived-then-compared, not inherited. The core-file claim is **deliberately NOT upgraded**: the leg proves which code path executed (two log lines, `file:line`-mapped at source) and then says plainly that the resulting registry field is unreadable from a running-game surface. Artifact `reports/97_VERIFICATION_LAUNCH.md` carries the predictions **pushed before the first launch** (`1844095`) and the two places they were wrong. | ⛔ **Anything requiring a SECOND Lua load in ONE process** — the console is the only route and `ConsoleExec`/`debug` are blacklisted from mod code, so an unattended session cannot reach it at all. That single reload is what four separate open questions all need. ⛔ **Any registry FIELD not exposed through a log line or a probe** — a status is readable, a boolean flag on an `active` entry is not. ⛔ **Every screen surface**: nothing drew, and by standing rule a screen event may not be witnessed unattended anyway. ⛔ **The opt-in pack's own runtime behaviour** — it is now not loading and needs an owner Mod-Manager tick; that is an inherited state, not just an inherited question. ⛔ **Warm-save territory entirely**: no save was opened, so aggregate save footprint, the load passes' autosave path, uninstall/reinstall, contended damage, and any precondition needing an existing colonist or building remain exactly as links 1–4 left them. ⛔ **Packed install, TestKit-off, junction-pulled — run B, the gate, still unrun.** ⛔ Console platforms · non-English · any foreign mod (L8) · aggregate failure containment (L5) · registry↔package↔card (L6) · globals and namespace (L7). | **run A** — fix pack + TestKit, unpacked via junction, cold boot, new colony, opt-in absent (legs A/B/C); ⛔ never packed, never TestKit-off | *(this commit)* |
| **5** | **L5 — failure & containment** | Every route by which pack code can throw, and the catcher at each, censused mechanically over all **76** `Code/*.lua` in `metadata.lua` order (`tools/l5_containment.py`): **234** column-0 statements classified by throw-risk (the code that runs OUTSIDE every pcall the pack owns), **six entry classes** enumerated with counts — 75 `apply` · 10 `DataPatch` passes · 3 `OnDataReady` callbacks · 29 module-level `OnMsg` handlers (+9 core) · 53 method wraps · 14 `SetGlobal` · 6 game-time + 2 real-time threads · 1 `PeriodicRepeatInfo` slot — **29** pcall sites in 15 files, and **14** iterating message handlers crossed against whether each carries a per-ITEM guard. Then the adjudications a census cannot do: the containment chain re-derived at Src **by symbol** end to end (`pdofile` `lib.lua:242-251` → `ModDef:LoadCode` `Mod.lua:490-520` → `ModsLoadCode` `:2254-2275`; `procall` `cthreads.lua:15-21`; `ThreadErrorHandler` `:137-141`; ⭐ `ReportModLuaError` `Mod.lua:2958-2993`, which no census of our own code can see) · every `[LUA ERROR]` shape in **all 73** archived logs attributed BY NAME to its declaring file, including the three one-offs nobody had ever traced · the three 08-19 verification legs re-counted for error, apply-failure and mod-flag lines. | **mechanical for the four censuses** — extractor-emitted, every row cites file:line, artifact `reports/L5_CONTAINMENT_MAP.md`. **MEASURED for the corpus work** — error-line counts and the 0-occurrence negatives taken from the archived logs, not reasoned. **Source-read at Src by symbol for every catcher**, not inherited; the engine's player-facing mod-error route had never been derived by this project and is now `EF-065`. ⛔ **One own-instrument defect found and disclosed** (census 5 read only the handler literal and saw 2 of 14 sweeps; it now follows one level of delegation) — fixed before any count above was taken. ⛔ Every containment verdict is nevertheless source-derived: see the next column. | ⛔ **Nothing was run in a game — fifth link, and the refusal is REASONED, not defaulted (artifact §7): all three of this lens's open questions need the pack to THROW, and every route to make it throw is barred — editing `Code/` (record-only + the no-instrument rule), a TestKit probe (the 96 count is on the store card), or a console line (unattended sessions cannot type).** ⛔ **The C side of `map:MapForEach`** — whether its loop `procall`s the callback per object — is not derivable from Lua, and it is what decides the severity of two of three unguarded-sweep findings. ⛔ **Whether `OnMsg.OnLuaError` fires for a THREAD error** (the engine raises `OnThreadError`, a different message) — so whether the pack's 8 own threads are quieter than its wrappers is unknown. ⛔ **`content_path` in the PACKED case** was not compared to the unpacked one, so the engine's mod-flag match may behave differently in configuration B — the gate. ⛔ **Neither engine message box has ever been rendered**, so wording, placement, and stacking with the pack's own dialog are unobserved. ⛔ **The 53 method wrappers were NOT traced to their callers individually** — "who holds the pcall" is answered structurally, not per site; that is a ~53-row job a re-take owes. ⛔ **Runtime cost of a swallowed throw** — unmeasured by anything. ⛔ **TestKit tree excluded a FIFTH time** (`Code/` only) — and this lens has a specific reason to want it: it is the one component measured to have emitted `[LUA ERROR]` lines of its own. ⛔ **Warm-save territory entirely** — no save opened, so no load-time repair pass has been watched running. ⛔ **Packed install, TestKit-off, junction-pulled — run B, the gate, still unrun.** ⛔ L6 (promise vs behaviour), L7 (environment & namespace), L8 (adversarial) — **entire**, except one L8-adjacent shape flagged in the artifact so L8 need not re-derive it. | dev tree, unpacked, source-derived at Src + all 73 archived retail logs (mixed rigs, each labelled in the artifact); **no launch** | *(this commit)* |
| **6** | **L6 — promise vs behaviour** | The five surfaces, censused mechanically over all **76** `Code/*.lua` (`tools/l6_promise_map.py`): the **75** Register ids **alias-resolved** (13 modules pass a `FIX_ID` local, which a plain grep cannot join) and tested against the derivation rule the README hands a modder; `metadata.lua` `code` vs `items.lua` vs disk, **membership AND order**, because `ForEachModItem` walks items by index; every module against every `docs/agent/bugs/` entry that names it. Then the veto: **the three gates `00_Core` owns**, and a scope-aware census (balanced-paren, string- and comment-aware) of everything outside them — 19 `WhenActive` file-scope handlers, 7 `function OnMsg.X()`, 13 `DataPatch`, 3 `OnDataReady`, 10 file-scope method installs, 7 threads, 2 `GameVar` — with **every file-scope site read at its lines**. The mod-env route re-derived at Src by symbol (`ModEnvMeta.__index`/`__newindex` `Mod.lua:1546-1563`, `safe_rawget` `:1577-1583`, `Loading` `autorun.lua:1`/`:423`/`:560`) to answer whether a **foreign** mod's write can even reach the table we read. Dead-coded targets (`tools/l6_reachability.py`): **13** global replacements + **55** class/table members, alias-resolved, counted against the whole shipped tree (**4,446** Src files) by call shape **and** by string dispatch, the four thinnest globals read at every site **and one level up**. The player-facing fix list, the store card, `README.md` and the `description` that **ships inside `metadata.lua`** checked against the module set and against each other, including the site's git history across the 2026-08-15 module-set change. | **mechanical for the censuses** — extractor-emitted, every row cites file:line, artifact `reports/L6_PROMISE_MAP.md`. **Source-read at Src by symbol for every engine route**, not inherited: the upload/`SaveDef`/`UpdateCode` chain, the mod-env metatable, and the retail `T()` return type were each read at their lines this session. **MEASURED where a measurement existed** — the archive's 76 logs by build, and the three 2026-08-19 retail legs. ⛔ **Three own-instrument defects found and disclosed** (a call-shaped pattern blind to a function passed as a VALUE; a negative lookahead behind `\s*` that backtracks and read **all 19** gated handlers as ungated — it would have inverted the verdict; a census counting shipped code quoted **inside our own headers**) — all three fixed before any count was taken, plus a fourth kept as a disclosed limit (blind to string dispatch). ⛔ **One negative was drafted and then withdrawn** as unsampled rather than refuted. | ⛔ **Nothing was run in a game — sixth link; the refusal is REASONED, not defaulted (artifact §7): one open question needs mod code that reads an undeclared global — barred by record-only, the no-instrument rule, the store card's 96-probe count and the no-console limit — and the other needs a second mod that does not exist, loading in an order nobody can set (`EF-054`), on a rig where reaching a configuration by junction costs an enable (`EF-055`, H-08).** ⛔ **Preset-FIELD patches are unswept for dead targets** — the dead-code sweep covers globals and class/table members only; a preset field we write that nothing reads is the same defect class, and this is the largest unswept surface in the lens. ⛔ **Whether the engine's `ModEnvMeta` asserts report in a RETAIL build is UNMEASURED** — the only measurement is a MarsDebug log, and the 74 retail logs are **not a control** because the condition was never sampled there. ⛔ **The veto has never been exercised end to end by an actual foreign mod** — every verdict about it is source-derived. ⛔ **`ListFixes()` has never been observed**, though it is the surface the README points a modder at — 0 occurrences in the archive. ⛔ **The store and site pages as RENDERED** are still check-at-paste; nothing is live, so no published surface has been read at its destination. ⛔ **The sibling save-rescue mod has no `items.lua` at all** and whether that is safe was read but NOT verified — another repo, routed. ⛔ **TestKit tree excluded a SIXTH time** (`Code/` only). ⛔ **Packed install, TestKit-off, junction-pulled — run B, the gate, still unrun.** ⛔ L7 (environment & namespace) and L8 (adversarial) — **entire**; one mod-env reading is L7-adjacent and is left as a finding for L7 to re-derive rather than a verdict, and one load-order half belongs to L8. | dev tree, unpacked, source-derived at Src by symbol + all 76 archived logs (build-labelled) + read-only cross-checks of two sibling repos; **no launch** | `36d8817` + *(this commit)* |
| **7** | **L7 — environment & namespace** | ⭐ **The global map taken from the COMPILER, not from a pattern** (`tools/l7_env_map.py`, new): every `Code/*.lua` compiled with Lua 5.3, dumped unstripped and parsed for `SETTABUP`/`GETTABUP` on the `_ENV` upvalue, so scope is decided by the compiler and shadowing/closures/params/`local _ENV` cannot fool it — **17** global write sites over **12** names and **1,618** read sites over **187** names, all `file:line`. The three routes bytecode structurally cannot see, enumerated separately and folded in: **13** `SMRFixPack.SetGlobal` call sites, **2** `_G[expr] =` sites, **2** `GameVar` registrars (one alias-resolved). The mod-env route re-derived at Src by symbol (`ModEnvMeta.__newindex`/`__index` `Mod.lua:1546-1563`, `safe_rawget` `:1577-1583`, `LuaModEnv`'s own-key overrides `:1600-1607`, `GameVar` `lib.lua:1040-1055`). Three crosses never run before: every written name against the **149**-entry `ModEnvBlacklist`; every written name against its **Src declaration** to test whether the strict-global create-assert can fire; and — the inverse of L6's dead-code sweep — all **187** read names against the whole shipped tree (**4,446** Src files / **10,956** column-0 + registrar definitions, then a second indentation-tolerant pass over the residue). **The TestKit tree swept for the first time in seven links** (20 files, 7 write sites, 2,062 read sites) and crossed against the pack in **both** directions. Packed-vs-unpacked re-derived at Src (`Mod.lua:1724-1740`, `:1748`, `:1755`, `:1770`, `:1800`, `:1849`, `:857-862`, `ModContentPath` `:6`) and **measured against all 76 archived logs** for the engine's own load-mode line. `Platform` tested for across all 76 files. | **mechanical, and the instrument is CONTROLLED before use** — a hand-written battery of **23** snippets (including `do local z = 1 end z = 2`, `local x = 1 do local x = 2 x = 3 end x = 4`, `function Cls:m()`, `local _ENV = {} w = 1`, a global created inside a `pcall`'d closure) passes 23/23 before any count below was taken; artifact `reports/L7_ENVIRONMENT_MAP.md`. **Source-read at Src by symbol for every adjudication** — the five vanilla `GameVar` declarations, the blacklist body, the two mount branches and the duplicate-id tie-break were each read at their lines this session, not cited from record. **MEASURED for the load-mode work** — the 66-log packed/unpacked witness is a count off `docs/archive/*.log`, not an inference. ⛔ **One own-instrument defect found and disclosed** (the definition sweep was column-0-anchored and under-detected 11 indented `if FirstLoad then` declarations, e.g. `Presets` `Dlc.lua:312`, `empty_table` `lib.lua:15`) — fixed by a second pass before the result was taken. ⛔ **One structural blindness kept and disclosed rather than closed**: a bytecode census sees `NAME = value` and cannot see a global created inside engine code we call. | ⛔ **Nothing was run in a game — seventh link, and the refusal is REASONED, not defaulted (artifact §7): this lens's two open questions both need the live `_G` enumerated, and all three routes to it are barred — the console (unattended sessions cannot type; `ConsoleExec`/`debug` blacklisted), a TestKit probe (⛔ the 96-probe count is a player-facing claim on the store card), and an instrument in `Code/`. The fourth route, loading the mod packed, IS run B and spec §6 keeps B terminal.** ⛔ **The runtime `_G` has never been enumerated by anything** — this link proves what the *source* writes; nobody has listed the globals actually present in a live process and diffed them against vanilla, and that is the direct falsifier for the "no sixth name" result. ⛔ **The packed load path remains underived-by-execution**: `MountPack`, `def.packed`, `metadata.lua` read from inside the archive, and the `Mod/<id>/` preview path have never run for this mod — now MEASURED as 66 of 66 archived sessions `unpacked`, zero `packed`. ⛔ **`CheckModPackSignature` was not read**, so whether the packed branch is even taken on this rig is still open (stage 4.2's own flag). ⛔ **No packed build exists on disk anywhere** — stage 2 packaging is outstanding work, not merely unverified. ⛔ **The TestKit was swept for NAMESPACE only** — its second-load behaviour, its own containment, and the `[LUA ERROR]` lines it has been measured to emit are all still unswept. ⛔ **No console platform has been touched** — that the pack never branches on `Platform` is a different claim from behaving correctly there. ⛔ **Whether the engine's `ModEnvMeta` asserts report at all in a RETAIL build** is still unmeasured (link 6's item); this link makes it moot for this pack but does not settle it. ⛔ **Whether a FOREIGN mod's globals collide with ours** is L8's and untouched — the two-tree cross here covers our own two mods only. ⛔ **Warm-save territory entirely** — no save opened. ⛔ **Packed install, TestKit-off, junction-pulled — run B, the gate, still unrun.** ⛔ L8 (adversarial / hostile modder) — **entire**, including the restore discipline of the 21 vanilla global writes, which link 1 explicitly assigned there. | dev tree, unpacked, source-derived at Src by symbol + all 76 archived logs + Lua 5.3 bytecode of both shipped trees; **no launch** | *(this commit)* |
| **8** | **L8 — adversarial / hostile modder** | ⭐ **The pack's PUBLISHED INPUT SURFACE, executed rather than reasoned about** (`tools/l8_hostile_input.py`, new): the three globals `00_Core.lua:11`/`:15`/`:17` adopt from `_G` are a contract with foreign code, and the **real shipped source** of `00_Core` + 3 modules was run under Lua 5.4 once per seeded value, with `pdofile`'s per-file `pcall` reproduced (`lib.lua:242-251`) so ABSENT and `error` are distinguished — **2 controls + 8 hostile values**, covering the non-table cases, the fork/partial-table cases, a throwing `__index`, and the two well-formed-but-wrong veto shapes. Blast radius measured against a mechanical count: **75 of 76** `SMRFixPack.Register(` occurrences are at **column 0**. Then the **DEFERENCE census** (`tools/l8_deference_map.py`, new, alias-resolved over five forms): every patch site tested for whether the module anywhere names the PRIOR value of its target — **66 sites** (16 global + 50 method), one direction sound by construction. The restore discipline link 1 assigned here: all 16 global replacements swept for a restore path (**2 have one**), both read at their lines in both directions. `EF-058` re-derived at Src by symbol (`autorun.lua:423` `ModsLoadCode()` vs `classes.lua`'s `function OnMsg.Autorun()`) and **amended in place**. Both blame surfaces derived: `OnMsg.OnLuaError` read at `Mod.lua:3001-3015` and `ReportModLuaError` at `:2948-2993`, plus a census of the pack's own self-checks by kind (**238 shape vs 7 content**, 69 modules). | **mechanical for both censuses, and both instruments are CONTROLLED before use** — the hostile harness's two controls reproduce the documented veto behaviour and the documented no-veto behaviour before any hostile row was taken; `l8_deference_map.py --selftest` passes **11/11**, including the three shapes that would have inverted the result. **Source-read at Src by symbol for every engine route** — the `OnLuaError` loop, the `ReportModLuaError` body, the `autorun`/`classes` ordering and the `classes.lua` flattening header were each read at their lines this session, not cited from record. **Read by hand at their lines** for every `reads-prior` candidate spot-checked, both `set_installed` helpers, and 8 replacement headers. ⛔ **Three own-instrument defects found and disclosed** (a call-shaped pattern that matched the INSTALL SITE and would have scored every replacement as chaining — i.e. inverted the census; `function OnMsg.X()` counted as a method patch, inflating 24→30; a fifth alias form missing, reproducing link 1's exact failure) — all three fixed, each with a selftest case, before any count was taken. ⛔ **One limit KEPT and disclosed**: the `C.M = expr` shape cannot separate a method install from a data-field write on an aliased table — 1 hit, read and reclassified (51→50), not silently dropped. | ⛔ **No foreign mod exists in any measurement, by design** — standing baseline policy; every verdict about foreign interleaving, the veto route and the mod-flag box is source-derived. ⛔ **The one available second mod could not be loaded**: the opt-in pack wraps two of the same methods but is disabled in account state and needs an **owner tick** (`H-08`); asked on the checklist, not assumed. ⛔ **`ReportModLuaError`'s batching is UNDERIVED** — whether the multi-mod case is one box listing several mods or one box per mod turns on `CreateRealTimeThread`'s start semantics, and it is **not defined in `cthreads.lua`** (a C export); neither box has ever rendered. ⛔ **Link 1's C1 inheritance-shadow check cannot see a FOREIGN subclass** — complete against 4,446 Src files, unbounded against mods. ⛔ **`SMRFixPack_Optional`'s unguarded index is LATENT, not live** (0 optional-gated files) and was not exercised even in simulation. ⛔ **The hostile-input results are Lua semantics under `lupa` 5.4, not a retail observation.** ⛔ **Preset-FIELD patches unswept from this lens too** — L1 left them non-mechanical, L6 left them unswept for dead targets, L8 leaves them unswept for foreign contention: ⭐ **three lenses have now named the same surface and none has swept it, and it is the largest unswept CODE surface in the chain.** ⛔ **Warm-save territory entirely** — no save opened, so a save written with a foreign mod present and loaded without it (or the reverse) is untouched. ⛔ **TestKit**: namespace only (L7); its own containment and second-load behaviour unswept after **eight** links. ⛔ **No console platform, ever. No non-English run, ever.** ⛔ **Packed install, TestKit-off, junction-pulled — run B, the gate, still unrun**, and no packed build exists on disk. ⭐ **THE LENS POOL IS NOW EXHAUSTED AND THE TERRITORY IS NOT** — §9 of the artifact reads all eight rows and sorts the residue for the terminal audit. | dev tree, unpacked, source-derived at Src by symbol + shipped source executed under Lua 5.4; ⛔ **no third-party mod installed**; **no launch** (an owner-costed ask, not a refusal) | *(this commit)* |
| *(launch rehearsal — ⛔ NOT a link, no lens taken, rotation undisturbed)* | — (`98_LAUNCH_REHEARSAL.md`, spec §6) | **Stage 1 in full**: `upload_preflight` 20 guards, 0 FAIL, exit 0. **Stage 2 as far as it can be taken without a game**: the packer's file selection re-derived at Src (`GedModEditor.lua:716-732`) into a predictor (`tools/pack_predict.py`) and an archive reader (`tools/pack_list.py`), then both run against the **real engine-built `.fpk` of 2026-08-17** found in `TmpData` — **80/80 by name**, and all 80 entries **extracted and byte-compared to the working tree** (78 identical, 2 differ, and the 2 are exactly `git diff 7824cbc..HEAD`). Provenance of that archive traced to its own session log (`MarsDebug…19.30.31`, `Initializing ged app: ModEditor` ⇒ Ged route, not console). The blacklist route re-derived at Src by symbol (`ModEnvBlacklist` `Mod.lua:1274`/`:1322`, `ModEnvMeta.__index` `:1546-1547`, `safe_rawget` `:1577-1583`). `CheckModPackSignature` + `AreModSignaturesRequired` read at `:87-89`/`:49-52`. The packed/unpacked mount and folder-name path re-derived end to end (`:1724-1743`, `:1757`, `:853-862`, `:1770`, `:1849`, `GetVersionString` `:1176-1178`). ⭐ **And the ten pass criteria audited for decidability BEFORE any was scored** — four repaired, each against a measurement: 75 `applied` lines counted in a real log, `v1.00-000` read off the format string, 21/73 + 52/73 `LUA ERROR` and 66/66 `unpacked` re-counted over the whole corpus, and the six error sites enumerated. | **mechanical + MEASURED, and the instrument is CONTROLLED against a real artifact** — the predictor is not asserted, it reproduces an archive the engine built, name for name; the content reconcile runs through `flpk_extract.py`'s own `extract()`. ⛔ **One own-instrument defect found and disclosed** (a hand-rolled zstd-frame-magic scan reported 7 differences where the real extractor reports 2 — the magic bytes occur inside compressed data) — fixed before any number was taken. **Source-read at Src by symbol for every engine route**, not inherited. Artifacts: findings `SWEEP_FINDINGS.md` LR-F1…LR-F14; owner script `PLAYTEST_CHECKLIST.md` item 52. | ⛔⛔ **RUN B, ENTIRELY — not one criterion scored, and the gate is still unrun.** ⛔ **Stage 2 is OWNER-BLOCKED and that is structural, not a schedule**: `DbgPackMod` and `ReloadLua` are `ModEnvBlacklist` keys, so no mod code — pack, TestKit or probe — can build the archive; the console is the only route and unattended sessions cannot type. ⛔ **Run B has NO DRIVER**: every unattended primitive lives in the TestKit and B turns it off, so criteria **5, 7, 8, 9** are owner work, not log reading, and only 1/2/3/4/6/10 survive a boot-and-close leg. ⛔ **No launch this session and no `EF-056` exposure** — nothing pulled, staged, enabled or disabled. ⛔ **The packed load path still never executed**: LR-F6 proves the branch is *taken* on PC, nothing has watched `MountPack`/`def.packed`/in-archive `metadata.lua` run. ⛔ **The preview image has never been SEEN** on the packed path (now source-settled to *resolve* in all three folder-name cases — that is a different claim). ⛔ **No save round trip with the pack packed, ever.** ⛔ **Run A's falsifier block** — console, therefore owner; A's suite half deliberately not re-run (runtime-identical tree, and `items.lua` is not read at load). ⛔ **The 08-17 upload sitting's 7 logs are not in `docs/archive/`**, so every "all 73 logs" count in this chain excludes the one session in which this mod was packed. ⛔ Everything links 1–8 left unswept is untouched here — preset-FIELD patches, the 53 wrappers' callers, warm-save territory, the TestKit's own containment, console platforms, non-English, any foreign mod. | dev tree, unpacked, source-derived at Src by symbol + the real 08-17 `.fpk` + all 73 archived logs; ⛔ **no launch**, ⛔ **no packed install**, ⛔ **run B unrun** | *(this commit)* |

---

## Seed notes for link 1

The row above is **not a sweep** — it is the pre-chain work of 2026-08-17,
recorded here so the first link knows the small amount that is genuinely covered
and does not re-do it.

**Settled by measurement, do not re-derive unless you doubt it:**
- the shipped package is **80 files**, `docs/`/`tools/`/`CLAUDE.md`/`README.md`/
  `.git`/`.claude` all **zero** — read out of the real `.fpk`;
- `*` **does** cross `/` in `MatchWildcard`, which is what settles the
  `*/docs/*` question `WORKFLOW` §882-887 had owed since 08-13;
- `tools/upload_preflight.py` passes 20 guards, and was falsifier-tested by
  deleting the `image` field (it caught it, exit 1).

**Explicitly NOT settled, and first in line:**
- ⛔ neither 2026-08-17 core fix has been run in a game;
- ⛔ the mod has **never** been loaded packed — only unpacked via the junction;
- ⛔ every gate reading this project owns was taken with the TestKit loaded, and
  the TestKit **mutates `_G`**;
- ⛔ no aggregate anything: save footprint, runtime cost, collision map.
