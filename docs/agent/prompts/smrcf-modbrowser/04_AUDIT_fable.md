# C·04 — terminal adversarial audit · fresh context · TRUSTS NOTHING FORWARD

**You are the adversary.** Read `README.md`, then `STATE.md`, then this, then
`## Notes from upstream`. Every "done" upstream is a **claim**.

## 0 · Staleness check
```
git log --oneline -10
git pull
```

## 1 · 🗒 Live todo list from your first action.

## 2 · The audit

### 2.1 Inbox audit — zero orphans.

### 2.2 Re-derive, do not inherit
Every citation at `ModTools\Src`, **by symbol**. Logs re-counted by you and
byte-verified. Prediction commits proven in `git` to precede their launches.

⚠️ **This entry has already been wrong once**, in the direction of closing a real
defect as a design choice. Check the *opposite* error too: did the chain talk
itself into treating defect 3 (the thumbnail cache — no dev comment, no sibling)
as a defect when it is a defensible simplification? **A behaviour found
intentional is tier I and declining it was always an allowed outcome.**

### 2.3 If a §1.5 replacement shipped — the highest-risk item here
- Was the body captured **byte-verified against `Packs\Lua.fpk`**, not just `Src`?
- Does the self-check detect a changed target and **stand down** rather than
  running a stale copy?
- Is it on the fpk re-verification list?
- Did the owner actually approve it, on the checklist, before it was built?
  ⛔ **A §1.5 that shipped on an agent's own say-so is a process failure**
  regardless of whether the code is correct.

### 2.4 The hyperlink re-enable
- Was the falsifier actually exercised, or merely written down?
- Is the module revertible at runtime, as specified?
- Was "no XText regression" claimed from a sample big enough to carry it? Name
  the number of pages and mods.

### 2.5 Scope discipline
Did anything creep toward fredware's §5N — a formatter, a font cascade, caching
policy, capability the vanilla path never had? **Restoring a disabled path is
repair; adding capability is not.** Read the modules, not the summaries.

### 2.6 Status words
`tested-attended` needs the owner to have **seen** it, with re-readable evidence
— for these items that means screenshots. `tested-unattended` is ⛔ **closed to
screen events**, and all three are screen events.

### 2.7 Consistency + drift
Front matter vs heading tags vs regenerated `INDEX.md`; counts tool-emitted;
card / site / `metadata.lua` swept if anything shipped; `mkdocs --strict` if
public pages moved; **every drift recorded, never silently fixed.**

## 3 · Scope fence
**IN:** audit, corrections, integration, folder-empty gate, owner report.
**OUT:** ⛔ no new runs · ⛔ no new features · ⛔ no launching the game. A
correction to a shipped module is allowed and must be disclosed as one.

## 4 · Stop conditions
- A verdict cannot be re-derived from primary evidence → **do not sustain it.**
- A §1.5 shipped without owner approval → **that is the finding**, and it goes on
  the checklist regardless of code quality.
- The primary log is missing or mismatched → a finding, and an R8 process failure.

## 5 · ⛔ What may not be claimed
- ⛔ **"Audit-sustained"** for anything not re-derived from primary evidence.
- ⛔ **"The mod browser is fixed."** Three separable defects, separately
  dispositioned; say which.
- ⛔ **Any player-visible claim without a screenshot.**
- ⛔ **A release implication.** Nothing in this chain is a release gate, and the
  thumbnail-cache awareness on checklist item 34 stands whether or not anything
  shipped.

## 6 · Close-out — the folder-empty gate
One commit: corrections applied · `C52` in step with what actually shipped ·
`STATE.md` extended (byte caps, doccheck-enforced; evict resolved material, never an obligation) ·
counts re-emitted by the tool · doccheck GREEN (+ `mkdocs --strict` if public
pages moved) · `git rm` this file — **the folder must end EMPTY** · grave named ·
push.

**End the owner report with the kickoff line for the next queued chain**, and say
in one sentence whether ④ has happened.

⭐ **If this was the LAST chain of the set to close**, delete
`agent/prompts/SMRCF_CHAIN_SET.md` in the same commit and name its grave.

## Notes from upstream
