# Chain prompt 2 — the sitting: simplest first, banked as it goes, the owner's leads welcome

**Read `README.md` in this folder first — binding chain rules apply.**
ATTENDED — the owner is at the keyboard; prep's brief below sets the
estimate. Start with `git log --oneline -10` + `git pull`. Todo list up front
— the owner reads it live to time their involvement; update it the moment an
item moves.

**Read path**: this folder's README · the brief in "Notes from upstream"
below · each briefed entry AT ITS CURRENT STATE (the brief cites entries;
you act on the entry — and prep already archive-cross-checked them, so a NEW
contradiction you find is a finding, not a formality) · `WORKFLOW.md`
"Co-runs" in full.

## How this sitting runs (the rules that bit batch-1, now up front)

1. **Simplest first, banked immediately.** Run the payload in the README's
   order. THE MOMENT a leg completes, write its card/verdict into this file
   (or the entry, per the brief's card skeletons) BEFORE starting the next
   leg. A course change must never cost a result already earned.
2. **⛔ The owner-override rule (owner, 2026-08-05, verbatim in WORKFLOW).**
   If the owner deviates to follow a lead: state the plan's position ONCE,
   then no back-on-track reminders until the lead closes, they ask, or the
   sitting is ending. YOU keep the ledger — every line handed to them, what
   came back, what is outstanding — visibly in the todo list. Their leads
   have found new bugs and changed test outcomes before; instrument and
   witness the lead like a planned leg (witness discipline applies to
   whatever runs, planned or not).
3. **First executions are first executions.** `StartBombard` runs under
   `pcall` with the result printed and its liveness witness read before any
   verdict — whatever prep verified.
4. **Every zero states its sampled condition; every finding names what was
   forced.** A refusal (a gate that says the condition is unsampleable) is a
   result — record it and move on; batch-1's most valuable output was a
   refusal.
5. **Instruments added mid-sitting obey the instrument rule** — witness,
   printed `pcall`, type-checked inputs, settling window. The S4/S5 defects
   were both same-day tooling that skipped this.
6. **Tier discipline**: tag every moment Tier A (eyes) / HANDS-ONLY / Tier
   B/C as the brief says; the owner's verdict words are recorded VERBATIM at
   the moment they are said.
7. **Record actual minutes per moment against the estimate as you go** —
   scored later per the override rule (owner deviation is theirs; rig misses
   are ours). Do not report "saved" minutes.
8. **PT-22 log review reads the WHOLE log before close** — batch-1's review
   missed two organic healings; the audit will diff your review against the
   raw log, so leave nothing unexamined (attribute, don't dismiss).

## Close-out (before the recording commit)

Disarm → `PROBE SWEEP:` in BOTH repos → delete staged/throwaway saves (the
leg-S FIXTURE save survives, NAMED in the commit message) → verify
`TEST2H TRAIN` untouched (MD5 + mtime) → archive the sitting log `git add -f`
→ doccheck green → commit `-F` → push. Append the handoff (per-leg verdicts
with evidence lines · predictions vs actuals · your numbered ledger · owner
decisions taken live, verbatim · routed gaps · minutes actuals vs estimate)
to `03_FABLE_AUDIT.md` "Notes from upstream", and delete this file in the
same commit.

## Stop conditions

- Any `[LUA ERROR]` naming pack code → stop the leg, record verbatim,
  continue with independent legs if game state allows.
- The owner leaves → finish the current read, save nothing unplanned, record
  what ran; remainder stays TAKEABLE. Simplest-first means what's banked is
  maximal — close honestly, never rush leg T.
- Leg T started → nothing else interleaves; the sitting ends with it.
- Context runs short → self-split at a clean commit boundary; banked cards
  make the split cheap.

## ⛔ What you may not claim

- Not `tested` for anything the owner's eyes were not on (tiers bind).
- Not "F99 reachable" from leg Q — forced both halves, rate datum only.
- Not "F26 verified" beyond what the five integrity checks actually read.
- Not a clean uninstall from anything but the Mod-Manager-disable load
  (leg T) — a toggle answers nothing.
- Not minutes "saved"; actuals against estimate, scored per the override
  rule.

## Notes from upstream

*(Prompt 1 writes the brief here: run order, measure-moments with minutes +
console-driving overhead, predictions with falsifiers, SKIP lines, card
skeletons, prep's ledger.)*
