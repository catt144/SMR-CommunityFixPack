# Chain prompt 2 — the sitting: simplest first, banked as it goes, the owner's leads welcome

**Read `README.md` in this folder first — binding chain rules apply.**
ATTENDED — the owner is at the keyboard; prep's brief below sets the
estimate. Start with `git log --oneline -10` + `git pull`. Todo list up front
— the owner reads it live to time their involvement; update it the moment an
item moves.

**Read path**: this folder's README · the brief in "Notes from upstream"
below · each briefed entry AT ITS CURRENT STATE (the brief cites entries;
you act on the entry — and prep already archive-cross-checked them, so a NEW
contradiction you find is a finding, not a formality) · `WORKFLOW.md`
"Co-runs" in full.

## How this sitting runs (the rules that bit batch-1, now up front)

1. **Simplest first, banked immediately.** Run the payload in the README's
   order. THE MOMENT a leg completes, write its card/verdict into this file
   (or the entry, per the brief's card skeletons) BEFORE starting the next
   leg. A course change must never cost a result already earned.
2. **⛔ The owner-override rule (owner, 2026-08-05, verbatim in WORKFLOW).**
   If the owner deviates to follow a lead: state the plan's position ONCE,
   then no back-on-track reminders until the lead closes, they ask, or the
   sitting is ending. YOU keep the ledger — every line handed to them, what
   came back, what is outstanding — visibly in the todo list. Their leads
   have found new bugs and changed test outcomes before; instrument and
   witness the lead like a planned leg (witness discipline applies to
   whatever runs, planned or not).
3. **First executions are first executions.** `StartBombard` runs under
   `pcall` with the result printed and its liveness witness read before any
   verdict — whatever prep verified.
4. **Every zero states its sampled condition; every finding names what was
   forced.** A refusal (a gate that says the condition is unsampleable) is a
   result — record it and move on; batch-1's most valuable output was a
   refusal.
5. **Instruments added mid-sitting obey the instrument rule** — witness,
   printed `pcall`, type-checked inputs, settling window. The S4/S5 defects
   were both same-day tooling that skipped this.
6. **Tier discipline**: tag every moment Tier A (eyes) / HANDS-ONLY / Tier
   B/C as the brief says; the owner's verdict words are recorded VERBATIM at
   the moment they are said.
7. **Record actual minutes per moment against the estimate as you go** —
   scored later per the override rule (owner deviation is theirs; rig misses
   are ours). Do not report "saved" minutes.
8. **PT-22 log review reads the WHOLE log before close** — batch-1's review
   missed two organic healings; the audit will diff your review against the
   raw log, so leave nothing unexamined (attribute, don't dismiss).

## Close-out (before the recording commit)

Disarm → `PROBE SWEEP:` in BOTH repos → delete staged/throwaway saves (the
leg-S FIXTURE save survives, NAMED in the commit message) → verify
`TEST2H TRAIN` untouched (MD5 + mtime) → archive the sitting log `git add -f`
→ doccheck green → commit `-F` → push. Append the handoff (per-leg verdicts
with evidence lines · predictions vs actuals · your numbered ledger · owner
decisions taken live, verbatim · routed gaps · minutes actuals vs estimate)
to `03_FABLE_AUDIT.md` "Notes from upstream", and delete this file in the
same commit.

## Stop conditions

- Any `[LUA ERROR]` naming pack code → stop the leg, record verbatim,
  continue with independent legs if game state allows.
- The owner leaves → finish the current read, save nothing unplanned, record
  what ran; remainder stays TAKEABLE. Simplest-first means what's banked is
  maximal — close honestly, never rush leg T.
- Leg T started → nothing else interleaves; the sitting ends with it.
- Context runs short → self-split at a clean commit boundary; banked cards
  make the split cheap.

## ⛔ What you may not claim

- Not `tested` for anything the owner's eyes were not on (tiers bind).
- Not "F99 reachable" from leg Q — forced both halves, rate datum only.
- Not "F26 verified" beyond what the five integrity checks actually read.
- Not a clean uninstall from anything but the Mod-Manager-disable load
  (leg T) — a toggle answers nothing.
- Not minutes "saved"; actuals against estimate, scored per the override
  rule.

## Notes from upstream

**THE BRIEF — written by prompt 1 (Opus, unattended, 2026-08-10), which is
consumed in the same commit that wrote this.**

Run conditions for everything below: retail `Mars.exe` **1.0.7.396349**, one
cold load of a staged `Copy-Item` of `TEST2H TRAIN` (MD5
`103B320A1434513BC8773553096A8958`, mtime 2026-08-03 22:21:48 — **verified
byte-identical before and after prep**), map `BlankBigCanyonCMix_09`, pack
**81/81 active AS READ**, sol 333. Cold load 9,676 ms by the engine's own line;
whole confirm process 142,520 ms; **0 engine error lines**. Raw:
`docs/archive/cb2confirm_Mars.exe-20260810-10.20.03.log`.

---

### 0 · What prep changed before you read anything else

**Job 1, the archive cross-check (the S7 repair) found TWO stale entries, both
corrected visibly.** The rule earned itself again five days after it was
written:

- **[C42](../../bugs/C42.md)** — the within-session read the entry asks for
  **ran on 2026-08-05** and landed on the checklist rider; the entry never got
  it and still said the read was "routed as a gap". Now on the entry.
- **[F21](../../bugs/F21.md)** — the 2026-08-05 ride-along result was on the
  checklist rider only. Now on the entry, **with its reader gap closed** (below).
- **[F35](../../bugs/F35.md)** — not staleness but a false premise in the parked
  leg-5 instrument, caught before it ran (below).
- **[F99 checklist rider](../../../PLAYTEST_CHECKLIST.md)** — its status line
  knew only about the 2026-08-04 attempt; the two 2026-08-05 samples are now
  named there too.

⚠️ **One archive hazard, NOT edited because the archive is append-only.**
`PLAYTEST_ARCHIVE.md`'s PT-47 section (inside the 2026-08-03 pre-redesign
snapshot) still carries the **broken** `StartBombard(UIColony:GetCityAtMap(
MainMap), …)` recipe with no correction marker. The correction lives on the F26
entry. **Do not resurrect the recipe from the archive** — leg R below carries
the working form.

---

### 1 · Run order (the owner's simplest-first rule, with what prep changed)

| # | leg | why here | owner needs |
|---|---|---|---|
| — | **F21 restamp witness** (ride-along) | free, runs in background | 1 console line |
| — | ~~C42~~ | **SKIP — see §3** | none |
| 1 | **P — popup keystone + F85** | cheapest owner moments; both routes CONFIRMED live | eyes + hands |
| 2 | **Q — F99's untested cell** | console-only, no eyes; fixture CONFIRMED | 2 console lines |
| 3 | **R — PT-47 bombardment** | destructive, and the one reading that is eyes by nature | eyes |
| 4 | **S — PT-35 turbine fixture** | needs a fresh reload first; owner UI placement | hands |
| 5 | **T — PT-53 E uninstall** | process mutation, ends the sitting | hands |

---

### 2 · The measure-moments list, with minutes — AND the console-driving budget stated separately

⛔ **The S6 rule is why this estimate is bigger than batch-1's.** The rig has no
input path into a running game: **you type every console line.** Batch-1
promised ~24 minutes counting only eyes and hands, and that model is what broke.
Both columns below are yours.

| moment | tier | what you do | eyes/hands min |
|---|---|---|---|
| **M1** leg P keystone | Tier A | watch for the corner notification, **save**, **load**, click it, answer the popup | **6** |
| **M2** leg P / F85 | **HANDS-ONLY** | rebind Quick Save → **F9** in Options ▸ Key Bindings, then press it under the open popup; answer; load the save | **5** |
| **M3** leg P / §3.6 corner | Tier B, **OPTIONAL** | only if a sol boundary is near — leave the distress popup open across it | **0–3** |
| **M4** leg Q | Tier C | nothing but the two lines; no eyes at all | **0** |
| **M5** leg R volley | **Tier A — the one reading that is eyes by nature** | camera **low** on the named dome, then: scatter or a rank of parallel trails? then notification appears+clears, decals fade | **4** |
| **M6** leg S placement | HANDS-ONLY | place **one** Large Wind Turbine, apply **one** building upgrade | **5** |
| **M7** leg T uninstall | HANDS-ONLY | quit to menu, Mod Manager ▸ **disable the pack**, load the save | **5** |
| | | **eyes/hands subtotal** | **25–28** |
| | | **console driving — ~16 lines at ~30 s each** (S6) | **+8** |
| | | **TOTAL HONEST ESTIMATE** | **≈ 33–36 attended minutes** |

⛔ **What this estimate does NOT contain, deliberately:** any minute for you
following your own lead. If you deviate, the plan's position gets stated **once**
and then dropped, and the session carries the handed/checked/outstanding ledger
in the todo list. Your leads are a proven discovery channel; only the rig's own
misses get scored.

---

### 3 · SKIP lines — each with the condition prep actually sampled

- **⛔ C42 ride-along — SKIP `<no traversal witness in 90 bounded tries at 1 Hz,
  speed 20, over 4 passages / 50 elements — `units` empty on every sweep>`.**
  Prep built the witness poller the entry asked for and ran it. This is the
  **third** empty denominator on this save. Since the entry's own mechanism says
  the stale entry is **permanent**, an empty list at the *last* sweep means
  either no traversal completed in ~30 game-minutes or the mechanism is wrong —
  and the reading cannot tell which. **A fourth empty sample buys you nothing**;
  what C42 needs is a different fixture, not a better poller. Full fork on the
  entry.
- **⛔ PT-47 interception check — UNSAMPLED, not negative.** `0` defence towers
  on this save (13 domes). Say "unsampled" on the card; never "no interception
  happened".
- **⛔ F21 — this leg cannot re-earn `tested`** and is not asked to. It is a
  ride-along that produces one witnessed reading.
- **PT-42 / leg 3, PT-37 / leg 1** — already closed by batch-1; their
  instruments are **deleted** from the parked sources rather than carried, so
  they cannot burn context in the sitting.

---

### 4 · Per-leg brief — subject-finder named for every moment (the S2 rule)

Every moment below names the instrument that FINDS its subject, and prep
**confirmed each subject exists on the staged copy**. A moment whose subject
could not be found is in §3, not here.

#### Ride-along · F21 restamp witness — the reader gap is CLOSED
**Subject-finder:** `CB2.ReadF21Subjects()` → prep found **117** candidates;
named subject `Colonist#2000018161` at `StationSmall#4552`.
**Line:** `CB2.RideF21()` — runs in the background, prints its own verdict.
⭐ Batch-1's `spent_time=nil` was **the reader, not the game**: no such field
exists on any class. The real fields read fine on the first attempt (station
`avg=516309` vs trains `avg=47968–183186`). Derivation and traps on the F21
entry.

#### Leg P — popup keystone + F85 (SIMPLEST, so first)
**Subject-finder:** `CB2.ReadPopupSubjects()` → prep found **200** enabled
storybits carrying replies (`AnythingForLove`, `ApplicantProfiling`,
`ArcadiaCross`, `Armageddon`, …) **and 50 available breakthrough techs**.
⭐ **This is new**: batch-1 could not find a choice-popup route at all — the
documented launch-issue route was measured dead. **Both of leg P's routes are
live on this save.**
**Lines:** `CB2.PKeystone("AnythingForLove")` → save → load →
`CB2.PKeystoneRead("after reload")` → click + answer · then `CB2.PChoicePopup()`
for F85.
⚠️ Answering the breakthrough popup calls `SetTechDiscovered` — staged copy only.

#### Leg Q — F99's untested cell (FORCED BOTH HALVES)
**Subject-finder:** `CB2.ReadTrackQ(2)` → prep found **13 of 17** tracks with a
mid-element that passes `BreakTracks`' own filter. Named:
`TrackBase#4569`/`#4573`, `#4619`/`#4606`, `#4730`/`#4863`, `#5361`/`#5933`.
**Lines:** `CB2.QBreak(2)` then `CB2.QComplete()`.
⛔ `CB2.QComplete()` **refuses** unless 2+ **distinct** tracks carry a
**witnessed** break — a completion over a half-built cell would re-sample the
cell F99 already has two zeros for. ⚠️ `el.broken` is a **table**; endpoint and
station elements are silently skipped by `BreakTracks`, which is why every break
is witnessed. Watch line: `TrackElement.lua:805`.

#### Leg R — PT-47 bombardment (F26)
**Subject-finder:** `CB2.RPickDome()` → 13 domes; it names which are on
**MainMap** and refuses the others.
**Lines:** `CB2.RPickDome()` then `CB2.RBombard()`.
⛔ **`StartBombard` is `[NEVER RUN]`** — it goes through `CB2.Try` with the
result printed whatever it says, and the "volley ENDED" verdict is gated on
having *seen the missile table non-empty first*.
⭐ **Both batch-1 corrections re-verified on this tree today**, not inherited:
`WaitBombard` still opens `local map = MainMap` in **both** the shipped copy
(`Bombardment.lua:55-56`) and ours (`Fix_BombardmentSpread.lua:91`), and
`g_IncomingMissiles` is a MapVar on MainMap (`:52`). `StartBombard`
(`:156-161`) only spawns a **game-time** thread — **nothing happens while
paused.**

#### Leg S — PT-35 turbine FIXTURE build
**Subject-finder:** `CB2.ReadTurbineFixture()` → prep read
`researched=false`, **`discovered=nil`**, `0` Large Wind Turbines.
**Lines:** fresh reload first · `CB2.SGrant()` · owner places · `CB2.SSave()`.
⛔⛔ **The parked instrument was wrong and prep MEASURED it wrong before it ran.**
Batch-1 planned `CheatResearchAll()`. That researches a breakthrough only when
`discoverable or IsTechDiscovered(id)` (`Cheats.lua:84`);
`TechFields.Breakthroughs.discoverable = false` (`TechFieldPreset.lua:279`) and
prep's live read says **`discovered=nil`** — so on this save **it would have
granted nothing** while researching the whole rest of the tree. Replaced with
`UIColony:SetTechResearched("FrictionlessComposites")`, the exact call F35's own
2026-08-01 measurement proved.
**The saved fixture is the one deliberate SURVIVOR of this sitting** — name it in
the recording commit.

#### Leg T — PT-53 E uninstall half, DEAD LAST
**Subject-finder:** ⛔ **there isn't one, and prep says so rather than
pretending.** Mod-Manager / main-menu driving is **DESCOPED by measurement**; no
console read establishes the row is reachable. It is your hands and your eyes,
confirmed at the sitting and nowhere else.
**Line:** `CB2.TSave()` then the manual disable + load.
⛔ A **toggle** cannot answer an uninstall question — it reads clean by
construction. Only the **Mod-Manager disable** does.

---

### 5 · Predictions, numbered, each with its falsifier

1. **Leg P keystone:** the storybit's popup thread SURVIVES the save/load.
   *Falsifier:* `IsValidThread` reads **false** after the reload, or the corner
   notification opens nothing.
2. **Leg P keystone:** answering after the reload still applies the outcome.
   *Falsifier:* the reply lands but no effect/outcome popup follows.
3. **F85:** a rebound-to-F9 Quick Save **LANDS** while the choice popup is open
   (the shield is UI reachability, not the save system).
   *Falsifier:* no new file in the save list, or the binding UI refuses F9 →
   F85 drops toward I/R4 and becomes documentation only.
4. **F85:** loading that save **voids** the choice — popup gone, breakthrough
   not discovered. *Falsifier:* the popup re-opens, or the tech reads discovered.
5. **Leg Q:** every break the finder named is **witnessed**.
   *Falsifier:* a break with no witness — which would mean prep's filter is
   wrong, not that the game is.
6. **Leg Q:** the completion produces **zero** `TrackElement.lua:805` throws.
   *Falsifier:* one or more throws — which would be **the first reproduction
   since 2026-08-03** and moves F99 substantially. ⛔ Either way this is a rate
   datum for one cell and says **nothing** about reachability.
7. **Leg R:** `StartBombard` executes without raising and
   `MainMap.g_IncomingMissiles` becomes non-empty.
   *Falsifier:* `ok=false` in the TRY line, or peak 0 after 3 minutes.
8. **Leg R:** the volley **ENDS**. *Falsifier:* the 3-minute bound hits with
   peak > 0 — F26's own stated FAIL, and the entry says revert the fix.
9. **Leg R:** the missiles arrive as a **scatter**. *Falsifier:* a rank of
   parallel trails (the old behaviour).
10. **Leg S:** `SetTechResearched` moves `researched` false→true in one call on a
    save where `discovered` is nil. *Falsifier:* still false afterwards — which
    would also retro-confirm that `CheatResearchAll` was never going to work.
11. **Leg T:** the Mod-Manager-disabled load is clean of engine error lines
    naming pack code. *Falsifier:* any such line. ⚠️ A clean read bounds **that
    save on that path**; it is not a general zero-persisted-state proof.
12. **F21:** the subject's `transport_ticket.start_wait` **CHANGES** as `stage`
    becomes `"Traveling"`. *Falsifier:* unchanged → the wrapper did not fire →
    a defect reading that goes on the entry verbatim.

---

### 6 · Card skeletons (bank one the moment its leg completes)

```
LEG <x> — <entry/PT> — <PASS / FAIL / UNSAMPLED / REFUSED>
Run conditions: build 1.0.7.396349 retail · save <NAME> (Copy-Item of TEST2H
  TRAIN, MD5 103B320A…8958) · map <…> · pack <n>/<n> active AS READ · load <k>
  of the process · uptime <…> · engine error lines in window: <n>
FORCED: <exactly what the rig made happen>
ORGANIC: <what stayed the game's own — or "nothing", said plainly>
Readings: <raw log lines, quoted, not paraphrased>
Witness: <the observation that proves the counter could have been false>
Owner verdict (tier <A/B/HANDS-ONLY/C>): "<their words, verbatim, at the moment>"
Predictions: <n HELD / n FALSIFIED>, each named
Falsifier for this card in one sentence: <…>
⛔ Not claimed: <…>
Minutes: estimate <n> / actual <n>
```

---

### 7 · Prep's own unforeseen-issues ledger — 6 entries, 0 of them the game's fault

Numbered so the audit can diff it against batch-1's 8.

1. **P1 · A stale entry, twice, in one cross-check.** C42's and F21's 2026-08-05
   results reached the checklist and never reached their entries. The S7 rule is
   five days old and caught both on its first real use. *Cost avoided:* a brief
   that told you C42 was unsampled for a reason that had already been ruled out.
2. **P2 · A parked instrument built on a false premise
   (`CheatResearchAll`).** Batch-1's leg-5 source asserted Frictionless
   Composites was discovered; prep read `discovered=nil` live. The leg would have
   granted nothing and researched everything else. **Second chain running in
   which a documented `[NEVER RUN]`-class recipe was broken as written** — rule
   10's whole reason for existing.
3. **P3 · The G1 resolution gate caught a dead name in an owner-facing
   line.** A `say()` string told you to type a function that does not exist. At
   an attended console a dead name is a line you type that cannot work — the
   `U1.ErrorWatchNote` class, this time in a message rather than a call.
4. **P4 · The correction note nearly broke its own gate.** Writing "the name was
   X" put X back in the file, so the G1 grep matched itself and reported RED
   forever. Spelled around — the exact defect batch-1's ERRORWATCH marker had.
5. **P5 · Four stranded staged saves, against an audit that said otherwise.**
   `CB1STAGE`, `CORUN0`, `CORUN1`, `U1STAGE` — ~223 MB — were still in the save
   directory, in your load list, while batch-1's terminal audit recorded *"all
   staged/throwaway saves gone from the save dir"*. Deleted this session with
   `TEST2H TRAIN` re-verified byte-identical. **The close-out gate checks both
   git trees and does not check the save directory at all** — that is the hole,
   and it is routed on the checklist.
6. **P6 · The C42 instrument worked and the fixture did not.** The poller did
   exactly what it was asked and returned an honest UNSAMPLED. The lesson is that
   **prep confirming a subject is not the same as prep confirming the subject can
   be produced** — three sessions have now spent effort on a save whose passages
   carry no observable traversals.

⚖️ **Recurrences vs batch-1:** P1 is S7 (caught, not repeated), P2 is the broken
recipe class, P3/P4 are G1 and the self-matching marker. **P5 and P6 are new.**

---

### 8 · Close-out reminders prep already half-did for you

- Prep **disarmed** (`DISARM GATE: GREEN`, `PROBE SWEEP: clean`), both repos'
  trees clean, `metadata.lua` restored with no BOM.
- Prep's staged copy and the four stranded ones are **deleted**; `TEST2H TRAIN`
  MD5 `103B320A…8958`, mtime 2026-08-03 22:21:48, **unchanged**.
- Prep's log is archived at
  `docs/archive/cb2confirm_Mars.exe-20260810-10.20.03.log` (`git add -f`).
- ⛔ **Your close-out must check the SAVE DIRECTORY too**, not just the two git
  trees — that is P5's whole point.
- The parked sources are `97_CB2Common.lua.txt` / `98_CB2Confirm.lua.txt` /
  `99_CB2Legs.lua.txt` + `CB2_ARM.ps1.txt` (**BOM'd**, S3). Arm with
  `CB2_ARM.ps1 arm-sitting` — it refuses to arm the sitting while the confirm
  payload is present, because that file drives the game and would fight you.
  **G1 GREEN and parse GREEN on all three as parked**; the sitting still treats
  every first execution as a first execution.
