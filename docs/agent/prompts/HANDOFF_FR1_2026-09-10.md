# Handoff — end of the 2026-09-10 afternoon/evening session → the next session (model-agnostic)

⛔ ONE-SHOT: read this first; `git rm` it once every item below is done, routed to its home,
or restated in its own prompt. It replaces nothing — the records it points at win if they
disagree. Written by `smr-bugfixpack-04` at the owner's stop. **Verify every specific against
`git log` + the tree** (Claude AND Codex sessions commit here; Codex sessions do not appear in
`ListAgents` — foreign uncommitted files with no live Claude peer are probably the owner's
Codex run: ask the owner).

## 0 · Orient
`git log --oneline -15` · `git pull` · `git status --short` · `ListAgents` · `agent/STATE.md` ·
`prompts/DISPATCH.md` §0–§3. Open a **live todo list**, one item per numbered task below; the
owner reads it to decide when to step in.

## 1 · Where things stand (verify, don't inherit)

- **v7 is WORDED, NOT UPLOADED.** `f66dfa1` applied the release outbox to every surface: fix-list
  rows 46 → 48, count word **Forty-eight** in all five card copies, `last_changes` = v7's note.
  In it: C74+C77 (`0cfc53a`, seven silent hit-moment FX, tested-attended) and C83 (`05f8145`,
  arrivals kept out of dead domes, tested-attended; sibling C84 closed as intentional). The
  pack + upload is the OWNER's (`UPLOAD_WORKFLOW.md`, H-02/H-03). After the owner confirms both
  listings: `RELEASE.md` §4–§5 (`POST_UPLOAD_CLOSE.md`, then clear the outbox — never before).
  `RELEASE.md`'s count-word gate was repaired `0bf4718` (it grepped "Eighty…" — vacuous since v6).
- **Reporter replies** for the C74 and C83 Steam comments: `reports/FIELD_REPORT_REPLIES.md`
  (from "Steam comments answered by the v7 update") — post only after the upload is live.

## 2 · NEXT ON DECK (owner, 2026-09-10): FR-1, the Linux crash

`prompts/FR1_LINUX_SITTING.md` (`2ed4278`, 09-10) is ready — an attended sitting on the owner's
RTX 3070 laptop running Mint 22.2 (memory: Linux test rig). **Fire it when the owner says the
laptop boots Mint** (checklist 136). Nothing since `2ed4278` changes it: the sitting runs with
mods DISABLED against a vanilla new-game crash, so the v7 upload is irrelevant to it. Still run
its staleness anchor (`git log` since `2ed4278`, vanillahunt README §2b FR-1).
Then STATE's standing NEXT: `prompts/DLC_DEEP_CHECK.md` (owner framing in its banner: deep on
the new DLC functions, BOUNDED — never an exhaustive per-row census).

## 3 · Owner items (both in checklist 144 — none blocks the upload)

1. **The owed v6 post-upload sitting** (STATE "OWED": A3/F118 · A10 · A5 c2 · A9 c4/c5 · F117's
   station recipe · the kit's first `RunAll()`). Recommendation: run it on the FIRST v7 boot —
   one sitting, both versions' checks. If ruled, the sitting uses the checklist "THE SITTING
   RAN" block + `bugs/F117.md` §Control; add a v7 smoke line (the `applied` boot lines for
   `SilentHitMomentFX` and `ArrivalDeaths`).
2. **The Steam sounds thread.** A long post was drafted in conversation ("what we're fixing /
   silent on purpose / can't fix or haven't nailed down yet / still checking") — whether and
   which version the owner posted is unknown. If the posted text says "still checking", two
   leads are OWED: see §4.1. The promises it made are recorded in `FIELD_REPORT_REPLIES.md`.

## 4 · Loose ends (each homed; listed so nothing is dropped)

1. **Unconfirmed sound leads** (`reports/C74_SOUND_SWEEP.md` "Unreachable even WITH presets",
   agent B's, NOT re-derived): DroneHub `ConstructingDrones` Hit1-4 particles; one sound keyed
   to `dig-reveerse` (typo of `dig-reverse`). Re-derive from the 1.1.0 tree; file each as a
   candidate or close it with the line; if the Steam post promised it, draft the follow-up in
   `FIELD_REPORT_REPLIES.md`. Also open there: the Hydroponic Farm stub (10 sounds, empty
   `FarmHydroponic:StartAnimThread`, `Farm.lua:1033-1034` — looks deliberate; design question,
   owner's) and the Metatron `End1..7` particles (fixable, untimed, deliberately out of v7).
2. Pending owner decisions that could shape hotfix 3+, none blocking: ck142 (rec none), ck135
   (`luafn.py` delimiter; tools-only), F60 REMOVE candidate (audit F-4).
3. Unexplained, verbatim, attribution only if asked: `Missing spot 'Top' in
   'ElectricityGridElement' state 'idle'` (homed: SESSION_LOG 09-10 lookback).

## 5 · Working practice learned today (also in memory)

- **Shared index, unreachable peer:** when a peer's uncommitted `C##.md` rows sit in the regenerated
  `bugs/INDEX.md` and the peer cannot be asked, commit HEAD's index + ONLY your hunks via
  `git hash-object -w` + `git update-index --cacheinfo`, verify `git diff --cached --name-only`,
  and commit WITHOUT a pathspec (a pathspec commit takes the work-tree file). Worked example:
  `e57d7dc` (memory `commit-with-pathspec-shared-index`).
- **A grep gate keyed on today's literal goes vacuous when the literal changes** — make zero hits
  a FAIL (`0bf4718`).

## 6 · Close-out
Route every result to its home (entries, checklist, STATE only if the kernel changed,
SESSION_LOG). doccheck GREEN; guarded pathspec commit; push; `git rm` this file when §2–§4 are
done or restated elsewhere (§2's FR-1 is "restated" once `FR1_LINUX_SITTING.md` is fired).
