# A·02 — terminal adversarial audit · fresh context · TRUSTS NOTHING FORWARD

**You are the adversary.** `01_PROBE` is gone by the time you read this; its
full text is recoverable at the grave named in its close-out commit. Your job is
to disbelieve it. Read `README.md` in this folder, then `docs/agent/STATE.md`,
then this.

## 0 · Staleness check

```
git log --oneline -10
git pull
```

## 1 · 🗒 Live todo list from your first action. One item per section below.

## 2 · The standing rule you are here to honour

`CHAIN_METHOD` §4.0, owner 2026-08-04: **even a single truly unattended item is
a minimum chain of two — the volume tier executes, the top tier audits
adversarially against the archived logs.** You are the second half. Every
"done" upstream is a **claim** until you have re-derived it.

`CHAIN_METHOD` §2.6 on what your tier actually buys: on a run whose executor
self-corrects visibly, the audit's yield is **certification plus residue**, not
rescue. Expect to sustain the verdicts and find hygiene. Both outcomes are the
job; neither is a disappointment.

## 3 · The audit

### 3.1 Inbox audit
Every item in `## Notes from upstream` below is accounted for: delivered,
routed with a named owner, or explicitly declined with a reason. **Zero orphans**
is the standard (18 prompts once managed it).

### 3.2 Re-derive, do not inherit
- **Every source citation** the chain used — at `ModTools\Src`, **by symbol**,
  not by the line number you were handed. `EF-057` and the two route reversals
  are why: the project has been wrong about a route in both directions inside
  one week, with every cited line correct both times.
- **Re-count the log yourself**, and verify it byte-identical to the game's own
  (MD5 against the source file if it still exists). A count you did not perform
  is a claim.
- **The prediction commits precede their launches** — prove it in `git`, with
  timestamps, not by assertion.

### 3.3 The four answers, adversarially
For each of jobs 1–4, ask the hostile question:

1. **Marker reachability.** If the count was zero — was the *right thing*
   counted, on the *right maps*, with `FeatureType` matched exactly? A zero from
   a mis-typed label is indistinguishable from a real zero and would wrongly
   kill a real defect. If the count was non-zero — were the markers on a map
   where `DustStormsDisabled` was actually true?
2. **`AsyncPopsDownloadFile`.** Was the control run (the three neighbours)? A
   bare `nil` proves much less than `nil` beside three `function`s.
3. **Map generation.** If it reported "drivable", was anything actually
   generated, or is that an inference from a `type()` read? Those are different
   claims and chain D will spend launches on the difference.
4. **Jumbo Cave census.** Were the owner's ORIGINAL saves left untouched? Did
   `EF-056`'s pre-copy rule get honoured — and is that verifiable, or asserted?

### 3.4 The detectors — the highest-risk item in the chain
`EF-058`: a wrapper on a base class watches nothing while flattened copies do
the work. **Four bites.** So:
- Was every carrying class patched, or only the base?
- Was the wiring proven **off live instances**, or only off the class table?
- Are they genuinely log-only — no `SetCommand`, no state mutation, no thread?
- Do they stand down cleanly, and is the save contract intact?

⛔ If a detector is armed but has never been seen firing, the record must say
**armed and unwitnessed**, not "working".

### 3.5 Consistency pass
Entry front matter vs heading tags vs the regenerated `INDEX.md`; `STATE.md`
counts re-emitted by the tool and never hand-typed; the checklist's item 34 in
step with what actually happened; no doc claiming a status the evidence does not
carry.

### 3.6 Drift-evidence capture
`CHAIN_METHOD` §2.8: **a silently-corrected instance is destroyed evidence.**
Every drift you find — including ten-second fixes — gets recorded, not quietly
repaired. The corpus is worth more than the tidiness.

## 4 · Scope fence

**IN:** audit, corrections to records, integration, the folder-empty gate, the
owner report.
**OUT:** ⛔ **No new measurement runs.** ⛔ No fix code. ⛔ No launching the
game. If the audit finds the chain needs another run, that is a **finding routed
to the owner**, not something you do.

## 5 · Stop conditions

- A verdict cannot be re-derived from archived evidence → **do not sustain it.**
  Downgrade it in the record and say why.
- The primary log is missing or does not match → **that is a finding**, and by
  R8 it is also a process failure worth writing down (it has happened three
  times).
- You find yourself agreeing with everything → re-read §3.2. Independence is the
  only thing your tier is buying here.

## 6 · ⛔ What may not be claimed

- ⛔ **"Audit-sustained"** for anything you did not re-derive from primary
  evidence. Sampling is fine; saying you sampled when you did not is not.
- ⛔ **A status promotion.** This chain measures. Promotions belong to the chain
  that builds and verifies the fix.
- ⛔ **"Reachable"** for the marker gate on a non-zero count alone — reachable
  means a player route, and a marker on a map nobody visits is not one.
- ⛔ Any claim about what a player sees. Nothing in this chain had eyes on it.

## 7 · Close-out — the folder-empty gate

One commit: corrections applied · `STATE.md` extended (60-line cap — evict
resolved material to `archive/SESSION_LOG.md`, never evict an obligation) ·
checklist item 34 updated with what the probe answered · `python
tools/doccheck.py` GREEN · `git rm` this file — **the folder must end EMPTY** ·
commit naming the grave · push.

**End your owner report with the kickoff line for the next queued chain**
(`CHAIN_METHOD` §4.6) — B, C and D are all unblocked by this chain and are
independent of each other, so name all three and say which you would run first
and why. And state plainly, in one sentence, **whether ④ has happened**, because
if it has not, none of them should start.

## Notes from upstream

*(01_PROBE appends here in the commit that deletes itself. Anything below this
line arrived from the executor; treat all of it as claims.)*
