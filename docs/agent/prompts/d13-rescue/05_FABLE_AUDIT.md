# Chain prompt 5 — adversarial audit, integration, chain close

**Read `README.md` first — binding chain rules apply. You are the terminal
prompt: this folder must be EMPTY when you finish.** Staleness check (all
repos), todo list. Precedents set the floor: the split-optins terminal audit
and corun-pt60/-pt15 (SESSION_LOG 2026-08-12/-11 — logs byte-compared over
the FULL length and read whole, every conclusion re-derived from the logged
numbers, residues the upstream left unruled get ruled, upstream precision
errors corrected visibly even when no verdict moves).

**Every "done", "PASS", "clean", "complete" and predicted-vs-measured verdict
upstream is a claim.**

## Job 1 — audit the record

* Byte-compare every archived log against its on-disk original over the FULL
  length; read each whole. Recount every tally from the verdict lines
  themselves; re-derive every matrix-cell verdict; settle every ABSENCE claim
  yourself from the archived bytes (EF-047) — especially (r3)'s residue-zero,
  which is this chain's headline claim.
* **⛔⛔ The exposed set gets a THIRD derivation** — yours, from the shipped
  trees as they stand at audit time (they may have moved: prompt 3 edited
  docs, possibly probes). Membership diff against `D13_EXPOSED_SET.md`; every
  difference settled by source reading. The KEEP/REMOVE lists get the same
  treatment the split audit gave the persisted-name inventory: re-derive,
  then check the matrix readings against them name-by-name.
* **The doc-correction sweep audited**: run your own sweep for stale
  counts/denominators in both repos' current docs (archive excluded); any
  survivor is a finding you fix in place.
* Four-OFF-states discipline verified per cell: the gate line exists beside
  every uninstall-flavored reading, or the reading is demoted.
* Save close-out: staged saves "deleted, listing verified" BY NAME (or "gone —
  verified" if STATE says the EF-051 hold lifted — check, never assume);
  autosave pre-copies reconciled; the standing four MD5s re-read; junction
  state = standing config (both packs + kit in, artifact OUT).
* Whole-log sweep per WORKFLOW: unexplained lines verbatim with age; F99/C45
  watches; cheat markers vs disclosures; a missing archived log is an
  automatic finding.

## Job 2 — the release-gate acceptance test (FIX_POLICY §3a)

With the chain's docs in their final state, answer: does EVERY site in the
audited exposed set carry a recorded disposition with its reason? Is the
table's denominator the derivation's own? Does the D13 entry now hold the
truth (status, the artifact's location, the uninstall-procedure draft, the
version-skew statement)? Does `MOD_DESCRIPTION`'s rebuild (step ③, NOT this
chain) have everything it needs waiting — named, not implied? Any gap → fix
now, re-run the check.

## Job 3 — the ledger

Sitting-class misses vs the standing stacks (recurred = repair WORKFLOW
surgically; NEW = record); owner-time honesty (predicted zero at keyboard —
actual, and whose; plus anything the chain COST beyond minutes, the split's
lost-autosave precedent); economics one line. CHAIN_METHOD one row ONLY if
this chain taught the method something new — judge honestly; a second clean
run of a proven shape earns silence, not a row.

## Job 4 — integrate and close

All repos: entries carry verdicts (D13's status to the measured truth — the
attended after-sweep is still owed, so `built`+verified-unattended is the
ceiling here; `tested` waits for the sitting); STATE everywhere it exists
(this repo: D13 chain CLOSED + NEXT per the release line; the artifact repo:
its own honest state); checklist: the D13 rider block for the ONE combined
sitting written as measure-moments (PT-20 redo done RIGHT — state-3/full
restart, the named re-check — + the D13 attended after-sweep + F102's minute
on `Sylmacaink BH25`), each moment with its instrument and verdict words,
per the co-run protocol; SESSION_LOG newest-first HERE (cite the
pre-deletion sha; the artifact repo's log records its own birth if it keeps
one); delete every remaining file in this folder in the closing commit.
doccheck GREEN everywhere it exists, push what has a remote. **The owner
report ENDS with the next kickoff per STATE's release line: the ONE combined
sitting (its prepped brief is this audit's deliverable — say where it lives
and what the owner says to start it), with step ③ MOD_DESCRIPTION ×2(+1)
queued behind it.** Carry to the owner: any decision this chain surfaced and
did not consume, stated once, batched.

## Stop conditions

- A matrix verdict fails audit and the logs cannot settle it → correct
  visibly, re-route to the owner, keep closing.
- Your third derivation moves the set and the movement invalidates a shipped
  disposition or a cleaner list entry → that is a defect in the shipped
  artifact: file it, route it, do NOT quietly patch the artifact from the
  audit chair.
- The chain ran partial → audit what ran, inventory the remainder as
  TAKEABLE work items, still empty the folder.

## ⛔ What you may not claim

- Not "the exposed set is closed" without your own third derivation agreeing
  or every difference settled at source.
- Not "residue-zero" without (r3)'s archived-log absence read re-run by you.
- Not "ready to ship beside the pack" — that is the release checklist's call
  after the attended sitting and the portal work; yours is "built and
  unattended-verified, sitting prepped".
- Not "gone" for any staged save while the EF-051 hold stands (re-check the
  hold's status in STATE before writing any close-out line).

## Notes from upstream (prompt 4 appends here)

**From prompt 4 (Opus, the verification matrix — 2026-08-13). ⭐ THE MATRIX RAN
AND THE ARTIFACT WORKS. Nine launches, ~8 min of machine time, owner cost ZERO
minutes, zero `[LUA ERROR]` in any cell.** Staleness at my run: fix-pack
`8136d05` (my own predictions commit, on top of `cc67adf` from the concurrent
`public-docs` chain), opt-pack `98c37ea`, TestKit `da432f8`, rescue `aaff837`.

⛔ **READ FIRST: `agent/reports/D13_VERIFICATION.md`.** §1 is the fixture
provenance, §2 the predictions **committed before the first launch** (that
commit is the proof — `git log` the file), §4 the measured results. Everything
below is a pointer into it, not a substitute.

### The one owner gate: SPENT, and measured rather than assumed

Checklist 26 was still open in text when I started, but the owner had already
done it: `rs_ownertick_Mars.exe-20260813-11.16.44.log:302` reads
`[CommunitySaveRescue] SaveRescue: ready`. **The log is the measurement; the
unticked checklist line was stale.** Item 26 is now struck and STATE's open
owner decisions drop 2 → 1.

### The matrix — predicted vs measured

| cell | config (junction-set) | predicted | measured |
|---|---|---|---|
| r0 | fix+opt+kit | `74/74`+`8/8`, rescue ABSENT, **78/0/16/0 of 94** | ✅ exact |
| r1 | +rescue | pass `removed 0`; **84/0/10/0**; interlock IDENTICAL | ✅ exact |
| r2pre | kit only | before-inventory + manufacture, R4-verified | ✅ |
| **r2** | kit+rescue | all 11 REMOVE gone, both KEEP kept, heals bounded | ⭐ ✅ `removed 1617` by name, survivors **0**, KEEP **2**, `meteors 1, rains restarted 1` |
| r2b | kit+rescue | fresh process finds nothing | ✅ `removed 0`, no dialog |
| r3 | kit only | residue-zero both halves | ✅ inventory; ⚠️ log half CORRECTED (below) |
| r3z | none | zero-mods log-clean read | ✅ 87 lines, 0 mod lines, clean `Debug::Done()` |
| r-restore | fix+opt+kit | rig as the owner left it | ✅ dials `5x`/`+2`, 7 toggles on |

**Logs archived in this commit** (`git add -f`, R8): `archive/rs_r0_*`,
`rs_r1void_*`, `rs_r1_*`, `rs_r2pre_*`, `rs_r2_*`, `rs_r2b_*`, `rs_r3_*`,
`rs_r3z_*`, `rs_rrestore_*`, `rs_ownertick_*`.

### ⛔ Four things to attack first — they are where I am weakest

1. ⚠️ **D10's witness is SYNTHETIC.** No save in the folder carried an
   `SMRFixPack_F35_*` modifier (censused: r0 and r2pre both read **0**), so one
   was planted with the id and shape `90_SaveSanitizer:84` writes. The KEEP
   headline is now **SAMPLED** rather than assumed-absent — which is what §6
   doubt 3 asked for — but it is **not a native observation**, and every claim I
   make about D10 says so. If you judge a planted modifier not to be a witness,
   that is the finding to write.
2. ⚠️ **The rains and meteor heals ran against MANUFACTURED pre-rewrite
   residue.** `SMRFixPack_fixed_loop` and a dead `Meteors` thread exist in no
   save we hold; the derivation only ever claimed that population exists in the
   wild. Both were written onto a byte copy and survived a save round trip
   before anything was claimed (§1, §4.1).
3. ⛔ **§10.9(4) is CORRECTED, not satisfied** (§4.5, plus the annotation now in
   `D13_EXPOSED_SET.md` §10.9). "Zero lines naming it" is FALSE: the engine
   prints `Savegame references Mod Save Rescue ... which is not present`. It is
   a standing behaviour — **both shipped packs produce the identical line**
   (`rs_r2pre_*:140-141`) — and it self-clears on the next save. Re-run the
   absence read yourself off the archived files; your charter requires it.
4. ⛔ **My harness voided a run, and the ARM gate passed it GREEN** (§4.6).
   `arm` was not idempotent, `98_RSRun.lua` was listed twice in `metadata.lua`,
   executed twice, and started **two racing payload flows**. I first read the
   doubled output as an engine behaviour (`PostLoadGame` twice per load) and was
   wrong; comparing against archived logs is what killed that. Fixed both ways
   (strip before insert; the gate now **counts** and fails on anything but one).
   **The void log is archived as `rs_r1void_*` deliberately** — audit the re-run
   against it.

### Deviations chased to ground, so you need not re-chase them

* **P0.5 REFUTED, in the good direction.** A pulled junction produces **no log
  line at all** — not for the rescue mod (r0), not for any of the four (r3z). I
  had misread `EF-055`'s "non-modal log line": `ModMessage` appends to
  `ModMessageLog`, not to the log file. So "the artifact is not a standing rig
  mod" costs the owner nothing, and the question I meant to route does not exist.
* ⭐ **D1/D2 promoted SOURCE → MEASURED.** `SMRFixPack_MeteorLatch` = `1.0.1`
  with the packs loaded, `nil` with them pulled **before the artifact acted**.
  §10.2 argued it from `persist.lua:136-142`; it is now measured — and it is
  exactly why a `MeteorLatch` absence must never be credited to the tool.
* ⭐ **`EF-055` holds for a third mod:** pulled for r0, restored for r1, and the
  mod came back **enabled with no owner action**.
* **r-restore vs r0, eight junction round trips apart:** every residue row from
  D6 onward byte-identical. Two rows moved, both explained — four transient
  objects, and 2 of 1533 `reserved_at` handles, because **the live fix pack
  writes that field while the game runs** and the two cells settled at different
  speeds. A count would have read 1533 both times and said nothing; that is the
  argument for handle sets.
* ⚠️ **A deferred-deletion trap.** Immediately after `DeleteThread(Meteors)` the
  instrument read `IsValidThread = true` and I nearly filed the meteor restart
  UNSAMPLED. After the round trip it read `false`. Only the pre-registered
  wording stopped a false negative entering the record.

### Instrument defects in MY reader — do not quote these numbers

1. ⚠️ `RESIDUEROW ... D7 ... on 4 object(s) of **0 AllRockets**` — that row's
   population resolves through `IsKindOf(obj,"RocketBase")`, which matched
   nothing on this world. Count and handle set are right; **the denominator is
   wrong.** Deliberately NOT fixed mid-matrix: changing the instrument between
   cells would have broken the cell-to-cell comparability the design rests on.
2. `DroneControl` includes rockets (its 25 handles overlap `payload_set`'s) — a
   class-hierarchy fact, so "25 DroneControls" is not "25 drone hubs".

### Close-out inventory

* **`EF-056`:** both autosaves pre-copied before launch 1 and **byte-identical**
  at close (`Autosave Sol 311` `D5BCC...`, `(2)` `98F55...`); **no new autosave
  was written** — the rotation never fired.
* **Protected four re-read, all unchanged:** `CP15PT15` `D2887D...`, `CP60RT`
  `746757...`, `PT35FIXTURE` `D72132...`, `Autosave Sol 311` `D5BCCF...`.
* **Staged saves — deleted, listing verified BY NAME** (⚠️ `EF-051` HOLD stands,
  so never "gone"): `RESCUESTAGE` `RESCUEWIT` `RESCUEDMG` `RESCUECLEAN`.
* **Save directory: 92 entries at open, 92 at close, ADDED none, REMOVED none.**
* **DISARM GATE GREEN**; `PROBE SWEEP: clean` over all four `Code/` trees;
  TestKit tree clean; `git status` clean in the opt-in and rescue repos.
* ⚠️ **Cheats:** enabled on the rig, and the fixture lineage was built on cheated
  colonies. **Intersection with these readings: NONE** — every reading is a
  persisted mod-authored name, a label modifier, a thread handle or a registry
  count, and no cheat writes or clears any of them.
* **Owner-minutes consumed: ZERO** (predicted zero). Their 15-second tick was
  spent before this leg began and is not chargeable to it.

### Routed to you or the owner, not fixed by me

1. ⛔ **`tested` is NOT granted and must not be.** WORKFLOW reserves it for a
   keyboard pass; this is the unattended ceiling. D13 keeps the `built`
   vocabulary word — ⚠️ and note that **the status vocabulary has no slot for
   "unattended-verified"** (`doccheck` rejected `verified`), which is why the
   measurement lives in the narrative. That gap is a tooling question,
   deliberately not touched mid-chain.
2. **The uninstall text owes one sentence** about the engine's savegame
   mod-reference notice (§4.5). D13 owns that text; MOD_DESCRIPTION ×3 is step ③.
3. **Still unowned, inherited from prompt 2b and not filed by prompt 3 either:**
   the Crystals-mystery repeater that can double `CrystalFlyAway` broadcasts per
   save/load with the pack installed. Source-derived, unmeasured. It has now
   passed three prompts without a home — **please either file it or kill it.**
4. **The queued `FactionFundingCheck` PASS→SKIP repair (TestKit `62f03da`) is
   still queued** — it read PASS in both tallies, so nothing here forces it.
5. ⚠️ **Concurrency:** the `public-docs` chain committed `cc67adf` mid-session
   and has **unstaged work in this shared tree** (`public-docs/02_QA.md`, and an
   untracked `agent/prompts/CAPTURE_SITTING.md`). I touched neither. My commits
   stage by explicit path; **my first commit used `git add -A` before I had read
   the fence** — I verified with `git show --stat` that it carried only my four
   files, but I flag it rather than hide it. Their `CAPTURE_SITTING.md` says it
   must not run while this chain is live: **this chain's game legs are now
   done**, so that constraint releases once the folder empties.
