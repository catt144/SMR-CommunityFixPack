# Chain prompt 5 — adversarial audit, integration, chain close

**Read `README.md` first — binding chain rules apply. You are the terminal
prompt: this folder must be EMPTY when you finish.** Staleness check (all
repos), todo list. Precedents set the floor: the split-optins terminal audit
and corun-pt60/-pt15 (SESSION_LOG 2026-08-12/-11 — logs byte-compared over
the FULL length and read whole, every conclusion re-derived from the logged
numbers, residues the upstream left unruled get ruled, upstream precision
errors corrected visibly even when no verdict moves).

**Every "done", "PASS", "clean", "complete" and predicted-vs-measured verdict
upstream is a claim.**

## Job 1 — audit the record

* Byte-compare every archived log against its on-disk original over the FULL
  length; read each whole. Recount every tally from the verdict lines
  themselves; re-derive every matrix-cell verdict; settle every ABSENCE claim
  yourself from the archived bytes (EF-047) — especially (r3)'s residue-zero,
  which is this chain's headline claim.
* **⛔⛔ The exposed set gets a THIRD derivation** — yours, from the shipped
  trees as they stand at audit time (they may have moved: prompt 3 edited
  docs, possibly probes). Membership diff against `D13_EXPOSED_SET.md`; every
  difference settled by source reading. The KEEP/REMOVE lists get the same
  treatment the split audit gave the persisted-name inventory: re-derive,
  then check the matrix readings against them name-by-name.
* **The doc-correction sweep audited**: run your own sweep for stale
  counts/denominators in both repos' current docs (archive excluded); any
  survivor is a finding you fix in place.
* Four-OFF-states discipline verified per cell: the gate line exists beside
  every uninstall-flavored reading, or the reading is demoted.
* Save close-out: staged saves "deleted, listing verified" BY NAME (or "gone —
  verified" if STATE says the EF-051 hold lifted — check, never assume);
  autosave pre-copies reconciled; the standing four MD5s re-read; junction
  state = standing config (both packs + kit in, artifact OUT).
* Whole-log sweep per WORKFLOW: unexplained lines verbatim with age; F99/C45
  watches; cheat markers vs disclosures; a missing archived log is an
  automatic finding.

## Job 2 — the release-gate acceptance test (FIX_POLICY §3a)

With the chain's docs in their final state, answer: does EVERY site in the
audited exposed set carry a recorded disposition with its reason? Is the
table's denominator the derivation's own? Does the D13 entry now hold the
truth (status, the artifact's location, the uninstall-procedure draft, the
version-skew statement)? Does `MOD_DESCRIPTION`'s rebuild (step ③, NOT this
chain) have everything it needs waiting — named, not implied? Any gap → fix
now, re-run the check.

## Job 3 — the ledger

Sitting-class misses vs the standing stacks (recurred = repair WORKFLOW
surgically; NEW = record); owner-time honesty (predicted zero at keyboard —
actual, and whose; plus anything the chain COST beyond minutes, the split's
lost-autosave precedent); economics one line. CHAIN_METHOD one row ONLY if
this chain taught the method something new — judge honestly; a second clean
run of a proven shape earns silence, not a row.

## Job 4 — integrate and close

All repos: entries carry verdicts (D13's status to the measured truth — the
attended after-sweep is still owed, so `built`+verified-unattended is the
ceiling here; `tested` waits for the sitting); STATE everywhere it exists
(this repo: D13 chain CLOSED + NEXT per the release line; the artifact repo:
its own honest state); checklist: the D13 rider block for the ONE combined
sitting written as measure-moments (PT-20 redo done RIGHT — state-3/full
restart, the named re-check — + the D13 attended after-sweep + F102's minute
on `Sylmacaink BH25`), each moment with its instrument and verdict words,
per the co-run protocol; SESSION_LOG newest-first HERE (cite the
pre-deletion sha; the artifact repo's log records its own birth if it keeps
one); delete every remaining file in this folder in the closing commit.
doccheck GREEN everywhere it exists, push what has a remote. **The owner
report ENDS with the next kickoff per STATE's release line: the ONE combined
sitting (its prepped brief is this audit's deliverable — say where it lives
and what the owner says to start it), with step ③ MOD_DESCRIPTION ×2(+1)
queued behind it.** Carry to the owner: any decision this chain surfaced and
did not consume, stated once, batched.

## Stop conditions

- A matrix verdict fails audit and the logs cannot settle it → correct
  visibly, re-route to the owner, keep closing.
- Your third derivation moves the set and the movement invalidates a shipped
  disposition or a cleaner list entry → that is a defect in the shipped
  artifact: file it, route it, do NOT quietly patch the artifact from the
  audit chair.
- The chain ran partial → audit what ran, inventory the remainder as
  TAKEABLE work items, still empty the folder.

## ⛔ What you may not claim

- Not "the exposed set is closed" without your own third derivation agreeing
  or every difference settled at source.
- Not "residue-zero" without (r3)'s archived-log absence read re-run by you.
- Not "ready to ship beside the pack" — that is the release checklist's call
  after the attended sitting and the portal work; yours is "built and
  unattended-verified, sitting prepped".
- Not "gone" for any staged save while the EF-051 hold stands (re-check the
  hold's status in STATE before writing any close-out line).

## Notes from upstream (prompt 4 appends here)
