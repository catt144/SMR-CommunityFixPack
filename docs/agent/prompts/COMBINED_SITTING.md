# The ONE combined sitting — PT-20 redo (state 3) + D13 attended after-sweep + F102's minute

**Prepped by the `d13-rescue` terminal audit (2026-08-13). Owner kickoff: open a
session on this file and say "run the combined sitting" — the agent does all
prep with the game closed and tells you when to sit down.** This is a CO-RUN
(WORKFLOW co-run protocol binds in full: live todo list updated per item — the
owner reads it to decide when to step in; R2 execution markers; EF-050 verbatim
savenames; EF-051 hold wording — "deleted, listing verified", never "gone";
⛔ EF-056 — byte-copy EVERY autosave BY NAME before any launch that loads a
copy of a real campaign; cheats are expected on playtest saves and attributed,
never re-litigated).

**Why one sitting:** STATE ② — these three owe owner-attended minutes and
nothing else on the release line does. Estimated attended time: **25–40 min**
(state-3 round trip + ~10 min ordinary play + two dialog witnesses + one
eyeball minute). Everything else is rig-side.

## Agent prep, game closed, before the owner sits

1. Staleness check all four repos; PROBE SWEEP clean; doccheck GREEN.
2. Byte-copy every autosave BY NAME (EF-056) and record MD5s.
3. Stage byte copies (never originals): the PT-20 host save per its checklist
   block, and a BIG real-campaign copy for the after-sweep (the D13 charter:
   "uninstall on a big save"). Verbatim names recorded.
4. Restore the rescue junction (`EF-055`; the owner's tick is already spent —
   it comes back enabled, measured 2026-08-13). Both packs stay IN for moment C.
5. Instruments per the split/rs precedent: config-gate reads before anything,
   cell banners, residue reads by handle set. Parked `.txt` until armed.

## The measure-moments (instrument · verdict words)

| # | moment | who/what | instrument | verdict words |
|---|---|---|---|---|
| C | **F102's minute** (packs ON) | owner loads `Sylmacaink BH25`, eyeballs the three crystal deposit signs: new art renders, deposits selectable | eyes + `SMRFixPack.ListFixes()` | "signs render / selectable: yes-no", `ExoticDepositSign [active]` quoted. Closes the local half of F102 (checklist item 11) |
| A | **PT-20 redo, done RIGHT** | owner: Mod-Manager DISABLE of both packs + **FULL PROCESS RESTART** (state 3 — the D13 four-states doctrine; the old 98-vs-98 may have measured state 2) + ~10 min ordinary play | rig-side loads/saves/log reads; ⛔ the `pack=0/0` gate line BESIDE every reading | the PT-20 block's own re-check re-run and its result stated as "supersedes / confirms the old 98-vs-98"; 0 `[LUA ERROR]`; every pack-naming log line accounted (the state-3 six-line inventory, D13 entry 2026-08-10, is the template) |
| B | ⭐ **D13 attended after-sweep** — rides the SAME state-3 window (packs disabled, rescue junction in) | owner loads the staged big-save copy and **WATCHES THE SCREEN** | eyes (the one instrument unattended mode lacks — the dialogs write NO log line) + the pass's own log line | ① report dialog RAISED y/n, text vs frozen spec §10.5 quoted from memory/screenshot; ② `pass: removed N …` line quoted verbatim; ③ save, reload: removed 0 AND **no dialog** y/n; ④ after the owner re-enables packs + restarts (re-enable is owner-only): stand-down dialog ONCE y/n + `removed 0`. ⛔ Clean run ⇒ D13 earns `tested`; any KEEP name missing or REMOVE survivor ⇒ stop the leg, record verbatim, route |
| D | optional | CAPTURE_SITTING passes that fold in (its brief says which — checklist item 24 wants most of the capture list riding this sitting) | per that brief | per that brief |

## Close-out (rig-side, same session)

Restore = standing config (both packs + kit junctions IN, rescue junction OUT);
r-restore-style readback (gates 74/74 + 8/8, dials `5x`/`+2`, 7 toggles);
staged saves deleted, listing verified BY NAME; autosave pre-copies reconciled
byte-for-byte; whole-log sweep (F99 `:805` / C45 `Quantum Comet` watches);
archive logs `git add -f` in the SAME commit as the claims; SESSION_LOG;
checklist items ticked (11 local half; PT-20; D13 → `tested` if earned);
STATE's ② cleared and ③ (MOD_DESCRIPTION ×2(+1)) promoted to NEXT.
