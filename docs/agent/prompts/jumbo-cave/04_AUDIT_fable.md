# D·04 — terminal adversarial audit · fresh context · TRUSTS NOTHING FORWARD

**You are the adversary.** Everything upstream is gone; the graves are named in
its close-out commits. Read `README.md` (⛔ THE LINE, ⚖️ the discriminator),
then `STATE.md`, then this, then `## Notes from upstream`.

## 0 · Staleness check
```
git log --oneline -10
git pull
```

## 1 · 🗒 Live todo list from your first action.

## 2 · The one adjudication only you can make

**Did the chain stay on the right side of THE LINE?**

The whole validity of `C25`'s upgrade rests on the rocks having been placed by
the game's own scatter at an elevated density — not by us. So:

- Was the density change a **parameter the game's generation reads**, or did
  anything place, move or nudge an individual rock? Read the payload, not the
  summary.
- Was the elevated map the **same seed and same terrain** as 01's baseline? If
  terrain moved, the control is void and every conclusion drawn from the
  comparison goes with it.
- Was the multiple honest? A density so far outside the shipped distribution
  that rocks necessarily jam each other has *manufactured* the confound the
  discriminator was meant to detect. **Judge the number, and say what you judged
  it against.**

## 3 · The discriminator — rule it, do not inherit a ruling

For every stranded rock: what occupied the neighbouring hexes at failure time?

| surroundings | verdict you may grant |
|---|---|
| terrain | ⭐ **trigger demonstrated** — `C25`'s open question closes |
| other rocks | consequence proven live; **trigger stays open**, and the entry must say so in those words |
| mixed / ambiguous | **do not grant the stronger reading.** Record the ambiguity |
| nothing stranded | record the exposure denominator and move `C25` toward `wontfix`, the way `C49` went |

⛔ **03 was forbidden from adjudicating this. If it did anyway, that is a finding
and the ruling is still yours.**

## 4 · The ordinary audit

- **Re-derive every citation** at `ModTools\Src` **by symbol**. The project has
  been wrong about a route in both directions in one week with every cited line
  correct both times.
- **Re-count the logs yourself** and verify them byte-identical to the game's own.
- **Prediction commits precede launches** — prove it in `git`, with timestamps.
- **Inbox audit**: every upstream note delivered, routed, or explicitly declined.
  Zero orphans.
- **The detector**: `EF-058`, every carrying class patched, wiring proven off
  live instances. If it never fired, is that a null result or a broken
  instrument? **Those are different and the record must not blur them.**
- **Owner verbatims are in the archived LOG**, not only the transcript. If any
  are transcript-only, say so — a future re-audit will never be able to read them.
- **Save contract**: the owner's originals untouched, `EF-056` pre-copy honoured
  and *verifiable*, the staged save named and disclosed.
- **Drift capture**: every instance recorded, including ten-second fixes. A
  silently-corrected instance is destroyed evidence.

## 5 · Scope fence
**IN:** audit, adjudication, record corrections, integration, folder-empty gate,
owner report.
**OUT:** ⛔ no new runs · ⛔ no fix code · ⛔ no launching the game · ⛔ **no fix
design.** If the trigger is demonstrated, the *remedy* is a fresh decision and a
fresh chain — fredware's shape (destroy the blocker through the vanilla
destruction path) has never been adjudicated by us.

## 6 · Stop conditions
- A verdict cannot be re-derived from archived evidence → **do not sustain it.**
- The primary log is missing or mismatched → a finding, and an R8 process failure
  (third instance of that class already).
- The chain aborted at 01 → run the **reduced form**: post-mortem into
  `CHAIN_METHOD` if the method learned something, route the respec/abandon
  decision to the owner, empty the folder. A recorded kill is the gate working.

## 7 · ⛔ What may not be claimed
- ⛔ **"Trigger demonstrated"** on anything but a terrain-stranded rock.
- ⛔ **`tested-*`** of any flavour — there is no fix in this chain to test.
- ⛔ **A frequency or a player-impact rate.** One staged colony at elevated
  density says nothing about how often a real player meets this.
- ⛔ **"Audit-sustained"** for anything you did not re-derive from primary
  evidence.

## 8 · Close-out — the folder-empty gate
One commit: adjudication written onto `C25` with provenance per row · corrections
applied · `STATE.md` extended (60-line cap; evict resolved material to
`archive/SESSION_LOG.md`, never an obligation) · checklist updated · doccheck
GREEN · `git rm` this file — **the folder must end EMPTY** · grave named · push.

**End the owner report with the kickoff line for the next queued chain**
(`CHAIN_METHOD` §4.6), and say in one sentence whether ④ has happened — if it
has not, nothing else in this set should start.

⭐ **If this was the LAST chain of the set to close**, delete
`agent/prompts/SMRCF_CHAIN_SET.md` in the same commit and name its grave too.

## Notes from upstream
