# Chain prompt 3 — adversarial audit, integration, chain close

**Read `README.md` first — binding chain rules apply. You are the terminal
prompt: this folder must be EMPTY when you finish.** Staleness check, todo
list. Precedents: unattended-2's audit (SESSION_LOG 2026-08-11 — logs
byte-compared and read whole, the discriminating diff RE-DERIVED, every Src
site re-read) sets the floor; batch-2's close record sets log fidelity.

**Every "done", "PASS", "SKIP", verdict and owner quote upstream is a claim.**

## Jobs

**Job 1 — audit the record.** Byte-compare every archived log against its
on-disk original over the FULL length; read the whole sitting log. Per item:
* **PT-15** — the before/after trap + grid numbers exist at their claimed
  values, the R4 round trip held, the ~1000× claim is arithmetic on logged
  numbers (recompute it), forced/organic labels present, and **any `tested`
  grant quotes the owner VERBATIM** — a grant without the quote is struck to
  the mechanism tier, visibly.
* **C39** — before/enact/after/revert reads all present; the entry's open
  sign question answered by NUMBERS (which direction did performance/Comfort
  move for the four-Workshop class vs the Service control); the revert
  restored baseline; first-execution discipline visible (pcall lines).
  Re-derive the CONCLUSION from the logged numbers yourself — the entry
  moves `cand` → confirmed/refuted/resized on what the log holds, not on the
  sitting's summary. If confirmed, the repair decision is NEW owner material
  → checklist, options stated, nothing built.
* **F85** — the Ctrl-F9 evidence recorded either way, routed to the
  checklist decision item, NOT decided anywhere.
* **EF-051** — both listings present; rule on the caveat: clean post-launch
  listing → retire the WORKFLOW "never say gone" clause (dated edit) and
  close EF-051's falsifier ⭐; a returned stray → EF-051 re-opens, say so
  everywhere it is cited.
  ⭐⭐ **CHANGED 2026-08-11 — you now have TWO samples, not one, and the first
  is not ours.** Prompt 1 found that the post-untick launch already happened
  (owner's own, 09:47:15, three minutes after the untick commit) and that none
  of the 14 strays returned — evidence table in `agent/facts/EF-051`, which
  routed the caveat's retirement HERE rather than retiring it itself. **Audit
  that finding as a claim like any other** (the commit time, the
  `steam_autocloud.vdf` write, the absence of all 14) before you lean on it.
  ✅ **The two-file shortfall that briefly looked like a hole is RESOLVED, and
  the resolution is itself a claim to check.** 53 pre-existing against a 55
  baseline; the owner confirmed *"I haven't deleted any old saves"*, and
  `archive/SESSION_LOG.md` (corun-batch-1 close) had already recorded the
  missing pair — the game's own rolling `Autosave Sol 351/356`, described there
  as files that "rotate out on their own", replaced by the new colony's
  `Autosave Sol 11/16`. **Re-derive the arithmetic yourself** (55 − 2 + 2 + 1 =
  56) and check the archive quote before letting it clear the retirement.
  ⚠️ **Two methodology findings already landed in EF-051 — confirm they read
  correctly and are not overstated:** (1) baselines must be recorded as NAMES,
  never a count — a count cannot survive one play session, since two rotations
  and three creations net to +1 and hide both movements; (2) a `Copy-Item`
  byte-copy has the SAME metadata shape as a Cloud restore (fresh
  `CreationTime`, old `LastWriteTime`), so the signature that originally
  identified the restore is not diagnostic once anyone has staged a save.
* Whole-log sweep: F99 `TrackElement.lua:805` (an ORGANIC hit reopens F99 —
  route it), C45 `invalid pos with no holder` (second occurrence — record
  per entry), unexplained lines verbatim with age. ⛔ A missing archived log
  is an automatic finding.

**Job 2 — status honesty.** PT-15/F07 (and F15 if the rider ran) move only
to what the archived log supports; C39's heading tag and index row must
agree; the checklist PT-15 section states its final state (archive-ready or
what remains); no `tested` anywhere the owner's eyes were not.

**Job 3 — the ledger.** Sitting misses vs the standing harness-rule blocks
(u1 1–4, b1 1–6, b2 1–9 as amended): recurred (a rule failing twice is
broken — repair it in WORKFLOW surgically) vs NEW. Owner-time honesty:
promised vs actual, owner-directed deviation separated from rig misses
(only the owner rules their own time out of scope). Economics one line.

**Job 4 — integrate and close.** Entries carry their verdicts; checklist
final; STATE chain CLOSED + outcomes + NEXT (cap 60); SESSION_LOG record
newest-first; CHAIN_METHOD one row ONLY if this chain taught the method
something new. Delete every remaining file in this folder in the closing
commit (cite the pre-deletion sha in the SESSION_LOG record). doccheck
GREEN, push. **The owner report ENDS with the next kickoff** (rule 14):
expected front is the **PT-20 redo co-run** (pack-off run condition, owner
disable click + FULL restart + ~10 min organic play + explicit re-enable
hand-back) — **its chain is not authored**; say so and name what authoring
takes.

## Stop conditions

- A load-bearing verdict fails audit and the logs cannot settle it →
  correct visibly, re-route to the owner, keep closing.
- The sitting was partial → audit what ran, inventory the remainder as
  TAKEABLE riders, still empty the folder.

## ⛔ What you may not claim

- Not `tested` without the owner's verbatim verdict on that reading.
- Not a C39 repair decision — if confirmed, options go to the owner.
- Not the F85 disposition — evidence routes, the owner rules.
- Not "Steam Cloud is fixed" beyond what the two listings sampled.

---

# Notes from upstream

*Written by prompt 2 (the sitting), 2026-08-11, game QUIT before this was
written. Log archived at `docs/archive/cp15sitting_Mars.exe-20260811-15.09.30.log`
(206,861 bytes, 1,659 lines, MD5 `491483C73FC1E935A9A3A8945E5C296B`,
`cmp`-verified identical to the original, `git add -f`).*
*⛔ Everything below is a CLAIM. Line numbers are into that archived log.*

## 0. Run conditions, as read

Retail `Mars.exe` **1.0.7.396349** · map `BlankBigTerraceCMix_10` · pack
**81/81 modules active AS READ** at every leg gate · `WispRewards` active ·
MODE=REAL (read off disk by the ARM gate) · staged copy `CP15STAGE.savegame.sav`
(`Copy-Item` of the owner's `PT-15.savegame.sav`, MD5 identical at launch) ·
ARM GATE GREEN (23 legs all `CP15.Guard()`-gated, 60 definitions, resolution
cross-check over payload **and** brief) · DISARM GATE GREEN, `PROBE SWEEP: clean`.

**Whole-log sweep: 0 `[LUA ERROR]` · 0 `TrackElement` (F99) · 0 `invalid pos`
(C45).** Unexplained/non-pack lines, verbatim with position, none attributed to
us: `[Braze] SessionStart error … could not be resolved` (L189/196, offline
telemetry, also present pre-sitting), `[ResManager Error] Cannot find file with
base path: Animations/LawOfficeDoor_idle.hgacl` and `…_opening.hgacl` (L211-212,
vanilla asset, at load), `not understood` (L250, a console typo of ours),
`!!! Quick Build !!!` ×4 (owner cheat marker, disclosed).
Cheat markers counted: `ObjCheat CheatFill` ×**16**, `ObjCheat CheatRepair`
×**2**, `!!! Quick Build !!!` ×**4** — owner verbatim reason: *"This is a
minimal colony it will take significantly more time if I need to build
infastructure (time) to feed, repair, build and maintant everything for the
colony those are the reasons"*.

## 1. Per-leg verdicts, with the lines that carry them

| leg | verdict | key log lines |
|---|---|---|
| 0 gate/fixture | PASS — `LightsMystery`/`Mystery 11`, `g_ColonyNotViableUntil=-1` ⇒ approval PASSED, countdown ARMED | L286-L324 |
| 1 F85 Ctrl-F9 | **CLOSED — not a route** | L380-L412 (esp. **L408**, **L412**) |
| 2 C39 | **ANSWERED — MISSING UPLIFT** | L648-L734, revert L738-L769 |
| A SpeedRamp | 50× measured; ramp's own pick OVERRIDDEN | L833-L860 |
| 3 march | organic to the wisp choice | L917-L1234 |
| 4 PT-15/F07 | **FIXED, owner-witnessed** | L1264-L1364, R4 **L1368-L1456** |
| 5 F15 rider | **batch grant gone** | L1493-L1614 |
| — | ⭐ NEW `C46` found | L1592-L1601 |

**Owner verbatims, exact:**
* F85 — *"I didn't get any save notification, we are still not sure quick saves
  actually exist"* (**L412**, said BEFORE the structural control ran).
* F85 — *"I cannot do anything until I answer game locks console and all buttons
  until a choice is made."* (chat only — **not in the log**, flagged as such).
* C39 — *"more than double when I reverted it dropped to 124 for the dinner"*
  (chat only — **not in the log**; the numbers it describes are at L722-L725).
* PT-15 — *"the screen shot is what I see, so it is working"* (chat only —
  **not in the log**), given against an infopanel screenshot reading
  *Wisps captured 95 / Power production 95 / Power grid 218.5*.

⚠️ **Three of the four owner quotes exist only in the session transcript, not in
the archived log.** Only the F85 one was pushed through `CP15.Note`. If the audit
requires log-resident verbatims for a `tested` grant, **F07's grant is exposed on
that ground** and the audit should rule on it rather than assume. See §4.

## 2. ⭐⭐ Things prep got WRONG, found by re-deriving from Src

1. **`MassFireflySpawn` is NOT dead code.** Prep recorded it as *"a plain ungated
   global with no caller anywhere in the tree — dev scaffolding"*, and chain rule
   12 was amended to offer it as Option B on that basis. **The mystery calls it
   itself**: `Mystery 11.generated.lua:337-351` places a `Sinkhole` within
   `random_pos_label_dist = 35000` of the Large Water Tank, waits `hour == 4`, and
   calls `MassFireflySpawn(sinkhole_one)`. ⇒ **Option B was never needed and was
   never offered**; the 95-wisp trapful is the shipped mechanic and the wisp
   supply is "as shipped" with no asterisk. **Chain rule 12's amendment (B) rests
   on a refuted premise — the audit should decide whether it is withdrawn.**
2. **A "before the choice" `TrapRead` is IMPOSSIBLE.** Both this chain's brief
   (leg 4) and the payload menu instruct *"call BEFORE the choice and IMMEDIATELY
   after it"*. The `SA_WaitChoice` popup is modal and **locks the console**
   (owner, verbatim, §1). It also does **not** appear to hold game time —
   `fireflies_caught` was 30 when it fired and **33** at the first read after.
3. **`SpeedRamp` picks the WRONG rung.** It ranks by safety alone and recommended
   **400×** (measured **20.72×** effective), then *set the game to it*
   (L856-L860). Overridden by hand to 50×. A rung can be "safe" **by being slow**.
   Full table in `agent/facts/EF-052`.
4. **Prep's "fired workers do not snap back" did not hold** — both C39 buildings
   read full worker counts immediately after `:Deactivate()` (L751-L760).
5. Minor: `Platform.cheats` reads **`nil`**, not the `false` F85 recorded.

## 3. ⚠️ THE SITTING'S OWN BIGGEST MISS — ours, not the owner's

**The organic wisp-choice reading was LOST and had to be re-taken FORCED.** The
owner reached *Prisoners of Light* and asked whether the choice mattered. The
agent answered **"STOP — do not answer yet"** and went to read Src. The popup
does not hold game time; the clock ran from night into **hour 14**, the wisps had
gone home at 4 AM (`Firefly:GoHome`), and the click therefore ran `Change(0)` on
an **empty** trap — `wisps=0`, `APPLICABLE=false` (L1245-L1292). The wisp-count
precondition was raised by the agent only *after* issuing the stop.
⇒ recovery was `CP15.ForceMode("free", "I-KNOW-THIS-IS-FORCED")` with 95 wisps
present. **The recovered experiment is arguably better** (same trap, same count,
paused, before/after on one variable) **but it is FORCED, and "observed on the
player's own organic click" is a claim this chain can no longer make.**
Ledger this as a NEW rig miss: *an agent stop-instruction issued while a
time-sensitive gate is open, before checking the clock.*

## 4. What the audit should press hardest

* **F07's `tested` grant.** It has owner eyes (screenshot + verdict) but the
  verdict is **transcript-only**, and the decisive enact was **FORCED**. Both are
  stated in the entry. Rule on whether that clears the bar or gets struck to the
  mechanism tier — the sitting did not assume it does.
* **Recompute the ×1000 claim** from L1441: `wisps=95`, `amount=95000`,
  `broken-would-be=95`. And note the **UI divides by 1000** (owner screenshot:
  *Power production 95*, grid *218.5*, vs logged 95,000 / 218,520) — a reader who
  misses that will think the panel contradicts the log.
* **F15's delayed reading is CONTAMINATED and the entry says so.** `status.points
  0 → 2,543,000` over ~7 game hours against 1,500 expected from destructors; the
  colony's own labs were running and the leg has no baseline rate to subtract.
  **The clean discriminator is the paused immediate Δ = 0** (L1529 vs L1558).
  ⛔ The leg's own printed ratio `2543/1500` must NOT be quoted as a finding.
* **`C46` is PRE-RELOAD ONLY.** The phantom-production state was saved
  (`CP15F15.savegame.sav`) but never reloaded and re-read. One load closes it.
* **EF-051** — second sample: final listing **59 `.sav` by NAME** (§5), every
  delta named, none of the 14 strays back, no extensionless file. ⚠️ The
  `steam_autocloud.vdf` "free second witness" prep proposed **returns nothing**:
  it WAS rewritten at this launch (09:47:15 → 15:09:29), and prep framed only a
  *non*-change as informative. Do not treat its rewrite as evidence either way.

## 5. Save-directory close-out — NAMES, not a count

**Final: 59 `.sav`.** Reconciliation, every delta named: 57 pre-launch baseline
− `CP15STAGE` (deleted at close-out) + `CP15PT15` + `CP15F15` + the owner's own
`PT-15 post save`, with `Autosave Sol 11/16` rotated out for `Autosave Sol 76/81`
(same two rolling slots — the behaviour that caused last week's two-file
confusion, seen happening live).

```
asttest, asttestnoans, asttestnoans2, Autosave Sol 76, Autosave Sol 81, CP15F15,
CP15PT15, D10test1, dronetest1, Dronetest2, F59 Test1, f59 test2, F90 Alert,
F95 Baseline, F95 fresh, f95 healed, No disasters underground setup, PT-15,
PT-15 post save, PT-20TEST, PT-20TEST-B, PT35FIXTURE, pt611uninstall, PT61save,
randotest, saint test, Save A post research setup, Save A setup pre research all,
savesavetest, T1-UNINSTALL, T1-UNINSTALL-R, T2-Uninstall, T2-UNINSTALL-R, test 2,
test 2 pre events, Test 2 pre test, Test 2B, Test 2C, Test 2D, TEST 2E, TEST 2F,
TEST 2G, TEST 2H, TEST 2I, TEST 2I TEST, TEST 2I TEST2, TEST 2I TEST3,
TEST 2I TEST4, Test2 H, test2 J, TEST2H TRAIN, TestE, TestF, USA Sol 2,
USA Sol 298, USA Sol 3, USA Sol 302, USA Sol 55, USA Sol 59
```

**⭐ TWO SAVES DELIBERATELY KEPT — owner decision at the keyboard, they are NOT
strays:** `CP15PT15.savegame.sav` (95 wisps, mode `free`; rebuilding it costs a
~3-hour mystery playthrough) and `CP15F15.savegame.sav` (the post-destroy C46
state). Both are recorded in `agent/bugs/C46.md` and the checklist.
**The three protected files BYTE-VERIFY unchanged:** `PT-15` `5D0D81A3…18C06`
(48,086,314) · `TEST2H TRAIN` `103B320A…8A958` (55,667,524) · `PT35FIXTURE`
`D721329D…74738` (54,424,001).

## 6. Owner-time honesty

Brief promised **~45–90 min**; actual **~3h 10m** of attended wall clock
(process uptime 15:09 → 18:29). The overrun is **ours to own, with one
exception**: the march itself was the owner playing their own colony, and prep
had already warned the mystery was far longer than the plan assumed. Rig-side
costs: the lost organic wisp reading (§3), a `SpeedRamp` recommendation that had
to be overridden, a march repeatedly dropped to 1× by the HUD (EF-052), and three
separate asks before the cheat disclosure was captured. **Owner deviations bought
evidence and none is scored against them** — the extra passenger rocket and the
all-shifts activation are what made C39 readable at all.

## 7. Entries already updated by this prompt (audit them as claims)

`F07` (→ `tested`, heading tag + frontmatter + row) · `C39` (→ `**OBSERVED**`) ·
`F85` · `F78` (+2 organic firings) · `F02` (+1) · **`C46` NEW** ·
**`EF-052` NEW** · `PLAYTEST_CHECKLIST.md` (C39 rider, F85 decision item, PT-15
section, C46 decision) · `STATE.md`. Both generated INDEX files regenerated.
doccheck GREEN at commit.
