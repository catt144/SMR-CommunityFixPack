# fixtoggles 11 — the full attended sitting (ATTENDED)

Link 11 of `fixtoggles`. **Needs the owner at the keyboard.** ⚠️ **Link 10 rewrites this body with the real script**;
if you are reading this placeholder, 10 has not run — stop.

Binding regardless of the script: README rules 1–18; `prompts/perma/GENERAL_USE_PROMPT.md`'s live-sitting rules; the
**stale-probe gate** before any test (`grep -rln "TEMPORARY" Code/ ../SMR-BugFixPack-TestKit/Code/`, a `PROBE SWEEP:`
line in every result commit); every owner verbatim through the harness note primitive (`*.Note(...)`, WORKFLOW `corun-pt15` item 3) as spoken; every log a verdict cites
archived into `docs/archive/logs/` in the same commit; status words only for what was witnessed
(`tested-attended`); SKIPs by name, never a total.

## Close-out

Results into checklist 148 and the entries; SESSION_LOG entry; outbox to 99; strike your row; `git rm` this file; push.

## Notes from upstream

- (link 10 writes the script above and appends here)
