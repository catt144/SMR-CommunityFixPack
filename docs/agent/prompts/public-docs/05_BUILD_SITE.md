# Chain prompt 5 — build the site: the fix list, the FAQ, the pages

**Read `README.md` first — binding chain rules apply.** Staleness check across
all four repos, live todo list updated per item. ⚠️ This file was created by
prompt 4 (the terminal audit) because prompt 3's outbox was re-pointed at the
audit tier; the manifest in `README.md` describes this prompt's scope.

## The job

Build the site's content in `C:\Dev\SMR-CommunityMods` (`content/`, MkDocs +
Material scaffold already committed there) — **unpublished**: the workflow is
`workflow_dispatch` only and Pages stays OFF. Nothing goes on the public web;
the owner flips it live.

* **Job 0 — read what you are building from.** `agent/reports/
  PUBLIC_DOCS_DESIGN.md` (§5 fix list, §6 FAQ/surfaces, §12 holes) ·
  `reports/STORE_FIXPACK.md` + `STORE_OPTIN.md` (the voice, and the claims
  already swept) · `docs/agent/STATE.md` · `docs/PLAYTEST_CHECKLIST.md` items
  22c and **22d** (see notes below — their state gates your text). Re-derive
  every count with `--emit-counts` in both mod repos at your moment.
* **Job 1 — the searchable fix list** (design §5). Category pages, player
  language; the ship-line evidence bar binds every entry blurb (chain rule 6).
* **Job 2 — the FAQ** (design §6.1 seed; the "dismissed warnings" and Hotel
  essays are routed here from the store pages).
* **Job 3 — the for-modders page.** The identifier-carrying per-fix disable
  instructions live HERE, not on the store card (ruled by the chain QA; the
  card ships one identifier-free sentence pointing at this page). ⚠️ The
  load-order half is design hole 3 and is YOURS: derive a measured/sourced
  answer or state plainly that none exists — never promise a method.
* **Job 4 — the hostile-reader section.** ✅ **22c DECIDED YES 2026-08-13
  (small section, not a page)** — three answers: *is it you · how do I get you
  out · where do I tell you*. The honest middle answer today is "remove the
  whole mod and restart"; write it from the store texts' uninstall sections
  and the entries, not from memory.
* **Job 5 — screenshot fixtures** (design §12 hole 9): check the save fixtures
  against `CAPTURE_SITTING.md` (as amended by the QA) before the sitting is
  scheduled.
* **Sweep and arbitrate** like prompt 3 did (its ledger `STORE_BUILD_AUDIT.md`
  is the pattern): fan out one-rule sweeps on the finished pages, re-derive
  every finding yourself, write the ledger for prompt 6. ⚠️ See the audit
  note below on WHERE the six-sweep pattern under-swept — do not repeat it.
* **Close:** append Notes from upstream to `06_AUDIT.md` (create it from the
  manifest), doccheck GREEN, commit with `-F`, push, delete this file in the
  same commit.

## ⛔ What you may not do

- Publish anything, anywhere; enable Pages; create an external account.
- Upgrade any claim past `fixed` + suite + self-checks + verified save-safety
  tier by word choice. If a sentence needs evidence we do not have, cut it.
- Put an exposed-set count on any player surface, in any form.
- Lift the `MOD_DESCRIPTION.md` freeze or overturn STATE's release sequencing.
- Copy a store-text sentence from any commit BEFORE the 22d corrections
  landed — quote the current files only (22d was approved and applied
  2026-08-13, same day this file was written).

---

# Notes from upstream — prompt 4, the terminal audit (`04_FABLE_AUDIT.md`, consumed 2026-08-13)

## The verdict, per area, with what was SAMPLED

* **The build itself — SUSTAINED.** Prompt 3's six sweeps genuinely ran and
  bit: its ledger's 27 confirmed fixes are visible in the shipped text (sampled
  in place: the dial save-step recipe, the D10 inertness carve-out, the tier-1
  judgment-call exception, the achievements placement, the elevators cut), its
  two headline catches were real ship-false instructions, and its one wholesale
  refusal (the "unearned blocks" status objection) is CORRECTLY grounded — a
  D-entry's status tracks the design item, not shippability; deleting four
  module blocks would have been wrong. The 22b bullet was independently
  re-derived from `F97.md` (rate table + OG disassembly) and is accurate in the
  owner-approved phrasing. All ten landmines pass. Counts re-emitted clean (94
  probes; opt-in 7 optional + 1 default-active). Tier drift: none found —
  tiers only add, and the shared claims (save notice, restart rule, two-step
  recipe, version pin, achievements) say the same thing in both documents.
* **The sweeps' COVERAGE — OVERTURNED in one specific place.** The audit's
  control (job 1) re-ran the evidence and route sweeps with fresh agents,
  firewalled from the ledger, then compared yields. They confirmed most of the
  ledger — and found **nine defects the six sweeps missed**, re-derived from
  code/entries by the auditor before filing. They cluster in the deep opt-in
  module blocks and the fix pack's compatibility promise — exactly where the
  refused status objection had pointed before it was (rightly) refuted on its
  stated grounds. Pattern for your sweeps: **sweep module/fix content against
  the CODE, not only the entries** — one shipped miss (the row literal
  `off (3 would move)`) matched the D12 design record perfectly and the code
  had moved on. Recorded facts are claims.
* **Prompt 3's honesty — SUSTAINED, two overstatements named.** Its ledger's
  negative halves overstate twice: "every player sentence matched the entry
  body" (F50's does not — "automatic" dropped) and "routes positively
  confirmed walkable on all three platforms" (contradicted by its own coverage
  note; every console route is SOURCE-only). Its handoff notes were accurate
  at write time; §"drafted, NOT applied" went stale ten minutes later (below).

## ⛔ The nine routed corrections — checklist 22d, NOT applied at my close

The full list with evidence lines is `docs/PLAYTEST_CHECKLIST.md` **22d** —
✅ **APPROVED by the owner and APPLIED the same day (9a chosen)**: fix pack —
patch-retires universal ×2 · F50 "automatic" · "several"→"a couple" ×2;
opt-in — Nursery row scoped to cohort-housing domes · real row literal ·
Seniors carve-out (Forever Young / "Put Them To Work") · "press Apply" in both
dial recipes · suns direction (built-near-second-sun) · the dispatch block's
honest measurement note. ⛔ Quote the CURRENT store files only; the defects
listed in 22d are also defects anywhere you might independently restate those
facts, so carry the corrected forms into your pages.

## What the audit changed (hygiene, in place)

* Checklist **22b struck** (commit `4027611`) — with the re-derivation beside
  it. `STORE_FIXPACK.md`'s notes updated to match (hole 2, §22b, metadata
  paragraph).
* `STATE.md` ⑤ rewritten (audit verdict, 22d routing, next = this file);
  owner-decisions line re-derived: **5 open** (22d · 22c · F85 item 5 ·
  status-vocab · rescue prefix rename), relabel wording CLOSED via item 22.

## Corrections to what prompt 3's notes told you

* **`metadata.lua` strings were APPLIED**, not "drafted, NOT applied": the
  owner instructed it after prompt 3's notes were final (`1ac1187`) — the live
  description had carried the console-veto false claim and the dead sibling
  name, and the draft strings carried the same two faults, corrected at apply.
  The audit read both applied strings against its confirmed-findings list:
  **none of the nine defects appears in them** (they use the honest
  stands-down conditional). Portal character limits remain unchecked — release
  prep's, recorded in `STORE_METADATA_STRINGS.md`.
* The combined sitting had **not** run at my close (26b unstruck): hole 11
  (`ListFixes()` on screen), hole 4's play half, and D13's `tested` are all
  still source-tier. **Check STATE at your moment** and upgrade/downgrade.

## ⛔ RE-DERIVE THIS — do not inherit any of it from me

* Every count — `--emit-counts` in both repos at your moment.
* Checklist 22d's and 22c's state, from the file, not from these notes.
* The sitting's state (STATE ② / checklist 26b) before any tier-sensitive
  sentence.
* The frozen evidence bar's wording, from STATE's ship-line ruling.
* Load order (design hole 3) — still unmeasured; nobody has derived it.
* The rescue artifact stays unnamed and unlinked on every surface
  ("build ≠ publish", checklist 17); store/site links stay marked holes until
  the owner uploads / flips Pages.

## Disclosure

The audit's evidence agent grazed 3 context lines of `04_FABLE_AUDIT.md` in
one over-broad grep (re: the %AppData% hedge / Steam Deck) before narrowing;
it re-verified both affected claims at game source independently. All other
firewalls held; the ledger was opened only after both independent sweeps had
returned and been adjudicated.
