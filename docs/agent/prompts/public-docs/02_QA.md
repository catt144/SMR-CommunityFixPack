# Chain prompt 2 — fresh-context adversarial QA of the public-docs design

**Read `README.md` first — binding chain rules apply, especially rule 4 (player
language), rule 6 (no unearned claim) and the ⛔⛔ exposure hazard.** Staleness
check across all three repos, live todo list updated per item.

⭐ **You are the fresh-context adversarial gate** (`memory: fresh-context QA
before implementing`). Prompt 1 designed; you attack. Prompts 3 and 4 build only
what survives you. **Your verdict is BUILD or REWORK, and REWORK is a legitimate
outcome** — say which sections, and why.

⛔ **You are not a proofreader and not a second designer.** Do not restyle, do
not re-decide settled questions to your taste, and do not add surfaces. Attack
the design's *claims* and its *route*.

---

## ⭐ CONCURRENCY — the D13 chain may still be running, and IT has priority

Owner decision 2026-08-13: this chain runs concurrently with
`agent/prompts/d13-rescue/`. ⛔ **On any clash — a file, a fact, a count, a
checklist item, a rebase — THEIR version wins and yours re-derives.**

1. ⛔ **Do NOT edit `docs/agent/STATE.md`** while their chain runs. §10 of the
   design report holds this chain's requested STATE line as a **request**, with
   what to drop for it. ⭐ **If D13's chain has gone quiet by the time you run
   (check: is `agent/prompts/d13-rescue/` empty?), landing that line is YOUR
   job** — re-derive it against STATE as it then reads, do not paste it.
2. ⛔ **Stage by explicit path. Never `git add -A` / `-u` / `.`** — and note that
   even `git add <path>` takes the WHOLE file including another session's
   unstaged edits to it. That has already happened once in this tree.
   `git pull --rebase` immediately before each commit.
3. ⛔ **Not yours:** `agent/prompts/d13-rescue/`, `agent/bugs/D13.md`,
   `agent/facts/EF-023.md`, `agent/reports/D13_EXPOSED_SET.md`.
4. ⚠️ `docs/PLAYTEST_CHECKLIST.md` is written by both chains — **re-read it
   immediately before you touch it.**

If doccheck goes red for something you did not touch, it is probably their
in-flight edit: `git status`, confirm whose it is, do not "fix" it.

---

## Job 0 — read what you are attacking

⛔ **`docs/agent/reports/PUBLIC_DOCS_DESIGN.md` is the deliverable under
review.** Read it whole before anything else. Then, and only then:
`README.md` (this folder) · `docs/agent/STATE.md` ·
`docs/archive/MOD_DESCRIPTION.md` (FROZEN, 748 lines, the tone reference) ·
`docs/PLAYTEST_CHECKLIST.md` "Decisions waiting on you" (items 14, 15, 17–19,
21–25) · **the site repo at `C:\Dev\SMR-CommunityMods`** (4 layout specimens in
`content/` + `mkdocs.yml` + the publish workflow) · enough of `docs/agent/bugs/`
to test §5 and §6 yourself.

⚠️ **THE SITE MOVED AFTER THE DESIGN WAS WRITTEN.** Owner ruled checklist 21 the
same evening: the site got its own repo, **`catt144/SMR-CommunityMods`**
(public), and the scaffold left this tree — `mkdocs.yml` and `content/` now sit
at that repo's root, the `public-site/` wrapper is gone, and so is
`.github/workflows/publish-site.yml`. ⛔ **Nothing is on the public web**;
`workflow_dispatch` only, Pages not enabled. **Every `public-site/…` path in the
design report is pre-move — translate, do not edit the record.**

⭐ **There are now FOUR repos plus a local kit**: fix pack · opt-in pack ·
**save rescue** (`C:\Dev\SMR-CommunitySaveRescue`, built by D13's prompt 3,
`689a396`) · **site** · TestKit. Chain rule 1's "all three repos" staleness check
is stale itself. **Check all of them.**

⛔ **Re-derive every count with `python tools/doccheck.py --emit-counts` in both
repos.** The design report's §0 numbers are claims like any other.

---

## Job 1 — is the architecture right?

The design rests on one load-bearing move: **the surface set is decided by the
searcher, not by the FAQ** (§1). Attack it.

* Is the three-reader split real, or is it a tidy fiction that a fourth reader
  breaks? ⭐ Name the reader it misses, if there is one.
* Is "the store card owes the searcher one link and nothing else" right — or is
  it the mistake a mod page cannot afford, given that most players never leave
  the store page at all?
* §1's table assigns every reader to exactly one owning surface. **Is any cell
  wrong?** In particular: does the *evaluator* really get served by a site, or
  do they go straight to the repo regardless of what we link?
* ⚖️ **The reframe of the owner's question is an argument, not a fact.** If the
  peer creators' split is *not* purely platform-forced, the reframe weakens.
  Check it against what is actually knowable and say so.

---

## Job 2 — the packaging finding, re-derived from source

§2 claims that **the site would have shipped inside the uploaded mod package**
had it stayed in the fix pack repo. ⚠️ **The topology decision no longer rests
on it — the owner ruled and the site has moved.** The finding still matters for
two reasons: it is cited to the owner as **checklist item 23**, which is still
open and now covers `tools/`, `CLAUDE.md`, `LICENSE` and `.gitattributes`; and if
it is wrong, an owner decision was taken partly on a false premise and should be
told so.

⛔ **Re-derive it yourself from `<game>\ModTools\Src`, do not inherit it.** The
cited route is `CommonLua/Classes/GedModEditor.lua:678-741` —
`CreatePackageForUpload` → `AsyncListFiles(content_path, nil, "recursive")` →
`ignore_files` wildcard filter → `AsyncPack`. Confirm or refute:

1. That this is the **only** packer on the upload path (`:749`, `:764`, `:793`).
2. That the recursion really enumerates the whole mod folder.
3. That `tools/`, `CLAUDE.md`, `LICENSE` and `.gitattributes` match **none** of
   our eight `ignore_files` patterns (both mod repos — and check the new rescue
   mod's `metadata.lua` too, which nobody has looked at for this).
4. ⚠️ §12 hole 7 — `.github/` vs `*.git/*` — is marked unverifiable from `Src`
   because `MatchWildcard` is an engine function. **Confirm that it has no Lua
   body**, or find it and close the hole.

⭐ **If the finding is wrong, say so loudly** — it is cited to the owner as
checklist item 23 and it strengthens a recommendation on item 21.

---

## Job 3 — hunt for unearned claims, in BOTH directions

Rule 6 is the rule this project most easily breaks by accident.

* **Over-claiming.** Walk §4.5's ✅ ALLOWED table and ask of each phrase: *does
  the frozen evidence bar actually support this?* The bar is `fixed` + suite +
  per-fix self-checks + the verified save-safety tier — nothing more. Then walk
  the five specimen blurbs (§5) and the judgment-calls draft (§9) hunting for
  any sentence that upgrades it.
* **Under-claiming.** ⭐ **A design that hides a strong, earned claim is also
  wrong** and costs the owner real reach. Is anything in §4.2 or §6.2 marked
  weaker than the evidence supports?
* ⛔ **§6.2's achievements row is a NEW source citation made by prompt 1**
  (`CommonLua/Classes/Achievement.lua:61-63`, `:77`, `Lua/UI/SaveLoad.lua:102`).
  **Re-derive it.** It is about to become a factual claim on a store page aimed
  at console players, and the frozen file made the same claim with nothing on
  record behind it. ⚠️ Check specifically whether the claim's *scope* is right —
  "any mod enabled" vs "mods that block achievements", and what
  `config.Mods and #ModsLoaded > 0` actually gates.
* ⛔ **§0's three staleness corrections** (display name decided · specimens carry
  the dead working title · `install.md` contradicts the frozen file on restart)
  are claims. Check each against git and the code.

---

## Job 4 — attack the five specimens

§5 draws four hard cases and one control and claims the format survives them.

* **Does the format actually work**, or does "Worth knowing" become a dumping
  ground that appears on 60 of 81 entries? ⭐ Test it: pick **five entries prompt
  1 did not use**, at least two of them `wontfix` or `fixed*`, and draft them in
  the format. Report how many needed the fourth beat.
* ⛔ **§5.3 records a trap prompt 1 nearly fell into**: `F55`'s index title names
  two halves and only one shipped. **Sweep for the same failure elsewhere** —
  how many of the 81 shipping F-entries have a title that describes more than
  the module actually delivers? That number decides whether prompt 4 can work
  from titles at all.
* **§5.1 (`F97`) and §5.2 (`F102`) are the two blurbs most likely to be wrong in
  the player's favour.** Re-read both entries and check the blurbs say no more
  than the entries do — especially `F102`'s refusal to claim a cure.
* **§5.6's negative rule** (`F76` gets no entry, ever) — is it stated tightly
  enough that a future session cannot revive the quarantined draft text?

---

## Job 5 — is anything exposed, and is anything player-hostile?

* ⛔ Re-read the README's exposure framing **including its 2026-08-13
  correction** (`docs/` being public is a FEATURE; the separation is about
  audience and job, never secrecy). Then check the design proposes nothing that
  generates player pages from `docs/`, and nothing that treats the repo as
  something to hide.
* ⛔ **No player surface may contain an exposed-set count** in any form (§8).
  Check the design proposes none, anywhere — including in the save-safety
  skeleton and the specimen blurbs.
* **Rule 4 sweep:** does anything the design proposes to *publish* carry a file
  path, a function name, an `F##`/`D##` id, or house jargon? ⚠️ The design
  report itself is an agent doc and cites these freely — that is correct. Judge
  only what it proposes to put in front of a player.
* ⚠️ §8B's sitting loads saves. Confirm it states `EF-056` correctly and that
  its two-restart ordering actually works given that a Mod-Manager toggle needs
  a full restart.

---

## Job 6 — sizing and sequencing

* §3 sizes the work at ~22–30 agent hours and ~2–3 owner hours plus art.
  **Is the fix list (item 5, 8–12 h for ~81 entries) credible?** Time your own
  five drafts from Job 4 and extrapolate honestly.
* Does the design respect `STATE.md`'s release sequencing (MOD_DESCRIPTION is
  item ③, after D13 ① and the combined sitting ②) without overturning it?
* ⛔ Does it lift the `MOD_DESCRIPTION.md` freeze anywhere? It must not — §3
  treats the file as raw material to be *split*, not edited.
* **Is the D13 seam (§8) operational enough that prompts 3 and 4 never guess?**
  Try to find a sentence they would have to write that the seam does not cover.

---

## Job 7 — the decisions routed to the owner

⛔ **ALL FIVE (21–25) WERE DECIDED THE SAME DAY THEY WERE ROUTED.** Do not
re-litigate them; audit whether the project acted on them correctly.

* **22 — judgment-calls wording.** ✅ The five bullets ship as drafted; the
  closing line was **CUT**. ⛔⛔ **The owner's challenge to that line exposed a
  false claim in the frozen file, and this is the most important thing on your
  desk.** §9.1 records it: the per-fix veto is read at mod load
  (`00_Core.lua:384-388`), so **the developer console cannot disable a fix** —
  yet `MOD_DESCRIPTION.md:487-493` tells players to use the console.
  ⇒ **Re-derive §9.1 from the code yourself**, including whether
  `SMRFixPack.WhenActive` gives any of the five a mid-session route. It is
  source-verified and **never play-verified** (PT-62's `P13` lever has never run).
* ⭐ **Then generalise it, because that is the real finding.** §9.1's standing
  instruction says *a claim about what a player can DO needs a ROUTE check, not
  a source citation.* **Apply it as a sweep**: walk every "you can…" sentence in
  the frozen file and in §§4–6 of the design, and for each one name the route
  and say whether a retail player on PC, Xbox and PlayStation can walk it.
  ⚠️ Two false claims have now been found in `MOD_DESCRIPTION.md` in a single
  reading (the F76 explainer, §5.6; the console veto, §9.1). **Treat every
  remaining claim in that file as unverified.**
* **23 — `ignore_files`.** ✅ Ruled: add the patterns at launch prep, **all three
  mods**. Recorded on `WORKFLOW.md`'s release steps. Job 2 re-derives the
  finding behind it.
* **24 — preview art.** ✅ Ruled: build a plain text-on-image **floor** so launch
  is never art-blocked. **Not built yet** — the only unbuilt artifact from this
  round. Not your job unless the design's sizing was wrong about it.
* **25 — roster wording.** ✅ Ruled and applied: `BUG_LIST_AUDIT.md`'s heading
  is now *"Not used as sources, and why"* and *"an aggregator repack"* is now
  *"aggregates other authors' fixes"*. ⛔ **Verify every assessment is otherwise
  byte-unchanged** — the ruling was tone-only, and a silent softening of a
  finding would be a different act than the one authorised.
* ⭐ Finally, the process question worth asking once: prompt 1 routed five
  decisions and all five came back same-day, one of them overturning a claim
  that had survived three prior reviews. **Was anything routed that prompt 1
  should have decided itself — or, more interestingly, anything DECIDED that
  should have been routed?**

---

## Close

* Verdict, first line: **BUILD** or **REWORK** (with the sections named).
* Append **Notes from upstream** to `03_BUILD_STORE.md` (create it — the
  manifest in `README.md` describes it): your verdict, every correction you
  made, every claim you refuted, and every hole still open.
* ⭐ **If `agent/prompts/d13-rescue/` is empty, land the requested STATE line**
  (design report §10) — re-derived, not pasted. If it is not empty, carry the
  request forward.
* Update `docs/PLAYTEST_CHECKLIST.md` if any routed decision changed.
* `python tools/doccheck.py` GREEN. Commit with `-F` (PowerShell 5.1 splits
  `-m` on embedded quotes), push, **delete this file in the same commit.**

## ⛔ What you may not do

- Publish anything, anywhere, or create an external account.
- Generate a site from `docs/`.
- Lift the `MOD_DESCRIPTION.md` freeze or overturn STATE's release sequencing.
- Write a claim the frozen evidence bar does not support.
- Decide the topology, the judgment-calls wording, the roster wording or the
  preview art **for** the owner.
- Edit `agent/bugs/D13.md` or anything else in D13's set.
- Write the actual store descriptions or fix list — that is prompts 3 and 4.

---

## Notes from upstream — prompt 1 (`01_DESIGN.md`, consumed 2026-08-13)

⛔ **The deliverable is `docs/agent/reports/PUBLIC_DOCS_DESIGN.md`, not this
section.** Read it whole. This is the map and the deltas.

**⚖️ Manifest re-shaped, and why.** `README.md`'s manifest put the design in
prompt 1's Notes-to-prompt-2. The design ran to ~700 lines of tables, specimen
copy, a shot list and a FAQ seed — material prompts 3 and 4 will *read while
writing*, long after prompt 2 is deleted. Prompt files delete themselves; the
material must not. ⇒ **The design is a report in `agent/reports/`** (which is
where `docs/README.md` says a design goes), and this section carries the
decisions, the deltas and the holes. Nothing else in the manifest changed.

### The decisions, each with its reason

| # | decision | reason |
|---|---|---|
| **D1** | **The surface set is decided by the SEARCHER, not by the FAQ** | The peer creators' doc split is forced by the *platform* (one unformatted blob, length limit, no search). Ours is not: 81 repaired defects is a database, and a database's only usable entry point is a query. The FAQ falls out as a cheap by-product |
| **D2** | **No surface serves two readers** — card owns the scroller, site owns the searcher and evaluator, repo is receipts | Every classic mod-page failure is one surface doing a second surface's job |
| **D3** | **The site's OWN repo** — ✅✅ **RULED AND DONE the same evening**, `catt144/SMR-CommunityMods` | ⭐ **Source-verified argument:** `public-site/` would have **shipped inside the uploaded mod package**. One `ignore_files` line closes that; a separate repo makes it structural rather than maintained |
| **D4** | **Three tiers** — one sentence · ~10 lines · everything; each stands alone | The standalone test is *"if a reader stops here and acts, is anything they now believe false?"* — sharper than "is it complete" |
| **D5** | **All four install-gating answers in tier 1, including the weak fourth** | Three strong answers plus a silent omission is what costs credibility with the evaluator, who writes the forum replies |
| **D6** | ⛔ **Kill the count as a headline** — categories instead | "74 fixes" invites "which ones?", is three different numbers depending on what you count, and answers a question nobody has |
| **D7** | **An explicit allowed/forbidden vocabulary** (§4.5) | Rule 6 is broken by word choice, not by false facts |
| **D8** | ⛔ **Never quote our internal status vocabulary** in player text | `fixed`/`tested` are house words; in player English "tested" over-claims and exposing the 41/40 split re-opens a distinction the owner's ship line deliberately froze |
| **D9** | **Fix-list format = 3 beats + an optional 4th ("Worth knowing") + a label for judgment calls** | The 4th beat gives caveats somewhere to live other than inside "After the fix"; the label is the surface the 08-04 relabel package has been waiting for |
| **D10** | ⛔ **The public fix list contains fixes we shipped — never defects we investigated** (§5.6) | `F76` was drafted for the mod page in the right voice and then REFUTED. Its prose flags nothing; only the entry does |
| **D11** | **Console screenshot → site only, never the store card** | On a card it says "you need a developer console" to a 15-second scroller and to console players who have none; on the site it is the answer to a question the reader arrived with |
| **D12** | **One screenshot sitting, ordered mod-OFF-first** | A mod toggle needs a full restart, so a naive shot list costs one restart per before/after pair. Ordered: 2 restarts, 45–60 min |
| **D13** | **A four-line standing rule on other people's mods** (§7) | Compatibility copy will touch it repeatedly; getting it right once beats per-page |
| **D14** | **The judgment-calls wording is DRAFTED, not asked for** (§9) | It has been open nine days because it asked the owner to compose. Now it asks them to approve |

### ⚠️ Deltas — things prompt 1 found that the brief did not know

1. ⛔ **The opt-in mod's display name is DECIDED and SWEPT** —
   **"Community Fix Pack: Opt-In Modules"**, owner 2026-08-13, 15 sites / 11
   files, pushed `e17586b`. `README.md` and `01_DESIGN.md` both list it as owed.
   **It is closed.** Use the decided name.
2. ⛔ **Anything in a mod repo's root ships inside that mod's upload** unless
   `ignore_files` names it — `tools/`, `CLAUDE.md`, `LICENSE`,
   `.gitattributes` today. Source-verified via `GedModEditor.lua:678-741`.
   **Routed as checklist 23; not fixed** (`metadata.lua` is code, outside the
   scope fence). ⚠️ The site was on that list until it moved out.
3. ✅ **Two of the three stale specimen strings were fixed during the move**
   (decided display name in `index.md`; the "name not yet chosen" line in
   `install.md`). ⛔ **The third was deliberately left broken and labelled**:
   `install.md`'s restart line now carries a `do not publish as written` banner,
   because two of our own documents claim it both ways and neither was checked
   against the opt-in pack's code (§12 hole 4 — **yours to close or to hand on**).
4. ⛔ **`MOD_DESCRIPTION.md` must be SPLIT, not edited.** It documents a
   one-mod product: all 8 opt-in modules under *Mod Options → **Community Fix
   Pack***, which is now false everywhere it appears.
5. ⭐ **The achievements claim is now source-cited** — `DoModsBlockAchievements()`
   returns true on exactly `Platform.playstation or Platform.xbox or
   Platform.windows_store` (`Achievement.lua:61-63`). The frozen file made this
   claim with nothing on record. **Job 3 re-derives it before it ships.**
6. ⚠️ **`F55`'s index title names two halves and only one shipped.** Blurbs must
   be written from entry bodies and status tags, never index titles. **Job 4
   sweeps for how widespread this is** — it decides prompt 4's whole method.
7. **The counts, re-derived 2026-08-13:** fix pack 74 modules / 75 files / 160
   index rows in 125 entries; opt-in 8 modules / 9 files / 9 rows; 88 shared
   probes. **81 F-entries carry a shipping status (41 `fixed` + 40 `tested`)
   against 74 modules** — some modules bundle several defects. ⛔ The
   entry→module mapping has never been derived; D6 makes it optional.

### The holes, all ten

Listed in full at **`PUBLIC_DOCS_DESIGN.md` §12**. The four that matter most to
you: the rescue artifact's name/link/publish status (D13 + a release-time owner
call) · the uninstall-cleanliness sentence's Reading A vs B (owner, at launch) ·
**load order, where we have no measured answer at all** · and the opt-in restart
question, where two of our own documents disagree.

### What was NOT done, deliberately

* **`docs/agent/STATE.md` was not edited** — D13's while their chain runs, and
  their prompt 3 landed as `689a396` while prompt 1 was closing. The requested
  line and what to drop for it are in §10. Landing it is yours **if** D13's
  folder is empty when you run. ⛔ **Re-derive it** — STATE has moved since, and
  the repo count in it must be counted, not copied.
* **No store description, no fix list, no site page was written.** Prompt 1
  drafted **five specimen blurbs** to prove the format on the hardest cases, and
  nothing else. That was the instruction and it held.
* **`agent/bugs/D13.md` was not read for content** — it is being rewritten
  mid-flight. The seam in §8 is drawn from `README.md` and the checklist, both
  stable.
* **Nothing in `reports/BUG_LIST_AUDIT.md` was rewritten.** §11 recommends two
  edits to the third-party roster and routes them as checklist 25.
