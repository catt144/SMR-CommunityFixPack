# Chain — release-3 (STATE's step ③: the descriptions ×2(+1), disposition table, metadata)

**Authored 2026-08-14 by the session that closed the public-docs chain.** STATE
line ③ names this work and, until this folder existed, nothing kicked it off.
**Kickoff: open a session on `01_BUILD_DESCRIPTIONS.md`.**

## What this chain is

The release front's last agent step before the owner's launch afternoon (④).
Everything it consumes is already built and audited; this chain **assembles and
finalises**, it does not author claims:

* the two store cards — `agent/reports/STORE_FIXPACK.md` / `STORE_OPTIN.md`
  (player text between the `═══` rules; R1 applied 2026-08-14)
* the site — `C:\Dev\SMR-CommunityMods\content\`, terminal-audited 2026-08-14,
  ledger `agent/reports/SITE_BUILD_AUDIT.md`
* D13's deliverables — `D13_EXPOSED_SET.md` (§2b footprint, §10 artifact,
  the 27-site disposition table), `D13_VERIFICATION.md`, D13 `tested` 08-14
* `agent/reports/STORE_METADATA_STRINGS.md` — strings applied `1ac1187`;
  ⚠️ portal character limits never checked

⛔ **`docs/archive/MOD_DESCRIPTION.md` is FROZEN and known-false in ≥6 places**
(F24/F28 bullets promise deleted fixes; the sensor-tower causation is inverted;
see `SITE_BUILD_AUDIT.md` W1–W3). It is raw material only where a card already
split it out. **Treat every claim in it as unverified.**

## Binding chain rules (inherited from the public-docs chain, verbatim in force)

1. **Staleness check first, every prompt**: `git log --oneline -10` + `git pull`
   in all FOUR repos (fix pack, opt-in, save-rescue, site). Live todo list,
   updated per item — the owner reads it to decide when to step in.
2. **Inbox/outbox in writing**: each prompt appends to the next prompt's
   `## Notes from upstream`, commits, deletes its own file in the same commit.
   Folder emptiness is the terminal prompt's done-condition (this README goes
   in the terminal prompt's closing commit).
3. **Recorded facts are claims** — every count via `--emit-counts` at your
   moment, never from prose. Including every number in this README.
4. **PLAYER LANGUAGE IS A HARD GATE** on anything a player reads: no file
   paths, function names, `F##`/`D##` ids, or house words.
5. **Nothing is published by an agent.** Drafts land in the repo; the owner
   pastes to portals and flips anything live. ⛔ No store link, no Pages, no
   external account.
6. ⛔ **No claim past `fixed` + suite + self-checks + verified save-safety
   tier by word choice.** The evidence campaign is frozen. If a sentence needs
   evidence we do not have, cut it. ⚠️ The two still-open hedges (does the
   listing command reach the SCREEN; the live toggle-flip) stay hedged exactly
   as the site states them — the sitting did NOT settle either
   (`COMBINED_SITTING.md:367`; capture pass C untaken).
7. **`python tools/doccheck.py` before any doc commit; red blocks.**
8. ⭐⭐ **The `context` handoff binds every prompt**: write drafts to their real
   files immediately, commit at every natural boundary, never `git add -A`.
   On the owner's bare word `context`: stop at a safe boundary, commit, write
   `<NN>_<NAME>_B.md` beside this file handing on STATE and POSITION (never
   facts — point at sources, make the successor re-emit), delete your own file
   in the same commit, tell the owner in one line.

## Scope fence

**In:** the final per-product description files ×2 (+1 conditional, below); the
release disposition/uninstall assembly; resolving the §10.5-vs-code gap; the
metadata/portal prep the owner's ④ sitting reads from.
**Out:** publishing anything; new fixes; re-opening frozen evidence; touching
the site's pages (terminal-audited — a defect found there is FILED, not edited
in passing); preview-image ART (commission problem, already routed);
`unattended-3` (F85 flip + C39 compensation — ⛔ NOT this chain; it is gated on
the owner's open F85 severity decision, checklist item 5, and is authored
separately if the owner rules build).

## Stop conditions

- A description needs a fact the project does not have → mark the hole, route
  it, never invent it.
- The §10.5 resolution grows an owner dimension (it may — see prompt 1 Job 0)
  → route to `PLAYTEST_CHECKLIST.md` "Decisions waiting on you" and continue
  with the rest.
- Anything wants to weaken a hedge or upgrade a claim → rule 6, cut or hedge.

## Manifest

| # | file | what it does |
|---|---|---|
| 1 | `01_BUILD_DESCRIPTIONS.md` | Job 0 the §10.5 gap · then assemble the final description files, the uninstall/disposition assembly, the portal-prep sheet. Outbox = prompt 2 |
| 2 | `02_AUDIT.md` | terminal: player reading of every final surface, the shipped-module control re-run over every NEW sentence, portal-prep sanity, route the ④ launch checklist to the owner, empty the folder |

Model placement lives in the FILENAME only; bodies stay model-neutral
(`CHAIN_METHOD.md` §2.10). Two prompts sit under §4.0's six-prompt bar, so this
decomposition needs no top-tier ratification beyond the session that wrote it.
